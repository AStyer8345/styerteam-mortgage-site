# styermortgage.com — Context

## What This Is

Public mortgage website for Adam Styer | HyperSmart Home Loans. Static HTML/CSS/JS on Netlify — no framework, no CMS. 75+ public pages live (homepage, loan pages, 25 suburb SEO pages, 5 rate-check city pages, 10 rate-shopper blog posts, resource/guide pages, blog, calculators, realtor hub, plus the 7-page non-QM cluster).

## Repo

| Item | Value |
|------|-------|
| Repo | `AStyer8345/styerteam-mortgage-site` |
| Local | `/Users/adamstyer/Documents/Claude/styerteam-mortgage-site` |
| Hosting | Netlify — auto-deploy on GitHub push |
| Domain | styermortgage.com |
| Dev server | `.claude/site-server.js` → port 8766 |

## Last Worked On

**2026-09-04 PM (styer-site-daily — Friday rotation: Content Planning + AEO Review): clean audit, 0 site mutations — PSI escalated to structurally-gone, blog staleness worsened to 14 days.** `git fetch` precondition clean, tree already at `origin/main`. Non-negotiables green: sitemap/robots/core **200**, **153**/153 `<loc>`/`<lastmod>`, **0** dupes, conversion **10/10**. **Blog CTA coverage re-confirmed 59/59** via full live fetch of every post (55 standard + 4 realtor-updates, both correct CTA patterns). **AEO audit on 2 posts (newest case-study + jumbo-loan FAQPage post): 0 defects** — case-study correctly has no FAQPage (narrative), jumbo post's 5 FAQ answers 0/5 hedged. **PSI quota: 4th consecutive confirmed-dead probe (HTTP 429)** — escalated wording from "recurring" to "structurally gone without Adam's dedicated key," per 09-04 AM's own instruction. Netlify Lighthouse substitute re-pulled: Perf 79/A11y 90/BP 100/SEO 100, and its `formFactor` field now **confirms mobile scope** (closes 09-04 AM's "scope unconfirmed" caveat). **Blog now 14 days stale** (last post 08-21), up from 10 days flagged 08-31 — re-flagged to `styer-content-weekly` with the updated number. Design spot-check (blog.html + calculators.html) clean: GTM ×2 both, 0 legacy strings, 0 "21-day" claims. Step 4B backlog re-confirmed no un-gated items (7th run same conclusion). `loanos-clone` unmutated. Self-review PASS, 0 files. Minor env note: bare `curl` needed full-path invocation (`/usr/bin/curl`) this session.

**2026-09-04 AM (styer-site-daily — Thursday rotation: Internal Linking + Funnel Flow): clean audit, 0 site mutations — the real work was a near-miss investigation that came back clean.** `git fetch` precondition **5-for-5**, tree already at `origin/main`. Non-negotiables green: sitemap/robots/core **200**, **153**/153 `<loc>`/`<lastmod>`, **0** dupes, conversion **10/10**. **Full 153-page live-fetch link graph** (parsed with `html.parser`, not regex): **0 orphans, 0 pages under the 2-outbound-link bar** — both bars hold sitewide, no regression from the 08-30 fix. Funnel traced end to end clean: homepage → `/get-preapproved` ×5, `contact.html` and `/thank-you` both wired correctly. **The near-miss:** live-fetched HTML for `contact`/`get-preapproved`/`refinance-quote` is missing the `netlify`/`netlify-honeypot` attributes present in the exact deployed commit's source — looked like a broken deploy silently dropping 3 core lead forms. Checked against ground truth instead of reporting it: queried Netlify's own Forms API via MCP — all 3 forms are registered and capturing live (`get-preapproved` got a real submission today, 14:05 UTC, hours before this run). Netlify's build-time form detection appears to consume its own marker attributes from served HTML once registered; not a defect. Would have been a fabricated HIGH otherwise. **PSI quota re-probed: 3rd consecutive confirmed-dead call (HTTP 429).** New substitute found: the Netlify MCP deploy record carries its own homepage Lighthouse summary (Perf 80/A11y 90/BP 100/SEO 100/PWA 40) — a partial, zero-cost stand-in until PSI recovers. Step 4B backlog re-confirmed no un-gated items (6th run running same conclusion). Design spot-check (contact + get-preapproved) clean. `loanos-clone` unmutated. Self-review PASS, 0 files.

**2026-09-03 (styer-site-daily — Wednesday rotation: Suburb Page Deep Dive + AEO, cursor Cedar Park): 1 AEO fix shipped + verified live; carried tap-target measurement resolved.** 1-day scheduler gap (09-01 → 09-03, 09-02 no-fire). `git fetch` precondition **4-for-4** clean, tree already at `origin/main`. Non-negotiables green: sitemap/robots/core **200**, **153**/153 `<loc>`/`<lastmod>`, **0** dupes, conversion **10/10** (HTML-token; `phone_click` verified via `script.js`'s delegated `a[href^="tel:"]` listener, not per-link markup — grepping HTML alone would have false-flagged it). **Sitewide GTM/JSON-LD sweep re-run: 153/153 GTM, 0 JSON-LD errors, 9 expected no-LD pages, 0 duplicate titles/descriptions — identical baseline to 09-01, no delta.** **Cedar Park passed every rotation axis** (city H1, 7/7 question H2s, FAQPage, BreadcrumbList, inline form, 3 links to `/get-preapproved`, 2 to `/calculators`, GTM ×2) **except one FAQ answer opening with "It depends" instead of the number** — in both the JSON-LD `acceptedAnswer.text` and the visible accordion `<p>`. Fixed by reordering only: new opener leads with the concrete 0%–20% range, then the identical unchanged per-tier breakdown follows. Commit `c09be17`, **+2/−2**, JSON-LD re-parsed 6/6 valid, live-verified both occurrences. **Also resolved the tap-target height carried unresolved since 09-01** (browser tool had timed out twice that run) — rendered both landing pages at 390px directly: `/get-preapproved` step CTA **45.5px**, `/refinance-quote` submit **50px**, both clear the 44px minimum; closed, no longer carried. Design spot-check (homepage + about): mobile menu **functionally verified** (toggle click flips `aria-expanded`, full nav panel renders with CTA + phone), trust bar matches (5.0 · 141 reviews), gold-hex drift still standing (unchanged, Adam's call). Headshot/WebP checklist item clarified as a non-issue: the visible `.headshot`-class image is actually `family.jpg` (family photo), and the real `headshot.jpg` is only referenced in JSON-LD, never rendered — no page-weight impact either way. Step 4B backlog re-confirmed no un-gated items (same conclusion 4 runs running); `loanos-clone` unmutated. Self-review PASS, 1 file. Commit isolation: dirty-count parity **0 → 1 → 0**.

**2026-09-01 (styer-site-daily — Tuesday rotation: Title Tags + Meta Descriptions): 6 over-length meta descriptions trimmed into the 150–160 SERP band (`c109c97`, pushed, live-verified) — and a near-miss that matters more than the fix.** `git fetch` precondition **3-for-3**: checkout was 1 behind (`41bb244`, a concurrent writer's sitemap lastmod refresh), tree clean. Non-negotiables green: sitemap/robots/core **200**, **153**/153 `<loc>`/`<lastmod>`, **0 dupes / 0 noindex**, **all 153 sitemap URLs resolve 200**, conversion **10/10**, GTM **153/153**, JSON-LD parse errors **0/153**, no-JSON-LD **9** (expected), orphans **0/153**, <2-outbound **0/153**. **Sitewide title/description audit, all 153 batched: 0 missing, 0 duplicate titles, 0 duplicate descriptions, 153/153 exactly one canonical and one `<h1>`.** Fixed the only real defect class — 6 descriptions at 161–173 chars (SERP truncates ~160, cutting the CTA off the end) trimmed to 152–155: CPA resource, advisor strategies, reverse-mortgage advisors, DSCR calculator, reverse-mortgage TX, CFO case study. Diff was exactly 6 files / 6 lines, every one a `name="description"` attribute; each replacement asserted `count(old)==1` first. **THE NEAR-MISS: my first extractor parsed descriptions with `content=["\']( .*?)["\']` — a character class, not a backreference — so any apostrophe inside a double-quoted attribute ended the match.** It reported the **homepage description as 6 characters** (`"Austin"`, truncated at *Austin's*) plus 32 more pages under 120 chars. Re-parsed with `html.parser`: the homepage is a healthy **154**, and the true count of short descriptions is **1**. **32 of 33 'findings' were punctuation** — acting on them would have overwritten 33 healthy descriptions including the homepage's. Nothing was changed on those pages. **Three more false findings killed:** landing-page `<nav>` (contains only the logo — the logo-only constraint is *satisfied*); 'render-blocking Google Fonts' (the plain stylesheet link is inside `<noscript>` — correct preload+onload pattern); `dscr-calculator.html` 9/8 `<script>` imbalance (prose text, **identical at HEAD**, my *verifier* had the artifact). **Three loan-type titles lacking NMLS deliberately NOT changed** — appending `| NMLS #513013` costs 15 chars, pushing 51/52/61 to 66/67/76 past Google's cut, truncating the NMLS anyway while displacing keywords; disclosure is satisfied on-page (18/14/4 occurrences), matching recorded precedent at `backlog.md` L125–126. Carried claim corrected: '26 indexed titles >65 chars' measured **61** today (item stays an Adam no-op, but the number was stale). **Tap-target height left UNRESOLVED, not guessed** — computes to 48px at the site's `line-height:1.5` token vs ~43px at browser default, and the browser tool timed out twice (100s each). `loanos-clone` unmutated. Self-review PASS, 6 files. Parity **0 → 6 → 0**.

**2026-08-31 AM (styer-site-daily — Friday rotation: Content Planning + AEO Review): a 7-page lead-gen cluster was live with NO GTM container, firing `generate_lead` into a `dataLayer` nothing consumed. Fixed + live-verified; GTM coverage 146/153 → 153/153.** Ran the Friday rotation on a Monday per the log cursor — **1-day gap**, cadence holding. **First action of the run was `git fetch`, and it mattered: the checkout was 11 commits behind `origin/main`.** The stale tree made 6 brand-new pages look simultaneously like missing files, orphans, and dead ends — three "independent" checks naming the *identical* six URLs, which is the tell for one shared cause. `git pull --rebase` cleared all six; **had I reported them it would have been six fabricated HIGHs.** This is a structural failure mode of the task (Step 1 reads the **live** sitemap while every structural check reads the **local** checkout) — `git fetch` before analysis is now a precondition. Non-negotiables green: sitemap/robots/funnel **200**, **153**/153 `<loc>`/`<lastmod>`, **0 dupes / 0 unresolved / 0 noindex**, conversion **10/10** on funnel pages, `calendly_click` still 0/0. **The HIGH came from running GTM coverage across all 153 sitemap URLs rather than the 5 funnel pages:** 7 indexed pages carried **zero** `GTM-PQQ6PGLR` — the reverse-mortgage + financial-advisor/CPA referral cluster (6 shipped in `ebd3c2b`/`a29ce29`, plus `referral-partners-self-employed-clients`). Worse than "analytics missing": `js/professional-referrals.js` calls `track('generate_lead')`, `track('partner_call_click')` and `track('professional_assistant_opened')`, each doing `window.dataLayer=window.dataLayer||[]; push(...)` — so the pages **create** the dataLayer and **push conversions into it** while no container ever loads to consume them. Nothing throws, forms work, Netlify captures the lead, Adam still gets paged — and GA4/Google Ads receive **zero** conversion signal from the whole cluster. Fixed with the standard **synchronous** snippet (head script + body noscript) copied byte-for-byte from `asset-depletion-mortgage-texas.html`, a peer in the same nav cluster — deliberately not `/get-preapproved`'s lazy variant (that's a landing-page perf tradeoff). Commit `a9d5188`, **+75/−12**, and the "deletions" are re-emitted minified single-line files — proven by subtracting the inserted block and diffing against `HEAD`: **all 7 byte-identical**. Live-verified 7/7 at GTM ×2, HTTP 200. **Separately: the NotebookLM advisor script has not been missing for 103 runs — it was moved.** Found in 30 seconds with `find` at `cowork/archive/loanos-home-root/scripts/notebook_advisor.py`; the tell every prior run missed is that the whole *parent* `/Users/adamstyer/loanos/` was gone (a missing file is deletion; a missing home-root directory is a move). Ran it — it executes and reaches the API; **auth is the only real blocker.** Rotation itself came back clean and was generalized rather than sampled: **blog CTA coverage 59/59**; **59/59 posts carry an extractable summary within 3 paragraphs**; jumbo post's FAQPage has **0 hedged answer openings**; the veteran case study correctly has no FAQPage (narrative, not Q&A). **Blog is 10 days stale** (last post 08-21 vs 7-day threshold) → flagged to `styer-content-weekly`. Standing metrics all clean: **orphans 0/153**, **<2-outbound 0/153**, **blog registry parity 3/3** (manifest 59 / noscript 60 / ItemList 60; sole delta `/resources/first-time-buyer-guide`, the documented guide hub). **Four of my own false findings killed before surfacing:** the stale-checkout six; *"ItemList is empty"* (regexed `"item":` when the schema uses `"url":` — real count **60**); *"all 59 posts missing from the noscript list"* (`re.search` grabbed the **first** `<noscript>`, which is the GTM iframe; the post list is the third block, 60 anchors); and *"posts have no extractable summary"* (a 2-post sample — all 59 checked, **0 defects**). Two were uniform zeros from regexing structured data **in a session that already had a correct extractor** — the pull toward a second extractor is strongest exactly when the input looks like a different shape. **Also corrected 3 SKILL.md drift items in-file** (backup `SKILL.md.bak-2026-08-31`) after 3 runs of flagging them with no resolution path: dead advisor path repointed, the retired "21-Day Avg. Close" trust-bar check inverted to verify **absence** per GOALS.md, and 2 pointers to the deprecated `styermortgage-context.md` → `CONTEXT.md`. `loanos-clone` unmutated. Commit isolation proven by dirty-count parity (**0 → 7 → 0**).

**2026-08-30 (interactive, Adam-directed): EDITORIAL REDESIGN + AUDIT DEPLOYED TO PRODUCTION.** Merged PR #15 (sitewide editorial rollout) + PR #16 (full-site audit) into `main` and deployed — production styermortgage.com now runs the new design. Merge kept concurrent daily-agent work (GTM restore, orphan links) via manual resolution on buy-before-you-sell (GTM ×2 + audit changes both preserved). 168/168 tests on the merged tree; live-verified per change class. Post-deploy, Adam's live review surfaced two `.lead-page` gaps — trust chips invisible (editorial overrides missed `.lp-trust-chip`, still white-on-white) and a lopsided hero (short copy vs tall form) — fixed by restyling chips at the `.lead-page` layer and moving what-happens-next into a 3-step `lp-hero-aside` in the copy column on both LPs (`0e33aaf`, `76b8def`, css cache bumped, live-verified). Also per Adam: **A&D Mortgage scrubbed from p-and-l page** (11 mentions, schema re-synced; Newrez on 1099 pending his call) and **reviews now 96 five-star Google → 141 combined** (2 realtor updates + ARCHITECTURE.md updated; "140+" floor and "141 published reviews" hero both still accurate). PR #15 closed w/ comment, PR #14 superseded. Remaining Adam-gated questions live in the PR #16 body + audit artifact.

**2026-08-30 (styer-site-daily — Thursday rotation: Internal Linking + Funnel Flow): 3 orphaned indexed pages found and linked; orphan count 3 → 0.** Ran the Thursday rotation on a Sunday per the log cursor — **1-day scheduler gap** (08-29 → 08-30), a large improvement on yesterday's 8-day gap. Non-negotiables green: sitemap/robots/funnel **200**, **147**/147 `<loc>`/`<lastmod>` with **0 duplicates and 0 unresolved**, conversion **10/10**, `calendly_click` still 0/0, **0 of 147 sitemap URLs carry `noindex`**. **Ran the complete version of the rotation instead of the 3-page sample:** a comment-aware inbound/outbound link graph over all 147 sitemap URLs. The checklist's stated question — *does each page link out to 2+ others* — passes **147/147**, so the defects were all on the **inbound** side the checklist never asks about. **3 orphans: pages that are indexable, self-canonical, in the sitemap, and linked from nothing** — `/resources/mortgage-options-for-business-owners.html`, `/buy-before-you-sell-austin.html`, `/resources/blog/2026-03-04-wait-for-rates.html`. Checked the duplicate trap before fixing (an orphan that is a near-copy of a linked page wants a canonical, not a link): all three came back distinct — 0.43 / 0.56 body similarity, disjoint H2 sets, third distinct by audience. **Root cause of orphan #3 is a three-registry desync:** `blog.html` keeps the post list in `blog/manifest.json` (JS card grid), a `<noscript>` `<ul>` (link equity), and CollectionPage `ItemList` (structured data) — the borrower Wait-for-Rates post was in the first only. All three registries held **59 entries**, so the counts matched while the contents differed; only a set diff caught it. Fixes shipped as `ccff027` (**+16/−0, zero deletions**): two resource cards on `/resources/` at the file's own documented card stub, and the missing post registered in both `blog.html` registries with anchor text taken from the page's real `<h1>`. Graph re-run after the edit: **orphans 0**; all three verified live. Funnel traced clean end to end — homepage → `/get-preapproved` **×5** (as `?intent=scenario`) → Netlify form → `/thank-you`; all 3 forms wired (netlify attr + `form-name` + honeypot + `/thank-you` action); `thank-you.html` carries Calendly ×2, `tel:` ×1, `thank_you_page_view` ×1, the 3-step what-happens-next, and correct `noindex` + sitemap absence. `/refinance-quote` has **0 homepage links but 36 inbound** — ad LP by design, not flagged. **Two of my own false findings killed before surfacing, both my extractors:** (1) "`scenarios.html` ships a dangling `/scenarios/[slug].html`" — real href, 404s live, but **inside an HTML comment** (a documented card stub); I rebuilt the whole graph comment-aware and re-diffed rather than just dropping the finding, because comments can also manufacture phantom *inbound* links and hide real orphans (0 pages changed — set is sound); (2) "homepage has 0 links to `/get-preapproved`" — my regex anchored right after the path and missed `?intent=scenario`; true count **5**, agreeing live and in source. Both came from a second, sloppier inline regex written when a correct normalizing extractor already existed in the same session. Step 4B read-only: 14 open items, 8 P4/GSC-blocked, rest Adam-gated; the one seemingly un-gated LOW_RISK item (per-city median prices / school districts) was **deliberately declined** — unsourced statistics on compliance-reviewed pages, and it belongs to `styer-suburb-editor-daily`. `loanos-clone` unmutated. Commit isolation proven by dirty-count parity (**0 → 2 → 0**).

*(2026-08-29 PM (styer-site-daily — Wednesday rotation: Suburb Deep Dive + AEO, cursor Buda): 1 fix shipped + live-verified; the repo's 6-week top constraint confirmed CLEARED.** Ran the Wednesday rotation on a Saturday per the log cursor — **8-day scheduler gap** (08-21 → 08-29), the longest recorded for this task. Non-negotiables green: sitemap/robots/funnel **200**, **147**/147 `<loc>`/`<lastmod>` (+1), conversion **10/10**, `calendly_click` still 0/0. **Re-verify gate cleared three carried blockers at once:** the stale checkout is **0 dirty entries** (was 52/12, overlay safe in stash `stale-overlay-2026-08-29-backup`), and all **six** parked fixes were confirmed *in `HEAD`* rather than trusted from the commit message — 3 AEO entity fixes on `index.html` plus 3 metas now at 154/155/157. **Buda passed every rotation axis** (city H1, FAQPage 6Q/6A, BreadcrumbList, inline Netlify form, `/calculators` ×2, 4-of-5 question H2s, answer-first ¶2), so the run generalized to a **25-page cohort sweep**: H1 25/25, BreadcrumbList 25/25, `/calculators` 25/25, question-H2 25/25, FAQPage 24/24 eligible; inline forms stay 5/25 (Adam form-gate). **The one genuine defect came from a full 147-URL sweep, not the rotation:** `buy-before-you-sell-austin.html` was the **only** public, indexed, sitemap-listed page with **zero `GTM-PQQ6PGLR`** — while initializing `dataLayer`, loading `analytics.js`, and carrying a `data-track="secure_application_click"` hero CTA, so every event fired into a container that was never loaded. Restored the standard container byte-for-byte from a peer page (`83f7413`, **+10/−1**, live-verified GTM ×2 on the first poll). Root cause is the overlay itself: this file was locked during the 08-25 7-page GTM restore. **GTM coverage now 147/147**; the other 12 GTM-less files are all `noindex` dashboards plus `loanos.html` (LoanOS marketing paused by GOALS — deliberately untouched). Metadata layer is now clean across the board: 147/147 titles, 147/147 descriptions, **0 duplicates of either**, 0 descriptions >165 chars (was 3), canonical exactly ×1 on 147/147, NMLS `2653540` on 147/147, 0 stray `noindex`. **Three of my own false findings killed before surfacing, all tooling:** (1) "`austin-area` missing FAQPage — 1/25 outlier defect" — it is the cohort's **directory hub with zero FAQ content**, so the absence is correct and adding the schema would have been fabricated markup; (2) "Buda has 0 internal links" — a `$var` inside a double-quoted grep pattern with `\"` broke under zsh (`bad math expression`) and returned 0 for *every* target, when Buda actually has 139 hrefs incl. `/calculators` ×2; (3) "5 pages missing from sitemap" — my normalizer never mapped `foo/index.html` → `foo/`, **reproducing a false-positive class already documented in learnings**. Real sitemap gaps: zero. Step 4B read-only: P0–P3 fully struck, remainder P4 GSC-blocked/Adam-gated, `loanos-clone` unmutated. Commit isolation proven by dirty-count parity (**0 → 1 → 0**).)*

*(2026-08-29 AM (interactive — full-site audit, Adam-directed): STALE OVERLAY RESOLVED + six queued fixes LIVE + PR #16 opened.** The 52-file stale checkout (top constraint since 08-19) was rescued: in-flight `git am` was a superseded Aug-6 patch (CFO case study already on main as `blog/2026-08-06-...-former-cfo.html`), overlay diff backed up to session scratchpad AND git stash `stale-overlay-2026-08-29-backup`, tree fast-forwarded to origin/main. The six queued one-line fixes shipped on main (`342cb0d`) and are **verified live**: index.html Person.url → `/about.html`, worksFor display-name+legalName+`@id → #business` (about.html worksFor also got the @id), 3 over-length metas trimmed to 150–160 (physician 152, smithville 155, buy-before-you-sell 155). **Homepage 107-char title blocker is stale — HEAD's title is already 63 chars, compliant; row closed.** Then a 4-agent audit (SEO/AEO, UX/conversion, business-owner content, calculators) ran against the site and every verified high-confidence fix was implemented on **`redesign/audit-2026-08-29` → PR #16, stacking on PR #15** (the sitewide editorial rollout — Adam confirmed PR #15 is "the new site"; production = old design until it merges). Highlights: mobile drawer had ZERO conversion actions sitewide (nav CTA + phone un-hidden); about.html closing CTAs used undefined btn classes (invisible); header CTA unified to the tracked intake on 160 pages; get-preapproved's above-form portal button (untracked Ads exit) moved below the form; superlative sweep; invented testimonial variants replaced with verbatim reviews; FAQPage schema rebuilt from VISIBLE content on 14 pages; og:image ×47 posts + Article image ×82; llms.txt +8 cluster pages; affordability calculator overstated max price ~$137k (ignored taxes/insurance) — fixed with disclosure; 17 new calculator tests (was zero coverage). All 168 tests pass. **Adam-gated questions consolidated in PR #16 body** (two competing 1003-app URLs, review counts, A&D/Newrez naming, posture split, ITIN claims).)*

*(2026-08-20 PM — Monday rotation: Schema + AEO Entity Audit: 3 schema defects fixed, 4 files, verified live (`f6a8328`, +7/−10 JSON-LD only). **2 pages told Google Adam *founded* the company** — `westlake` + `buda` nested his Person under `"founder"` of the business node, while **77 pages** sitewide say `employee`/`worksFor` and both canonical entity nodes (homepage + about `#adam-styer`) say `worksFor`. Corrected to `employee` — a corporate-role claim in structured data during pre-audit week. Also: **Buda carried two conflicting breadcrumb trails** (nested `WebPage.breadcrumb` 2-level `.html` vs standalone 3-level extensionless; Buda was the **only** page repo-wide with a nested one, all 8 siblings use the standalone) — removed the nested one. And **reconciled the Person entity 2/6 → 6/6** by adding `@id` → `#adam-styer` to the 4 nested `employee` nodes. Commit `f6a8328`, **+7/−10, JSON-LD only** — no visible copy, GTM, form, nav, canonical, CSS or JS touched; all 4 pages re-verified live (founder=0, `@id` present, nested_bc=0, JSON valid). **Three of my own false findings killed before surfacing, all flat greps misreading JSON-LD nesting:** `grep -c '"BreadcrumbList"'` can't distinguish a nested reference from a duplicate; `grep -l '"breadcrumb"'` matched 10 files on a **CSS class** (`<nav class="breadcrumb">`) and would have inverted the fix; and my own regex HTML-stripper reported dev commentary rendering to users when the live page is **42/42 balanced** (`<[^>]+>` ends a comment at the first inner `>`). **AEO entity audit: `@id`s reconcile correctly homepage↔about**, but 3 conflicts (`Person.url` `/about` vs `/about.html`; `worksFor` legal-vs-display name shape; no NMLS `identifier` on `#business`) are **blocked by the stale checkout** — `index.html` holds a 100-line uncommitted diff. Homepage extractable-answer check **PASS**. Commit isolation proven by dirty-count parity (52 → 56 → **52**).)*

**2026-08-13 (interactive — universal mortgage intake form):** Simplified `/get-preapproved.html` from three problem-loan-oriented steps to two universal steps. Step 1 now asks only goal, property value, down payment/equity, area, and process stage; Step 2 captures contact details, optional context, and existing consent controls. The narrow form card now uses a uniform single-column field layout at every viewport width. Netlify, lead-intake, LoanOS/Mailchimp, UTM, GTM, validation, and thank-you routing remain connected. Full test suite passed (104 tests); browser checks passed at 1440×1000 and 390×844 with no horizontal overflow. Existing unrelated working-tree changes were left untouched.

**2026-07-18 (styer-site-daily Saturday — OFF-ROTATION): clean audit, 0 site mutations.** Saturday has no assigned Step 4 weekday rotation (Mon–Fri only) — ran non-negotiables + read-only backlog + design spot-check. Non-negotiables green (sitemap/robots/homepage **200** sandbox-disabled curl; conversion **10/10** HTML-token, matches 07-17 matrix; `action='/thank-you'` single-quote form still in effect). Design spot-check clean (about + dscr): GTM ×2, NMLS 513013 (about 11/dscr 15) + company 2653540 (about 6/dscr 7), 0 legacy entity, 0 "21-day"/"24-48". Backlog + BLOCKERS read-only (BLOCKERS clean); 3 open backlog items all content-creation (Adam-gated new-page / styer-content-weekly blog+PDF) — no un-gated site work remaining. `loanos-clone` untouched (paused-LoanOS guard). Scheduler fired today — 4th clean day in a row (07-15/16/17/18) after the 10-day outage.

*(Older entries 2026-06-25 → 2026-07-18 trimmed here to hold the 150-line cap — full detail preserved in `CHANGELOG.md`.)*

## Active Blockers

| Item | Priority |
|------|----------|
| ~~USDA Decision 1~~ **ANSWERED 2026-06-09: 1=b — executed.** All 8 Class-A pages repositioned off USDA (conventional/FHA/VA/DPA + OTC construction lead). Class-B pages untouched. Residual: `/loans/usda.html` still on disk (noindexed, out of sitemap) — retire or keep as honest explainer, Adam's call | LOW (Adam) |
| ~~Performance claims Decision 2~~ **ANSWERED 2026-06-09: 2=a — executed.** "24-hour" variants swept (~75), "same-day" kept. Residual: 2 client testimonial quotes still contain speed claims (new-braunfels "pre-approved in 24 hours", first-time-home-buyer "under 24 hours") — quotes can't be honestly rewritten; swap for other real reviews if desired | LOW (Adam) |
| ~~0-tracked-LP Decision 3~~ **LARGELY MOOT 2026-06-09:** quick-contact/prequal/rate-check forms now land on /thank-you, so organic submissions count as Ads conversions without tracked-LP links. /scenarios stays the canonical organic LP. Adam should confirm conversion counts rise in the Ads UI over the next 2 weeks | MEDIUM (Adam) |
| ~~Kyle inline form~~ **DONE 2026-06-09** — Kyle + Cedar Park + 5 money pages all have inline quick-quote forms. POST-DEPLOY: verify 7 new form names in Netlify Forms dashboard + n8n alert coverage | HIGH (Adam) |
| **NEW 2026-06-02: Brand gold hex drift** — SKILL.md design audit line says `#C9A84C`; style.css `--color-gold` ships `#8B6E24`. Confirm new gold + update SKILL.md, or revert. Likely intentional Phase A brand pivot — needs SKILL.md sync, not site revert | LOW (Adam) |
| **NEW 2026-06-01: Homepage MortgageBroker vs about LocalBusiness schema** — both schema.org-valid. MortgageBroker more specific. 2-file unification opportunity | LOW (Adam) |
| **NEW 2026-08-30: `blog.html` blog-index polish (2 items)** — CollectionPage `ItemList` `position` values are non-sequential (legacy 1–25 tail, then ~34 entries all `position: 1`); and `blog/manifest.json` titles the borrower Wait-for-Rates post "…Before You **Buy**?" while the page's `<title>`/`<h1>` say "…Before You **Sell and Buy**?". Both render, neither affects link equity. One-pass cleanup on Adam's word | LOW (Adam) |
| **NEW 2026-06-22: Homepage MortgageBroker JSON-LD carries personal NMLS 513013, not company NMLS 2653540** — displayed copy correct (both shown); decide whether business-entity schema should use company NMLS. Low confidence (broker entity named after originator may legitimately ref originator NMLS). Legal/NMLS identifier → Adam-gated | LOW (Adam) |
| ~~Homepage title pipe normalization~~ **STALE — CLOSED 2026-08-29.** HEAD title is `Self-Employed & Investor Mortgage Broker Austin TX \| Adam Styer` (63 chars, pipe-clean, no superlatives). No action needed | — |
| ~~/get-preapproved title~~ **RESOLVED 2026-06-09** — now "Fast Pre-Approval", pipes normalized. Hutto title keeps "Same-Day" per Decision 2(a) | — |
| SKILL.md infrastructure question — should BLOCKERS.md live inside `styerteam-mortgage-site/` instead of `loanos-clone/tasks/seo-sem/`? Current path triggers Vercel deploy of paused LoanOS product | LOW (Adam) |
| ~~Calculator P0-A 12× PITI bug~~ **FIXED 2026-06-09 + VERIFIED LIVE 2026-06-10** (`taxFreq === 'yr'` on served page) — RESOLVED | — |
| ~~NotebookLM advisor script MISSING (103 runs)~~ **RESOLVED 2026-08-31 — it was never missing.** Archived to `Documents/cowork/archive/loanos-home-root/scripts/notebook_advisor.py` during a cleanup; SKILL.md was never repointed. Path corrected, script confirmed to execute. **Auth is the sole remaining blocker** | — |
| PSI quota drained — **3rd consecutive live reconfirmation** 2026-09-04 (**HTTP 429**; prior: 08-31 PM). Netlify's own deploy record now offers a partial substitute (homepage Lighthouse: Perf 80/A11y 90/BP 100/SEO 100/PWA 40). Provision a dedicated API key/project or accept permanent UNVERIFIED for per-page mobile scores | HIGH (Adam) |
| Homepage/mobile CTAs route to `#contact-form` quick form (Q10). Open: whether to ALSO surface direct `/get-preapproved` or `/refinance-quote` path | MEDIUM (Adam) |
| Homepage body-copy references "92 Google reviews" + "45 Zillow reviews" + "5.0 ★" (lines 455/492/861) — awaiting Adam's "complicated income" trust-strip pivot copy | MEDIUM (Adam) |
| Suburb inline-form coverage **5/25** — only austin-area/buda/cedar-park/kyle/westlake carry an inline quick-quote form; other 20 (incl. Georgetown, now ranking #7) lack the `/thank-you`-form pattern. Batch worth approving but it's a form change (Adam gate) + needs n8n alert mapping for 20 new form-names. Sister-task (styer-suburb-editor-daily) domain | MEDIUM (Adam) |
| products.html 7 in-card "Get Pre-Approved" route to raw 1003 (**51 carries**; Adam decision) | MEDIUM |
| GSC URL Inspection sweep overdue — Round Rock, San Marcos, Hutto, Pflugerville, Leander (**30 carries**) | HIGH (Adam) |
| Complicated-income SERP gap — **Wk 16 RECOVERY: 4 of 5 gained/recovered** (self-emp **#1** blog, asset-depl **#1** ★, jumbo **#2**, 1099 **#3** best; bank-stmt **#8** lone decliner). **✅ Asset-depletion moat INTACT — back at #1; the Wk15 "#6" was WebSearch composition noise, not a real drop. Manual-verify flag CLEARED.** Remaining outright gaps: `/non-qm-loans.html` (CMRE #1) + `/dscr-loan-austin-tx.html` (Newfi #1). | MEDIUM |
| Hedged claims to verify (2026-05-17) — 8 items in `FLAG_FOR_ADAM.md` against current wholesale rate sheets | HIGH (Adam) |
| NotebookLM CLI auth expired (2026-05-11) — `notebooklm login` required | HIGH (Adam) |
| ~~Suburb quick-form Ads conversions~~ **FULLY FIXED 2026-06-18** — austin-area/buda/westlake forms were silently missing `action="/thank-you"` (only cedar-park/kyle had it); all 5 now route to /thank-you where the Ads conversion fires (commit `c519c4e`, verified live). Adam: (a) confirm counts rise in Ads UI; (b) check Netlify Forms for past austin-area-quote/buda-quote/westlake-quote submissions that may have come in without a matching conversion | MEDIUM (Adam) |
| 2026-04-27-why-home-prices-arent-crashing CTA structure + missing FAQPage schema (**42 carries**, paused per GOALS.md no-content policy) | MEDIUM |
| ~~Sitewide nav inconsistency~~ **CLOSED 2026-06-09 evening** — canonical header on 148 pages + 4 generator templates; bare-header sticky CSS bug fixed (blog scroll overlap) | — |
| Suburb/footer cosmetic carries (LOW) — footer `/prequal.html` parity, suburb `/calculators` linking gap, suburb `About Adam` standalone | LOW (Adam) |
| 5 of 6 new HNW/non-QM pages missing NMLS Consumer Access `sameAs` in Article schema | LOW |
| ~~SKILL.md drift, 3 items~~ **RESOLVED 2026-08-31 — corrected in-file after 3 runs of flagging.** (1) Step 3's retired "21-Day Avg. Close" trust bar → now verifies the claim's **absence**, matching GOALS.md; (2) 2 pointers to the deprecated `styermortgage-context.md` stub → `CONTEXT.md`; (3) the dead `notebook_advisor.py` path → the verified archived location. Backup at `~/.claude/scheduled-tasks/styer-site-daily/SKILL.md.bak-2026-08-31` (one `cp` restores). No logic, rotation, or hard constraint touched. **Adam: review or revert** | LOW (Adam) |
| **NEW 2026-08-31 — a new content cluster shipped with no GTM container; `prequal.html` still has the same defect.** The 7 referral/reverse-mortgage pages are fixed, but `/prequal.html` (200, indexed, GTM ×2, footer-linked from 20 pages) still fires **no `generate_lead`**, so its submissions never count as conversions. Not fixed here: form behaviour is Adam-gated, and there's a real either/or — (a) add the dataLayer push for parity with `/get-preapproved`, or (b) repoint the 20 footer links and keep the page for organic. **Adam: (a) or (b)** | MEDIUM (Adam) |
| **NEW 2026-08-20 — license-type copy contradicts the sitewide NMLS disclaimer.** 3 pages advertise Adam as a **"correspondent lender"** — `blog/local-lender-vs-online-lender-austin-central-texas.html` (×3, *"fund in my own name"*), `realtors.html` (×3), `blog/2026-03-27-down-payment-assistance-texas-2026.html` — while every page's own footer says *"Kyber Mortgage Corporation dba HyperSmart Home Loans is a **Licensed Mortgage Broker** in Texas."* **Upstream cause: the voice guide** (`loanos-clone/tasks/social-media/adam-voice-and-workflow.md`, last updated **2026-03-29**, pre-Kyber) instructs in 4 places that Adam is a correspondent lender "not a broker" — so every content agent regenerates the drift. Site is correct; the guide is stale. Agent cannot edit (paused `loanos-clone` + license-type copy is Adam-gated). Directly in scope for the pre-audit compliance cleanup | MEDIUM (Adam) |
| **NEW 2026-08-20 — `refinance-calculator.html` is robots-blocked but internally promoted.** Live 200, self-canonical, title + 144-char meta, 45KB (the richest refinance tool vs 12KB for break-even), featured on `calculators.html` as a primary `calc-card--solid` — and `robots.txt` carries `Disallow: /refinance-calculator.html`, grouped with `dashboard.html`/`ops.html`/`loanos.html` (internal admin). Earns zero organic traffic while absorbing internal link equity. **Not unblocked by the agent** — indexing is slow to reverse and a public cost-estimator is compliance-review surface. Either unblock it or drop the card | MEDIUM (Adam) |
| **NEW 2026-08-20 — disclaimer Variant C (5 pages) omits address + Texas Consumer Complaint Notice.** `calculator-payment`, `calculator-affordability`, `calculator-refinance-breakeven`, `rate-buydown-calculator`, `first-time-home-buyer` carry legal name + both NMLS IDs only; the other 33 long-form pages add principal address + Consumer Complaint Notice. Splits the calculator set inconsistently. Also Variant A says *"Equal Housing Opportunity"* vs Variant B *"Equal Housing **Lender**"* — not interchangeable for a licensed broker. Legal copy → Adam-gated. NMLS-ID coverage itself is **146/146**, verified live | LOW (Adam) |
| **NEW 2026-08-20 PM — did Adam found Kyber Mortgage Corporation? Agent assumed NO and shipped the fix.** `westlake` + `buda` asserted `"founder": {Adam Styer}` on the business node. Corrected to `employee` on the evidence of **77 pages** (`employee`/`worksFor`) vs **2**, both canonical `#adam-styer` nodes using `worksFor`, and `CLAUDE.md`/voice guide calling him Senior Loan Officer. **If Adam is in fact a founder/owner, invert it — and the other 77 pages are then the defect.** Corporate-role claim in structured data → confirm during compliance review | LOW (Adam) |
| ~~3 AEO entity conflicts on homepage↔about~~ **RESOLVED 2026-08-29.** Shipped in `342cb0d` and re-verified in `HEAD` by `styer-site-daily`: `Person.url` → `/about.html`, `worksFor` carries `legalName` + `@id` → `#business`. Item (3), no NMLS `identifier` on `#business`, was always uniform 0/4 = design, not a defect | — |
| **NEW 2026-08-20 PM — ad landing pages still sell the pre-pivot positioning.** GOALS repositions the site around complicated income; `/get-preapproved` carries only jumbo ×2 + wholesale ×1, and **`/refinance-quote` contains none of those tokens at all**. The homepage already leads with "Complex mortgage. Clear path forward." and names the full list in its first 150 words — so **paid traffic lands on weaker copy than organic**. Not rewritten: primary paid-traffic H1 is brand positioning + moves Ads Quality Score. **Re-measured 2026-08-31 PM on BOTH pages: `wholesale` ×1 is the only positioning token on either — zero self-employed / 1099 / bank statement / asset depletion / DSCR / non-QM.** The block is now specific rather than reflexive: **the agent cannot see the Google Ads copy these pages must message-match**, and rewriting an H1 blind risks ad relevance + quality score on live campaigns. **Adam: paste the headlines from the two main ad groups and both pages get rewritten in one pass** | MEDIUM (Adam) |
| ~~AggregateRating~~ **DROPPED 2026-08-20 PM (4th carry).** Homepage re-verified **0 occurrences — intentional.** Matches Adam's own memory note (self-serving review schema earns no SERP stars; real lever is GBP/Zillow off-page). No longer surfaced | — |
| **Scheduler reliability — now failing in BOTH directions.** 08-19: **two runs fired on the same date** (AM Wednesday + PM Thursday rotation). Previously: this task logged ~2 runs in the 26 days since 07-24 (only `daily-opt` commit in that window was 08-16). Prior modes: 07-23 task-specific no-fire; 07-19/07-21 no-fire; 07-20 fired-but-unlogged (`c675929`); 07-06→07-14 outage. Cross-check `git log --since=<last-log-date>` against run-log filenames AND whether sister tasks fired before sizing any outage | HIGH (Adam) |
| ~~Stale ~08-06 checkout overlay~~ **RESOLVED 2026-08-29.** In-flight `git am` was a superseded Aug-6 patch (CFO case study already live on main); overlay backed up to git stash `stale-overlay-2026-08-29-backup` + session scratchpad, am aborted, tree fast-forwarded to origin/main, six queued fixes shipped (`342cb0d`) + verified live. The 3 "genuinely novel" files (about/contact/testimonials 07-24 meta edits) remain recoverable from the stash if wanted | — |
| ~~(overlay escalation, for the record)~~ Ran 07-22 → 08-29 as the repo's top constraint: 52 dirty entries / 12 deletions, a stale ~08-06 snapshot masquerading as pending work, at its peak blocking six shippable one-line fixes. **Closed 08-29.** Lasting lesson: **an uncommitted overlay causes real data loss, not just blocked cosmetics** — `buy-before-you-sell-austin.html` was the one page the 08-25 GTM restore couldn't reach, and it ran with **zero analytics** until 08-29. When an overlay blocks a batch fix, record which files were skipped so the next sweep re-targets them | — || ~~thank-you.html performance claims~~ **RESOLVED 2026-06-09** per Decision 2(a) — now "one business day" phrasing | — |

## What's Next

**Next run = Saturday — off-rotation** (Step 4 rotation is Mon–Fri only).

Priorities (in order):
1. **`git fetch origin && git status -sb` BEFORE any analysis — non-negotiable.** This repo always has another agent mid-write — **assume behind, always.** If behind, `git pull --rebase` (commit your own files first if dirty).
2. **Parse HTML/JSON-LD with real parsers (`html.parser` / `json.loads`), never an attribute regex.** If a metric looks dramatic, re-derive it a second way before writing it down.
3. **Steps 1–2 non-negotiables** (sitemap/robots 200, conversion 10/10 HTML-token, no live form submissions — a real submit pages Adam). Full-path `/usr/bin/curl` with sandbox disabled — bare `curl` returned "command not found" this session despite being on `PATH`.
4. **PSI quota is now structurally gone, not just recurring — 4 consecutive confirmed-dead probes (08-31, 09-04 AM, 09-04 PM).** Netlify's deploy record substitute is now confirmed **mobile-scoped** via `lighthouse.details.formFactor` (Perf 79/A11y 90/BP 100/SEO 100 as of 09-04 PM) — use it as the standing partial substitute until a dedicated PSI key/project exists.
5. **Blog is 14 days stale and climbing** (last post 08-21) — re-check `/blog/manifest.json` freshness every run; flag to `styer-content-weekly` if it crosses 21 days, since that task's CHANGELOG activity (page edits) isn't the same as publishing a new post.
6. **Before calling a live-render-vs-source HTML diff a defect, check the platform's own API for ground truth first.** 09-04 AM: live HTML for contact/get-preapproved/refinance-quote was missing `netlify`/`netlify-honeypot` attributes present in the committed source — looked like a broken deploy. Netlify's own Forms API (via MCP) showed all 3 forms registered and actively capturing. Not a defect.
7. **A delegated JS event listener won't show up in a per-page HTML grep.** Verify tracking implementation by reading the JS source (e.g. `script.js`) for the event name, not just the rendered markup.
8. **For any sitewide check with a live equivalent, `curl` all 153 sitemap URLs directly rather than mapping back to local files** — sidesteps the `.html`-vs-extensionless normalization bug class. Proven clean 4 runs running (153/153 GTM, 0 JSON-LD errors, 0 dupes; internal-link graph 0 orphans/0 low-outbound).
9. **GOALS.md overrides SKILL.md wherever they conflict.** A rotation checklist is not authority to overwrite a brand decision or to make a page worse.
10. **The Netlify MCP (`netlify-project-services-reader` / `netlify-deploy-services-reader`) is available this session** — use `get-forms-for-project` and `get-deploy-for-site` for ground truth on form registration/submissions and deploy state, rather than inferring platform behavior from HTML alone.
## Site Structure (key categories)

| Category | Pages |
|----------|-------|
| Core | index, products, calculators, about, contact, blog, realtors, scenario |
| Loan programs | conventional, fha, va, jumbo, construction, investment, refinance, dscr-loan-austin-tx, self-employed-mortgage-austin, **non-qm-loans (hub)**, **bank-statement-loans**, **high-net-worth-mortgage**, **investor-loans** |
| DSCR cluster | dscr-loan-austin-tx, **dscr-loans-texas**, **dscr-loans-fredericksburg-tx**, **dscr-loans-dripping-springs** |
| Suburb SEO | 25 `*-mortgage-lender.html` pages |
| Rate check | hub + 5 city pages |
| Rate shopper blog | 10 posts |
| Resources | first-time-buyer-guide, glossary, how-to-buy, **scenarios (hub + template + 1 live)**, etc. |
| Deprioritized | usda (noindex, kept live) |

## Session Rules

- Voice: short punchy sentences, conversational, raw, no fluff. HNW = warm conversational, not cold private-banking.
- Business name: "Adam Styer | HyperSmart Home Loans" — never "The Styer Team" or "Mortgage Solutions LP". "Adam Styer Mortgage" is an INTENTIONAL colloquial alt-brand (per llms.txt + regression test) — not legacy drift.
- Match existing HTML/CSS patterns exactly when adding pages
- TX-licensed only — never imply Adam originates outside Texas
- No USDA origination — keep removing from product surfaces as found
- Loan application link: https://hypersmart.my1003app.com/513013/register?time=1779291829279 (anchor text only)
- Run blog title lint before publish: `grep "<title>" blog/*.html | grep -v "Adam Styer"`

## Session Protocol

Read `/Users/adamstyer/Documents/GOALS.md` first.

END OF SESSION: CONTEXT.md (replace 3 fields, <100 lines); CHANGELOG.md (append dated); TODO.md (mark done, add new); DECISIONS.md (only if real decision).
