# Daily Run — 2026-09-04 — Thursday rotation (Internal Linking + Funnel Flow)

Followed `TOMORROW_PRIORITY` from the 09-03 log. Clean audit — **0 site mutations.** The
session's real work was a thorough near-miss investigation on the funnel forms that ended in
"not a defect," plus a fresh discovery of an alternate PSI substitute.

## NOTEBOOKLM

Status: **Timed out (auth).** Re-probed live:

```json
{"error": true, "code": "ERROR", "message": "Authentication expired or invalid. ... Run 'notebooklm login' to re-authenticate."}
```

Same single blocker as every prior run.

## STEP 1 — SITEMAP + CORE PAGES

| Check | HTTP | Result |
|---|---|---|
| `sitemap.xml` | 200 | ✅ **153** `<loc>` / **153** `<lastmod>` / **0** duplicates |
| `robots.txt` | 200 | ✅ |
| `/` · `/get-preapproved` · `/refinance-quote` · `/thank-you` · `/blog` · `/contact` · `/calculators` · `/about` | 200 (via `-L`) | ✅ |

`git fetch origin && git status -sb` — tree already at `origin/main`, clean. Non-negotiable
precondition held again.

## STEP 2 — CONVERSION TRACKING

Status: **10/10 ✅.** HTML-token matrix (no live submits, per standing practice since 08-31),
identical to the 09-03 matrix:

| Page | GTM | generate_lead | lead_type | thank_you_page_view | tel: | h1 |
|---|---|---|---|---|---|---|
| `/` | 2 | — | — | — | 6 | 1 |
| `/get-preapproved` | 2 | 1 | `purchase_prequal` | — | 2 | 1 |
| `/refinance-quote` | 2 | 1 | `refi_quote` | — | 2 | 1 |
| `/thank-you` | 2 | — | — | 1 | 2 | 1 |

`phone_click` re-confirmed present in `script.js`'s delegated `a[href^="tel:"]` listener (1
occurrence), not per-page markup.

## STEP 3 — LANDING PAGE AUDIT

Nothing carried unresolved from 09-03 (tap-target closed last run). No re-audit needed per
"compare against yesterday's log."

## STEP 4 — THURSDAY ROTATION: INTERNAL LINKING + FUNNEL FLOW

**Built the full internal link graph from a live fetch of all 153 sitemap URLs** (not local-file
path mapping — the proven-safer method since 08-29/08-30's normalization bugs), parsed with
`html.parser`, not regex.

| Check | Result |
|---|---|
| Pages with <2 relevant outbound internal links | **0 / 153** |
| Orphans (0 inbound links from any sitemap page) | **0 / 153** |

Both bars the checklist asks for (2+ outbound per page; no orphaned indexed pages) hold clean
sitewide — no regression from the 08-30 fix (3 orphans → 0) or the 08-29 `buy-before-you-sell`
GTM restore.

**Funnel traced end to end:**
- Homepage → `/get-preapproved.html` **×5** (incl. `?intent=scenario&source=homepage_hero`,
  `homepage_scenario_section`, `homepage_sticky_mobile`) — all resolve.
- Homepage → `/refinance-quote` — **0 direct links**, confirmed by design (36 inbound from ad
  landing-page traffic elsewhere per 08-30 log; not re-flagged).
- `contact.html` — form POSTs to `/thank-you`, hidden `form-name=contact`, honeypot `bot-field`
  present, submit listener pushes `generate_lead` / `lead_type: contact_form` to `dataLayer`
  before native submission proceeds.
- `/thank-you` — Calendly widget (`calendly.com/adamstyer/15minutes`) present, `tel:` link
  present, "What Happens Next" 3-step block present (×3 matches), `thank_you_page_view` fires
  once, page carries `noindex` and is correctly absent from the sitemap.

**A near-miss investigated to the end, not surfaced as a defect.** Live-fetched HTML for
`contact.html`, `get-preapproved.html`, and `refinance-quote.html` is **missing the bare
`netlify` / `netlify-honeypot="bot-field"` attributes** that exist in the exact committed
source at the currently-deployed commit (`git show 6fc369c:contact.html` — the commit the
Netlify MCP confirms is the live, `ready`, production deploy — has them; the served HTML does
not). On its face this reads like a broken deploy that would silently drop three of the site's
highest-value lead forms. **Before writing that down, queried Netlify's own Forms API via MCP**
(`netlify-project-services-reader` → `get-forms-for-project`, site `a28d3f8a-…`): `contact`,
`get-preapproved`, and `refinance-quote` are all registered, and **`get-preapproved` received a
live submission today at 14:05 UTC** — hours before this run. Submissions are being captured
correctly right now. The most parsimonious explanation is that Netlify's build-time form
detection consumes its own marker attributes (`netlify`/`netlify-honeypot`) from the served
output once a form is registered — consistent with the live HTML's reordered, single-quoted
attributes (a serialization signature, not a manual edit) and with every tested form on the
site showing the identical pattern. **Not a defect. Would have been a fabricated HIGH — "forms
may be silently failing" — if the live-render diff had been reported without checking the
Forms API ground truth.**

## STEP 4A — PSI QUOTA RE-PROBE

Re-probed live (not carried on faith): `HTTP 429`, `"Quota exceeded for quota metric 'Queries'"`.
**Third consecutive confirmed-dead probe** (08-31 PM, 09-03 not probed, today). Per
`RECURRING_ISSUES` wording rule, tightening language: "drained" → "drained, reconfirmed twice
live, most recent 2026-09-04."

**New substitute source found:** the Netlify MCP's `get-deploy-for-site` response for the
current production deploy carries its own Lighthouse plugin summary — `Performance: 80,
Accessibility: 90, Best Practices: 100, SEO: 100, PWA: 40` (homepage `/` only, generated at
build time by Netlify's own Lighthouse plugin, not PSI). Not a full substitute (single page,
mobile-only scope unconfirmed, no historical trend), but a zero-cost data point on every deploy
while PSI stays drained. Logged to learnings for future runs to reference before calling PSI
data fully unavailable.

## STEP 4B — SEO/SEM BACKLOG (read-only)

`loanos-clone` **not mutated** — standing guard. `backlog.md` + `BLOCKERS.md` re-read: same 2
open, explicitly Adam-gated items as the last 5 runs (`products.html` 7-card 1003 routing —
MEDIUM_RISK; `/prequal.html` `generate_lead` parity — form-behavior decision). SEOSEM-001 stays
closed at evidence level per memory instruction, not re-measured. No un-gated ZERO_RISK/LOW_RISK
item exists.

## STEP 5 — DESIGN AUDIT

Spot-checked `contact.html` + `get-preapproved.html` (today's funnel-relevant pages, rotating
off yesterday's homepage/about): GTM ×2 both, 0 "21-day"/"24-48" occurrences, 0 legacy brand
strings (`The Styer Team`, `Mortgage Solutions LP`). Clean.

## RE-VERIFY GATE — claims checked live this run

- Sitemap 200 / 153-153-0: still true, re-measured.
- Conversion 10/10: still true, re-measured.
- Orphans 0/153, <2-outbound 0/153: still true, re-measured via full live-fetch graph (not
  carried from 08-30's fix).
- PSI quota drained: still true, **3rd live reconfirmation**, HTTP 429.
- SEOSEM-001 (program abandoned): not re-measured per memory instruction — stays closed.
- Netlify Forms capture on contact/get-preapproved/refinance-quote: **checked against ground
  truth (Forms API), not assumed** — confirmed working, including a submission today.

## METRICS

- Mobile PageSpeed `/get-preapproved`: **UNVERIFIED — quota drained (3rd confirmed-dead probe,
  2026-09-04)**
- Mobile PageSpeed `/refinance-quote`: same, UNVERIFIED
- Homepage Lighthouse (Netlify build plugin, alt. source): Perf 80 / A11y 90 / Best Practices
  100 / SEO 100 / PWA 40
- Google Ads Optimization Score: not this rotation (Monday item)
- Conversion Tracking: **10/10**
- Landing Page Mobile UX: **10/10** (nothing carried unresolved)
- Internal Linking: **153/153** pages ≥2 outbound, **0/153** orphans
- SEO Coverage: **10/10** (unchanged from 09-03 sweep, not re-run — no content shipped since)

## RECURRING_ISSUES (same issue 2+ runs)

- NotebookLM CLI auth expired — HIGH (Adam) — the single NLM blocker
- **PSI quota drained — HIGH (Adam) — 3rd consecutive live-confirmed dead probe (08-31 PM,
  09-04). Netlify's own Lighthouse plugin summary is a partial substitute (homepage only) until
  a dedicated API key/project resolves this.**
- GSC URL Inspection sweep overdue — HIGH (Adam) — gates all open P4 backlog items
- Blog stale — MEDIUM — owned by `styer-content-weekly` (note: that task shows a 09-04 in-flight
  CHANGELOG entry on `investor-loans.html`, so it is active, just not necessarily publishing a
  new *post*)
- Ad LP positioning mismatch — MEDIUM (Adam) — needs ad-group headlines
- Suburb inline-form coverage 5/25 — MEDIUM (Adam form-gate)
- `products.html` 7-card 1003 routing — MEDIUM (Adam)
- `prequal.html` `generate_lead` parity — MEDIUM (Adam form-gate)
- Broker vs correspondent-lender + stale voice guide — MEDIUM (Adam)
- Indexed titles > 65 chars (61, per 09-01 measurement) — LOW (Adam) — deliberate no-op
- Gold hex drift `#C9A84C` vs `#8B6E24` — LOW (Adam) — both tokens in real use
- Hedged claims to verify vs wholesale rate sheets — 8 items — HIGH (Adam)

## NOTEBOOK_INSIGHTS (cache)

- (carried) NotebookLM auth is the sole blocker on the NLM strategy query path.
- (carried) `git fetch` before analysis is a hard precondition — now 5-for-5.
- (carried) Parse HTML/JSON-LD with real parsers, never attribute regex.
- (carried) A delegated event listener is invisible to a per-page grep — check the JS source.
- (carried) Live-fetching all 153 sitemap URLs sidesteps the local-path normalization bug class.
- **New this run:** a live-render vs. committed-source HTML diff is not evidence of a broken
  deploy by itself — check the platform's own API (here, Netlify's Forms API) for ground truth
  before calling something broken. Netlify's build-time form detection appears to consume its
  own marker attributes (`netlify`, `netlify-honeypot`) from the served HTML once a form is
  registered; a missing attribute in curl output is expected behavior, not a regression, as long
  as the Forms API shows the form registered and receiving submissions.
- **New this run:** when PSI is quota-drained, the Netlify MCP's deploy record carries a
  Lighthouse plugin summary for the homepage as a zero-cost, always-available (if partial)
  substitute data point — check `get-deploy-for-site` before calling Core Web Vitals fully dark.
- **New this run:** the Netlify MCP (`netlify-project-services-reader` /
  `netlify-deploy-services-reader`) is available in this session and gives read access to
  registered forms, submission counts/timestamps, and full deploy metadata (commit ref, state,
  Lighthouse scores) — a faster ground-truth path than inferring platform state from HTML alone
  for anything Forms- or deploy-related.

## TOMORROW_PRIORITY

Next run = **Friday rotation (Content Planning + AEO Review)** if the log cursor holds:

1. `git fetch origin && git status -sb` before any analysis — non-negotiable, now 5-for-5.
2. Parse HTML/JSON-LD with real parsers, never an attribute regex.
3. Steps 1–2 non-negotiables (sitemap/robots/core 200; conversion 10/10 HTML-token; no live
   form submissions).
4. **Friday rotation:** check `/blog` freshness (>7 days = flag to `styer-content-weekly`);
   audit blog CTA coverage; AEO content audit on 2 posts (answer-first FAQ, question-phrased
   H2s, extractable summary near the top).
5. **Before calling any live-render vs. source difference a defect, check the platform's own
   API for ground truth first** (Netlify Forms/deploy state here) — today's near-miss would
   have been a fabricated HIGH otherwise.
6. Re-probe PSI — 3 consecutive dead probes now; if a 4th run confirms dead, this is worth
   escalating in wording from "recurring" to "structurally gone without Adam's key."
7. Re-run the sitewide GTM/JSON-LD live-fetch sweep only if content shipped since 09-03 (it
   hasn't been re-run today since nothing changed).

## FLAG_FOR_ADAM

### Net new this run:

- **Nothing new requiring your action.** Today's investigation (the missing `netlify` attribute
  in the live HTML of your contact/pre-approval/refinance forms) looked like a real problem at
  first glance but checked out clean against Netlify's own form records — your forms are
  registered and working; one even got a real submission today. No fix needed.

### Standing — unchanged, still yours:

- **Highest-leverage 2 minutes: run `notebooklm login` once in a terminal.** Same as every
  prior run.
- PageSpeed Insights quota — confirmed dead for the 3rd time (08-31 PM, today). Worth a
  dedicated API key/project if you want fresh mobile Core Web Vitals numbers again; until then
  I have a partial substitute (Netlify's own homepage Lighthouse score) but not per-page data.
- GSC URL Inspection sweep — still overdue, still gates 7 backlog items.
- Ad landing page positioning — still needs your ad-group headlines pasted in to rewrite safely.

## SELF-REVIEW GATE

Self-review: **PASS** — **0 files changed.** No site mutations this run; the funnel/link-graph
audit and the Netlify Forms investigation were both read-only. `loanos-clone` unmutated.
Commit isolation: dirty-count parity 0 → 0 (nothing touched).
