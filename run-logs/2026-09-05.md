# Daily Run — 2026-09-05 (Saturday — off-rotation)

Clean audit — **0 site mutations.** Saturday has no assigned Step 4 weekday rotation (Mon–Fri
only), per 09-04 PM's own `TOMORROW_PRIORITY`. Ran non-negotiables + Step 4B backlog read-only +
a design spot-check on 2 pages not recently checked.

## NOTEBOOKLM

Status: **Auth still dead**, re-probed live:

```json
{"error": true, "code": "ERROR", "message": "Authentication expired or invalid. ... Run 'notebooklm login' to re-authenticate."}
```

Same single blocker as every prior run. Using cached `NOTEBOOK_INSIGHTS` below.

## STEP 1 — GIT PRECONDITION + SITEMAP + CORE PAGES

`git fetch origin && git status -sb` — tree already at `origin/main`, clean (precondition holds).

**Found and cleared one stray artifact before analysis:** an untracked `CONTEXT 2.md` (macOS-style
conflict-copy filename, timestamped 20:55 on 09-04 — 3 hours older than the tracked `CONTEXT.md`'s
23:44 write). Diffed it against the real file before touching it: it was a strict subset — missing
the 09-04 PM entry and the intake-release note, and still carrying older changelog entries the
current file had already trimmed into `CHANGELOG.md`. No unique content, so removed rather than
left to accumulate (memory note: 7 of these `* 2.md` copies were previously found piling up
unaddressed in `loanos-clone/tasks/`). Untracked file, not a `git rm` — no commit needed.

| Check | HTTP | Result |
|---|---|---|
| `sitemap.xml` | 200 | ✅ **154** `<loc>` / **154** `<lastmod>` / **0** duplicates |
| `robots.txt` | 200 | ✅ |
| `/` · `/get-preapproved` · `/refinance-quote` · `/thank-you` · `/blog` · `/contact` · `/calculators` · `/about` | 200 (`-L`, full-path curl) | ✅ |

Sitemap count moved **153 → 154** since Friday PM — one page added by another agent between runs
(not investigated further; 0 dupes and lastmods all present, so not a defect on its face). Full-path
`/usr/bin/curl` worked without issue this session (last session's bare-`curl` quirk did not recur).

## STEP 2 — CONVERSION TRACKING

Status: **10/10 ✅.** HTML-token matrix, identical shape to prior runs:

| Page | GTM | generate_lead | lead_type | thank_you_page_view | tel: | h1 |
|---|---|---|---|---|---|---|
| `/` | 2 | — | — | — | 6 | 1 |
| `/get-preapproved` | 2 | 1 | `purchase_prequal` | — | 2 | 1 |
| `/refinance-quote` | 2 | 1 | `refi_quote` | — | 2 | 1 |
| `/thank-you` | 2 | — | — | 1 | 2 | 1 |

No live submits, per standing practice since 08-31.

## STEP 3 — LANDING PAGE AUDIT

Nothing carried unresolved from the 09-04 PM log. No re-audit needed per "compare against
yesterday's log."

## STEP 4 — OFF-ROTATION (Saturday)

No Step 4 rotation item due. Per `TOMORROW_PRIORITY`, ran Step 4B backlog + a design spot-check
instead.

## STEP 4A — PSI QUOTA RE-PROBE

```
429 Quota exceeded for quota metric 'Queries' and limit 'Queries per day'
```

**5th consecutive confirmed-dead probe** (08-31 PM, 09-04 AM, 09-04 PM, 09-05). Per 09-04 PM's own
instruction, this is confirmatory only — the wording already escalated to "structurally gone
without Adam's key" last run; not re-escalating further on an identical result.

## STEP 4B — SEO/SEM BACKLOG (read-only)

`loanos-clone` **not mutated** — standing guard. `backlog.md` + `BLOCKERS.md` re-read: same 2 open,
Adam-gated items as the last 7 runs (`products.html` 7-card 1003 routing; `/prequal.html`
`generate_lead` parity). SEOSEM-001 stays closed at the evidence level per standing memory
instruction — not re-measured.

## STEP 5 — DESIGN AUDIT

Rotated to `products.html` + `realtors.html` — both un-checked in recent spot-check history
(09-03 was homepage/about, 09-04 AM contact/get-preapproved, 09-04 PM blog/calculators).

| Page | GTM | Legacy brand strings | "21-day"/"24-48hr" | NMLS personal (513013) | NMLS company (2653540) | "correspondent lender" |
|---|---|---|---|---|---|---|
| `/products` | 2 | 0 | 0 | 14 | 3 | 0 |
| `/realtors` | 2 | 0 | 0 | 6 | 3 | **3** |

`products.html` clean. `realtors.html`'s 3 "correspondent lender" mentions are the **standing,
already-flagged** license-type/broker-vs-correspondent defect (first found 08-20, upstream cause
is the stale voice guide) — re-confirmed present at the same count, not a new finding, not fixed
here (Adam-gated per that blocker's own note).

## RE-VERIFY GATE — claims checked live this run

- Sitemap 200 / now 154-154-0: re-measured, count change noted, not a regression.
- Conversion 10/10: still true, re-measured.
- PSI quota drained: still true, **5th live reconfirmation**, HTTP 429.
- `realtors.html` correspondent-lender drift (×3): still true, re-measured at the same count as
  last check — no new information, standing Adam-gated item.
- SEOSEM-001 (program abandoned): not re-measured per memory instruction — stays closed.
- Step 4B backlog: re-read, same 2 open Adam-gated items, no un-gated work available (8th run
  same conclusion).

## METRICS

- Mobile PageSpeed `/get-preapproved` / `/refinance-quote`: **UNVERIFIED — quota drained, 5th
  confirmed-dead probe, 2026-09-05**
- Conversion Tracking: **10/10**
- Landing Page Mobile UX: **10/10** (nothing carried unresolved)
- SEO Coverage: **10/10** (unchanged, no content shipped since 09-04 PM)

## RECURRING_ISSUES (same issue 2+ runs)

- NotebookLM CLI auth expired — HIGH (Adam) — the single NLM blocker
- PSI quota — HIGH (Adam) — structurally gone without a dedicated key/project, 5 consecutive dead
  probes now
- Blog stale — MEDIUM — last checked 14 days (09-04 PM); not re-measured this run since no Step 4
  content rotation was due today; will re-check on next weekday run
- GSC URL Inspection sweep overdue — HIGH (Adam) — gates all open P4 backlog items
- Ad LP positioning mismatch — MEDIUM (Adam) — needs ad-group headlines
- Suburb inline-form coverage 5/25 — MEDIUM (Adam form-gate)
- `products.html` 7-card 1003 routing — MEDIUM (Adam)
- `prequal.html` `generate_lead` parity — MEDIUM (Adam form-gate)
- Broker vs correspondent-lender + stale voice guide (`realtors.html` ×3, re-confirmed today) —
  MEDIUM (Adam)
- Indexed titles > 65 chars (61, per 09-01 measurement) — LOW (Adam) — deliberate no-op
- Gold hex drift `#C9A84C` vs `#8B6E24` — LOW (Adam) — both tokens in real use
- Hedged claims to verify vs wholesale rate sheets — 8 items — HIGH (Adam)
- `refinance-calculator.html` robots-blocked but internally promoted — MEDIUM (Adam) — unchanged

## NOTEBOOK_INSIGHTS (cache)

- (carried) NotebookLM auth is the sole blocker on the NLM strategy query path.
- (carried) `git fetch` before analysis is a hard precondition.
- (carried) Parse HTML/JSON-LD with real parsers, never attribute regex.
- (carried) A delegated event listener is invisible to a per-page grep — check the JS source.
- (carried) Live-fetching all sitemap URLs sidesteps the local-path normalization bug class.
- (carried) A live-render vs. committed-source HTML diff is not evidence of a broken deploy by
  itself — check the platform's own API (Netlify Forms) for ground truth first.
- (carried) Netlify deploy record's `lighthouse.details.formFactor` confirms mobile scope for the
  build-time Lighthouse substitute.
- **New this run:** an untracked `filename 2.ext` sitting next to a git-tracked file is a stale
  conflict-copy artifact, not in-progress work — diff it against the tracked file before deciding;
  if it's a strict subset (older, already-superseded content), it's safe to remove without a
  commit. Don't let these accumulate silently the way they did in `loanos-clone/tasks/`.

## TOMORROW_PRIORITY

Next run = **Sunday — off-rotation** (Step 4 rotation is Mon–Fri only), unless the scheduler skips
to Monday.

1. `git fetch origin && git status -sb` before any analysis — still non-negotiable.
2. Parse HTML/JSON-LD with real parsers, never an attribute regex.
3. If Monday's run lands, that's the **Schema + AEO Entity Audit** rotation — also the weekly
   Google Ads strategy NotebookLM query day (will still be blocked on auth).
4. Re-check blog freshness on the next weekday run — it was 14 days stale as of 09-04 PM and no
   content shipped since; if it crosses 21 days, escalate directly rather than just re-flagging to
   `styer-content-weekly`.
5. Investigate the 153→154 sitemap delta only if it recurs unexplained; a single-page bump from a
   sibling task (suburb editor / content weekly) between runs is expected, not a defect.

## FLAG_FOR_ADAM

Nothing new this run. Standing items unchanged from 09-04 PM (NotebookLM login, PSI quota, GSC
sweep, ad LP positioning headlines) — see RECURRING_ISSUES above for current detail on each.

## SELF-REVIEW GATE

Self-review: **PASS** — **0 site files changed** on `styerteam-mortgage-site`. One untracked,
already-superseded artifact (`CONTEXT 2.md`) removed outside the repo's tracked tree — no commit
needed, no site content touched. `loanos-clone` unmutated (read-only backlog check). Commit
isolation: dirty-count parity 0 → 0.
