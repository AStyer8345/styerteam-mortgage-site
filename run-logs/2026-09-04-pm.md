# Daily Run — 2026-09-04 (PM fire) — Friday rotation (Content Planning + AEO Review)

Note on filename: `run-logs/2026-09-04.md` already exists from an earlier fire today logged as
"Thursday rotation" (the scheduler's known cross-midnight/double-fire quirk, tracked in
Active Blockers as "Scheduler reliability"). That log's own `TOMORROW_PRIORITY` pointed to the
Friday rotation, which is what this run executed — so this file is named `-pm` to avoid
clobbering it rather than guessing at a different calendar date.

Clean audit — **0 site mutations.** Followed the prior log's cursor (Friday rotation) and its
`TOMORROW_PRIORITY` checklist item-by-item.

## NOTEBOOKLM

Status: **Auth still dead**, re-probed live (not carried on faith):

```json
{"error": true, "code": "ERROR", "message": "Authentication expired or invalid. ... Run 'notebooklm login' to re-authenticate."}
```

Same single blocker as every prior run. Using cached `NOTEBOOK_INSIGHTS` below.

## STEP 1 — SITEMAP + CORE PAGES

`git fetch origin && git status -sb` — tree already at `origin/main`, clean. Precondition holds.

`curl` was not resolvable via `which`/bare invocation this session (`command not found`) despite
existing at `/usr/bin/curl` on `PATH` — worked when invoked by full path `/usr/bin/curl`. Noting
this as a minor environment quirk for future runs in case it recurs.

| Check | HTTP | Result |
|---|---|---|
| `sitemap.xml` | 200 | ✅ **153** `<loc>` / **153** `<lastmod>` / **0** duplicates |
| `robots.txt` | 200 | ✅ |
| `/` · `/get-preapproved` · `/refinance-quote` · `/thank-you` · `/blog` · `/contact` · `/calculators` · `/about` | 200 (`-L`, full-path curl) | ✅ |

## STEP 2 — CONVERSION TRACKING

Status: **10/10 ✅.** HTML-token matrix, identical to the 09-04 AM matrix:

| Page | GTM | generate_lead | lead_type | thank_you_page_view | tel: | h1 |
|---|---|---|---|---|---|---|
| `/` | 2 | — | — | — | 6 | 1 |
| `/get-preapproved` | 2 | 1 | `purchase_prequal` | — | 2 | 1 |
| `/refinance-quote` | 2 | 1 | `refi_quote` | — | 2 | 1 |
| `/thank-you` | 2 | — | — | 1 | 2 | 1 |

No live submits, per standing practice since 08-31.

## STEP 3 — LANDING PAGE AUDIT

Nothing carried unresolved from the prior (AM) log — tap-target and all other checklist items
closed as of 09-03/09-04. No re-audit needed per "compare against yesterday's log."

## STEP 4 — FRIDAY ROTATION: CONTENT PLANNING + AEO REVIEW

**Blog freshness:** last post is still `2026-08-21-veteran-asset-depletion-mortgage-case-study.html`
(confirmed via live `blog/manifest.json`, 59 posts total, sorted by date). That's **14 days
stale** against the 7-day threshold — up from 10 days when first flagged 08-31, and still
unresolved. Per SKILL.md, this is a flag to `styer-content-weekly`, not a fix this task makes
itself (styer-site-daily doesn't author new posts). Re-flagging with the updated (worse) number.

**Blog CTA coverage — full sweep, all 59 posts fetched live (not sampled):**

| Set | Count | Result |
|---|---|---|
| Standard blog posts (`/blog/*.html`) | 55 | **55/55** link to `/get-preapproved` or `/refinance-quote` |
| Realtor-updates (`/realtor-updates/*.html`) | 4 | **4/4** carry a conversion CTA *and* a realtor-facing link (correct pattern — not defects, per 07-17 precedent) |

**59/59 total, 0 missing.** Matches the 08-31 baseline (59/59), no regression.

**AEO content audit — 2 posts** (newest + one comparison, `html.parser`/`json.loads`, not regex):

1. **`2026-08-21-veteran-asset-depletion-mortgage-case-study.html`** (newest, narrative case
   study): No FAQPage schema — **correct**, this is a case-study narrative, not a Q&A page (same
   conclusion as 08-31's audit of this exact post). First substantial paragraph opens with a
   clear, concrete setup ("A conventional mortgage was not the best fit, so we explored a non-QM
   ..."), second paragraph gives the extractable definition of asset-depletion income. Passes.
2. **`2026-08-18-jumbo-loan-requirements-texas-2026.html`**: FAQPage present, **5/5 questions**,
   **0/5 hedged answer openings** (checked against `it depends` / `that depends` / `generally
   speaking` / `in most cases` — none match; even the "no single published answer" answer states
   the direct fact rather than hedging). First paragraph is a clear, on-brand extractable hook
   ("I'm Adam Styer, mortgage broker in Austin, NMLS #513013... The part that decides whether
   your file closes is the qualifying side"). Passes.

Both posts: **0 AEO defects found.**

## STEP 4A — PSI QUOTA RE-PROBE

Re-probed live directly against the PageSpeed Insights API (not the harness's cached memory):

```
429 Quota exceeded for quota metric 'Queries' and limit 'Queries per day'
```

**4th consecutive confirmed-dead probe** (08-31 PM, 09-04 AM, 09-04 PM/this run). Per the AM
log's own `TOMORROW_PRIORITY`: "if a 4th run confirms dead, this is worth escalating in wording
from 'recurring' to 'structurally gone without Adam's key.'" Escalating accordingly below.

**Netlify Lighthouse substitute, re-pulled via MCP** (`get-projects` → `get-deploy-for-site` on
the current production deploy, id `6a9b76f5b7069100092b0660`, commit `cef775b`, the AM run's own
push): `Performance: 79, Accessibility: 90, Best Practices: 100, SEO: 100, PWA: 40`. **New
clarification vs. yesterday's note:** the deploy record's `lighthouse.details.formFactor` field
explicitly reads `"mobile"` — so this substitute IS mobile-scoped (yesterday's log flagged that
as "unconfirmed"). Still homepage-only, still no historical trend, but one open question closed.
Performance ticked 80→79 vs. the AM pull — within normal Lighthouse run-to-run noise, not a
regression worth acting on.

## STEP 4B — SEO/SEM BACKLOG (read-only)

`loanos-clone` **not mutated** — standing guard. `backlog.md` + `BLOCKERS.md` re-read: same 2
open, Adam-gated items as the last 6 runs (`products.html` 7-card 1003 routing; `/prequal.html`
`generate_lead` parity). SEOSEM-001 stays closed at the evidence level per standing memory
instruction — not re-measured.

## STEP 5 — DESIGN AUDIT

Rotated to `blog.html` + `calculators.html` (funnel/content-adjacent pages, different from
09-03's homepage/about and 09-04 AM's contact/get-preapproved): GTM ×2 both, 0 "21-day"/"24-48"
occurrences, 0 legacy brand strings (`The Styer Team`, `Mortgage Solutions LP`) on either. Clean.
`refinance-calculator.html` is still referenced from `calculators.html` — matches the standing
robots-blocked-but-promoted blocker (08-20), no change, not re-flagged as new.

## RE-VERIFY GATE — claims checked live this run

- Sitemap 200 / 153-153-0: still true, re-measured.
- Conversion 10/10: still true, re-measured.
- Blog freshness: **worsened** (10d → 14d), re-measured against live manifest, re-flagged with
  updated number rather than carried stale.
- Blog CTA coverage 59/59: still true, re-measured via full live fetch of all 59 posts (not
  sampled).
- PSI quota drained: still true, **4th live reconfirmation**, HTTP 429 — wording escalated per
  the AM log's own instruction.
- SEOSEM-001 (program abandoned): not re-measured per memory instruction — stays closed.

## METRICS

- Mobile PageSpeed `/get-preapproved` / `/refinance-quote`: **UNVERIFIED — quota drained, 4th
  confirmed-dead probe, 2026-09-04 PM**
- Homepage Lighthouse (Netlify build plugin, mobile-confirmed): Perf 79 / A11y 90 / Best Practices
  100 / SEO 100 / PWA 40
- Conversion Tracking: **10/10**
- Landing Page Mobile UX: **10/10** (nothing carried unresolved)
- Blog CTA Coverage: **59/59**
- AEO (2 posts audited): **0 defects**
- SEO Coverage: **10/10** (unchanged, no content shipped since 09-04 AM)

## RECURRING_ISSUES (same issue 2+ runs)

- NotebookLM CLI auth expired — HIGH (Adam) — the single NLM blocker
- **PSI quota — now HIGH (Adam), reworded: structurally gone without a dedicated key/project.**
  4 consecutive live-confirmed dead probes (08-31 PM, 09-04 AM, 09-04 PM). Netlify's own
  Lighthouse plugin summary (now confirmed mobile-scoped) remains a partial, homepage-only
  substitute.
- **Blog stale — MEDIUM, worsening.** 14 days since last post (08-21), up from 10 days on 08-31.
  Owned by `styer-content-weekly`, not this task — flagging the updated number.
- GSC URL Inspection sweep overdue — HIGH (Adam) — gates all open P4 backlog items
- Ad LP positioning mismatch — MEDIUM (Adam) — needs ad-group headlines
- Suburb inline-form coverage 5/25 — MEDIUM (Adam form-gate)
- `products.html` 7-card 1003 routing — MEDIUM (Adam)
- `prequal.html` `generate_lead` parity — MEDIUM (Adam form-gate)
- Broker vs correspondent-lender + stale voice guide — MEDIUM (Adam)
- Indexed titles > 65 chars (61, per 09-01 measurement) — LOW (Adam) — deliberate no-op
- Gold hex drift `#C9A84C` vs `#8B6E24` — LOW (Adam) — both tokens in real use
- Hedged claims to verify vs wholesale rate sheets — 8 items — HIGH (Adam)
- `refinance-calculator.html` robots-blocked but internally promoted — MEDIUM (Adam) — unchanged

## NOTEBOOK_INSIGHTS (cache)

- (carried) NotebookLM auth is the sole blocker on the NLM strategy query path.
- (carried) `git fetch` before analysis is a hard precondition.
- (carried) Parse HTML/JSON-LD with real parsers, never attribute regex.
- (carried) A delegated event listener is invisible to a per-page grep — check the JS source.
- (carried) Live-fetching all sitemap URLs sidesteps the local-path normalization bug class.
- (carried) A live-render vs. committed-source HTML diff is not evidence of a broken deploy by
  itself — check the platform's own API (Netlify Forms) for ground truth first.
- **New this run:** the Netlify deploy record's `lighthouse.details.formFactor` field confirms
  the build-time Lighthouse summary is mobile-scoped — closes the "scope unconfirmed" caveat
  from 09-04 AM. Use this field directly rather than assuming/inferring scope next time.
- **New this run:** `curl` was unresolvable via bare invocation/`which` this session despite
  being on `PATH` at `/usr/bin/curl` — full-path invocation (`/usr/bin/curl`) worked
  immediately. If a future run hits `command not found: curl`, try the full path before assuming
  network tooling is broken.

## TOMORROW_PRIORITY

Next run = **Saturday — off-rotation** (Step 4 rotation is Mon–Fri only) if the log cursor
holds: run non-negotiables (sitemap/robots/core 200, conversion 10/10), Step 4B backlog
read-only, and a design spot-check on 2 pages not recently checked (candidates: `products.html`,
`realtors.html`, or a suburb page). No Step 4 rotation item is due.

1. `git fetch origin && git status -sb` before any analysis — still non-negotiable.
2. Parse HTML/JSON-LD with real parsers, never an attribute regex.
3. Re-probe PSI one more time for a 5th data point, but treat it as confirmatory only — the
   escalation already happened this run; further identical probes shouldn't re-escalate wording.
4. If content ships on the blog between now and the next run, re-run the CTA/AEO sweep; otherwise
   09-04's 59/59 + 0-defect result stands without re-fetching.
5. Blog freshness now 14 days and climbing — worth a direct one-line note to Adam if it crosses
   21 days, since `styer-content-weekly`'s in-flight CHANGELOG activity (investor-loans.html edit)
   isn't the same thing as a new post shipping.

## FLAG_FOR_ADAM

### Net new this run:

- **PageSpeed Insights quota is now confirmed dead 4 times running** (08-31 PM, 09-04 AM, 09-04
  PM). This has moved from "recurring hiccup" to "structurally unavailable without a fix on your
  end." A dedicated Google Cloud API key/project for PSI would restore fresh per-page mobile
  Core Web Vitals; until then, all I can offer is Netlify's own homepage-only Lighthouse score
  (confirmed mobile this run: Perf 79 / A11y 90 / BP 100 / SEO 100).
- **Blog is now 14 days stale** (last post 08-21), up from 10 days when first flagged. This is
  `styer-content-weekly`'s lane, not mine to fix, but the gap is widening — worth a check on
  whether that task is actually publishing new posts or just editing existing pages.

### Standing — unchanged, still yours:

- **Highest-leverage 2 minutes: run `notebooklm login` once in a terminal.** Same as every
  prior run.
- GSC URL Inspection sweep — still overdue, still gates 7 backlog items.
- Ad landing page positioning — still needs your ad-group headlines pasted in to rewrite safely.

## SELF-REVIEW GATE

Self-review: **PASS** — **0 files changed** on the site. No site mutations this run; every check
(sitemap, conversion, blog CTA/AEO sweep, PSI re-probe, design spot-check) was read-only.
`loanos-clone` unmutated. Commit isolation: dirty-count parity 0 → 0 (nothing touched).
