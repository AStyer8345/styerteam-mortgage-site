## 2026-06-08 — Competitive Wk 14: Query-Variant Discriminator on Re-Verify + Honest Ad-Landscape Limitation + Westlake TX-Disambiguation Finally Resolved + Phase A Compounds (65th Dead NotebookLM Run)

### Patterns

- **Query-variant is a re-verify discriminator, not a ranking change — re-run the EXACT prior query before declaring a drop.** This run's first jumbo check used "jumbo mortgage **lender** austin tx" and Styer did NOT appear in the returned set — looked like a downgrade from Wk 13's #2. Re-running the EXACT Wk 13 query "jumbo mortgage austin tx" surfaced Styer #2 (`/loans/jumbo.html`) cleanly. Adding/removing a single word ("lender") returns a materially different organic set. Reusable rule: when a re-verify appears to show a competitor/ranking drop, the FIRST hypothesis is query-variant drift, not a real SERP move. Always re-run the byte-identical prior query before writing "DOWNGRADED" into the re-verify gate. Cost: 1 extra search. Benefit: prevents false-downgrade noise from polluting the weekly report and the master growth log (which downstream daily-briefing amplifies).

- **WebSearch cannot see Google paid ads — state the limitation, never fabricate ad counts.** Step 3 (Google Ads landscape) asks for advertiser counts + ad copy. WebSearch returns organic-only. Prior reports that listed "N advertisers above organic" were not programmatically verifiable from this tooling. Correct autonomous-run output: name the limitation explicitly, infer what the organic composition implies (directory saturation on head terms = high paid competition; thin specialist field on long-tail = organic-winnable), and recommend a manual logged-in SERP or paid SEM tool (SpyFu/SEMrush) if Adam wants a real paid read. Reusable rule: when a SKILL step requires data a tool category structurally cannot return, the honest move is to flag the gap + offer the manual path — not to emit a plausible-looking number that the re-verify gate will then carry forward as "fact."

- **A suburb that's been "off-rotation, can't disambiguate to TX" can resolve once a dedicated page exists.** Westlake was dropped from rotation at Wk 11 (2026-05-18) because the SERP returned California "Westlake Village" results and didn't disambiguate to TX. This run, "westlake tx mortgage lender" returned Styer #6 via the dedicated `/westlake-mortgage-lender` page — first-ever TX-disambiguated top-10. The CA noise is still present (westlakemrtg, Westlake Mortgage Group, Dignified = CA) but Google now ranks the TX-specific page for the TX-qualified query. Reusable rule: re-test "permanently off-rotation due to ambiguous SERP" suburbs once a dedicated page ships — the page itself can become the disambiguation signal. Don't treat "can't measure" as permanent.

- **Phase A compounds week-over-week, not just in the post-skip catch-up.** Wk 13's 2/7→5/7 flip was easy to dismiss as a Wk-12-skip artifact (two cycles of work measured at once). Wk 14 (clean 7-day gap) shows continued movement WITHIN the 5: self-employed #3→#2, bank statement #6→#4. The repositioning is still climbing on a normal weekly cadence, not a one-time absorption. self-employed #2 now sits directly behind only LendFriend. Lesson: credit the trend, not just the jump — Phase A is an active climb, and the LendFriend schema wedge is the lever to convert #2→#1.

- **LendFriend schema wedge held a 3rd consecutive measurement (binary, Adam-controlled).** Re-fetched `/self-employed-home-loans`: 9-Q FAQ visible but NO JSON-LD FAQPage, NO AggregateRating, no review count (5/5 shown visually only). Same as Wk 11 + Wk 13. Adam's cluster already has FAQPage schema; the only blocker is the AggregateRating policy decision (verified count vs removed). This is the single highest-confidence competitive move and it's been a stable open lever for 3 weeks — worth escalating from "carry" to "do this and you pass #1 on self-employed."

- **NotebookLM 65th consecutive dead run — advisor script is now ABSENT, not just auth-expired.** Prior runs reported "auth expired since 2026-05-11." This run confirms `/Users/adamstyer/loanos/scripts/notebook_advisor.py` no longer exists (stale `loanos/` repo per CLAUDE.md). The failure mode escalated from "login required" to "script gone." The SKILL.md retirement diff (drafted 2026-04-26) is the correct fix and remains in Adam's apply queue. Stop framing this as a re-auth task; it's a SKILL.md retirement.

---

## 2026-06-05 — daily-opt Friday Content Planning + AEO: Content-Read Beats Grep-Count for Compliance Carries + Name-the-Doc-Conflict + Consolidate-Carries-into-One-Memo (64th Dead NotebookLM Run)

### Patterns

- **Content-read beats grep-count for compliance carries (NEW today — high value).** The USDA carry sat for weeks as "143 mentions across 12 suburbs — mechanical cleanup, Round Rock playbook." Every prior run REFINED the count (4→5→12 suburbs, 25→143 mentions) but never READ the content. Today's actual read of the heaviest pages flipped the entire frame: the 143 mentions are TWO different KINDS of content. 8 pages PROMOTE USDA zero-down as the lead product (hero subtitle + `<h3>`/`<h4>` section + FAQ "yes most of X qualifies") — that's advertising a product Adam states he doesn't originate, an accuracy/compliance problem. 4 pages STEER AWAY ("most of X is no longer USDA-eligible") — honest, helpful, keep. The "Round Rock 9 mentions = partial-pass cleanup miss" framing (carried 2 runs) was wrong: those 9 are honest steering context. Reusable rule: before sizing a compliance batch by grep count, READ the heaviest few instances to confirm they're the same KIND of problem. A raw count conflates "promotes the banned thing" with "honestly explains the banned thing doesn't apply." Counting harder produces a bigger wrong number; reading once produces the right question. This is the antidote to weeks of count-refinement-without-resolution.

- **Name the document conflict instead of re-carrying the symptom (NEW today).** The perf-metric carry appeared ~10+ runs as "N surfaces, Adam-batch." Counting surfaces never resolved it because the real blocker is that two authoritative docs contradict each other: GOALS.md says "no performance-metric marketing"; voice-guide line 48 says "same-day pre-approvals — a real differentiator." A sweep either violates GOALS.md or strips a voice-guide-endorsed differentiator. Reusable rule: when a carry never resolves across many runs, check whether two source docs give opposite instructions on it. If so, the output isn't "re-surface the count" — it's "name the contradiction, ask Adam to reconcile his own docs." That's a different, closeable ask. (Also: "21-day close" is already 0 sitewide — that GOALS.md win condition is MET; the remainder is same-day/24-hour, which is the part the voice guide defends.)

- **Consolidate ≥3 perennial Adam-gated carries into ONE decision memo with recommendations (NEW today).** Three suburb-cluster carries (USDA, perf-claims, 0-tracked-LP) had each been re-surfaced independently for weeks, inflating CONTEXT.md and FLAG_FOR_ADAM with repeated line items. Bundled into `run-logs/adam-batch-memo-2026-06-05.md`: exact file lists, one decision each, a recommended default answer each. Converts weeks of carry noise into a single "1=x, 2=y, 3=z" reply. Reusable rule: when ≥3 carries are all "Adam-decision-gated" and ≥2 weeks old, stop re-surfacing line-by-line — write ONE memo that makes each a one-line answer with a default. Lowering the COST of deciding is the highest-leverage move a stuck carry-run can make. Place the memo in the SITE repo (`run-logs/`), not `loanos-clone/tasks/seo-sem/`, to avoid triggering a paused-product Vercel deploy.

- **Live-submit on a production lead form injects fake leads — HTML-token inspection is canonical (reaffirmed).** Step 2 SKILL.md says "submit test form via browser automation." But the live site wires Netlify Forms → n8n → Salesforce; a real submit creates fake CRM leads + fires the lead pipeline. The curl HTML-token matrix (GTM / generate_lead / lead_type / dataLayer / netlify / thank_you_page_view / tel:) proves instrumentation presence without polluting the pipeline. This is the established, correct substitution for this task — never live-submit against production.

- **Friday rotation is mechanically light under GOALS.md "no new content" — the value is content-read on carries, not new content.** Blog CTAs (32/32) + AEO (healthy) passed with zero actionable mechanical work because content additions are paused. The run's value came from READING existing cluster content to reframe a carry, not from producing new content or refining counts. Reusable rule: on a rotation day where the defined mechanical work is healthy AND the backlog is GOALS-gated, spend the run reading the heaviest carry's actual content rather than re-grepping its scope.

- **NotebookLM 64th consecutive dead run.** Pattern unchanged. SKILL.md retirement diff still in Adam apply queue.

---
## 2026-06-03 — daily-opt Wednesday Suburb Deep Dive (Kyle): Carry-Refinement-Without-Commit + Single-Page-vs-Cluster Rotation Mismatch + Scenario.html Architecture Recognition + T+15h Propagation Canonical (62nd Dead NotebookLM Run)

### Patterns

- **Carry-refinement-without-commit is a valid Wednesday-rotation outcome on Adam-pivot territory (NEW today).** Wednesday rotation default = "audit one suburb + fix what's mechanical." Today's Kyle audit found ZERO mechanical fixes — everything cluster-routes to Adam-pivot decisions already in carry. The valuable output wasn't a commit, it was **refining the scope of existing carries with hard numbers**: USDA cluster grew 4→5 suburbs; complicated-income 0-CTAs grew 8→32 pages. Both refinements give Adam materially better information for batch review than a single-page commit would. Reusable rule: when a Wednesday deep-dive finds findings that all cluster into existing carries, the run's output is carry-refinement not commits. Quantifying scope expansion ("5 suburbs not 4", "32 pages not 8") is the canonical artifact. Add to Wednesday rotation playbook.

- **Single-page Wednesday rotation is wrong instrument for cluster cleanups (NEW today).** Kyle's 5 USDA surfaces are precedent-cleared (Round Rock 2026-04-29 — Decision Test passes: voice guide rules, reversible). Naive read: "just fix Kyle's 5 surfaces today as a Wednesday LOW_RISK commit." But the cluster is 5 suburbs and the same playbook applies to all 5 simultaneously. Fixing Kyle alone produces inconsistent state — Kyle clean while Smithville/Elgin/Florence/Jarrell stay dirty for an unknown duration. The cluster needs single-batch handling, not piecemeal fixes spread across multiple Wednesday rotations. Reusable rule: any compliance pattern that appears across ≥3 files in a same-type cluster needs batch-commit handling, not single-page rotation. Single-page is the right scope only for findings unique to that page. Decision Test #2 (irreversible?) does NOT capture this — the right gating question is "would partial-application produce visibly inconsistent state?"

- **Scenario.html is the new funnel architecture, not a competitor to tracked LPs (NEW today).** Adam's 2026-05-28 commit batch shipped `/scenarios.html` hub + scenario template + sitewide nav addition. Today's Kyle audit confirms suburb-page CTAs route exclusively through `/scenario.html` (3 CTAs on Kyle all point there). Previous framing of "missing `/get-preapproved` link is a funnel-architecture gap" was wrong — it's not a gap, it's a deliberate architecture choice. The decision Adam needs to make is whether tracked LPs (`/get-preapproved`, `/refinance-quote`) should be ALSO surfaced alongside `/scenario.html` for Google Ads attribution coverage, not whether they should *replace* it. Reusable rule: when auditing suburb-page CTAs going forward, frame as "does scenario.html absorb the funnel cleanly OR should tracked LPs run in parallel?" — not "missing tracked LP link." The earlier framing implied the absence was a bug; the new framing surfaces it as a Google Ads attribution decision.

- **T+15h propagation check on yesterday's commits is now a canonical AM-day-after artifact (NEW today).** Yesterday shipped 2 title fixes (texas-complaint-notice + rate-buydown-calculator). Today's 2-curl gate confirmed both propagated to live. This catches Netlify deploy failures, CDN cache staleness, and accidental rollback by sister-task pushes — three regression classes invisible to AM's T+75s check. Add to canonical "any-day-after-AM-commits" toolkit alongside the AM T+75s check. T+75s catches initial deploy; T+15h catches mid-cycle regressions. Cost: 2 curls. Benefit: closes the "verified at AM, then forgot" gap.

- **GOALS.md performance-metric ban scope is bigger than the headline-page carry implies (NEW today).** Previously framed as "homepage/get-preapproved/hutto title carries" (3 known surfaces). Today's Kyle audit found 3 MORE perf-claim instances on a single suburb page (meta + AEO answer-first body + bottom CTA). Spot-check on 24-suburb cluster would likely surface 40-60 more instances. Same Adam-pivot decision class, but the scope is materially bigger than the 3-page carry implies. Reusable rule: when an Adam-pivot decision is framed as "N headline pages," verify the same pattern doesn't recur across the suburb cluster — it usually does (the pages share vocabulary/templates). Quantify the cluster footprint and refine the carry before surfacing.

- **AEO answer-first paragraph can contain GOALS.md violations (NEW today).** Kyle's AEO answer-first `<p><strong>` paragraph (line 244) contains "with pre-approval in 24 hours" — a GOALS.md-banned performance claim. This is high-AEO-visibility content (LLMs extract `<p><strong>` content preferentially for direct answers). Future suburb-page edits that REMOVE such claims need to preserve the AEO answer-first format — replacement language must be both extractable AND GOALS.md-compliant. Reusable rule: when staging the perf-metric cluster batch, prioritize answer-first paragraph cleanup over meta-description cleanup (higher AEO impact + same edit cost).

- **NotebookLM 62nd consecutive dead run.** Pattern unchanged. SKILL.md retirement diff still pending Adam apply queue.

---

## 2026-06-01 — daily-opt Monday: Selective Sitemap-Lastmod Discriminator + Python JSON-LD Parse Catches Type Subclass Mismatches + Date-Prefixed Blog URL Discipline + GOALS.md Bounds Decision Test (60th Dead NotebookLM Run)

### Patterns

- **Sitemap-lastmod selectivity rule (refined again today, 3rd refinement in 4 weeks).** Of 128 files showing stale lastmod-vs-commit-date, only 3 warranted bumps. The discriminating question is: "did the touching commit change crawler-meaningful content?" Canonical URL normalization (mechanical, `1d09cc7`), cache-buster bumps (JS asset, `?v=20260531`), and link-href updates (internal-only) do NOT warrant lastmod bumps. Material content rewrites (homepage AEO `f3f8f07`), analytics fixes that visibly change page state (`15e8119`), and dateModified-changing commits DO. Reusable filter for ZERO_RISK auto-bump scope: `(adam-committed) AND (content-bearing-change) AND (>12h since commit OR sitemap-lastmod-only-edit-not-touching-page-content)`. Today's "10h since commit but sitemap.xml edit is separate file" interpretation is correct: the 12h newly-shipped window applies to the FILE in question, not metadata about it. Same-file content re-edit within 12h = defer; sitemap lastmod bump pointing AT that file = safe.
- **Python JSON-LD parse is the canonical Monday Schema-rotation toolkit; grep is unreliable for multi-line schema.** Today's first homepage check used `grep -c '"@type": *"MortgageBroker"'` and returned 2. The Python parse (`re.findall + json.loads`, iterate `@type`/`name`/`address.streetAddress`) returned 1 MortgageBroker + 1 Person + 1 FAQPage. The grep double-counted because schema spans multi-line and the `@type` field appeared twice in one block (parent + nested provider). Reusable rule: ANY Monday Schema-rotation entity audit MUST use Python JSON-LD parse, never plain grep. The Python path also catches type-subclass mismatches across pages (today's `MortgageBroker` on home vs `LocalBusiness` on about) which grep can't compare structurally. Added Python snippet template to canonical Monday toolkit.
- **Date-prefixed blog URL canonical form catches naive "title-only" URL guesses.** Sunday's run log recorded the physician post URL as `/blog/physician-mortgage-texas.html` (title-only guess). Today the actual canonical URL is `/blog/2026-05-30-physician-mortgage-texas.html` (date-prefixed). The 404 was a documentation defect, not a regression. Reusable rule: when verifying a blog post URL across runs, NEVER reconstruct from title alone — always extract the canonical filename from `sitemap.xml` or `blog/manifest.json`. Two-step (grep sitemap for slug → curl) is 1 grep + 1 curl, costs nothing, eliminates the wrong-URL false-404 class. Saves false-positive "physician post 404" escalation that would have been a same-week-as-launch regression alarm.
- **GOALS.md project-scope check bounds the Decision Test "try first" guidance.** Today's Step 4B backlog scan surfaced 5+ LOW_RISK items the Decision Test would normally have classified as "do it now": city-page enrichment, glossary additions, new blog content. ALL failed the GOALS.md "no new content beyond repositioning + compliance fixes" scope check. Correctly skipped. Reusable principle: Decision Test runs ON the work that's IN scope; GOALS.md runs BEFORE the Decision Test to bound scope. The two-gate sequence is: (a) is this work in GOALS.md scope this week? → if no, skip regardless of risk tier; (b) if yes, run Decision Test on whether to act or surface for Adam. Today: 3 sitemap-lastmod bumps passed both gates (compliance/SEO maintenance fits GOALS.md "compliance cleanup"; mechanical metadata fits Decision Test "try first"). Most LOW_RISK backlog items failed gate (a) and never reached Decision Test.
- **Schema-type unification (MortgageBroker vs LocalBusiness) is a 2-file Adam decision, not auto-edit territory.** Both schema.org types are valid. MortgageBroker is more specific (correct child class of LocalBusiness for this business). But: choosing direction is positioning territory (Phase A is "complicated income specialist" — does that lean MortgageBroker or LocalBusiness? both can work). Surface as LOW carry, do not unify without Adam's call. Generalized: schema type-class disagreements across same-business pages are Adam-pivot territory, not mechanical compliance.
- **Sister-task working tree pollution stable at 1 modified file + 13-15 untracked.** File-specific `git add` discipline preserved cleanly today: this task staged 6 files (sitemap.xml, 2026-06-01.md, latest.md, learnings.md, CONTEXT.md, CHANGELOG.md). Sister-task working tree untouched. Pattern continues — this repo has 3+ scheduled tasks operating on the same working tree; working-tree pollution is steady state.
- **Cache-buster sweep verification as canonical post-Adam-JS-change check.** Today's sitewide `?v=20260530b → ?v=20260531` overnight roll verified clean via 5-sample sweep (home + 2 LPs + 1 suburb + 1 loan-type). Cost: 5 curl + grep calls. Benefit: catches the regression class where Adam rolls the cache-buster but a build artifact serves stale JS. Add to canonical post-JS-change check list alongside conversion-tracking gate (which would catch a downstream symptom — cache-buster check catches it earlier).
- **NotebookLM 60th consecutive dead run.** Pattern unchanged. SKILL.md retirement diff still pending Adam apply queue.

---

## 2026-06-01 — Competitive Wk 13: Phase A Repositioning Flip + Lone Cedar Park Gap + LendFriend Schema Wedge

### Patterns

- **Repositioning shifts can show up in SERPs faster than expected — Phase A flipped 2/7 → 5/7 in 14 days.** GOALS.md Phase A repositioning to "complicated income specialist" landed in mid-May. Wk 11 (2026-05-18) measured 0/5 on the core complicated-income SERPs (self-employed, bank statement, non-QM, DSCR, 1099). Wk 13 (2026-06-01, after Wk 12 scheduler skip) measured **#3 / #6 / NR / NR / #7**. Three NEW top-10 wins in 14 days. Lesson: when comp run-cycle skips one week, don't write off the missed cycle — measure full delta and credit the work that happened on the unmeasured weeks. The Wk 12 skip absorbed cleanly into Wk 13 measurement.
- **Lone-suburb-gap pattern: when 8 of 9 measured suburbs rank, the remaining 1 has a structural reason, not a content reason.** Cedar Park is the only suburb not in top 10 across Wk 11 + Wk 13 measurements. Pattern across the Cedar Park SERP: CrossCountry Mortgage has TWO Cedar Park branch URLs in top 10 (#1 and #7), Bank of America also has a physical Cedar Park branch. Same "physical-presence dominance" pattern flagged on Georgetown Wk 9 and Pflugerville (Geneva wedge). Lesson: when 1 of 9 underperforms, the audit shouldn't ask "is the page worse?" — it should ask "what physical-presence structural pattern owns the SERP, and what's the broker-shopping-40+-lenders wedge?"
- **LendFriend schema gap is structurally beatable.** Fresh fetch confirmed: LendFriend (#1 strategic threat) has ~3,500 words on their self-employed page but NO JSON-LD FAQPage schema and NO AggregateRating schema. They display reviews visually but not structurally. Adam's pages already have FAQPage schema on the loan-type cluster (verified across Phase A pages in TODO carries). If the AggregateRating policy decision lands with verified counts re-added, Adam closes the schema gap completely. Lesson: when a strategic competitor is structurally weaker on a measurable lever (schema), that's the highest-confidence move because it's a binary that Adam controls.
- **Year-suffix URL pattern is a copyable freshness wedge.** MortgageAustin publishes content at URLs containing the year (e.g. `/self-employed-austin-texas-mortgage-income-2026/`). Adam's pages use `dateModified` in JSON-LD but the URL itself doesn't communicate freshness. The pattern is a deliberate SERP freshness signal — possible Phase B clone candidate (301 old → new year-suffix variant). Lesson: URL-level freshness is a separate lever from schema-level freshness; both can be deployed independently.
- **Bee Cave indexing carry resolved without manual GSC submission.** 7-run carry-forward flag for "Bee Cave page never indexed (40+ days)" cleared this cycle via simple `site:styermortgage.com bee-cave` check — page is now in Google's index. Lesson: long-carry-forward indexing flags can resolve naturally on Google's own re-crawl cadence; re-verify carry-forward indexing flags every comp run rather than treating them as permanently blocking until manual GSC action.
- **Physical-presence is the recurring suburb-loss pattern.** Leander dropped #4 → #6 (Big Life inserted at #5). Kyle dropped #6 → #8 (Movement Mortgage + Guild Mortgage entered, both physical Kyle branches). Cedar Park lone-gap (CrossCountry physical + BoA physical). Georgetown not-ranked-ever (most physical-presence dominated SERP). The pattern is consistent across the entire suburb portfolio: where physical branch competitors enter, Adam drops. The defense is the broker-shopping wedge — explicit "shopping 40+ lenders beats a single bank branch" framing.

### Carry-Forward (still unresolved)

- NotebookLM CLI auth expired (3rd run carry; broken since 2026-05-11) — Steps 0 + 6 of competitive SKILL.md skipped. Master log persists to local disk only.
- AggregateRating schema policy decision pending (carry from Wk 11) — retire fabricated reviewCount everywhere OR re-add with verified GBP/Zillow count. Blocks both LendFriend parity move AND non-QM/DSCR page audit.

## 2026-05-28 PM — daily-opt Thursday Same-Day Second Fire: 14-Adam-Commit High-Velocity Day + Homepage Quick-Form Conversion-Tracking Gap + Sitemap-Count Delta as PM Gate Signal + Wall-Clock Midnight Crossover (58th Dead NotebookLM Run)

### Patterns

- **High-velocity Adam-interactive-day (10+ commits between AM and PM) requires PM gate to re-frame AM findings against new state.** Today: Adam shipped 14 commits between AM 09:24 and PM 23:30 — highest single-day Adam-velocity in this task's history. Major work: scenarios system (hub + template + scenario #1 LIVE), `/add-scenario` skill, Phase 4 spoke-equity push, sitewide nav additions (Scenarios link 101 pages + direct phone in header), homepage Q10 UX overhaul (quick-form surfacing + mobile sticky CTA repoint), n8n loanGoal alert fix. AM's MEDIUM "complicated-income cluster 0 CTAs to tracked LPs" finding did NOT auto-resolve — but Adam's PM funnel direction (homepage-quick-form-first via `#contact-form` repoint) made the AM framing partially obsolete. The PM gate's job on high-velocity days isn't just to verify AM findings still hold; it's to re-frame AM findings against any Adam PM commits in the same domain. Refined PM framing: "Adam's funnel architecture is moving away from tracked-LP routing; tracked LPs may be deprecation candidates rather than restoration targets." Generalized rule: on any high-velocity day, the PM gate must recompute the implication of each AM finding, not just verify the surface condition.

- **Sitemap count is a high-leverage PM gate signal.** AM count = 134, PM count = 136, delta = +2 = Adam shipped 2 new pages (`/scenarios.html` + `/scenarios/oil-gas-royalty-asset-depletion.html`). Single grep, exact answer. Should be a mandatory PM gate check on any same-day-second-fire: catches between-AM-PM Adam work that added/removed pages from the sitemap. Pattern: "if AM sitemap count and PM sitemap count differ, AM's findings about page counts/coverage may be stale." Add to canonical PM-gate toolkit.

- **Newly-shipped UX (within 12h of Adam's commit) = HIGH_RISK file context, even for ZERO_RISK-looking fixes.** Today's homepage `#contact-form` lacks `generate_lead` dataLayer push — looks like a 1-line fix. But (a) Adam just shipped the UX hours ago, (b) the lead-intake JS may handle the push at submit time (commit `089439a` hints at this), (c) wrong-direction guess could create double-firing. Right move: surface to Adam, don't auto-edit. Generalized rule: any HIGH_RISK file (index.html, contact.html, get-preapproved.html) edited within the inter-fire window enters a "newly-shipped" period where even mechanical-looking fixes must defer to next-day verification. The PM gate's job is to *identify* the gap, not to *close* it on freshly-shipped surfaces.

- **149-file sitemap lastmod drift in one day is too broad for same-day-second-fire scope.** AM's TOMORROW_PRIORITY #3 said "any URL with commit-touched dateModified from last 7 days should be cross-checked against sitemap.xml lastmod and brought into sync as ZERO_RISK. Cost: 1 grep, 1 edit." Today's reality: 149 HTML files edited × N edits each ≠ 1 grep, 1 edit. The right TOMORROW_PRIORITY scope was: "files where AM run made `dateModified` bumps" (which is ~1 file/day typically). When Adam batches major work in one day, the sweep needs to be done as a separate AM rotation with risk-tier framing per file class (nav-only edits don't deserve lastmod bumps; content/scenario edits do). Refined TOMORROW_PRIORITY framing for AM tomorrow.

- **GOALS.md banned-phrase check should distinguish marketing vs storytelling vs admin contexts.** Today's loose grep `'21.day'` returned 6 file matches that looked like regressions of GOALS.md "no '21-day close' claim" rule. Precise grep `'21.day (close|closing|guarantee)'` returned 0 marketing claims — the 6 matches were: 1 scenario outcome ("Closed in 21 days at a rate within 0.25%" in scenario #1 storytelling, voice-guide allows real numbers in case studies), 1 admin placeholder (dashboard.html `<textarea placeholder="...Closed in 21 days">`, noindexed), 1 template negative-example (`scenarios/_TEMPLATE.html` explicitly says "Avoid performance-metric claims (no '21-day close')"). Reusable rule: when checking a banned-phrase against GOALS.md, scope to marketing tag contexts (`<h1>`, `<h2>`, `<meta>`, `<title>`, hero copy) — not file-wide loose-substring matches. False-positives on the loose grep would have caused the PM gate to falsely escalate a "GOALS.md regression."

- **Wall-clock midnight crossover during same-day-second-fire (reconfirmed today, 3rd run with this pattern).** Trigger time 2026-05-28 ~23:30 CT, execution finished 2026-05-29 01:17 CT. Run-log filename uses scheduler-trigger date (`2026-05-28-pm.md`) so AM/PM pair stays grouped under Thursday rotation. Pattern is now stable enough to write into the canonical Step 7 instruction: "if execution crosses midnight, use trigger date for filename, not write date."

- **Same-day-second-fire 0-mutation discipline holds under pressure.** Today's PM had multiple defensible mutation opportunities: (a) sitemap.xml lastmod for index.html (1-line fix, exact AM pattern), (b) homepage `generate_lead` dataLayer addition (1-line JS push). Both deferred. Reasoning: (a) the 149-file batch is the real scope; cherry-picking index.html would be inconsistent + premature, (b) index.html is HIGH_RISK + freshly-shipped UX = wrong window for any edit. The discipline pays off when PM commits would compound Adam's same-day batch in unhelpful ways. Right call: 0 commits, refined framing only.

- **NotebookLM 58th consecutive dead run.** SKILL.md retirement diff still in Adam apply queue.

---

## 2026-05-28 — daily-opt Thursday: Sitemap Lastmod Drift Pattern + Re-Verify Auto-Correction of Bad PM "Refinement" + Funnel-Flow Per-Cluster CTA Scan (57th Dead NotebookLM Run)

### Patterns

- **Sitemap lastmod drift vs in-page dateModified is a recurring class of finding after Adam's interactive sessions.** Adam's 2026-05-27 PM commit `6b64ebf` was *literally titled* "internal-link audit + dateModified bump on /non-qm-loans.html" — yet the sitemap.xml `<lastmod>` for that URL was missed. The pattern: interactive-session commits often update one canonical date (the JSON-LD `dateModified` in the page) and forget the other (sitemap.xml `<lastmod>`). Reusable Thursday-rotation byproduct: any URL with a commit-touched `dateModified` from the last 7 days should be cross-checked against sitemap.xml lastmod and brought into sync as ZERO_RISK. Cost: 1 grep, 1 edit. Benefit: avoids GSC re-crawl signal drift on the page that just got Austin-anchored. Added to tomorrow's TOMORROW_PRIORITY as a recurring sweep.

- **PM "refinement" passes can regress correct AM findings.** Today proved this: 2026-05-26 AM tracked the leander Audit Issue #5 superlative at "line 276" — correct. 2026-05-26 PM "refined" it to "line 280" and called AM imprecise. Today's `grep -n` returns line **276**. `git log -- leander-mortgage-lender.html` shows last commit was `db7ab16` 2026-05-25 (before AM 2026-05-26 even ran) — no edit could have shifted the line between AM and PM. PM agent miscounted. Reusable rule: when re-verifying a line-number-anchored carry, always `grep -n <pattern> <file>` and trust the grep output, never eyeball the count or trust a previous run's value. PM gate's value is precision — only when it actually uses precision tools. Added to CONTEXT.md "Tooling rules (carried)."

- **Thursday funnel-flow audit pattern: per-cluster CTA scan in 3 greps × N pages.** Today's audit chained: (a) `grep -oE 'href="[^"]+"' <page> | grep '^href="/' | sort -u | wc -l` for unique internal-link count, (b) targeted `grep -cE 'href="(/?(get-preapproved|prequal))[\"/]'` per conversion-CTA pattern (3 patterns: prequal, refi, calculators), (c) `grep -oE 'href="[^"]*"' <page> | grep -iE 'preapprov|prequal|refi|hypersmart|1003|calendly|register|tel:|/contact'` for full conversion-path inventory. 3 greps × 8 pages = clean read on cluster-wide funnel routing in <30 seconds. The pattern's value: it lifts an Adam-decision blocker from "one homepage finding" to "8-page cluster finding" — same blocker, much higher leverage for Adam's batch review. Added to canonical Thursday rotation toolkit.

- **GOALS.md performance-metric exclusion vs voice-guide differentiator tension is now a recurring carry class.** Three findings now in the same class: /get-preapproved title `24-Hour Turnaround` (2026-05-26 AM), /hutto-mortgage-lender title `Same-Day Pre-Approval` (2026-05-26 AM), thank-you.html lines 443+452-453 `Same-day review` / `24 hrs` / `one business day` (today). All three have: GOALS.md says no, voice-guide says yes. Reusable pattern: when both sources say opposite things, the right move is Adam-batch (not auto-edit either direction). The tension is real positioning territory — Adam may decide to keep all three (voice-guide wins), kill all three (GOALS.md wins), or partial (e.g., delete in headlines, keep in step descriptions). Surfacing in one batch lets Adam decide once.

- **Sister-task working-tree pollution stable at ~3-4 modified files + 8-12 untracked.** File-specific `git add` discipline preserved cleanly today. Files left untouched per discipline: `TODO.md` (sister-task NEEDS ADAM updates), `run-logs/latest.md` (will be overwritten by this run's `cp`), `run-logs/learnings.md` (this task owns it but sister tasks have their own entries — diff is purely additive), `run-logs/suburb-editor-queue.md`, 11 untracked sister-task logs. Pattern continues: this repo has 3+ scheduled tasks operating on the same working tree, working-tree pollution is steady state.

- **Wed 2026-05-27 = NO FIRE for styer-site-daily.** Scheduler-anomaly carry bumps to "Wed 2026-05-20 + Wed 2026-05-27 no-fire + 3 unscheduled weekend fires." Pattern persists. Adam's interactive session that day filled the rotation gap from the user side, but the automated cadence question is unchanged.

- **NotebookLM 57th consecutive dead run.** SKILL.md retirement diff still in Adam apply queue.

---

## 2026-05-26 PM — daily-opt Same-Day Second Fire: Wall-Clock Midnight Crossover Labeling + PM Gate Precision Refinement + AM Commit Live-Propagation at T+12h (56th Dead NotebookLM Run)

### Patterns
- **Wall-clock midnight crossover during a same-day-second-fire is a labeling-only concern.** This run was triggered 2026-05-26 23:07 (Tuesday PM) but the date system reminder rolled over to 2026-05-27 mid-execution. Three artifacts had to pick a date: (a) run-log filename → `2026-05-26-pm.md` (trigger date keeps AM/PM pair grouped), (b) CHANGELOG entry header → 2026-05-26 (trigger date so the entry sits with the AM commit it refines), (c) master log push → 2026-05-26 (same reason). The wall-clock midnight crossover does NOT change which SKILL rotation applies — Tuesday rotation per trigger time, NOT Wednesday rotation per write time. Reusable rule: any artifact that needs a date should use the scheduler-trigger date, not the wall-clock-write date, whenever they differ.
- **PM gate's job is precision, not new findings.** AM tracked "Audit Issue #5: 2 occurrences (homepage H1 + leander #276)." PM verified: H1 phrase still at index.html:362 ✓; leander framing is at line 280 (not 276) + ALSO in the page title tag. Refined count: 3 surface locations across 2 files, not 2 occurrences. No new content debt — the carry status stays Adam-pivot-blocked — but the carry text is now precise enough that Adam can locate every surface in one batch review. Pattern: when AM uses round-number carries ("N occurrences"), PM verifies exact line numbers, surface counts, and adjacent-but-related locations. Both runs serve the same finding; the precision is the value of the PM gate. Distinct from "PM = no copy-change piling" (which is about not shipping new commits on top of AM's commits) — this is about *refining the carry framing* without changing site state.
- **AM commit live-propagation check on the PM gate is cheap and catches a different class of regression than AM's T+75s check.** AM run did `curl -L` on 3 sample URLs at T+75s post-`git push` to confirm Netlify served the new title. PM ran the same 3 hits at T+12h to confirm the title is *still* being served live — catches deploy-pipeline regressions invisible to AM's check: CDN cache warming issues, edge propagation lag, accidental rollback by a sister-task push, Netlify build artifact corruption. Today 3/3 served the pipe-format title at T+12h. Add to canonical PM-gate toolkit: re-curl every AM commit's sample URLs at T+12h. Cost is 3 curl hits; benefit is closing the "AM verified, then forgot" gap.
- **Carry text refinement → CONTEXT.md + run-log + FLAG_FOR_ADAM updates only.** When PM refines a carry's text (e.g., 2→3 surfaces, line 276→line 280), the right update path is: (a) run-log gets a "refined this run" line, (b) CONTEXT.md Active Blockers entry gets updated text, (c) FLAG_FOR_ADAM "Refined carry text" section captures the diff. Do NOT bump carry counter or treat as a fresh blocker — same blocker, just better documented. Avoids carry-counter inflation that would obscure the actual age of the underlying issue.
- **NotebookLM 56th consecutive dead run.** SKILL.md retirement diff still in Adam apply queue.

---

## 2026-05-26 — daily-opt Tuesday: Title-Tag Pipe-Format Audit + Footer-vs-Title Disambiguation + Risk-Tier Routing of HIGH/MEDIUM Files + Cross-Repo Vercel-Deploy Gotcha for Paused Products (55th Dead NotebookLM Run)

### Patterns
- **Pipe-format audit pattern: two parallel greps establish whether canonical is real.** Before scoping a title-tag normalization batch, run two greps in parallel: `grep -l '<title>[^<]*Adam Styer NMLS' *.html` (non-compliant) and `grep -l '<title>[^<]*Adam Styer | NMLS' *.html` (compliant). The denominator tells you whether the canonical format is established convention (22 compliant + 14 not = 36 in cluster today — canonical is real, the 14 are tech debt) or whether the "canonical" is actually a minority recent style change you'd be amplifying. Today both forms had been used over time; the pipe-format is the modern convention (consistent with `a8d565c` 2026-05-22 commit that Adam approved on /investor-loans + /high-net-worth-mortgage). Pattern: any "should we normalize this format?" question is really a "what's the real distribution?" question. Two greps, 5 seconds, answer with confidence.
- **Footer-vs-title disambiguation: the same substring can require different treatments in different contexts.** `Adam Styer NMLS #513013` appears in 26 files in two distinct contexts: (a) `<title>` and `<meta ... :title>` tags — needs pipe normalization, (b) footer compliance line `... | HyperSmart Home Loans | NMLS #2653540 | Adam Styer NMLS #513013 | Licensed in Texas | ...` — does NOT need pipe normalization because the phrase is one semantic unit (personal-NMLS attribution within an already-pipe-separated footer). Adding pipe in the footer would create double-pipe ambiguity (`Adam Styer | NMLS #513013` reads as two units instead of one personal-NMLS attribution). Reusable rule: never `replace_all` a personal-attribution phrase. Always scope to specific tag types (`<title>`, `<meta ... :title>`) when the same substring has multiple semantic meanings. Today: explicitly tested this — 12 files still have the footer phrase intact post-commit.
- **HIGH_RISK file deferral discipline pays off.** Today's 14-file batch could have been one big commit. Routing index.html (HIGH_RISK homepage) + get-preapproved.html (MEDIUM_RISK paid LP) to FLAG_FOR_ADAM instead — per SKILL.md Step 4B risk tiers — kept the highest-leverage SERP/Ads-Quality-Score pages in Adam's review lane while still shipping 12 mechanical fixes. The 14-file split into 12/2 produces faster forward motion than waiting for Adam to approve all 14. Pattern: when a mechanical-compliance audit surfaces N files needing the same fix, sub-classify by surface-type (content page vs. homepage vs. paid LP vs. high-ranked SEO page) and ship the LOW_RISK batch immediately, BLOCKER the higher tiers.
- **Loanos-clone pre-push hook = full Next.js build = Vercel deploy of paused product.** SKILL.md Step 4B says BLOCKERS go to `loanos-clone/tasks/seo-sem/BLOCKERS.md`. But loanos-clone's `.git/hooks/pre-push` runs `npm run build` for ANY commit (even tasks/ docs), and `git push` triggers Vercel auto-deploy. With LoanOS product paused per GOALS.md, writing to that path means: (a) 90+s pre-push build for a docs change, (b) Vercel build minutes burned on paused infrastructure, (c) deploy of paused product. Today the right call was: revert the BLOCKERS.md change in loanos-clone, surface same content via this task's own FLAG_FOR_ADAM + run log. Generalized: scheduled tasks should own their BLOCKERS.md paths inside the repo where the work actually happens. Worth raising with Adam — possibly move the documented path to `styerteam-mortgage-site/tasks/seo-sem/BLOCKERS.md` (or `client-ops/clients/styer-mortgage/BLOCKERS.md`).
- **Tuesday rotation pivot via yesterday's TOMORROW_PRIORITY worked cleanly.** SKILL.md Tuesday default = Title Tags + Meta Descriptions. Yesterday PM correctly flagged that the meta-trim half was auto-resolved by sister task `db7ab16` and recommended either pulling Wednesday's Suburb Deep Dive forward OR doing a sitewide title-tag format audit. Today picked option (b), executed cleanly, found 14-file tech debt and a clean 12-file LOW_RISK batch. This is the pattern working as designed: AM run identifies state, PM run plans next AM, next AM executes. The "AM plans, PM verifies/refines plan" cycle is materially better than each run doing its own plan from scratch.
- **Conversion-tracking grep tooling cleanup.** Yesterday PM correctly used `curl -L` per `e24b973` force-redirect — and that's now SOP. But the netlify-form grep pattern was case-sensitive `"Netlify"` which only caught the capital-N comment, giving 1 hit. Today's `grep -ci 'netlify'` catches 4 hits per LP (lowercase JS submission + endpoint + error string + capital comment). The 4-vs-1 isn't a regression, it's a tooling visibility improvement. Pattern: when re-instrumenting a grep, prefer case-insensitive matches over case-sensitive when the underlying token has both. The conversion-tracking grep matrix should ALL be case-insensitive going forward.
- **TODO.md + suburb-editor-queue.md modifications + 8 untracked sister-task logs all left untouched.** File-specific `git add` discipline preserved. Sister-task working-tree pollution is now steady state; this task's commits NEVER include sister-task work product. The discipline matters most when the sister task has substantive in-flight changes (today: queue.md is sister-task scratch, TODO.md may have NEEDS ADAM updates from another agent's session). One `git add -A` collision could corrupt sister-task tracking.
- **NotebookLM 55th consecutive dead run.** Pattern unchanged. SKILL.md retirement diff still pending Adam apply.

---

## 2026-05-25 PM — daily-opt Monday Same-Day Second Fire: Force-Redirect `curl -L` Gotcha + Sister-Task Velocity Auto-Resolves 9/11 Superlatives + 6/7 Suburb Metas in 12 Hours + HTML-Entity Visible-Length Gotcha (54th Dead NotebookLM Run)

### Patterns
- **Post-`_redirects` force-flag change: conversion verification MUST use `curl -L`.** Today's interactive commit `e24b973 seo: force flag on _redirects rules` added the `!` flag to extensionless route rules for `/get-preapproved`, `/refinance-quote`, `/thank-you`. Result: without `-L`, curl receives only the 301 response (HTTP 301 + 11–12 bytes of plain text "Redirecting to..."), making conversion-token grep return 0 across all 3 LP routes. Initial PM gate falsely scored 0/10 conversion. Adding `-L` (follow redirects) restored the AM 10/10 score. Reusable rule: when `_redirects` adds force-flag, any curl-based grep on those routes must include `-L`. AM run already used `-L` (no AM regression). Add a sticky note: future Step 2 in SKILL.md should explicitly specify `curl -s -L` to be defensive against future force-redirect changes. Worth scoping a PR to SKILL.md when Adam has bandwidth.
- **HTML-entity gotcha when measuring meta description length.** bee-cave meta description: 162 source bytes, 158 visible characters. The 4-char difference is one `&amp;` entity (5 source chars → 1 visible char). Google's SERP truncation counts visible chars, not source bytes. Verifier should use `python3 -c "import html, sys; print(len(html.unescape(sys.stdin.read())))"` to get the visible-char count, not `wc -c` or naive Python `len()` on source. Reusable rule: any meta-trim audit should compare visible (entity-decoded) length to the 160-char SERP ceiling. Source-byte count is a noisy proxy. Today: bee-cave passes; previously it appeared to need a trim.
- **Sister-task velocity between AM and PM can auto-resolve major Adam-batch items.** Today: 12 sister-task seo-* commits between 08:04 AM and 23:08 PM. Net effect on daily-opt's findings: (a) Audit Issue #5 superlative count dropped 11→2 (9 occurrences resolved); (b) Tuesday's TOMORROW_PRIORITY #1 meta-trim batch completed via `db7ab16` (24 metas trimmed sitewide). Generalized pattern: **AM run plans, sister tasks execute, PM run verifies + auto-resolves.** Don't assume AM's TOMORROW_PRIORITY survives unchanged into next AM — the inter-run window is high-activity. PM re-verify-gate is now the canonical "what's still open?" snapshot rather than AM's findings.
- **Same-day second-fire discipline correctly produces 0-mutation runs even when work is available.** bee-cave could have been "trimmed" to satisfy literal byte-count ≤160. But (a) it's already visibly compliant via entity decode, (b) same-day-second-fire defaults to re-verify per CONTEXT.md What's Next, (c) bee-cave landed on suburb pages that just had a sister-task sweep (db7ab16) — touching it risks colliding with sister-task framing. Right call: surface as compliant, don't touch. Counter-example: the Decision Test says "if you can do it, do it" — but the Test's first question is "is there a genuine judgment call only Adam can make?" — and the answer here is YES at the meta-level (the SKILL.md cadence rule itself is a judgment call about when to write vs. verify). Same-day-second-fire wins over Decision Test #3 for this specific case.
- **Conversion-tracking "0 score" can be a tooling artifact.** Initial reaction to "0 tokens on all 3 LP routes" should be: "did the routing change?" before "did tracking break?" Today the answer was the former (force-redirect added) within 30 seconds of investigation. Pattern: if conversion check drops dramatically between AM and PM, check the redirect chain (`curl -sI`) FIRST, before assuming GTM/dataLayer regression. Saves a false-positive escalation.
- **CONTEXT.md "Last Worked On" entries should be replaced, not appended.** Today's PM update replaced 5 day's entries with 6 entries (AM + PM same-day are two distinct entries; the older 3 days condensed by 1 line each). Net result: CONTEXT.md still under 100 lines (97). The "replace, don't append" rule kept the file scannable. Future runs that fire 2x/day should explicitly maintain the AM/PM split for the most-recent day, then condense older days as they age out.
- **NotebookLM 54th consecutive dead run.** No change. SKILL.md retirement diff still pending Adam apply queue.

---

## 2026-05-25 — daily-opt Monday: Schema Audit Catches Auto-Resolved Address Mismatch + Suburb Meta Sweep Pattern + Sister-Task Boundary Discipline (53rd Dead NotebookLM Run)

### Patterns
- **Schema entity-consistency audits catch finding-state changes invisible to grep-based gates.** Today's Monday rotation found that the 2026-04-27 AM audit's address mismatch finding (homepage MortgageBroker `5718 Sam Houston Circle` vs about.html LocalBusiness `5900 Balcones Drive`) is now resolved — both schemas use the HyperSmart corp `9050 N. Capital of Texas Hwy, Ste 390 / 78759` address. The 2026-05-20 entity transition (Mortgage Solutions LP → Kyber Mortgage Corporation dba HyperSmart Home Loans) implicitly aligned them. A grep-only re-verify wouldn't have caught this — it required actually parsing the JSON-LD blocks across both pages and comparing address fields. Reusable pattern: when a finding is about cross-entity consistency (address, name, NMLS, phone), the verifier must extract the actual values and compare them, not just check presence. Add to Monday schema-rotation toolkit: a Python parse of all `application/ld+json` blocks on homepage + about, extract `address.streetAddress` for every `MortgageBroker/LocalBusiness/Organization/Person` entity, assert they match.
- **Suburb meta-description sweep is a high-leverage LOW_RISK batch.** Audit run found 11 of 24 suburb pages have meta descriptions >160 chars (Google SERP truncation limit). Today's commit `e05f134` trimmed the 4 worst non-sister-task offenders in a single 4-file commit (westlake 254→154, buda 235→143, hutto 234→158, round-rock 212→155). The trim pattern: drop hyperlocal data dumps (median price, ISD scores, tax rates) and lead with neighborhood names + 40+ wholesale lenders positioning per GOALS.md. SERP CTR depends on what shows above the fold; the dropped data is still in the page body. Reusable rule: meta description trim ≤160 chars is mechanical LOW_RISK; trim with intent (lead with neighborhood + GOALS.md positioning), not arbitrary truncation.
- **Sister-task fresh territory is off-limits even for clear improvements.** Cedar Park's meta description (276 chars) is the worst offender in the suburb cluster, but it was just rewritten by suburb-editor sister task on 2026-05-24 (commit `1b4d028`). Touching it today would step on suburb-editor's fresh rebuild. Right call: surface in run log as a finding, leave the fix to suburb-editor's next rotation. Reusable boundary: if a file's last commit author was a sister task and the change is <72 hours old, leave it. Surface findings for cross-task awareness, do not edit. This is the "cross-task awareness yes, cross-task ownership no" principle in practice.
- **Audit Issue counts drift between weekend gate runs and scheduled rotation.** Audit Issue #5 superlative sweep went 12 → 11 occurrences between Saturday PM and Monday AM (no daily-opt edits in between; Adam likely made one between-session edit on one of the 8 files). The Re-Verify Gate caught this; the batch is still 11 occurrences across 8 files (down from 12 across 8 files). Pattern continues: re-verify counts every run, don't increment carry counters on muscle memory. Each run that holds the count at N or sees N→N-k is information.
- **Monday's scheduled fire produced higher-leverage work than the 4 weekend fires combined.** Weekend fires (Sat AM/PM 5/22, Sat AM/PM 5/23, Sun 5/24) were all re-verify gate only or single-purpose compliance fixes. Today's Monday fire executed: schema rotation + AEO entity check + cedar-park verification + audit-finding auto-resolution + 4-file meta-trim batch + sitemap/conversion gates. The right rhythm is "scheduled Monday = full rotation work; off-schedule fires = re-verify gate only or single-purpose Phase A item." Documented this contract via CONTEXT.md What's Next.
- **NotebookLM 53rd consecutive dead run.** No change. SKILL.md retirement diff still pending Adam apply queue.

---

## 2026-05-24 — daily-opt Sunday Unscheduled Fire: 4-in-9-Days Weekend Fire Pattern Crosses Threshold + Sister-Task Coexistence Pattern (52nd Dead NotebookLM Run)

### Patterns
- **4 unscheduled weekend fires in 9 days is a steady-state cadence now, not an anomaly stream.** Sat AM/PM 2026-05-22, Sat AM/PM 2026-05-23, Sun AM 2026-05-24. The "FLAG_FOR_ADAM: scheduler anomaly" framing has now appeared in 4 consecutive run logs without action. Pattern recognition: this isn't a scheduler bug, it's an undocumented cadence. The right framing for the next run that flags this is: "scheduled task fires on weekends — should SKILL.md document this as expected behavior with weekend-specific instructions, or should scheduler config skip Sat/Sun?" Stop calling it an anomaly. Either acknowledge the cadence in docs or change the schedule.
- **Sister-task working-tree coexistence pattern: file-specific staging is the only safe operating mode.** Today suburb-editor shipped 3 commits before this task fired (cedar-park rebuild + HyperSmart asset sweep + IndexNow 403 logging). Suburb-editor's working tree at fire time had 7 untracked files + 2 modified files. Daily-site-opt task must never `git add -A` or `git add .` in this repo because: (a) it would sweep up sister-task work product mid-stream, (b) it would attribute that work to daily-opt's commit, (c) it could split work in flight across two commits owned by different agents. Use `git add <specific>` only. If a sister task left work uncommitted, leave it. Pattern is now formal: this repo has 3+ scheduled tasks operating on the same working tree, working-tree pollution is the steady state, not an exception.
- **Cross-task awareness is valuable; cross-task ownership is not.** Today's suburb-editor flagged IndexNow HTTP 403 in TODO.md NEEDS ADAM via commit `ed3f670`. Daily-opt's correct response: acknowledge in FLAG_FOR_ADAM as cross-task awareness, do NOT take ownership of the remediation. Each task owns its remediation lane. Surfacing same flag from two tasks creates surface-noise; ignoring entirely creates blind spots. Right balance: acknowledge once, point to owner, move on.
- **Sunday unscheduled fires are now the cheapest possible run.** With AM and PM Saturday already covering re-verify gates + audit-finding sweeps, Sunday's right run shape is: sitemap + conversion + entity scrub + 4-line gate on prior claims. Total tool calls: ~6. Total wall clock: <2 minutes of useful work. Don't pile copy changes on top of Saturday's commits, don't redo Saturday's rotation work, don't re-audit yesterday's findings without fresh signal. The TOMORROW_PRIORITY pattern of "Sunday = re-verify only" is the right contract.
- **Re-Verify Gate's stable-claim case is also valuable.** Today's gate had 11 claims checked, 0 auto-resolved, 0 demoted, 0 escalated. Some agents would treat "no state changes" as a wasted run. But the gate's job is *certainty*, not *change*. Verifying that the audit-Issue-#5 file:line table is still accurate before Adam reviews tomorrow is worth more than the 6 grep calls cost. A clean gate run is a positive output, not a null one.
- **NotebookLM 52nd consecutive dead run.** No change. SKILL.md retirement diff still in Adam queue.

---

## 2026-05-23 PM — daily-opt Saturday Second Fire: Re-Verify Discipline + Audit-Item Auto-Resolution Between Publication and Verification (51st Dead NotebookLM Run)

### Patterns
- **Same-day second-fire discipline: re-verify only, no copy-change piling.** Today's PM second fire correctly skipped the audit Issue #5 superlative sweep (9 files, 12 edits, brand positioning) even though all the audit-prescribed fix text was available on disk. Reasoning: H1 hero is Adam-decision territory under Decision Test #1, and shipping a sweeping copy commit on top of AM's compliance commit would (a) split the H1 from suburb-page positioning if Adam later refined the H1 wording, (b) double Adam's review surface in one day on positioning copy he hasn't approved, (c) bypass the GOALS.md "no new content beyond repositioning + compliance fixes" intent because superlative hedging is brand voice editing, not just compliance text substitution. Right call: surface the file:line table in run log + CONTEXT.md, leave for Adam batch approval.
- **Audit findings can auto-resolve between publication and verification.** 2026-05-17 audit Issue #6 (HNW meta "~30 states") flagged 6 days ago; today's PM re-verify gate found 0 occurrences live. Adam likely cleared it in a between-session edit, but the audit doc is unchanged. Pattern: audit doc is a snapshot of state-at-audit-time. Re-verify gate should run on audit findings before treating them as actionable, just like other live-state claims. Audit findings older than 7 days are especially worth re-verifying before working through. Add a "re-verify audit findings before working" check to the standard Step 5 audit-rotation toolkit.
- **3-fire-in-2-weekend pattern crosses the "scheduler anomaly" threshold into "undocumented cadence".** 2026-05-22 PM + 2026-05-23 AM + 2026-05-23 PM = 3 unscheduled weekend fires in 8 days. At this frequency, it's not noise — it's an implicit cadence the scheduler is producing without SKILL.md acknowledging it. Two options: (a) document the cadence so future runs default to "weekend = re-verify or Phase A backlog work" rather than treating each fire as anomalous, (b) tune the scheduler to actually skip weekends. Either way, the current state where every weekend fire surfaces a FLAG_FOR_ADAM scheduler-anomaly bullet is signal-rot.
- **Decision Test does its job specifically on brand positioning copy with audit-prescribed fix text.** Counter-intuitive case: the audit literally provides exact replacement text. Naive reading would say "test passes — just apply the audit's fix." But the H1 is brand positioning, so Decision Test #1 (genuine judgment call only Adam can make) still fails. The audit-prescribed text is a recommendation, not auto-apply scope. Reusable principle: audit recommendations on Adam-decision categories (brand voice, positioning, testimonials, legal/compliance) are surfaced for batch approval, not auto-implemented. Audit recommendations on mechanical-compliance categories (legal entity name, EHL coverage, NMLS Consumer Access link, schema validity) are auto-applied.
- **NotebookLM 51st consecutive dead run.** No change. SKILL.md retirement diff still in Adam queue.

---

## 2026-05-23 — daily-opt Saturday: Phase A Entity Coverage Gap Sweep + 2-Page Audit-to-Fix Pattern (50th Dead NotebookLM Run)

### Patterns
- **Coverage-gap audit via `comm -23` (set difference) is a 5-second compliance scan.** Today's run identified Kyber entity gaps using `comm -23 <(grep -rl "HyperSmart Home Loans" --include='*.html'|sort) <(grep -rl "Kyber Mortgage" --include='*.html'|sort)`. Returned 7 pages — 2 public-facing (buda + ftb-dpa-guide) + 5 admin/noindexed (dashboard, loan-dashboard, marketing-command-center, marketing-content, thank-you, loanos-waitlist). The audit method generalizes: any time the site enforces a new compliance pattern (entity name, EHL, NMLS Consumer Access link, GLBA disclosure), use `comm -23` to find pages that have the OLD anchor but not the NEW required element. Single-pass, fast, complete. Add to standard Step 5 design-audit toolkit.
- **Noindex + not-in-sitemap = compliance-optional, not compliance-required.** Of 7 gap pages, 4 were noindexed admin pages (dashboard etc.) and 1 was thank-you.html (also noindexed). These don't need the full legal entity disclosure because they're not crawler-facing nor user-discoverable. Right scope was the 2 public pages only (buda in sitemap + ranked, ftb-dpa-guide as DPA landing page). Pattern: when sizing a compliance sweep, intersect the gap list with `noindex` + `in-sitemap` status before scoping work. Cuts scope from 7 fixes to 2 fixes without losing compliance value.
- **Footer-pattern divergence between suburb pages (full-disclosure block) and landing pages (`lp-footer` minimal block) is intentional.** Round-rock/cedar-park use `<div class="footer-bottom"><div><p>copyright</p><div class="footer-nmls">disclosure</div></div></div>`. ftb-dpa-guide uses `<footer class="lp-footer"><div class="container"><p class="lp-nmls">one-line</p></div></footer>`. Both can carry Kyber legal entity; the shape differs because LP footers have less vertical real estate. Don't try to harmonize structural HTML — only harmonize the COMPLIANCE CONTENT (Kyber legal name, NMLS#, Consumer Access link, licensed-in-Texas statement). This preserves landing-page UX while satisfying audit. Reusable principle for any future compliance harmonization sweep.
- **Saturday unscheduled fires are now a 3-week pattern.** 2026-05-22 PM second fire + 2026-05-23 Saturday fire (despite CONTEXT.md anticipating none). When unscheduled fires happen, default to compliance/health sweep mode (Phase A items) rather than redoing rotation work. This run made 2 commits-worth of compliance progress that would have otherwise waited until Monday. Pattern: unscheduled-fire = opportunity for Phase A backlog burndown, not "nothing to do."
- **The Decision Test correctly approves single-file compliance fixes.** Both edits today were: (a) not a judgment call only Adam can make (pattern is established site-wide), (b) reversible via git, (c) attempted-and-successful. Both passed the Test cleanly. Counter-example: the buda Awards section "5.0 ★ Google Rating · 136+ Reviews" did NOT pass — it's the same category as homepage testimonial copy that's awaiting Adam's pivot per CONTEXT.md. Correct call was to fix the unambiguous (entity name) and leave the judgment-call (testimonial framing) for Adam.
- **Saturday's 75-second post-push deploy wait is correctly sized for Netlify.** First-fetch after `git push` returns the new content at exactly the 75s mark for this 2-page diff. Don't tighten below 60s (Netlify build can take that long); don't extend past 90s (wastes the run). 75s is the sweet spot for small commits to this site.
- **NotebookLM 50th consecutive dead run.** Pattern unchanged. Cached insights still doing the work. Adam apply queue.

---

## 2026-05-18 — daily-opt Monday: Residual-Claim Sweep + Schema-vs-Body Compliance Distinction + Two Carry Auto-Resolutions (42nd Dead NotebookLM Run)

### Patterns
- **Trust-bar/marketing sweeps catch headlines, not H3/H2 step-headings in deeper page content.** Adam's Sunday EVENING "21-day claim retired" sweep cleaned trust bars, footers, schema, and FAQ across ~95 files — but missed 6 suburb pages where the claim lived inside a "How I Work" step heading (`<h3>Step 4 — Close in 21 Days</h3>`). When sweeping a marketing phrase: grep across ALL `.html` looking at ALL match contexts, not just `<meta>` and JSON-LD blocks. Sub-headings in body content are the easiest miss because they don't look like "marketing copy" structurally. Reusable rule: a compliance phrase audit should produce a single mega-grep over the full repo with context (`grep -nE "<title>|<meta|<h1|<h2|<h3|"@type"' --include="*.html"`) and resolve every match individually.
- **Schema-level cleanup is necessary but not sufficient for compliance audit findings.** Today's homepage Re-Verify Gate caught that Adam removed the AggregateRating + 136+ Reviews at the SCHEMA level (audit finding #4 schema-level satisfaction ✅) but the same "5.0 stars / 45 Zillow" text still appears in body copy 2× on the homepage. A reasonable third-party compliance reviewer scans schema first but also reads body copy. Carry the audit item forward at body-copy level even when schema is clean. Better still: when picking up a "remove fabricated/exposed claim" audit item, run grep across both `application/ld+json` blocks AND visible body content as two separate verification passes.
- **PSI Monday-quota-refresh is now confirmed unreliable for 3 consecutive Mondays.** Project `583797351490` is being drained by some other consumer before this scheduled task fires every Monday. 10-of-10 consecutive drain. The carry already escalated to FLAG_FOR_ADAM HIGH, but the carry framing should now state "Monday refresh does NOT restore quota" rather than "may restore on Monday."
- **Auto-resolution on multi-week carries is real and worth checking.** Today's run cleared two carries that had been open 23 runs (style.css working-tree change, AggregateRating schema). Adam clears carries between scheduled fires — the Re-Verify Gate has to actually re-check the carry state rather than mechanically incrementing the carry counter. Two auto-resolutions in one run is the highest single-run resolution count this rotation cycle.
- **Scheduler-anomaly streak appears to have ended at 4 days / 7 anomalies.** Mon 5/18 fired exactly once at normal time (not the Sat double / Sun weekend pattern). Don't downgrade the HIGH flag yet — need 3 clean days in a row before classifying the streak as resolved. Watch through Tue–Wed.
- **`grep -c` on multi-pattern alternation defaults to BRE without `-E` flag.** Tested today: `grep -c '"@type": "FAQPage"\|"@type":"FAQPage"'` worked correctly (matched both spacing variants) — confirms basic regex `\|` alternation. For consistency, can also use `grep -Ec '"@type":\s*"FAQPage"'` which is more compact. Both produce same count. Pick one canonical form in fixtures.
- **NotebookLM 42nd consecutive dead run.** Same pattern. SKILL.md retirement diff still pending Adam apply.

---

## 2026-05-11 — daily-opt Monday: Alternation-Regex Gotcha + Stale-Backlog Auto-Resolution + PSI Monday-Drain (30th Dead NotebookLM Run)

### Patterns
- **`grep -E "a\|b"` is literal-pipe, NOT alternation.** In extended-regex mode (`-E`), unescaped `|` is alternation and `\|` is literal pipe. In basic regex (no `-E`), `\|` IS alternation. Today's first conversion-trace silently returned 0 for `"consent\|TCPA"` and `"netlify/functions/lead-intake\|netlify-lead-intake"` — would have falsely degraded the conversion-tracking score 9/10 → 6/10 if not caught. Re-running with proper `grep -E "a|b"` (no backslash) returned the expected 9 and 1. Defensive pattern: standardize on no-backslash alternation in `-E` mode. Add to Step 2 fixture as canonical regex. **Bonus implication:** yesterday's PM run log reported `TCPA-or-consent:5` with the same buggy `\|` regex — suggests either yesterday's `-E` was somehow not applied, or grep version behavior differs, or yesterday's report number was independently arrived at via a different code path. Either way, the canonical pattern going forward is `-E "a|b"`.
- **Stale backlog duplicates surface as ZERO/LOW_RISK queue items long after work is done.** Today's L79 SMS opt-in entry in `loanos-clone/tasks/seo-sem/backlog.md` was a duplicate of L107 (completed 2026-04-06 commit `6fb8883`) and stayed open for 5+ weeks because the strikethrough only happened on L107, not L79. Mitigation: when picking up a queue item, before scoping the work do a live re-verify on the change target (the suburb forms, in this case) — confirms whether the work is actually still needed. Generalizes the existing Re-Verify Gate to backlog items themselves. Matches Adam's standing feedback in `memory/feedback_stale_flags.md`. Action: when sweeping backlog, look for entries with no checkbox or open `[ ]` and grep the live state of the underlying claim before treating as actionable.
- **PSI quota "refreshes Mondays" is no longer reliable.** 2 consecutive Mondays now show 429 on Monday AM — some other consumer of API project 583797351490 is draining the quota before this scheduled task fires. The SKILL.md guidance "PSI quota refreshes Mondays" needs updating, OR Adam needs to provision a dedicated PSI API key for this task. Escalated to HIGH FLAG_FOR_ADAM.
- **AEO entity check on homepage is fully satisfied by 3-tier hero text.** H1 (entity + location), P1 (15w concise value prop), P6 (57w entity-rich paragraph) cover the AI Overviews extraction need without any single paragraph being heavy. The pattern is reusable on suburb pages too — H1 + 1 concise + 1 entity-paragraph is more extractable than one 80w mega-paragraph. Worth carrying for future suburb editor work: if a suburb page only has the 80w mega-paragraph format, splitting into 2 layers may improve extractability.
- **The Re-Verify Gate works on the BACKLOG file itself, not just live external state.** Today's auto-resolution wasn't a "live state changed" finding — it was a "this backlog entry was always a duplicate, work was already done, just never struck through." This is its own form of stale-flag rot. Generalize: re-verify gate should run on (a) live-state claims [original scope], (b) the agent's own carry list framing [2026-05-07 expansion], and (c) backlog duplicate detection [today's expansion]. Three flavors of stale-flag prevention.
- **NotebookLM 30th consecutive dead run.** Same pattern. SKILL.md retirement diff still pending Adam apply queue.

---

## 2026-05-11 — Competitive Week 10: 2 → 4 Suburb Top-10s + Bulk-Audit Insight + Refi SERP Volatility Confirmed

### Patterns
- **Rotation cadence under-measures wins.** First-time SERP checks for Pflugerville and Kyle (2 NEW suburb keywords this week's rotation) returned styermortgage.com at #4 and #8 respectively — meaning both pages were ALREADY ranking before any check happened. The 11-keyword, 9-week rotation creates large detection gaps where a page can rank for weeks-to-months before we measure it. Reframes the suburb-page program as materially MORE advanced than run logs reflected. Operationally: the "next SERP check" is now the highest-leverage move because a single 30-min batch audit of all 18 suburb keywords could surface 2-4 more wins. Replace 9-week rotation with bulk weekly audit OR "movers + new" priority pattern.
- **Hutto playbook validated across 4 distinct SERP shapes.** Hutto (template-quality competitors), Leander (directories + body-text mentioners), Pflugerville (out-of-state Geneva Financial branch with literal Arizona address as #1), Kyle (mix of credit unions + small local brokers + 2 LOs from same parent brokerage in top 10). The schema + neighborhoods + 2026-date refresh playbook works regardless of competitor shape. Pattern is now battle-tested, not speculative.
- **Refi SERP volatility = 3 different #1s in 3 weeks.** Cash-out: Joel #1 Week 8 → Arnaiz #1 Week 9 → Joel #1 Week 10 (RECLAIM). Refi: SouthStar #1 stable, but #2-3 churning weekly (Joel → Arnaiz → Arnaiz demoted). Arnaiz Mortgage demoted on BOTH refi keywords in one cycle. This means: (a) refi page deepening has a 2-3 week opening before some new entrant locks in; (b) thin-template lenders peak for 1 cycle then decay — confirming Arnaiz pattern (no suburb pages, no reviews, no BBB) is structurally weak; (c) "newcomer threat" framing was wrong — it's "newcomer rotation," and we should treat each new #1 as a temporary opening, not a sustained competitor.
- **ATX Mortgage Lending sitemap audit confirms 0/61 dedicated suburb pages.** Watchlist action from Week 9 (re-check sitemap.xml) executed via WebFetch. Result: ATX still has body-text mentions of all 9 target suburbs but ZERO dedicated location pages. Adam's first-mover advantage on suburb pages is preserved for at least another 2-week monitoring cycle (next check: 2026-05-25). Sitemap fetching is a reliable, fast competitive monitoring primitive — should be added to the standard Step 2 toolkit.
- **Same-parent-brokerage in same suburb top 10 = brand validation, not competition.** Logan Patterson (Mortgage Solutions LP) at #7 + Adam Styer (Mortgage Solutions LP) at #8 for Kyle. Two LOs from the same brokerage stacked on the same suburb SERP is unusual and a strong brand-strength signal. Don't frame as internal competition — frame as proof the parent brokerage's broker-network play scales across suburb pages.
- **Auto-loan SERP contamination is query-phrasing-dependent, not sticky.** Last week reported MortgageAustin.com demoted to #2 mortgage interp on "get pre-approved austin tx" with auto-loan results dominating. This week, qualifying the query as "get pre-approved austin tx mortgage" cleanly returned MortgageAustin.com to #1 with NO auto-loan pollution. Rule for future SERP checks on the pre-approval keyword: include "mortgage" in the query OR explicitly note in the report which interpretation is being measured. Don't treat the auto-loan pollution as a permanent SERP shift.
- **NotebookLM 23rd consecutive dead advisor run + working Step-6 push path.** `notebook_advisor.py` script still missing (verified 2026-05-11). BUT the `notebooklm` CLI binary at `/Users/adamstyer/.local/bin/notebooklm` IS working — it's used in Step 6 for the master log push. The two paths are distinct: advisor (dead) ≠ CLI (alive). Suggestion to Adam: the CLI could potentially be wrapped to replicate the advisor's `--notebook seo` query pattern. Either rebuild the advisor on top of the CLI OR formally retire the Step 0/4 advisor queries from SKILL.md.

---

## 2026-05-07 — Funnel Hub Closure + Programmatic Funnel Trace + Re-Verify-On-Self (22nd Dead NotebookLM Run)

### Patterns
- **Programmatic funnel trace via curl + grep covers ~90% of Step 2 conversion-check value when Chrome MCP is unpaired.** Today's HTTP-level trace verified: GTM container present, dataLayer present, generate_lead+lead_type strings present, form attributes (netlify, form-name, action), redirect path, tel: links, TCPA markup, trust bar text, robots noindex on /thank-you. Only thing it cannot verify: the actual form-submit firing the dataLayer event in real time. That's a 10% gap, not 100%. Don't carry-forward "Chrome unavailable, conversion 10/10" — downgrade to 9/10 and document what was verified. Reusable curl trace fixture worth adding to the SKILL.md as a Step 2 fallback.
- **When the parent hub of a cluster is missed during a sweep, the same change applies cleanly to the parent.** Yesterday's PM cluster sweep added cross-links to 7 cluster pages but skipped products.html (the loan-programs hub that fed the cluster). The hero+bottom CTA swap pattern — applied to 4 cluster pages 2026-05-06 — applies identically to the hub. Pattern is already validated, decision was already made; don't re-litigate, just apply. Today's swap was 2 line edits, post-deploy verification, total ~10 minutes.
- **Re-Verify Gate works on the agent's OWN backlog framing too, not just live-state claims about the site.** Wrote a backlog entry today framed as "site-wide footer link to /prequal.html (noindexed in robots.txt)" based on the agent rules' 2026-03-28 entry. Ran re-verify out of habit (different reasons): `grep prequal robots.txt` returned 0 — the Disallow entry was apparently removed during the AEO crawler-allowlist expansion. /prequal.html is currently HTTP 200, no `<meta name="robots">`, listed in sitemap.xml. Caught + corrected within the same run before propagating to dashboard. Lesson: re-verify before-state when scoping a backlog item, not just after-state when verifying a fix. The backlog itself can rot.
- **Form sharing across two pages without dataLayer parity is a silent conversion-tracking gap.** /prequal.html and /get-preapproved.html share Netlify form-name="get-preapproved" — submissions land in the same form. But /prequal.html has 0 generate_lead/lead_type dataLayer events; /get-preapproved.html has them. Net: prequal.html submissions count as Netlify Forms leads but NOT as GTM/GA conversions. The two-form scheme was probably an artifact of a 2026-03 redesign. Adding the dataLayer push to prequal.html would close this without site-wide footer changes. Cheaper than the 20-page footer cleanup, same result for tracking purposes.
- **Loanos-clone pre-push hook flake recovery: rm -rf .next + retry works.** ENOENT on `.next/export/500.html → .next/server/pages/500.html` rename during pre-push build is a stale-cache artifact, not a code problem. Standard fix: `rm -rf .next && npm run build` then push. Worth adding to loanos-clone CLAUDE.md gotchas if it recurs.
- **NotebookLM 22nd consecutive dead run.** No new framing. Adam queue bottleneck.

---

## 2026-05-06 PM — Cluster Internal-Linking Sweep + Netlify Quote-Style Rewrite (21st Dead NotebookLM Run)

### Patterns
- **Cluster internal-linking audits should use an N×N matrix.** A 2-deep nested grep (each page × each sibling) on a content cluster produces an N×N coverage matrix in <2s and makes gaps visually obvious. Today's matrix on the 7-page non-QM cluster surfaced 3 pages at 3/6 (the hub plus two leaf pages), all missing the same 3 DSCR siblings (Texas/Fredericksburg/Dripping Springs). Without the matrix view, the gap was invisible — each page's individual Related list "looked complete." Reusable pattern for any future content cluster (suburb pages, loan-type pages, blog tag clusters).
- **Hub pages are the highest-priority candidates for cross-link gap closure.** non-qm-loans.html is the cluster's routing center. A hub at 3/6 cross-links sends readers to "DSCR Austin" but never tells them about the 3 geo-targeted DSCR pages — losing both the human funnel AND PageRank distribution. Hub gaps should be flagged HIGHEST priority within any cluster sweep.
- **Netlify rewrites BOTH `.html` stripping AND attribute quote-style (double → single) on internal hrefs.** Earlier learning (2026-04-23) flagged `.html` stripping; today's PM verification caught the additional quote-style flip. Source HTML: `href="/dscr-loans-texas.html"`. Served HTML: `href='/dscr-loans-texas'`. Live re-verify greps must be tolerant to both. Path-agnostic regex pattern: `href=['\"]/<page>(\.html)?['\"#?]` works for both source and served.
- **PM-run pattern: pull TOMORROW_PRIORITY items forward when concrete + reversible.** Today's PM run advanced Thursday's "Internal Linking + Funnel Flow" rotation by completing the cluster cross-linking half (the funnel trace half requires Chrome — not available in scheduled context, deferred). Same pattern as 2026-04-29 PM (AEO insertion) and 2026-04-30 PM (bookkeeping commit). PM run produces correct framing for Thursday's AM run, which itself is valuable.
- **Working-tree style.css orphan change preserved untouched.** Found uncommitted `header.scrolled .nav-dropdown a` color/hover rules in working tree — clearly Adam's pending fix from after the d031be5 nav-restructure (otherwise dropdown menu items become invisible on scrolled-header state). Per CLAUDE.md "Touch only what the request requires," NOT mine to commit. Used `git add <specific files>` instead of `git add -A` to avoid sweeping it in. Surfaced in FLAG_FOR_ADAM MEDIUM. Pattern: when a scheduled task finds orphan working-tree changes, use file-specific staging and surface in run log; do not auto-commit other-author work.
- **NotebookLM 21st consecutive dead run.** Same pattern. SKILL.md retirement diff still pending Adam apply. The carry-forward NOTEBOOK_INSIGHTS continues to do the work; the failed-script call adds noise to every run log.

---

## 2026-05-04 — Competitive Week 9: First Leander Top-10 + Two New Refi Threats + Bee Cave 31 Days Unindexed

### Patterns
- **Hutto playbook reproduces on Leander.** `leander-mortgage-lender.html` (last edited 2026-04-28) appeared at #6 for "leander tx mortgage lender" — first top-10 ranking for Leander since tracking began. Same trajectory shape as Hutto: deepen page → schema (FAQPage + AggregateRating verified on disk) → indexed → ranks within 1-2 SERP cycles. This is the validating signal that the suburb-page strategy is the right one. Replication action set: GSC URL Inspection → neighborhood depth (Crystal Falls, Travisso, Caballo Ranch, Bryson) → Leander ISD by name → CapMetro Red Line angle. Goal: #6 → top 4 within 2 cycles.
- **Refi SERP volatility = newcomer opportunity.** Two new entrants in 2 weeks on refi keywords: Joel Richardson/FCM (Week 8 #1 cash-out) → Arnaiz Mortgage (Week 9 #1 cash-out, #2 refi). Joel demoted to #3 cash-out within one cycle. Both got to #1 with NO suburb pages, NO visible review counts, NO BBB ratings — pure E-E-A-T plays from license footprint + page templating. The refi SERP is rolling weekly. This is opportunity, not threat: a content+schema push on `/loans/refinance.html` could land Adam in top 10 for refi keywords for the first time, in a 2-3 week window. Missed signal in prior weeks treated this as competitive — the right read is "rankings are unstable, push now."
- **NEW competitor pattern: programmatic location templates without dedicated suburb pages.** ATX Mortgage Lending (NMLS #2445761, very recent license) entered #4 for "mortgage lender austin tx" with body-text mention of all 9 suburbs Adam targets (Leander, Cedar Park, Liberty Hill, Georgetown, Round Rock, Hutto, Pflugerville, Kyle, Buda) but ZERO dedicated suburb pages. This is a different threat shape than Big Life (which has dedicated pages but template content) — ATX has neither yet, and is winning on root-domain + body-text alone. If they add dedicated pages, they become a structural competitor. Watchlist action: re-check `atxmortgagelending.com/sitemap.xml` in 2 weeks.
- **Re-verify gate caught two simultaneous downgrades this week.** MortgageAustin.com (was #1 mortgage interp for "get pre-approved austin tx") → demoted to #2 with Mazda Central #1. Joel Richardson/FCM (was #1 cash-out) → demoted to #3 with Arnaiz Mortgage #1. Both demotions would have been silently carried forward without the gate. Pattern confirmation: on any volatile vertical (refi, pre-approval), expect rotation, run the gate.
- **Re-verify gate: cleared one stale claim (Vista Lending #2 home loan).** Vista was at #2 last week; this week not in top 10 for "home loan austin tx" (SouthStar moved up). Auto-resolved in this run's gate.
- **Bee Cave indexing = blocking, not delayed.** 31 days post-sitemap, zero `site:` results. This has now been flagged in 5+ consecutive weekly competitive reports without resolution. The escalation path is "Adam manual GSC URL Inspection" — no agent action will fix this. Pattern: when something requires Adam manual action and 5+ runs have flagged it, the FLAG_FOR_ADAM channel is failing — needs a different surfacing mechanism (e.g., a top-of-CONTEXT "BLOCKING" line that can't be missed).
- **18 suburb pages on disk, 2 ranking top 10.** Cluster-progress denominator is now visible: 16+ suburb pages on disk, 2 in top 10 (Hutto, Leander). The 9 unranked-but-existing suburb pages are the next opportunity tier. Triage by commercial volume: Pflugerville, Cedar Park, Round Rock = Tier A (highest opportunity, partial indexing already). Kyle, Liberty Hill, Buda = Tier B. Smaller markets (Florence, Jarrell, Manor, Elgin) = Tier C.
- **Georgetown SERP = physical-presence ceiling.** 6 of 10 Georgetown results have physical Georgetown addresses. Different threat shape from Hutto (template-quality competitors) and Leander (directories + body-text mentioners). Georgetown requires a different angle: "broker shops 40+ lenders" vs "we're local" — and a Sun City retiree-jumbo specialty hook may be the wedge.
- **NotebookLM 19th consecutive dead run.** Same pattern. SKILL.md retirement diff still pending Adam apply.

---

## 2026-05-03 — Re-Verify Gate: Count-Method Drift vs Real Drift (18th Dead NotebookLM Run)

### Patterns
- **`grep -oi <substring>` counts substring occurrences, not lines — and a single HTML link contains the substring twice.** Today's how-to-buy USDA re-verify returned 8 matches; Saturday PM had reported 6. The page hasn't been edited since 2026-04-23 (commit d2ef146). The "drift" was a measurement artifact: each `<a href='/loans/usda'>USDA</a>` link contains "usda" twice (URL slug + visible text), so 2 link locations + 4 body mentions = 8 substring matches across 5 lines. To prevent fake-drift in future re-verify gates, either: (a) cite both `grep -coi` (line count) AND `grep -oi … | wc -l` (substring count), or (b) standardize on substring count and never switch methods between runs. Page-impact answers want line count; SEO-impact answers want substring count. Pick one and stick with it inside a single claim's lifecycle.
- **Re-verify gate doing its job in the opposite direction:** today's gate didn't auto-resolve a stale claim — it caught a stale *count* on a still-true claim. Same discipline (re-check live before reporting), different outcome shape: the blocker stays open, the metric was wrong. This is its own positive signal — gate prevents reporting drift, both real and apparent.
- **Sunday-after-thorough-Saturday repeat is the cheapest possible run shape.** AM Sat = thorough sitemap + re-verify. PM Sat = repeat gate (no drift). Sun AM = repeat gate (one count correction, no actionable). Three runs, one honest correction, zero false alarms, zero site mutations. This is exactly what the "skip only if AM was incomplete" rule was trying to enable.
- **NotebookLM 18th confirming dead-script check.** No change. SKILL.md retirement diff still pending Adam apply.

---

## 2026-05-02 PM — Same-Day PM Re-Verify Confirms No Drift (17th Dead NotebookLM Run)

### Patterns
- **Same-day PM repeat of AM re-verify gate is cheap and confirms no drift between runs.** AM run 2026-05-02 was thorough Saturday "sitemap + re-verify only". PM ran the identical gate against live state ~hours later: zero drift on 9 claims. Useful discipline when AM was thorough and PM has no rotation: rather than skip PM entirely, repeat the gate to formalize "AM commit propagated to live AND nothing drifted in the gap." Skip only if AM was incomplete or known-broken.
- **NotebookLM 17th confirming dead-script check.** No change. SKILL.md retirement diff still pending Adam apply (concrete patch in 2026-04-26 AM `latest.md`). Pattern for next run: continue the count, do not re-pitch the patch — Adam's queue is the bottleneck, not surfacing.

---

## 2026-04-30 PM — TOMORROW_PRIORITY Pair Stale-Detection + "Cluster Effectively Complete" State + Voice-Guide Deferral Discipline

### Patterns
- **TOMORROW_PRIORITY *pairs* (not just filenames) need live-verified completeness state at insertion-attempt time.** AM run today named DSCR + how-to-choose-lender as Friday's AEO pair with the qualifier "verify `<header>` template before insertion." PM run executed the verify gate and found BOTH already had leading `<p><strong>` from prior sessions (DSCR was added 2026-04-28; lender pre-cluster). The lesson refines yesterday's filename-verification rule: confirm not just that the file exists, but that the cluster work is actually still needed at AM-run planning time. The fix: AM run's audit script should grep the leading-`<strong>` state inline before naming the pair, not just look up the cluster-progress count.
- **Cluster denominator drift caught by fresh audit method.** AM run reported "9/16 OK, 7 remaining"; fresh audit (find first `<p>` after `</div>` closing newsletter-author-bar; check leading `<strong>`) shows 11/16 OK, 5 remaining. Two posts were silently fixed in prior sessions and the audit script missed them. Generalized rule for any cluster-progress claim: re-run the audit script at the start of each session before reading the prior count. Counts age fast.
- **"Cluster effectively complete" is a valid cluster-state.** The 5 older-template posts still flagged as NEEDS_AEO are NOT behind schedule. 4 are explicitly deferred per voice guide (rate-volatility / life-devotional cluster: oil-prices, surrender-it stolen-car, ai-trap, why-rates-improved-bond-rally — voice guide says "not every post needs a CTA / Some posts should just end" + the stolen-car post performs *because* it has zero mortgage content). Adding answer-first paragraphs to personal-story posts dilutes voice for marginal AEO gain. The 1 remaining (why-home-prices-arent-crashing) has unusual structure (duplicate sub-title h2 + meta-date `<p>` between author-bar and first content) and requires Adam's structural decision. Net: the older-template AEO sprint is *effectively complete* pending one structural call. Future runs should not keep flagging "5 remaining" as if it's an open queue — it's a closed cluster modulo Adam's decision.
- **Voice-guide deferral is not a regression of the AEO sweep.** Initial AEO planning (2026-04-27 PM learnings) explicitly carved out the rate-volatility/life-devotional cluster as "separate pass at the end — different question framing." Three weeks later, the right move is to *not do that pass* — these posts are doing their job in voice-only form. The "16 NEEDS_FIX" denominator was wrong from the start; the realistic target was always ~11-12.
- **PM run as bookkeeping run is also a valid PM-run shape.** Last few PM runs were AEO-insertion runs. Today's PM run had no mechanical AEO insertion available (cluster effectively complete) and no Adam-decision-actionable item. The right move was a bookkeeping commit: stale-claim auto-resolve + cluster restatement + Friday TOMORROW_PRIORITY refresh. Not every PM run produces HTML changes; some produce *correct framing* for tomorrow's AM run, which is itself valuable.
- **NotebookLM 13th consecutive dead run.** SKILL.md retirement diff still pending Adam's apply.

---

## 2026-04-29 PM — AEO Cluster +2 (austin-rates-march, DPA-texas) + Filename Verification Lesson

### Patterns
- **TOMORROW_PRIORITY filenames must be live-verified before insertion.** Morning run named `2026-03-28-how-to-take-mortgage-pre-approval` in TOMORROW_PRIORITY #5 — the actual file is `2026-03-28-how-long-does-mortgage-pre-approval-take.html`. PM run caught this with `ls blog/2026-03-28*.html` (~5 seconds). Lesson for next runs: on TOMORROW_PRIORITY items that name specific files, confirm with `ls` before reading or editing. The pre-approval post still needs work but the filename now points correctly for Thursday's run.
- **Distinct-angle pattern works even when existing P1 IS answer-first.** DPA-texas's existing first paragraph already led with "first-time buyers can access 3% to 5%…" — a structurally answer-first opener, just lacking `<p><strong>` wrapper. The right move was NOT to wrap that paragraph but to add a NEW answer paragraph above it that frames a different facet of the answer (the TDHCA vs TSAHC comparison). This produces two AI-extractable layers: existing P1 = "what's available," new body answer = "how the two programs compare." Both serve AI Overviews for different query intents. Generalized rule: distinct-angle works on three sub-cases now — (a) voice-y existing intro + machine-extractable new layer; (b) existing intro that already contains the answer mid-paragraph (don't replace, add); (c) existing intro that IS answer-first but lacks `<strong>` wrap (add a different-facet answer above, don't fight over the same wording).
- **PM run cadence is becoming a pattern.** Wednesday April 29 fired both AM (08:04) and PM (23:08), same as April 27. The PM run's job is NOT to redo the AM rotation — it's to advance TOMORROW_PRIORITY items that are concrete + reversible + don't require fresh decisions. AEO `<p><strong>` insertion is the canonical PM-run task: pre-defined insertion pattern, distinct-angle rule, post's-own-numbers reuse — all fully-specified by morning's NOTEBOOK_INSIGHTS.
- **NotebookLM 11th consecutive dead run.** Same as 10th. Diff pending Adam's apply.

---

## 2026-04-28 — Re-Verify-Before-Write Saves a Whole AEO Insertion + Em-Dash Char-Count Gotcha + Auto-Compliance USDA Removal

### Patterns
- **Re-verify-before-write rule for AEO body answers: grep `<p><strong>` BEFORE inserting.** TOMORROW_PRIORITY #4 from yesterday's PM run lined up the next pair: DSCR + how-to-choose-lender. Both posts use the newsletter-author-bar template, so the insertion path was identical. But when I read how-to-choose-lender to plan the AEO insertion, I found it already had a `<p><strong>` answer at line 190 ("To choose a mortgage lender in Austin TX, compare independent brokers…"). Likely added in an earlier session that wasn't tracked in the cluster audit. Saved an entire insertion + commit + dedup-risk. Lesson: before any AEO insertion, run `grep -A2 'newsletter-author-bar' <file> | grep -A1 'p><strong'` (or equivalent) to confirm the post is actually missing an AEO answer. The audit-script-derived list of "16 needing fix" is a snapshot in time and may be stale by the time it's worked through. Re-verify each individual file at insertion time, not only at audit time.
- **Em-dash byte vs. char gotcha when targeting 160-char meta descriptions.** `wc -c` counts bytes, not chars. UTF-8 em dash (—) = 3 bytes = 1 char; en dash (–) = 3 bytes = 1 char. So a meta description with 1 em dash and "162 bytes" is actually 160 chars (right at the cap). After my first pass, two pages (first-time-home-buyer + rate-check) showed `wc -c` outputs of 162-163; raw byte count put them under-cap, but I trimmed conservatively to 152-160 chars to absorb future em-dash inflation. Validation rule: when meta is "over 160 by a hair," use `python3 -c 'print(len(<string>))'` to get the actual char count. Don't waste a trim cycle on byte-count phantom-overage.
- **USDA removal on meta descriptions is auto-doable; body USDA removal still requires Adam decision.** Voice guide explicitly says Adam does NOT do USDA. So removing "USDA" from a meta description is just enforcing the existing rule — no judgment call, low risk, fully reversible. But on body copy, USDA references can be in: loan-program tables (potential broken link if "/loans/usda.html" referenced); FAQ schema entries (rebuild required); supporting-product narratives (substantive rewrite). Each touches reader-facing product menu in ways that affect perceived offering. Decision split: meta auto-fix; body Adam-decision. Today's first-time-home-buyer.html meta change was the small-surface auto-fix. The body USDA cleanup on how-to-buy + Smithville/Elgin/Florence/Jarrell remains carry-forward in FLAG_FOR_ADAM.
- **`how-to-choose-lender` already had AEO answer in v1 → audit script was missing a "leading p+strong in article body" detection mode for newsletter-author-bar posts.** The "16 NEEDS_FIX" count is one too many. Should now be "15 NEEDS_FIX, 1 already done." Next audit script pass should improve detection: walk DOM `<article class="blog-post"> > <section> > <div.container-narrow> > <div.article-body> > <div.newsletter-author-bar>` then check the next `<p>` for leading `<strong>` and 40-80 word count. If yes → already done. If no → NEEDS_FIX. The current script only looked at "first `<p>` after `<h1>` in `<article>`," which missed the case where the AEO `<p><strong>` was inserted as the actual first content paragraph after the author block.
- **TOMORROW_PRIORITY's "do this FIRST" instruction works under the re-verify gate even when the gate cancels the work.** Yesterday's PM run wrote `TOMORROW_PRIORITY #4 = DSCR + how-to-choose-lender pair`. Today's run picked it up first, executed the re-verify gate, and the gate canceled half the work (lender already done) — but the OTHER half (DSCR) still got done. Net result: TOMORROW_PRIORITY drove correct prioritization, the gate saved redundant work, and the cluster still advanced (4/16 → 6/16). This pairing of "directive priority" + "re-verify gate" is the right structure for sprint-style serial work.
- **NotebookLM 9th consecutive dead run.** Same pattern. Diff still pending Adam's apply. CONTEXT.md WAITING_ON_ADAM section suggested in 2026-04-27 AM learnings still hasn't been added; that suggestion remains worth implementing.

---

## 2026-04-27 PM — Older-Template AEO Cluster: 4/16 Closed + Second Insertion Pattern Confirmed (`blog-article-body`)

### Patterns
- **Second older-template insertion pattern confirmed: `blog-article-body` posts.** FTB (`2026-03-30-first-time-home-buyer-programs-austin-tx-2026.html`) does NOT use `newsletter-author-bar` — instead it uses `<article class="blog-article">` containing `<div class="blog-article-header">` (with `blog-article-meta`, `blog-article-title`, `blog-article-subtitle`, `blog-article-author`) followed by `<div class="blog-article-body">` with the article paragraphs. The author block closes inside `blog-article-header`, so there's no need to walk past it. Insertion point: immediately after the opening `<div class="blog-article-body">`, before the first `<p>`. Cleaner than the newsletter-author-bar pattern because it requires no "skip past author block" logic.
- **Voice-extraction layering: when existing first `<p>` is voice-y/casual, DON'T replace — add an extraction layer above.** FTB's existing first paragraph is "If you're buying your first home in Austin in 2026, there's money on the table most buyers leave behind." That's voice. It hooks human readers. Replacing it with a machine-extractable answer would weaken the human funnel. Solution: add the AEO answer as a distinct paragraph ABOVE the voice-y opener. Both serve different audiences. Two-layer extraction-then-voice works whether the existing paragraph already contains the answer (fha-vs-conv pattern) OR has voice but no extraction (FTB pattern). Rule unifies: AEO answer paragraph always goes ABOVE the existing first paragraph; existing paragraph is preserved untouched.
- **Voice-y casual openers like "there's money on the table most buyers leave behind" are exactly what the AEO answer paragraph should NOT try to be.** The job is division of labor: voice paragraph builds trust, answer paragraph gets indexed by AI Overviews. Trying to merge them produces stilted prose that does neither job well.
- **VA template = newsletter-author-bar (older `<header>` pattern). FTB template = blog-article-body.** Both are in the "older `<header>`-template" cluster but they have structurally different insertion points. Future audit script should detect which pattern each post uses and emit the correct insertion point. Likely most older-template posts are one of these two patterns; DSCR + how-to-choose-lender + self-employed need a quick template-check before the next run inserts.
- **Pair cadence updated:** ~~cash-out + fha-vs-conv~~ ✅ → ~~va-eligibility + ftb-programs~~ ✅ → dscr + how-to-choose-lender (next) → self-employed + DPA → how-long-pre-approval + market-report → rate-volatility / life-devotional (separate pass).
- **NotebookLM 8th consecutive dead run.** Same as 7th-run note above: SKILL.md retirement diff still pending Adam's apply; cached-learnings fallback continues to work. No new info to surface.
- **PM run note (process):** the daily task fired twice today (08:04 AM + 23:08 PM). The PM firing is not a normal pattern but worked cleanly because the AM run's `TOMORROW_PRIORITY #4` line provided a self-contained next-task description (the next AEO pair). Lesson: TOMORROW_PRIORITY is also serving as "if a second run fires today, here's exactly what to do" — keep it concrete and self-contained.

---

## 2026-04-27 AM — Older `<header>`-Template AEO Cluster: 2/16 Closed + about.html Address Schema Mismatch Discovered

### Patterns
- **Older-template AEO insertion point confirmed: after `</div>` of newsletter-author-bar, before first content `<p>`.** Both posts in this run had identical structure: `<section class="section"><div class="container container-narrow"><div class="article-body"><div class="newsletter-author-bar">...</div>` followed by the first content paragraph. The clean insertion point is between the closing `</div>` of newsletter-author-bar and the first `<p>`. This is the pattern for all 14 remaining older-template posts.
- **When the existing first paragraph already contains the answer (mid-paragraph `<strong>`), DON'T replace — add a distinct-angle paragraph above.** fha-vs-conventional already had `<strong>if your credit score is below 640... start with FHA</strong>` embedded in "The short answer:" — replacing would lose the conversational warmth. Adding a new answer-first paragraph above with a distinct angle (decision-frame + MIP-vs-PMI life-of-loan distinction) preserves human voice AND satisfies machine extractability AND avoids duplication. This is a refinement of the distinct-wording rule from the rate-shopper series.
- **Use the post's own numbers/examples in the body answer — eliminates compliance risk and avoids reader-confusion.** Cash-out body answer used 22% credit-card → 7% mortgage / $8K–$12K closing costs / Texas 80% LTV — all already in the post body. Fha-vs-conv body answer used 640/680 credit / 5%+ down / PMI cancels at 20% / MIP stays for <10% down — all already in the post FAQ + table. Rule: scan post body for existing illustrative numbers before writing the body answer; reuse them.
- **NEW second audit script for older-template posts works correctly.** Logic: find `<article>` (or `<main>`) → find first `<h1>` inside → find first `<p>` after that `<h1>` → HAS_AEO if leading `<strong>` and 40-80 words. Skip posts using `blog-post-header` template (already audited). Found 16 posts needing fix (1 already had it: 2026-04-03-condo-mortgage). After this run: 2/16 closed, 14 remaining. Cadence: 2/AM-run paired by topic.
- **Pair cadence by topic for older-template AEO sprint:** ✅ cash-out + fha-vs-conv (refi+FHA cluster) → va-eligibility + ftb-programs (loan-type/buyer-segment) → dscr + how-to-choose-lender (investor+advisor) → self-employed + DPA → how-long-pre-approval + market-report. Rate-volatility / life-devotional posts (oil-prices, austin-rates-march, why-rates-improved, spring-market-update, ai-trap, surrender-it) get a separate pass at the end — they have a different question framing (event-driven vs evergreen-question). Topic pairing keeps each session focused on a single vocabulary cluster.
- **NEW: Schema entity-consistency audit caught a real address mismatch on about.html.** Homepage MortgageBroker schema = `5718 Sam Houston Circle, Austin TX 78731` (matches the about.html Person schema). About.html LocalBusiness schema = `5900 Balcones Drive, Suite 100, Austin TX 78731`. Two different streets in the same ZIP. Likely the about.html LocalBusiness was added later and used the company HQ address while the homepage uses Adam's office address. Either is defensible but they MUST match for AEO/local-pack signals. Adam decision item — do not auto-fix without confirmation. Lesson: schema audits should compare addresses across MortgageBroker, LocalBusiness, and Person entities — not just check that each one is "valid" in isolation.
- **Re-Verify Gate is now catching same flags 2-3 runs in a row that don't change state because they require Adam decision.** USDA cleanup, about.html timeline-date, thank-you.html alt-paths, and now about.html LocalBusiness address — all reach the gate but stay carry-forward because the resolution path is "Adam decides." This is correct behavior (no muscle-memory drift) but suggests a meta-improvement: a "WAITING_ON_ADAM" section in CONTEXT.md so these don't keep cycling through ISSUES FOUND each run as if they were new.
- **NotebookLM 7th consecutive dead run.** Pattern is now: SKILL.md edit drafted 6 days ago, still pending Adam's apply, falling-back-to-cached-learnings working fine. The flag is no longer telling Adam anything new — it's telling future-me "the patch is still pending." Suggestion for next time: fold this into CONTEXT.md WAITING_ON_ADAM and remove from FLAG_FOR_ADAM HIGH-ESCALATED tier until status changes.

---

## 2026-04-27 — Competitive Week 8: SERP-Wide Snapback + Hutto #1 Lost + Round Rock Dropped

### Patterns
- **First negative-direction week in 4 weeks of suburb-page tracking — and it was algorithmic, not regression**: styermortgage.com went from 2 keywords in top 3 (Hutto #1, Round Rock #2) to 1 keyword in top 3 (Hutto #2). Both pages still have correct schema (136 reviews) and recent edits (Hutto 2026-04-25, RR 2026-04-23). Big Life reclaimed Hutto #1 without measurable content updates — no neighborhood names added, no 2026 dates, no schema changes, still ~25 reviews. This means our content advantage is intact but algorithmic factors (freshness, click-through, possibly inbound link gain) tilted toward Big Life this week. Defense move: GSC URL Inspection re-indexing request to push our updated schema to next crawl, not more content rewrites.
- **The "content velocity" thesis got a sharp counter-example**: Last week (2026-04-20), Nest Mortgaging was the headline new threat — 6+ keywords ranking, 10+ 2026-dated posts, "content machine" out of Kyle TX. This week: zero appearances in any tracked keyword. Total disappearance in 7 days. AsertaLoans (last week's new #1 cash-out refi entrant) also gone. Old incumbents re-emerged: AustinHomeLoans.com back at #1 home loan, Vista Lending back at #5 lender, Highlander back at #2 broker, Sente back at #10 home loan. The SERP snapped back ~3-4 weeks toward older/established domains. Lesson: pure content velocity without domain authority is brittle. Our suburb-first strategy is structurally more durable than competing on head-term content velocity.
- **Round Rock #2 → not found fits "new page sandbox" pattern**: Round Rock page deepened 2026-04-19, ranked #2 the very next Monday (2026-04-20), gone from top 10 by 2026-04-27. Classic Google sandbox-bounce for newly-deepened pages — they appear, get tested by clicks/dwell, then drop while Google re-evaluates. Recovery path: GSC URL Inspection (forces re-crawl) + don't over-rewrite (let Google re-stabilize the ranking). Don't add 5 new sections in panic — you'll trigger another sandbox cycle.
- **CrossCountry Mortgage 2-branch dominance on Cedar Park is structural**: Cedar Park SERP has CrossCountry at #1 AND #3 with two physical branch listings. This is a structural advantage we cannot match (we don't have physical branches). For any suburb where CrossCountry has 2 branches, the realistic ceiling is #4-#6. Cedar Park strategy must shift from "out-content" to: (1) realtor preferred-lender placements (cedarparktxliving.com is #2 — relationship play), (2) hyper-local content depth (Cedar Park ISD, Brushy Creek Lake, Twin Creeks, Buttercup Creek), (3) "no branch needed — we're an independent broker" positioning.
- **Lone Star Financing is the structural suburb leader**: Has #1 Round Rock + #1 Lakeway with dedicated `/round-rock/` and `/lakeway/` pages. Same template, multi-suburb expansion, well-tenured domain. They are the suburb competitor to model and the structural leader to displace one suburb at a time with hyper-local depth.
- **AustinHomeLoans.com re-emerged with 40-year tenure moat**: Schutze Brothers, "native Austin feel", 2,000+ closed loans, 245+ 5-star reviews, A+ BBB, 40+ Years. Back at #1 for "home loan austin tx." Confirms: don't waste resources targeting head-term Austin keywords. Suburb-first is the correct strategic call.
- **"Get pre-approved austin tx" SERP is contaminated with auto-loan results — this is itself an opportunity**: Half the top 10 are car dealerships and credit union auto loan pages (Roger Beasley Mazda, Long Motors, Bentley Austin, etc.). MortgageAustin.com still holds #1 for the mortgage interpretation, but the title-tag battle is winnable by anyone who explicitly hooks "MORTGAGE pre-approval" vs auto. Title rewrite opportunity for `/mortgage-pre-approval-austin.html`.
- **NotebookLM Step 0/4 — 6th consecutive run dead**: `notebook_advisor.py` still absent from disk (verified 2026-04-27). The 5th-run patch drafted on 2026-04-26 has not been applied. Pattern continues — falling back to cached learnings + direct strategic reasoning works fine. The skill needs a SKILL.md edit; flagging again creates noise.
- **Re-verify gate caught 9 prior claims this week — record number**: Hutto #1 (downgraded), Round Rock #2 (cleared), AsertaLoans (cleared), Nest 3-position dominance (cleared), Vista cleared (reversed), Highlander cleared (reversed), AustinHomeLoans cleared (reversed), Sente "not appearing" (partially updated), Nest #1 home loan (cleared). The gate is doing its job — without it, this report would have been a graveyard of last week's now-false claims. Pattern: in a high-volatility SERP week, the re-verify gate is the single most important step.

---

## 2026-04-26 PM — should-i-refinance AEO Body Answer + blog-post-header Template 14/14 Complete

### Patterns
- **`blog-post-header` template AEO cluster now 14/14 complete** — Tonight's PM run closed the last gap: `2026-04-17-should-i-refinance-austin-tx-2026.html`. The post used the exact same `<header class="blog-post-header"> → <p class="blog-post-intro"><strong>... → </header>` anti-pattern as the rate-shopper series. Same fix worked: body `<p><strong>` (56 words) inserted after `</header>`, distinct angle from the intro. Cluster total: 10 rate-shopper posts (closed 2026-04-23 → 2026-04-26 AM) + 4 dated 2026-* posts (3 already done pre-audit + tonight's 1) = 14 posts. The `blog-post-header` template is now AEO-clean.
- **All 6 H2s already in question form on should-i-refinance — no statement→question conversion needed**: This is the first post in the AEO series where every single H2 was already a question. The post was apparently authored after the AEO question-H2 pattern was internalized. Lesson: scan H2s before assuming a conversion is needed; sometimes the only gap is the body answer paragraph itself.
- **Audit method needs a second pass for the older `<header>` template (~16 dated 2026-* posts)**: The `<header class="blog-post-header">` audit script returns NO_BLOG_HEADER for posts that use the older `<header>` (page nav) + `<article class="blog-article">` template. Inspecting 5 representative posts (cash-out, fha-vs-conventional, va-eligibility, ftb-programs, dscr) shows they have `<p><strong>` paragraphs throughout the body but NOT as the answer-first leadoff after `<h1>`. They need a different audit method: find first `<p>` after `<h1>` inside `<article>`/`<main>`, check if it starts with `<strong>` and is 40-70 words.
- **Same-day double-run pattern continues to compound progress** — Today's morning run + tonight's PM run combined to: (1) complete the rate-shopper series (10/10), (2) close the last `blog-post-header`-template gap, (3) audit + propose the next AEO target cluster with cadence. Three discrete deliverables in one calendar day. The morning-PM rhythm is now the proven pattern for accelerating cluster work.
- **Next AEO sprint cadence proposed: 2 posts per AM run, paired by topical cluster**: cash-out + fha-vs-conventional → va-eligibility + ftb-programs → dscr + how-to-choose-lender → self-employed + (next loose thread). Pairing keeps each session focused on a single topic family and lets the body-answer wording reuse vocabulary across the pair.
- **thank-you.html uncommitted change discovered tonight (not from this scheduled task)**: `git diff` showed an unstaged modification to thank-you.html exposing `ty-alt-paths` for refinance + preapproval thank-you-types. Not staged in tonight's commit. Logged in FLAG_FOR_ADAM. Lesson for future PM runs: always run `git diff` and `git status` before staging, and only stage the files you actually touched this session — never `git add .` or `git add -A` even when "everything looks fine."

---

## 2026-04-26 — Rate-Shopper AEO Series Complete (10/10) + NotebookLM 5th-Run Concrete Patch

### Patterns
- **Rate-shopper AEO series 10/10 milestone — anti-pattern fully closed**: Today's run knocked out the final two posts (`what-to-compare-besides-mortgage-rate.html` + `is-the-lowest-rate-the-cheapest-loan.html`). Both had the predicted `blog-post-intro`-in-`<header>` anti-pattern. The body `<p><strong>` after `</header>` fix worked cleanly. The full series of 10 rate-shopper posts now has machine-extractable answer paragraphs in the article body, distinct from the styled `<header>` intros. This is the largest single-anti-pattern AEO sweep the site has completed — 10 posts, 4 runs, ~20 minutes per run.
- **AEO body-paragraph word-count zone — 50-70 words confirmed across all 10 posts**: Today's two new body answers landed at 60 and 64 words. All ten posts in the series fall in the 50-70 range. The "lead with answer in the first 12-15 words" rule held in every case. This is now the canonical word-count target for any future AEO body paragraph on this site.
- **Distinct-wording rule prevents thin-content/duplicate signal — 10/10 examples**: Every post in the series paired a styled `blog-post-intro` (factor list / total-cost frame / general statement) with a body answer that picked a different angle (practical method / time-horizon focus / specific dollar example). For a future series sweep on a different post cluster: scan the existing intro first, then write the body answer to attack the question from a complementary angle.
- **Aligning illustrative numbers to the post's own example builds consistency**: For the is-the-lowest-rate-cheapest body answer, used the post's existing Lender A (6.375%/$8,500) vs Lender B (6.5%/$3,000) numbers from line 196-204 instead of inventing new ones. This eliminates compliance risk (no new rate quotes), reads as a preview of the post's own argument, and avoids reader-confusion if numbers diverged. Rule: when adding an AEO body answer that includes illustrative figures, scan the post body first and reuse its own example numbers.
- **NotebookLM Step 0 — 5th-run concrete diff drafted instead of recurring flag**: After 4 runs of carrying NotebookLM as an escalating flag, today's run wrote a unified-style diff Adam can apply to SKILL.md as a one-shot edit. Rule for any agent-spec problem that has carried forward 4+ runs without resolution: stop re-flagging the abstract issue and write the concrete patch instead. The `notebooklm` binary at `/Users/adamstyer/.local/bin/notebooklm` exists but is a source-management CLI (used in Step 8) — not a Q&A advisor — so the cleanest patch retires Step 0 entirely and leans on the carry-forward NOTEBOOK_INSIGHTS pattern that's already working.
- **Re-Verify Gate — claims I don't own (no live verification path) carry forward without re-checking**: USDA cleanup on Smithville/Elgin/Florence/Jarrell/Liberty Hill and the how-to-buy loan table all depend on Adam's confirmation of his actual product offering, not on a live state I can curl. The Re-Verify Gate explicitly carves these out ("Claims this task does NOT own"). Carrying them forward without re-verification is correct behavior, not muscle-memory drift.

---

## 2026-04-25 PM — Rate-Shopper AEO 8/10 + NotebookLM 4th-Run Escalation

### Patterns
- **Rate-shopper AEO 8/10 milestone — only 2 posts remaining**: Today's PM run knocked out `what-delays-closing-when-you-switch-lenders.html` and `how-to-read-a-loan-estimate.html`. Both had the predicted `blog-post-intro` inside `<header>` anti-pattern. The body `<p><strong>` after `</header>` fix continues to work cleanly. Remaining 2 rate-shopper posts: `what-to-compare-besides-mortgage-rate.html` + `is-the-lowest-rate-the-cheapest.html` (or whichever is in the remaining set — confirm by grepping `blog-post-intro` inside `<header>` on each rate-shopper post). Sunday's run should finish the series.
- **AEO body-paragraph word-count zone — 50-70 words is the sweet spot**: Today's two new body answers landed at 56 and 66 words. Yesterday's were 60 and 54. All four read as direct answers without overflowing the visible viewport on mobile. Word-count target for the remaining 2 posts: 50-70 words, lead with the answer in the first 12-15 words.
- **Distinct-wording rule prevents thin-content/duplicate signal**: The `blog-post-intro` and the body `<p><strong>` answer cover the same question but from different angles. delays-closing intro emphasized "appraisal + waiting too long" — body emphasized "start within first week, here's why timeline holds." loan-estimate intro listed the 5 numbers — body emphasized "don't read every line, focus on five." This pattern keeps both paragraphs useful without triggering duplicate-content signals or feeling repetitive to a human reader.
- **Two-runs-same-day cadence proven again — same-day TOMORROW_PRIORITY can be executed by a same-day second run**: This is the second instance (also happened 2026-04-24) where the morning run set TOMORROW_PRIORITY for "Sunday" but the schedule fired again the same day, the second run executed it cleanly, and progress accelerated from 2/week to 4/week on the rate-shopper AEO series.
- **NotebookLM Step 0 — 4th consecutive run confirms dead, remediation patch should be drafted**: Step 0 has now been a no-op for 4 runs (2026-04-23, 04-24 AM, 04-24 PM, 04-25 AM, 04-25 PM = arguably 5 runs). Sunday's run should write the concrete SKILL.md diff — not just keep flagging — so Adam can approve a one-shot edit. Proposed replacement: detect `/Users/adamstyer/.local/bin/notebooklm` (which DOES exist), use it via the `notebooklm` skill, and otherwise fall back to cached `NOTEBOOK_INSIGHTS` from learnings.md.
- **Git rebase needed before push — sister scheduled task wrote between commit and push**: After commit, `git push` was rejected because `046948a` (suburb-deepener Phase 1c flag) had landed on remote during this run. `git pull --rebase origin main` cleanly replayed today's AEO commit on top. Pattern for future runs: when push is rejected, always check `git log HEAD..origin/main` first to see what landed, then rebase. Never force-push.

---

## 2026-04-25 — Footer Awards Sitewide Batch + Two Indent Variants + Pattern A vs Pattern B Discrimination

### Patterns
- **Two indent variants required two regex passes — single-line variant was the surprise**: Initial multi-line regex (`(\s*)star1<br>\n\s*star2`) caught 47 of 56 files. Inspecting the 9 remaining files revealed they all use a single-line variant: `<p>star1<br>star2</p>` with both stars on the same line, no internal newline. These are the auto-generated/minified-style root pages (austin-housing-market-2025, austin-down-payment-assistance, austin-mortgage-rates, calculators, closing-costs-texas, how-much-house-can-i-afford-austin, mortgage-glossary, mortgage-pre-approval-austin, rate-check-cedar-park). Lesson for future template fixes: always grep first with the broadest possible content match (`91 Google Reviews`), then inspect a sample from each "missing" group to detect formatting variants. Don't assume one regex catches all variants.
- **Pattern A (footer `<p>` Awards) and Pattern B (timeline `<span>` milestone) are NOT the same drift vector — discriminate before batching**: `grep -l "91 Google Reviews"` returns 57 files. 56 of those are footer Awards `<p>` blocks; 1 is about.html's milestone timeline `<span class="timeline-date">91 Google Reviews &bull; 45 Zillow Reviews</span>`. The timeline-date span is contextually different — it sits next to `<h4>5.0 Star Google Rating</h4>` as a record of when that milestone was reached. The TOMORROW_PRIORITY explicitly scoped to the `<p>` form, and the timeline span carries a "this is when this happened" implication that may be intentional. Rule: when a sitewide grep returns a file with a different surrounding structure than the rest, audit it separately and surface as a discrete decision rather than batching.
- **`\g<lead>` named capture group is the cleanest way to preserve indent in Python regex `.sub()` replacement**: `re.sub(r'(?P<lead>[ \t]*)pattern', r'\g<lead>replacement', text)` works across all indent levels (8-space and 10-space variants both passed the same regex). Avoids the brittle `\1` numeric backref + escape concerns.
- **88 pages now display "136+ Reviews" format after the batch**: Sitewide consistency check `grep -l "136+ Reviews" *.html blog/*.html | wc -l` returned 88. This is the new reference number for any future AggregateRating drift audit. If this number drops below 88 in a future run, a regression has been introduced.
- **6-of-10 rate-shopper AEO milestone reached**: After today's run, 6 of 10 rate-shopper blog posts have the AEO-correct `<p><strong>` body paragraph pattern (can-i-switch, how-to-compare, apr-vs-interest-rate, are-mortgage-lender-fees-negotiable, how-many-mortgage-quotes, local-lender-vs-online). Remaining 4: what-delays-closing, how-to-read-a-loan-estimate, what-to-compare-besides-rate, is-the-lowest-rate-the-cheapest. Same anti-pattern likely; same fix. Two-per-run cadence completes the series in 2 more Friday rotations.
- **NotebookLM Step 0 — 3rd consecutive run confirms dead**: `notebook_advisor.py` not at `/Users/adamstyer/loanos/scripts/` or anywhere on disk. The `loanos/` directory itself is gone (replaced by `loanos-clone/`). 3rd run = officially escalated. Sunday's run should propose the SKILL.md edit removing Step 0 outright if Adam hasn't restored the script.

---

## 2026-04-24 PM — Rate-Shopper AEO Continued + Footer Awards Sitewide Drift Discovery

### Patterns
- **Rate-shopper AEO anti-pattern is consistent across the entire 10-post series**: 4 posts now confirmed to have answer-first text only inside `<p class="blog-post-intro">` within `<header>` — can-i-switch (fixed AM), how-to-compare (fixed AM), apr-vs-interest-rate (fixed PM), are-mortgage-lender-fees-negotiable (fixed PM). Pattern: every rate-shopper post created in this series uses the same template. The remaining 6 posts (how-many-mortgage-quotes-should-i-get, local-lender-vs-online-lender, what-delays-closing, how-to-read-a-loan-estimate, what-to-compare-besides-rate, is-the-lowest-rate-the-cheapest) almost certainly have the same gap. Future Friday rotations should batch through these 2-at-a-time.
- **Footer Awards sitewide drift discovered — suburb-only fix on 2026-04-23 missed root pages and blog posts**: `grep -l "91 Google Reviews"` reveals 28 blog posts + 29 root pages still carry the old `★ 5.0 Stars · 91 Google Reviews / ★ 4.98 Stars · 45 Zillow Reviews` two-line format. Suburb pages were already fixed (round-rock spot-check confirmed standard format). The April 23 batch was scoped to `*-mortgage-lender.html`. Rule for future sitewide template fixes: ALWAYS run `grep -l "<old-string>" *.html blog/*.html *-mortgage-lender.html` to confirm the fix is truly complete across all directories. Class-name or filename-pattern globs miss whole categories.
- **Friday rotation discipline — DECISION TEST said batch the 57-file footer fix; CLAUDE.md "surgical changes" said don't**: The DECISION TEST in SKILL.md says "If you cannot answer YES to all three, you MUST attempt the work" and treats "do all 25 of them" as a non-blocker. CLAUDE.md says "touch only what the request requires." When TOMORROW_PRIORITY explicitly says "Pick 2 blog posts and check," that's the contract for the session — adjacent batch work goes into the next TOMORROW_PRIORITY rather than expanding scope mid-session. The 57 files are queued for the next run as a ZERO_RISK batch.
- **Two-runs-same-day pattern — preserve AM run via master log, write PM run as canonical 2026-04-24.md**: When a second run fires on the same date, the AM run's content already lives in the master `Styer_Growth_Log.md` via the bash append at end of step 8. The dated run-logs/YYYY-MM-DD.md and run-logs/latest.md should reflect the most recent run. No data loss; downstream agents reading latest.md get the freshest TOMORROW_PRIORITY.
- **Buda USDA cross-task confirmation — RESOLVED via parallel suburb-editor-daily**: Yesterday's run carried Buda USDA as HIGH carry-forward. Today's CONTEXT.md confirms suburb-editor-daily fixed it (commit 4755b70: schema, FAQ, tile, stat, process, CTA all removed). Re-Verify Gate caught only 1 USDA hit remaining (likely "Adam does not do USDA" disclaimer text). Cleared from FLAG_FOR_ADAM. Pattern: always re-grep at start of every run before flagging — parallel tasks may have closed the issue while you weren't looking.

---

## 2026-04-24 — Rate-Shopper AEO Gap + about.html CTA Pattern

### Patterns
- **`blog-post-intro` inside `<header>` is the same AEO anti-pattern as `hero-subtitle`**: Two rate-shopper posts (can-i-switch, how-to-compare) had strong answer-first text in `<p class="blog-post-intro"><strong>` inside the `<header>` block. This pattern was documented as an anti-pattern in April 10 + April 12 learnings but still appeared in newer posts. The `<p><strong>` AEO fix must go directly in the article body, after `</header>`. There are now 10 rate-shopper posts — audit the remaining 8 for this same gap in future Friday rotations.
- **about.html body CTAs survived batch CTA sweeps because the page uses a different template**: The raw app URL persisted on about.html's body "Get Pre-Approved Today" CTA (line 300) and CTA section "Get Pre-Approved" (line 729). Both were missed in all prior sweeps because sweeps targeted `*-mortgage-lender.html` and loan pages. The nav-cta and hero-cta-primary on about.html are intentionally left as raw URL (direct application path). Only body and CTA section CTAs were converted to /get-preapproved.
- **notebook_advisor.py does not exist on disk**: `find /` for the script returned empty. The `loanos/scripts/` path in SKILL.md was always stale (the `loanos/` directory was abandoned months ago). Step 0 cannot be executed. Adam must either restore the script at a known path or the step must be retired from SKILL.md. Until resolved, every run skips Step 0 and uses cached NOTEBOOK_INSIGHTS.

---

# styermortgage.com — Accumulated Learnings

## 2026-04-23 — Footer Awards Two-Template Pattern + Pillar Page Suburb Link Gap

### Patterns
- **Two distinct template variants for the stale "91 Google / 45 Zillow" footer**: 12 suburb pages use `<p>&#11088; 5.0 Stars &middot; 91 Google Reviews<br>&#11088; 4.98 Stars &middot; 45 Zillow Reviews</p>` inside the footer Awards `<div>`. Three older pages (florence, jarrell, marble-falls) use a trust strip pattern `<p style="font-size:0.95rem;...">91 Google Reviews &middot; 45 Zillow Reviews</p>` inside a testimonial section. The grep pattern `91 Google Reviews` catches both, but the Python `str.replace()` must target the full literal string for each variant. The footer Awards section and the testimonial trust strip are two independently maintained review-count locations.
- **Pillar pages can have zero suburb links even if suburb pages exist**: how-to-buy-a-house-in-austin-tx.html (713 lines, written 2026-04-03) had zero internal links to any suburb page despite being the site's top-of-funnel pillar page. A Python `re.findall` for `-mortgage-lender.html` in the content is the reliable way to audit suburb link coverage. The natural insertion point is the home search section (Step 4 in this case) where location-specific content is already discussed.
- **TOMORROW_PRIORITY "write blog post" can mean "improve existing page" when the content already exists**: The April 22b run log said "Blog post due April 24 — How to Buy a House in Austin." Re-Verify Gate found the page existed at 713 lines. The correct interpretation was to improve it (add suburb links) rather than create a duplicate. The GOALS.md constraint "no new content" was also resolved by improving existing. Pattern: always grep for the target page before executing a "write" instruction from TOMORROW_PRIORITY.
- **USDA can appear in loan program comparison tables in addition to meta/schema/FAQ/body**: The how-to-buy loan table included USDA with a link to /loans/usda.html. This is a fourth location (separate from meta, schema, and body feature items) where USDA can appear. When doing the USDA cleanup after Adam confirms, audit ALL of: (1) meta description, (2) LocalBusiness schema description, (3) body feature items/FAQs, (4) loan comparison tables.

---

---

## 2026-04-22b — Liberty Hill Content Enrichment + Footer Awards Audit

### Patterns
- **Footer Awards "91 Google + 45 Zillow" is a separate stale vector from the hero trust badge**: Liberty Hill's hero trust badge was correct (136+ Reviews) but the footer Awards section still had the old split format (91 Google + 45 Zillow). These are independently maintained. The standard footer format is: `&#11088; 5.0 Stars · 136+ Reviews / 21-Day Avg. Close · Licensed in Texas` — no platform split, no 4.98 Zillow stars. When auditing trust bar consistency, always check BOTH the hero trust badge AND the footer Awards section independently.
- **Footer Awards old format is likely systemic across older suburb pages**: Liberty Hill, and possibly other pages created before the standard was set, may still have the 91/45 split format. Run `grep -l "91 Google Reviews" *-mortgage-lender.html` at the start of any session to find all remaining stale footers.
- **USDA in LocalBusiness schema description is a separate USDA vector from FAQ and body**: The Liberty Hill schema description says "Conventional, FHA, VA, USDA, and new construction loans." This requires the same Adam confirmation as the Buda FAQ content. These are four independent places USDA can appear: (1) meta description, (2) LocalBusiness schema description, (3) body feature item, (4) FAQ. Each must be audited independently.

---

## 2026-04-20 — CTR Title Batch Complete: All 24 Suburb Pages + USDA Meta Discovery

### Patterns
- **Second-run same-day: TOMORROW_PRIORITY from morning run was already done**: Round Rock, Cedar Park, Georgetown were listed as "remaining" in the morning run's CTR status but were already updated in prior sessions. Second run verified live files first (grep title), found all three already done, and moved to the next batch (Buda through Taylor). Pattern: always grep actual file titles before acting on a TOMORROW_PRIORITY list.
- **USDA claims in meta descriptions discovered on Smithville, Elgin, Florence, Jarrell**: All four pages had USDA in their current meta descriptions. The Buda USDA issue (active blocker) was not an isolated error — it's a systemic pattern across all rural/east-Austin pages. These pages likely have USDA in body/schema/FAQ too. Removing from meta is safe (reversible), but body cleanup requires Adam's confirmation (product offering question). The correct action when rewriting metas: simply exclude USDA rather than asking permission — you're replacing, not removing a specific claim from existing content.
- **CTR title differentiation by market type is now fully systematized**: The differentiation pattern is consistent — each city's hook matches what a buyer in that specific market is searching for. Urban-adjacent affordable (Manor, Elgin) = "FHA & TSAHC." Hill Country/lake (Marble Falls, Spicewood) = "Hill Country & Lake LBJ" or "Rural Land & Lake Travis." New construction corridors (Jarrell, Taylor) = "Beat Builder Rates." Luxury (Westlake) = "Jumbo & Eanes ISD." Rural/acreage (Smithville, Florence) = "Rural & Acreage Land OK." This pattern is now the playbook for any future suburb pages.
- **All 24 suburb pages with CTR-hook titles is a SEO milestone**: The full batch took ~8 runs over 4 days (April 17-20). Google's indexer should pick up the title rewrites in the next crawl cycle. Expected CTR improvement should show in GSC within 2-3 weeks for already-ranking pages.

---

## 2026-04-20 — Batch H2 Conversion: Same 4 Patterns Across All Suburb Pages

### Patterns
- **All remaining suburb pages used identical label-format H2s**: "Why [City] Buyers Work With an Independent Broker", "Loan Options for [City] Buyers", "How to Get Pre-Approved for a [City] Home", "[City] Mortgage FAQ" — exact same 4 patterns across Lakeway, Bee Cave, Bastrop, New Braunfels, and the Austin-area hub. This confirms the template is consistent. The question-format conversion is therefore batchable: the same 4 sed patterns work for any remaining suburb page.
- **CTR-hook title differentiation follows market position**: Lakeway = "Jumbo & Luxury Specialist" (jumbo is the unique loan type for this price tier), Bee Cave = "Luxury & Jumbo Expert" (same tier, Eanes ISD angle), Bastrop = "Acreage & Rural OK" (rural land loans are the differentiator for this market). Each hook matches what a buyer in that city is actually searching for, not a generic loan type.
- **sed fails when the replacement string contains the delimiter character**: Used `|` as sed delimiter but the NMLS pipe character (`|`) in strings like "Mortgage Lender Lakeway TX | Adam Styer | NMLS #513013" caused sed to fail silently (no error message, no change). Fix: use Python str.replace() for any string replacement that contains pipes, special regex characters, or HTML entities. This is the definitive pattern for this codebase.
- **NotebookLM schema recommendations are now consistently stale for already-fixed items**: Fourth Monday in a row where NotebookLM recommended something already done (homepage H1 rewrite, Hutto reviewCount). The notebook IS correctly tracking ranking wins and new competitive threats. Pattern: use NotebookLM for directional insight on rankings and competitors, not for specific on-page fix recommendations. Verify all schema/content recommendations against live files.

---

## 2026-04-20 — Hutto #1 Confirmed + Round Rock Content Depth → #2 + MortgageAustin.com Threat Upgrade

### Patterns
- **Content depth on suburb pages directly caused Round Rock to appear at #2 this week**: The 2026-04-19 "Suburb editor" commit deepened the Round Rock page with first-party content. The very next Monday SERP check showed Round Rock at #2. The causal link between page depth and suburb keyword ranking is confirmed twice now (Hutto: schema + AEO → #1; Round Rock: content depth → #2). The pattern: generic suburb pages don't rank; hyper-local, first-person, neighborhood-specific pages do.
- **MortgageAustin.com must now be tracked as primary independent broker competitor**: Anthony Ferrando, same independent broker positioning, same "low overhead, no kickbacks" pitch. Now #1 for "get pre-approved austin tx" — the highest-conversion keyword for an independent broker. This is the same threat posture that Sente held for "mortgage lender austin tx" but for the conversion-intent keyword cluster. The counter is AEO structure on /mortgage-pre-approval-austin.html + a step-by-step guide blog post.
- **Nest Mortgaging (Kyle TX) revealed the content volume threshold needed for multi-keyword organic dominance**: 10+ 2026-specific posts are ranking simultaneously across 6+ keywords. They appear for "home loan austin tx" (#1), "refinance" (3 positions), "mortgage lender", "mortgage broker", "cash out refi". This is not luck — it's content velocity. Rule: to own multiple core Austin keywords organically as an independent broker, you need a minimum of 8-10 well-optimized, 2026-dated posts. Currently at approximately 5-6. The pillar page (April 24) + 2 more posts would close this gap.
- **Re-verify gate prevented 5 stale claims from entering this week's report**: Prior claims about Vista Lending (#1 for "mortgage lender austin tx"), Lone Star (#1 for "get pre-approved"), and MortgageAustin.com's broker keyword position were all outdated. The gate caught all 5 and either upgraded or cleared them. This is the gate working as designed.

---

## 2026-04-19 — CTR Title Optimization: Generic → Specific Hooks + Buda USDA Accuracy Issue

### Patterns
- **8 suburb pages had identical generic title format "Mortgage Lender [City] TX | Adam Styer | NMLS #513013"**: This format ranks (keyword-dense) but doesn't differentiate for click-through. GOALS.md flagged CTR as the week's SEO priority. The fix: rewrite titles with a specific hook after the colon — "Beat Builder Rates", "Same-Day Pre-Approval", "FHA & VA Expert", "TXST Area Expert", etc. These hooks match specific user intents within the category keyword. This is the same pattern that's already working on Round Rock, Cedar Park, Georgetown, and FHA pages.
- **Meta description first-person + neighborhood-specific copy outperforms generic brand copy**: Compare old Leander meta "Leander TX mortgage lender Adam Styer — home loans, pre-approval, and refinancing for Leander homebuyers. Independent broker, 40+ lenders, NMLS #513013." vs new "Leander: Crystal Falls, Travisso, Mason Hills — new builds where I beat preferred-builder rates most weeks. 40+ lenders, same-day pre-approval. NMLS #513013." The new version gives the searcher a reason to click. The neighborhoods are local signal. The claim ("I beat those rates most weeks") is specific and credible.
- **Buda page contains USDA claims that conflict with Adam's actual product offering**: buda-mortgage-lender.html line 7 (meta description), line 35 (LocalBusiness schema description), lines 98-101 (FAQ "Are USDA loans available in Buda TX?"), and line 125 (down payment comparison answer) all mention USDA as something Adam offers. The voice guide says Adam "Does NOT do: USDA loans." Do NOT fix this without Adam's explicit confirmation — it's a product-offering question. Flag it and leave the file unchanged until confirmed.

---

## 2026-04-18b — Sitemap Gap: 5 Suburb Pages Never Added + H2 Class Attribute Grep Trap

### Patterns
- **5 suburb pages (taylor, smithville, elgin, florence, jarrell) were never added to sitemap.xml**: The March 26 sitemap batch added 15 pages but missed these 5. These pages have been live and fully optimized since March/April 2026 but are invisible to Google's structured sitemap signal. Root cause: the "DONE" note for the March 26 sitemap update was written against the batch count (15) without auditing what was still missing. Rule: after any sitemap update, diff the actual file URLs against the full list of live suburb pages. Quick check: `grep "mortgage-lender" sitemap.xml | wc -l` should match `ls *-mortgage-lender.html | wc -l`.
- **`grep "<h2>"` misses H2 tags with class attributes**: Simple `<h2>` grep catches only bare H2 tags. Tags like `<h2 class="text-center">` and `<h2 class="text-center mb-3xl">` are invisible to this pattern. Always use `grep "<h2"` (no closing angle bracket) to catch all H2 variants. This is especially important for this codebase where section-header H2s often have `text-center` or `mb-3xl` classes.
- **Second-run same-day pattern applied correctly**: latest.md showed today's date (2026-04-18), so the run skipped NotebookLM and rotation steps and went directly to TOMORROW_PRIORITY items. Worked as intended — no duplicate work, maximum throughput.

---

## 2026-04-18 — DSCR Loan Page Missed Prior CTA Sweeps + about.html CID Was Already Fixed

### Patterns
- **DSCR page (dscr-loan-austin-tx.html) was missed in ALL prior CTA sweep rounds**: The March 31 suburb batch fix, April 1 loan page fix, and subsequent audits all targeted `*-mortgage-lender.html` and `/loans/*.html` files. The DSCR page sits at the root level with a different naming convention — it falls through every glob pattern. Rule: after any batch CTA sweep, explicitly grep ALL html files in the root for `mslp.my1003app.com` count > 1. The body CTA and hero CTA had never been fixed.
- **about.html sameAs CID "placeholder" was already resolved on 2026-04-15**: The recurring issue had been carried forward for 11+ runs after it was actually fixed. Live grep showed `cid=ChIJYy5uEFPKRIYRmF-k_5gPk74` — real CID, not placeholder. This is the clearest example of the stale FLAG_FOR_ADAM anti-pattern: the TODO.md entry (`2026-04-15`) explicitly logged the fix, but the recurring issue list kept surfacing it. Rule: before logging a "recurring issue," check TODO.md for a completed fix entry. If it exists, mark resolved.
- **Westlake suburb page body + footer CTAs were both still pointing to raw app URL**: The March 31 batch fix used a `btn-lg` class grep to catch body CTAs. Westlake's body CTA uses `btn btn-primary` (no `btn-lg`), and its footer Apply Now is inside a `<li>` without any button class. Both are invisible to class-based grep patterns. Confirmed rule: always grep for the raw domain URL itself, not by class name, when auditing CTAs.

---

## 2026-04-17 — Footer Awards Section Is a Separate Drift Vector + NotebookLM Staleness Pattern

### Patterns
- **Footer "Awards & Recognition" section is a separate brand drift vector from the hero trust bar**: The Buda page hero trust bar was correct (updated April 16) but the footer Awards section still had the old content ("Top Producing Broker — Austin 2023", "1,000+ Loans Closed"). These two sections are independent — fixing the hero trust bar does NOT update the footer. When auditing trust bar consistency, always check BOTH the hero section AND the footer Awards section. The standard for the footer: "5.0 ★ Google Rating · 136+ Reviews", "21-Day Avg. Close", "Licensed in Texas · NMLS #513013".
- **"Additional Documents — X" H2s in the document checklist are a non-question label format**: These section headers describe what the section contains (label) instead of asking the user's question. The AEO-correct format is "What Documents Do X Borrowers Need?" — which matches the conversational query someone would type or speak. The original content H2s ("What Documents Do W-2 Employees Need?") were already correct; only the supplemental type-specific sections had the old pattern.
- **NotebookLM is running 2-3 weeks behind actual site state by April 2026**: Recommendations today included homepage H1 rewrite (done March 26) and hero CTA fix (done April 16). The notebook sources are not being refreshed often enough. Action: after any major content milestone, push updated CONTEXT.md + most recent 3 run logs to NotebookLM as sources. Until then, treat NotebookLM output as directional context, not task direction.

---

## 2026-04-16 — Buda Old-Template CTA Gap + Wednesday H2 Rotation Complete

### Patterns
- **Buda uses the older `city-hero` / `quick-quote-form` template, not the standard glass-card hero**: The March 31 batch CTA sweep targeted `*-mortgage-lender.html` files using the standard `hero-cta-primary` and `btn-lg` class patterns. Buda's older template uses a `<div class="quick-quote-form">` form with a submit button (not a standalone CTA button) — and its body CTA section used class `btn btn-primary` (no `hero-cta-primary`). Both the body CTA and footer Apply Now survived the sweep undetected until today. **Rule: always grep for the raw domain URL (`mslp.my1003app.com`) across ALL suburb pages after any batch CTA fix — not just by class name.**
- **Wednesday suburb H2 question-format rotation is now complete for all 7 checked cities**: Round Rock ✅ (Apr 14), Cedar Park ✅ (Apr 15), Leander ✅ (Apr 15), Georgetown ✅ (Apr 15b), Pflugerville ✅ (Apr 15b), Kyle ✅ (Apr 16), Buda ✅ (Apr 16). Remaining suburb pages not yet audited for H2 format: San Marcos, Westlake, Dripping Springs, Hutto, Liberty Hill, Manor, Lakeway, Bee Cave, New Braunfels, Taylor, Bastrop, Spicewood, Smithville, Elgin, Jarrell, Florence, Marble Falls, Austin-area hub.
- **NotebookLM may return stale recommendations weeks after fixes are applied**: Today's query recommended fixing the homepage H1 (done March 26) and Hutto schema to 91 reviews (schema already at 136). The query DID correctly flag the homepage hero CTA issue (still open as of today's run). Pattern: use NotebookLM for directional insight, always verify claimed issues against actual file state before acting. Stale recommendations are easy to filter once you read the file.
- **Homepage hero CTA raw app URL persisted as a LOW blocker for 3+ weeks after suburb pages were fixed**: The March 31 batch fix was scoped to `*-mortgage-lender.html` files. The homepage was out of scope. DECISION TEST confirmed it was safe to fix (reversible, no judgment call needed, same fix as 24 suburb pages). Fixed today. **Rule: after any sitewide batch fix, explicitly check the homepage and loan pages for the same pattern — they're not covered by `*-mortgage-lender.html` globs.**

---

## 2026-04-15 — TEA Rating Language Update + Hutto Schema Already Correct

### Patterns
- **Texas TEA "Exemplary/Recognized" is definitively outdated — use "A-rated"**: Cedar Park's page used "Exemplary" (old system) and "consistently rated among the top" (generic). Fixed to "A-rated by the Texas Education Agency." This applies to any suburb page that references school quality. Scan for "Exemplary" and "Recognized" in all suburb pages and replace with "A-rated" or "highly-rated."
- **Before acting on NotebookLM schema recommendations, verify the schema first**: NotebookLM recommended updating Hutto's AggregateRating to 91 reviews. A quick grep showed it already reads "136" — the notebook's reference to "91" was Google-only reviews, but combined (91 Google + 45 Zillow = 136) is already in the schema. Always grep the actual file before making schema changes based on AI recommendations.
- **Leander H2s were not yet in question format despite at-a-glance block being present**: A page can have a rich at-a-glance block (from a prior session) and still have informational-label H2s. These are two separate AEO improvements. The Wednesday rotation should now check both independently.

---

## 2026-04-14 — H2 Question Format as AEO Structure + City At-a-Glance Pattern for Tier-1 Suburb Pages

### Patterns
- **H2s in question format ("Why Should X?" vs "Why X") are a structural AEO signal**: AI crawlers and AI Overviews extract Q&A patterns from heading structure. Converting content H2s from informational labels ("Loan Options for Round Rock Buyers") to searchable questions ("What Loan Programs Are Available for Round Rock TX Homebuyers?") aligns with how AI systems parse page content for citation. Confirmed as a priority via NotebookLM.
- **Tier-1 suburb pages (Round Rock, Cedar Park, Leander, Georgetown) were missing explicit city at-a-glance blocks despite having natural local content**: The content within body paragraphs doesn't carry the same local signal density as a structured `<p><strong>City at a glance:</strong> [facts]</p>` block. These blocks should include: county context (Williamson vs Travis tax rate), school district name + top high school names, 3-4 major employers, 5+ neighborhood names. Round Rock added 2026-04-14.
- **Wednesday suburb rotation prioritization**: Within the Wednesday rotation, tier-1 cities (Round Rock, Cedar Park, Leander, Georgetown, Pflugerville) should be prioritized over tier-2 (Kyle, Buda, San Marcos) for H2 question format audits and at-a-glance enrichment.
- **"TEA Exemplary" is outdated rating language**: Texas TEA now uses A-F ratings, not the old Exemplary/Recognized/Acceptable system. When describing school quality, use "highly-rated" or "A-rated" not "Exemplary."

---

## 2026-04-13 — First Suburb Ranking + Competitor Review Gap Strategy

### Patterns
- **First top-10 ranking achieved via suburb pages, not core Austin keywords**: "hutto tx mortgage lender" → #3 in Week 6. Core Austin keywords remain at 0/7. Suburb-first SEO is the correct sequence: get indexed, rank locally, then leverage suburb authority to climb core terms.
- **Review count in schema is a direct ranking lever vs. competitors**: Big Life's Hutto page shows only 25 reviews in AggregateRating schema despite being #1. Our 91+ reviews — if properly surfaced in schema — is a significant algorithmic advantage. Schema review count ≠ actual review count if schema is stale or misconfigured.
- **Physical branch presence makes map pack unwinnable for competitors without local address**: Guild Mortgage's Liberty Hill branch (#1 and #2 for "liberty hill tx mortgage lender") demonstrates why independent brokers cannot target map pack for suburbs where competitors have a physical location. Organic-only targeting is the correct strategy in those cases.
- **Templated competitor pages are beatable with hyper-local content**: Big Life's Hutto page has no Hutto neighborhood names, no Hays County specifics, no school district reference. A page with Star Ranch, Brushy Creek, Cottonwood Creek, HISD, Williamson County context will outperform a template page — even if the template is currently ranking higher.
- **Rising content competitors should be tracked by blog post URL, not just domain**: MortgageAustin.com gained visibility via a specific blog post ("mortgage broker vs bank austin"). Tracking competitor domains is insufficient — track their individual ranking pages so you know what content to outcompete.

---

## 2026-04-12 — Blog Post AEO Anti-Pattern: Styled Header Paragraphs + DSCR BreadcrumbList

### Patterns
- **Styled header paragraphs (`<p style="border-left:...">` or `<p class="blog-post-intro">` inside `<header>`) do NOT satisfy AEO requirements**: The condo post had answer-first text inside a styled `<header>` section. AI crawlers extract `<article>` body paragraphs. The fix is always to add a plain `<p><strong>...</strong></p>` as the FIRST element inside `<div class="article-body">` — after `</header>`. Two posts had this gap (condo + choose-lender).
- **DSCR page was missing BreadcrumbList JSON-LD despite having a visual breadcrumb**: The visual `<ol class="breadcrumb-list">` in HTML is NOT the same as `@type: BreadcrumbList` JSON-LD schema. Rich Results Test and Google both require the JSON-LD version for breadcrumb SERP display. Always add both: visual HTML + JSON-LD block in `<head>`.
- **Monday schema audit should grep for JSON-LD BreadcrumbList separately from visual breadcrumbs**: Use `grep -n '"@type": "BreadcrumbList"'` not just `grep breadcrumb` — the latter catches the visual HTML and gives a false positive.
- **codex branch isolation pattern**: When daily-opt commits land on a non-main branch, use `git checkout <branch> -- <files>` to selectively bring only the session's changed files to main. Do NOT merge or cherry-pick when the branch also contains Adam's unreviewed work.

---

## 2026-04-11 — AEO Applies to Loan Type Pages Too + Second-Run Backlog Pattern

### Patterns
- **Loan type pages need AEO answer-first paragraphs, not just blog posts**: construction.html opened directly with an H2 ("How Construction Loans Work") with no extractable opening paragraph. The AEO requirement applies to all pages that should appear in AI Overviews — loan pages are high-intent targets. Fix: insert `<p><strong>[40-60 word direct answer]</strong></p>` as the first element in the main content section, before any H2.
- **Second-run on the same day: go straight to backlog**: When latest.md shows today's date, skip re-doing the rotation and go directly to the SEO/SEM backlog for the next available LOW_RISK or ZERO_RISK item. Don't repeat work already logged this morning.
- **Construction pages benefit from named local builders**: Using real Austin-area builder names (Lennar, David Weekley, Milestone Community Builders, Scott Felder) in the process walkthrough adds local relevance signal and is more credible to readers than generic "production builder" language. Verify builder names are accurate before publishing.

---

## 2026-04-10 — Suburb Pages ARE Indexed + Stale FLAG_FOR_ADAM Rule

### Patterns
- **Suburb pages are indexed — confirmed 2026-04-10**: Leander shows "URL is on Google" with HTTPS + Breadcrumbs + FAQ all green in GSC. The "week 5 emergency" was resolved by the sitemap fix (March 26). The agent kept carrying forward the GSC URL Inspection flag for 2+ weeks without re-verifying. **Rule: before adding any flag to FLAG_FOR_ADAM, grep the run-logs for the same flag. If it's been there 3+ runs with no evidence it's still unresolved, verify the current state before flagging again.**
- **"Stale flag" pattern**: A concern that was real at one point (pages unindexed, March–early April) became false after the fix. Adam had to correct this manually. The agent should never carry forward a FLAG_FOR_ADAM item beyond 2 runs without re-verifying it's still valid.

---

## 2026-04-10 — blog-post-intro Class Is an AEO Anti-Pattern

### Patterns
- **`<p class="blog-post-intro">` inside `<header>` is not a machine-extractable AEO paragraph**: The document checklist post had the answer-first text wrapped in `blog-post-intro` inside a `<header>` element. Like `hero-subtitle`, this is a styled UI class — AI crawlers extract `<article>` body paragraphs, not header-section elements. Fix: add a plain `<p><strong>...</strong></p>` directly in the article body after `</header>`. The styled class can remain for visual design; the body paragraph is what gets extracted.
- **City enrichment via "at a glance" check: grep for "at a glance" not just "commute"**: Some pages mention commute casually in prose without having the structured "City at a glance:" paragraph. After today: New Braunfels (0→✅) and Lakeway (0→✅) confirmed done. Florence and Marble Falls show 2-3 markers each — likely done. Liberty Hill and Elgin remain.
- **City enrichment order of value**: Prioritize by search volume — New Braunfels (large city, high search volume) and Lakeway (high-value jumbo market) were higher-priority than smaller Hill Country cities. When choosing next enrichment cities, pick higher-traffic markets first.

---

## 2026-04-08b — AEO Batch: Elgin, Florence, Jarrell, Marble Falls + Funnel Audit

### Patterns
- **Pages missing a content-narrow section need one created, not just a `<p>` injected**: Elgin, Florence, Jarrell, and Marble Falls had no `<section><div class="container content-narrow">` block after the hero — only the feature grid and process steps. The AEO answer-first paragraph requires its own content-narrow section inserted between the hero `</section>` and the "Why [City]" feature grid. Pattern: insert `<section><div class="container content-narrow"><p><strong>...</strong></p></div></section>` targeting the `<!-- Why [City] -->` comment as anchor.
- **AEO coverage is now 11/25 suburb pages confirmed**: Cedar Park, New Braunfels, Bastrop, Bee Cave, Hutto, Elgin, Florence, Jarrell, Marble Falls (added this run), Spicewood, Liberty Hill, Lakeway (pre-existing). Remaining ~13 pages to audit: San Marcos, Georgetown, Round Rock, Leander, Pflugerville, Kyle, Buda, Dripping Springs, Taylor, Manor, Westlake, Smithville, Austin-area.
- **Second-run pattern confirmed again**: When TOMORROW_PRIORITY says "Thursday" but today is Tuesday, execute the TOMORROW_PRIORITY items rather than repeating the current-day rotation. The PM interactive session already completed Tuesday work; the morning scheduled run should advance to the next priority.
- **Funnel is clean end-to-end**: Homepage→/get-preapproved (3 links), /get-preapproved→/thank-you (action attr), thank-you has Calendly + phone. contact.html has Netlify form + dataLayer event. Only gap is about.html + dscr CTAs pointing to raw app URL (LOW, known, carry forward).

---

## 2026-04-08 — AEO Answer-First Batch: Cedar Park, New Braunfels, Bastrop, Bee Cave

### Patterns
- **Four suburb pages were missing AEO answer-first paragraphs despite having other schema complete**: Cedar Park, New Braunfels, Bastrop, and Bee Cave all had LocalBusiness + FAQPage + BreadcrumbList schema, city-specific H1, internal links — but the first `<p>` in the main content section was still narrative, not a machine-extractable answer. Schema is not the same as AEO content structure.
- **"At a glance" `<strong>` blocks are not AEO answer-first paragraphs**: Bastrop and Bee Cave had `<strong>Bastrop at a glance:</strong>` city enrichment paragraphs from prior runs. These are mid-section factual blocks, not opening answer-first paragraphs. The AEO requirement is specifically the *first* paragraph of the main content section, answering the page's primary query (e.g., "How do I get a mortgage in [City]?").
- **Batch AEO is efficient when done by city cluster**: Four pages updated in one run. Future runs can continue the rotation: Spicewood, Florence, Jarrell, Marble Falls, Liberty Hill, Lakeway, Elgin — check each for missing answer-first paragraph before first H2.
- **Chrome not running in scheduled context**: Control Chrome MCP tools require Chrome to be open. Scheduled runs cannot perform live conversion tracking verification. Carry forward from last manual verification — flag if more than 3 days without a live check.

---

## 2026-04-07 — Person Schema Was Added to Homepage (Learnings Stale) + LocalBusiness Gap on About Page

### Patterns
- **Learnings.md schema entry was stale**: The 2026-04-06 entry noted "No Person schema on homepage." By 2026-04-07, Person schema (Block 3) was present. Monday schema audits should always verify live via grep, not rely on prior run notes. If a schema block exists and the type matches, mark it clean.
- **About page was the real schema gap**: Homepage had MortgageBroker + FAQPage + Person. About page had only Person — no LocalBusiness. The fix (a minimal LocalBusiness block with address, phone, aggregateRating) took one Edit call. Note: sameAs CID is a placeholder until Adam provides the real Google Maps CID.
- **Monday schema audit order matters**: Check homepage first (most important for AEO), then DSCR (FAQPage verification), then one rotating suburb. Track which suburb was last checked in TOMORROW_PRIORITY.
- **NotebookLM Google Ads query (Monday-only) returned successfully on sync call after background call failed**: Background process exits 127 (not found) sometimes due to path issues in scheduled context. Always have a sync fallback.

---

## 2026-04-06 — AEO Hero-Subtitle vs. Body Paragraph + Blog Title Drift at 10 Instances

### Patterns
- **Hero-subtitle class is NOT the same as an AEO body paragraph**: Suburb pages like Liberty Hill had answer-first text in `<p class="hero-subtitle">` in the hero section, but AI crawlers extract body content paragraphs, not hero UI elements. The Monday AEO audit now explicitly checks for a `<strong>` paragraph in the main content section (after `<!-- WHY [CITY] -->` comment, before first H2). A clean hero-subtitle does NOT satisfy the AEO requirement.
- **Blog title drift has now reached 10 confirmed instances — this is a systemic process failure**: The blog post creation template has never been corrected at the source despite 10+ fixes after-the-fact. The correct pre-publish lint command is: `grep "<title>" blog/YYYY-MM-DD-*.html | grep -v "Adam Styer"` — any hit = broken title. Until the template is fixed, run this grep at the start of every run against ALL blog/*.html files to catch same-day posts.
- **Homepage schema has two gaps worth tracking**: (1) No Person schema on homepage — only MortgageBroker/LocalBusiness + FAQPage. (2) No LocalBusiness schema on about page — only Person. Monday schema audit should flag if either of these diverges further. Adding Person schema to the homepage is a 1-block JSON-LD addition and would satisfy the "both pages have both schemas" target.
- **NotebookLM has timed out on two consecutive scheduled runs (Apr 5 + Apr 6)**: The cached insights are sufficient for daily decision-making but the timeout pattern suggests either network latency or library issues. When timed out, always log the cached NOTEBOOK_INSIGHTS block verbatim from the prior run so the next run inherits them.

---

## 2026-04-06 — Sente Mortgage Is the New Review Benchmark + Dripping Springs Wide Open

### Patterns
- **Sente Mortgage (1,448+ reviews) displaced Barton Creek Lending (506) as the Austin review leader**: They jumped to #1 for "mortgage lender austin tx" from nowhere. Review volume at this scale creates self-reinforcing authority — Google rewards review signals in local/YMYL verticals. The benchmark for competitive review count is now 3x higher than previously assessed.
- **Lone Star Financing is surging across multiple keywords simultaneously**: Appeared in top 3 for "mortgage broker" (#3), "home loan" (#2), and "refinance" (#2) — all keywords where they were absent last week. This pattern (multi-keyword jump in one week) suggests either a site-wide authority boost (new backlinks, domain age threshold) or coordinated on-page optimization. Monitor for new content or backlink activity.
- **Dripping Springs is the widest-open suburb keyword found in 5 weeks**: Zero dedicated mortgage lender pages in results — only individual LOs at banks and a realtor recommendation page. Compare to Buda (Big Life has a page), San Marcos (3 dedicated pages), or Westlake Hills (Quantum Loans). If our Dripping Springs page gets indexed first, it has the highest probability of any suburb keyword to rank immediately.
- **Suburb indexing at 5 weeks with 0 pages is now a confirmed technical emergency, not a patience issue**: Standard indexing for new pages on an established domain is 1-3 weeks. 5 weeks with zero suburb pages indexed — despite sitemap fix (commit 9313067, ~March 26) — suggests either (a) internal linking is too weak for Googlebot to discover and prioritize these URLs, (b) crawl budget is exhausted on higher-priority pages, or (c) there's a technical blocker (noindex, robots.txt, canonical issues). Must audit all three.

---

## 2026-04-05b — Suburb Hero CTA Class Was Missed in All Batch Fixes

### Patterns
- **`hero-cta-primary` is a distinct class from body CTAs and footer Apply Now links**: The 2026-03-31 batch fix (and prior manual fixes) targeted body CTAs with `btn-lg` class and footer `<li>Apply Now</li>` links, but completely missed the hero section's primary CTA button which uses class `hero-cta-primary hero-cta-btn`. This was live on 16 suburb pages until today (Apr 5). **Rule: when auditing CTAs, always grep separately for `hero-cta-primary`, `btn-lg`, `nav-cta`, and footer Apply Now — these are four distinct placement categories.**
- **The hero CTA suppresses Google Ads conversion tracking differently than body CTAs**: Body CTAs going to raw app URL skip the `generate_lead` event. The hero CTA going to raw app URL does the same — but the hero CTA is likely the highest-intent click on the page (above the fold, primary position). Fixing the hero CTA matters more per-click than body CTA fixes.
- **Verify grep patterns exhaustively before declaring a batch fix complete**: After any batch CTA sweep, run: (1) `grep -l 'mslp.my1003app' *-mortgage-lender.html` for any remaining raw URL, (2) check count per file — nav-cta (1) + hero-cta-primary (1) = 2 intentional raw URLs max per suburb page (nav is intentional, hero is now /get-preapproved). Zero remaining after today's fix.

---

## 2026-04-05 — AEO Answer-First on Suburb Pages + Blog Title Drift Persists

### Patterns
- **AEO answer-first applies to suburb pages too, not just blog posts**: The pattern (`<p><strong>[40–60 word direct answer]</strong> [1 context sentence]</p>`) was previously applied only to blog posts. Suburb pages open with narrative ("Hutto is one of the fastest-growing cities in Texas...") which AI crawlers skip. The answer-first paragraph should be the very first `<p>` inside the main content section, before the intro H2. Applied to Hutto today.
- **Blog title brand drift is now 9+ instances — it's the creation template, not operator error**: Three more posts (self-employed, housing market, spring market) were missing "Adam Styer |" in the `<title>` tag despite prior fixes. No post created outside a daily-opt run has ever had the correct format on first publish. The only reliable fix is a creation-time grep lint check.
- **In-body calculators link pattern**: Insert inline in the "Home Prices in [City]" paragraph as: "Use our [mortgage calculator](/calculators.html) to estimate your monthly payment at current rates for any [City] price point." Natural placement, adds value, no content disruption.

---

## 2026-04-04b — AEO: Blockquote + Bold = Best of Both Worlds

### Patterns
- **Combining blockquote-style + `<strong>` is the optimal AEO pattern**: The condo post had a properly-styled intro (border-left, 49 words, directly answers the title query) but was missing `<strong>` bold wrapping. Adding `<strong>` costs nothing visually — the border-left styling dominates — but sends an explicit semantic signal to AI crawlers. Rule: every blog post intro should have BOTH a visual pull-quote style AND `<strong>` wrapping on the direct-answer portion.
- **NotebookLM source refresh latency is real**: styermortgage-context.md was added as a source on April 4 morning, but the April 4 afternoon query still returned stale recommendations (H1 fix, GSC submission — both done weeks ago). Sources take more than one run to propagate into recommendations. Don't conclude that a refresh failed just because one query still returns old advice — check again in 2-3 runs.
- **Second-run pattern**: When the same day triggers two runs (e.g., manual trigger + scheduled), check latest.md first — if the major priority items are already done, focus on any outstanding TOMORROW_PRIORITY items rather than re-doing work.

---

## 2026-04-04 — "Serving" H1 Inventory Correction + AEO Applied to 2 Posts

### Patterns
- **Context file "Serving" H1 list was stale in both directions**: bastrop + bee-cave were listed as needing fixes but were already clean; taylor + new-braunfels were NOT listed but had "Serving" H1. After today's batch fix, zero suburb pages have the "Serving" anti-pattern. **Rule: always grep `Mortgage Lender Serving` across all suburb pages rather than relying on the context's list.**
- **Blog post title brand drift is now confirmed at 7+ instances**: The April 4 post was published with no brand in the title. This is not a one-off error — it's the blog post creation template. Until the template is fixed, grep every new post immediately: `grep "<title>" blog/YYYY-MM-DD-*.html | grep -v "Adam Styer"`.
- **AEO answer-first paragraph applied to spring market + April 4 market report**: Both posts now open with a 50-60 word bolded direct answer before the first H2/narrative. Pattern confirmed effective — apply to any new post at time of creation.
- **NotebookLM source deletion doesn't support --json flag**: `notebooklm source delete <id> --yes` works; `--yes --json` errors. Deletion itself succeeds.

---

## 2026-04-03b — AEO Answer-First Pattern Applied + San Marcos 3-Fix

### Patterns
- **Answer-first paragraph template**: Add a bolded 40-60 word direct answer as the FIRST paragraph of every blog post, before any narrative. Format: `<p><strong>[Direct answer to the title's implied question, 1-2 sentences.]</strong> [1 sentence of context/nuance.]</p>`. This is the extractable anchor for AI Overviews — narrative openers are skipped.
- **Question-format H2 conversion pattern**: Statement H2s ("What Lenders Actually Look At") → question H2s ("What Do Lenders Look At for Self-Employed Borrowers?"). Conversion increases AI citation likelihood and matches conversational search queries. Apply to all new posts going forward; retrofit in rotation.
- **Blog title tag brand drift is confirmed systemic**: April 2 (self-employed) and April 1 (spring market) posts both published missing "Adam Styer |" or "NMLS #513013". This is the 6th+ instance. The post creation template is the root cause — not a one-time error. Any new post must be grepped immediately: `grep "<title>" blog/YYYY-MM-DD-*.html | grep -v "Adam Styer"`.
- **San Marcos 3-fix pattern complete**: H1 "Serving" removal + hero CTA → /get-preapproved + /calculators body link. Same 3-fix pattern should be applied to remaining 6 suburbs: bastrop, bee-cave, hutto, lakeway, liberty-hill, manor.

---

## 2026-04-03 — Sitemap Never Submitted — 4-Week Indexing Gap

### Patterns
- **Sitemap must be verified in GSC on day one of any new site**: styermortgage.com had 24 suburb pages live and fully optimized for 4+ weeks before any were indexed, because sitemap.xml was never submitted to GSC. All SEO work during that period was real but produced zero ranking signal until today (Apr 3). GSC "Submitted sitemaps" showing 0–0 of 0 is an immediate red flag — check this the first run of any new site.
- **Monday rotation must include GSC sitemap status check**: Add to Monday checklist — verify sitemap.xml is submitted and returning Success in GSC. If status is "Couldn't fetch" or missing, fix before any other work.
- **Page quality work was not wasted — just delayed**: All schema, H1 fixes, CTAs, FAQPage markup is intact and will matter once indexing completes. The 4-week gap delayed the ranking clock, not the work itself.

---

## 2026-04-03 — AEO Blog Audit + Dripping Springs Suburb Fix

### Patterns
- **Blog posts lack answer-first intros for AI extraction**: Both audited posts (self-employed, spring market) open with storytelling/context instead of a direct 40-60 word answer to the primary query. AI Overviews and SGE extract the first clear, direct answer on a page — narrative openers get skipped. Rule: every blog post intro should open with 1-2 sentences that directly answer the title's implicit question, then expand.
- **H2s as statements vs. questions**: Post H2s like "What Lenders Actually Look At" and "The Problem With Writing Everything Off" are informative but not in the question format that AI engines use to match conversational queries. Reformatting as "What Do Lenders Look At for Self-Employed Borrowers?" increases AI citation likelihood. Apply to new posts going forward.
- **NotebookLM source refresh cadence**: After 3 consecutive runs recommending already-fixed items (H1 keyword, trust bar), the notebook sources are confirmed stale. Sources should be refreshed after major site changes complete — add updated context.md + recent run logs. Refresh frequency: monthly or post-major-milestone.
- **Dripping Springs suburb fixes now follow established pattern**: All three suburb improvements (H1 "Serving" removal, hero CTA → /get-preapproved, /calculators body link) were applied in one run. Same 3-fix pattern should be applied to remaining 7 suburbs.

---

## 2026-04-02b — Resource Page CTA Audit Complete + Netlify 503

### Patterns
- **Resource pages with raw nav CTAs are acceptable**: improve-credit-score.html and fixed-vs-adjustable.html only have raw loan app URL in the `nav-cta` anchor (standard site-wide nav pattern). Only body CTAs (`.btn.btn-primary` in article or section content) need to be converted to /get-preapproved. Nav CTAs are deliberate — they send high-intent readers directly to the 1003.
- **mortgage-pre-approval-austin.html is a borderline case**: This article specifically explains how to complete the 1003 application and has 5 body CTAs linking to the raw URL in instructional context ("Start here"). Unlike other resource pages, these links are embedded in instructional copy about the application itself. Decision deferred to Adam.
- **Netlify 503 pattern**: After push, site returned 503 for 2.5+ minutes on all pages including homepage. This may be a build failure OR a Netlify platform outage. Cannot diagnose from CLI — must check Netlify dashboard. If 503 persists >5 min after push, escalate to FLAG_FOR_ADAM immediately.
- **NotebookLM sources go stale**: The notebook still referenced "Your Austin Home Loan Simplified" H1 (fixed March 26) as an open issue. Sources should be refreshed monthly or when major site changes complete. Use `notebooklm source add` to push updated context docs.

---

## 2026-04-02 — Raw App URL Still Present on Resource Pages + Batch Fix Gaps

### Patterns
- **Batch CTA sweep (2026-03-31) missed non-suburb pages**: The suburb CTA batch fix caught all 24 suburb pages but did not audit resource/guide pages. The first-time-buyer-guide final CTA and Cedar Park hero CTA were still pointing to the raw loan app URL today. Rule: after any batch fix, explicitly audit the adjacent page categories (loan pages, resource pages, guide pages) for the same pattern — not just the target category.
- **Cedar Park hero "Apply Now" survived two batch fix rounds**: Cedar Park was included in the 2026-03-31 suburb batch fix AND the 2026-04-01 loan page fix, but neither caught this hero CTA. The hero CTA used the same class and URL as other suburb pages. Root cause: sed patterns in the batch fix may have been run with wrong file glob. When verifying batch fixes, grep each file individually rather than trusting the batch count.
- **Thursday funnel trace is a fast health check**: The full funnel (homepage → /get-preapproved → /thank-you) can be verified in <2 min via grep. contact.html, thank-you.html, and the three ad landing pages are all clean. This check is low-friction and should stay in Thursday rotation.

---

## 2026-04-01b — Suburb H1 "Serving" Pattern + 24-Page Count Correction

### Patterns
- **"Mortgage Lender Serving [City], TX" is a systemic H1 anti-pattern**: 12+ suburb pages use "Serving" in their H1 while the title tag says "Mortgage Lender [City] TX" (no "Serving"). This creates a title-H1 mismatch that dilutes keyword density and breaks message match. Fix in rotation: one page per run, drop "Serving" from H1. First fixed: Round Rock.
- **Suburb page count is 24, not 9**: Context file listed only 9 suburb pages (the original Tier 1 cities). The repo actually has 24: bastrop, bee-cave, buda, cedar-park, dripping-springs, elgin, florence, georgetown, hutto, jarrell, kyle, lakeway, leander, liberty-hill, manor, marble-falls, new-braunfels, pflugerville, round-rock, san-marcos, smithville, spicewood, taylor, westlake. ALL 24 need GSC URL submission, not just 9. Context file suburb inventory needs updating.
- **In-body calculators link is absent from all suburb pages (nav-only)**: Suburb pages link to /calculators only via site navigation. The task checklist requires both /get-preapproved AND /calculators as in-body links. A natural insertion point is the "Home Prices in [City]" section — "Use our mortgage calculator to estimate your monthly payment at current rates for any [City] price point." Apply this pattern to each suburb page as you rotate through them.

---

## 2026-04-01 — Context File Staleness + Loan Page CTA Pattern

### Patterns
- **Context file "FIXED" entries cannot be trusted without file verification**: Two items marked "✅ FIXED" in `styermortgage-context.md` (loans/conventional + fha hero CTAs, thank-you 3-step section) were NOT present in the actual files. Root cause unknown — likely a session that logged the fix before confirming, or a subsequent session overwrote the file. **Rule: always grep the actual file before assuming context is accurate.**
- **All 8 loan pages had hero CTAs pointing to raw app URL**: The pattern was consistent — `class="btn btn-primary hero-cta-primary hero-cta-btn" target="_blank" rel="noopener">Apply Now` → raw loan app. Only conventional + fha were ever supposed to be fixed (2026-03-24) but weren't. When fixing one loan page, audit all remaining pages for the same pattern immediately.
- **Refinance page hero CTA should go to /refinance-quote, not /get-preapproved**: When batch-fixing loan page CTAs, make an exception for the refinance page — its conversion funnel is /refinance-quote. All other loan pages → /get-preapproved.
- **TCPA suburb forms appeared in context as open but were already present**: All 24 suburb pages already had TCPA checkbox before today's run. The TOMORROW_PRIORITY note was stale. Pattern: verify file state before doing work, not after.

---

## 2026-03-31b — Suburb CTA Sweep: All 24 Pages Fixed in One Batch

### Patterns
- **The raw loan app URL pattern was sitewide, not page-by-page**: After fixing Round Rock (2026-03-31) and Cedar Park + Leander + Georgetown individually, a count-based audit revealed 18+ remaining suburb pages still had body CTAs pointing to raw loan app URL. Batching the fix via `sed` across all `*-mortgage-lender.html` files took 2 minutes vs. the 24 days it would have taken at one page per day.
- **Two URL variants existed**: Most pages used the `?time=1767737197980` version; 6 newer suburb pages (elgin, florence, etc.) used the base URL without timestamp. Both required a separate sed pattern — always check for both variants when doing batch URL replacements.
- **Footer "Apply Now" links also bypass conversion tracking**: 6 suburb pages had an `<a>Apply Now</a>` inside a `<li>` in the footer contact section. These were missed by the body CTA sweep because they didn't have `btn-lg` class. Lesson: grep for the full domain URL, then audit by context — not just by class name.
- **Instance count after batch fix = 2 is the clean baseline**: nav "Apply Now" (class=nav-cta) + hero "Apply Now" (class=hero-cta-primary) are intentional. Any suburb page with count > 2 after the fix has a remaining issue.
- **Suburb hero quick-forms missing TCPA checkbox**: TCPA was added to /get-preapproved and /refinance-quote in 2026-03-24 but suburb pages were not updated. Discovered on Cedar Park today. Likely affects all 24 suburb pages — flag for tomorrow's run.

---

## 2026-03-31 — Suburb CTAs + Two-Manifest Resolution + Title Pattern Persists

### Patterns
- **Suburb pages CTAs link to raw loan app URL, bypassing conversion tracking**: Round Rock had both "Start Your Pre-Approval" and "Start Your Application" CTAs pointing to the 1003 app URL directly. This means those clicks are invisible to Google Ads and GA4 (generate_lead doesn't fire). Fixed by changing to /get-preapproved. Likely affects all 9 suburb pages — audit one per run in rotation.
- **blog.html now dynamically fetches manifest.json**: The recurring two-manifest divergence issue is RESOLVED. blog.html uses `fetch("blog/manifest.json")` — only one file needs updating when new posts are added. Going forward, only update blog/manifest.json.
- **New blog posts (created outside daily-opt runs) consistently miss "Adam Styer |" in title**: DSCR post and Rates post were both created between runs with "| NMLS #513013" instead of "| Adam Styer | NMLS #513013". This is now confirmed a template-level problem. Always grep new blog files for `NMLS #513013` without preceding "Adam Styer |" at the start of each run: `grep -l "| NMLS #513013" blog/*.html | xargs grep -L "Adam Styer | NMLS"`.
- **Temp-placeholder files persist silently**: A new 2026-03-30-temp-placeholder.html appeared with noindex + canonical to proper URL. Pattern: temp files are created during drafting, the proper-named version is deployed, but the temp file is left behind. These accumulate. Need Adam to authorize deletion policy.

---

## 2026-03-30 — Title Tag Length + Blog Manifest Pattern

### Patterns
- **Suburb page title tags can silently creep above 60-70 chars**: Westlake (99 chars) and Buda (104 chars) had rich descriptive titles that were too long for Google to display without truncation. The standard format "Mortgage Lender [City] TX | Adam Styer | NMLS #513013" is always safe. When creating suburb pages, verify title length before publishing.
- **New blog posts inherit the "missing Adam Styer |" pattern**: The March 30 post had "First-Time Buyer Programs Austin TX 2026 | NMLS #513013" — missing "Adam Styer |". This is now the 5th+ occurrence. Pattern: whoever creates new pages is likely copying from a template that omits the brand. Correct format: "[Title] | Adam Styer | NMLS #513013".
- **blog.html manifest divergence is now confirmed systemic (4 runs)**: Will not self-resolve. Either blog.html needs to fetch manifest.json dynamically, or a checklist step must be added to the post-publish workflow. Flag to Adam is escalated.
- **Temp-placeholder filenames create ongoing risk**: Two temp files remain in repo (`2026-03-06-temp-placeholder.html`, `2026-03-10-temp-placeholder.html`). The March 10 one has both a temp version and a properly-named version — duplicate content risk. The March 06 one has real content at a temp URL. These need a decision from Adam before cleanup.
- **noindex regression watch**: /get-preapproved noindex was confirmed present again today. Pattern: check this EVERY run as the first verification step, not just after changes.

---

## 2026-03-30 — Suburb Indexing Emergency + New Competitor Discovery

### Patterns
- **Suburb pages are NOT indexed after 4 weeks**: `site:styermortgage.com` returns zero results for any suburb keyword (Kyle, Pflugerville, Leander, Georgetown, Round Rock, Cedar Park). Homepage and product pages ARE indexed. Root cause: 15 pages were missing from sitemap.xml until ~March 26 fix. Even after fix, indexing takes time — but manual GSC submission is required NOW.
- **Barton Creek Lending Group has 506 reviews at 4.9 stars**: This is the largest review count found in the Austin mortgage market — 3x Highlander (~168), 2x Big Life Georgetown (~265). They currently have NO suburb-specific pages, but their domain authority and social proof make them a latent threat for any keyword they target.
- **Joel Richardson/FCM is actively rising**: Gained positions on multiple keywords simultaneously — now #4 for "mortgage lender austin tx" AND #1 for "cash out refinance austin tx" (displacing Austin Capital Mortgage). This signals active SEO investment. Needs a deep competitive analysis.
- **San Marcos is more competitive than Kyle**: 3 lenders have dedicated San Marcos pages (Capital Home Mortgage, Lone Star, Big Life) vs. Kyle where zero lenders had dedicated pages. San Marcos page must differentiate harder.
- **Westlake Hills is a luxury market**: Quantum Loans leads with jumbo/luxury emphasis. Barton Creek Lending dominates via reviews but has no dedicated page. Our Westlake page must emphasize jumbo loans, Rob Roy/Davenport Ranch, Eanes ISD. NotebookLM flagged title (96 chars → needs ≤60) and meta desc (193 chars → needs ≤155) as too long.
- **Big Life is expanding**: Added San Marcos page since last check (not present in Week 3). Their suburb rollout is accelerating — window to establish first-mover advantage is narrowing.
- **SERPs are shifting**: Multiple position changes this week — SouthStar Bank took #1 for refinance (new), CMG moved to #2 for broker, Max Leaman to #2 for home loan. More volatility than prior 3 weeks.

---

## 2026-03-29b — Noindex Regression + Title Pipe Pattern

### Patterns
- **noindex tags can silently disappear**: /get-preapproved was confirmed noindex,nofollow on 2026-03-22 but the tag was absent today. Likely caused by a file being overwritten or a new session rebuilding the page from scratch. Always grep for `noindex` on both landing pages at the start of every run, not just after changes.
- **Title tag pipe separator is a consistent failure mode**: "Adam Styer NMLS #513013" (missing pipe) appeared in 4+ blog posts and the /get-preapproved title/OG tags. The correct format is "Adam Styer | NMLS #513013". When generating new pages, always use the pipe format. Run a grep for `Adam Styer NMLS` (without pipe) monthly to catch this.
- **blog.html manifest divergence is a systemic problem**: Now missed 3 runs in a row. The two-manifest architecture (blog.html inline + blog/manifest.json) is fragile. Every new post needs both files updated. If this keeps happening, consider refactoring blog.html to dynamically fetch manifest.json instead of duplicating data inline.
- **Server-side tracking is becoming necessary in 2026**: NotebookLM flagged that standard browser-based tracking may lose 20-40% of conversion data due to ad blockers, iOS privacy changes, and cookieless browsers. Worth flagging to Adam for a future architecture decision.

---

## 2026-03-29 — Two-Manifest Problem on Blog

### Patterns
- **blog.html has its own inline manifest** separate from `blog/manifest.json`. Both must be updated when adding new posts. Prior sessions were only updating `manifest.json` — the blog listing page (`blog.html`) was missing the two newest March 28 posts entirely. Rule: any time a new blog post is added, grep for `"posts":[` in both `blog.html` AND `blog/manifest.json` and update BOTH.
- **Blog post URL drift from temp-placeholder filenames**: Surrender post was linked from blog.html as `/blog/2026-03-10-temp-placeholder.html` instead of the real slug. When a post outgrows its placeholder filename, the blog listing must be updated immediately. Watch for `temp-placeholder` in `blog.html` manifest as a red flag.
- **Title tag brand consistency**: Blog post titles should follow pattern "[Topic] | Adam Styer | NMLS #513013" — not just "| NMLS #513013". FHA post was missing "Adam Styer |". Check new posts before declaring them complete.

---

## 2026-03-28 — Sitewide Async Font Loading Fix

### Patterns
- **Async font fix must be applied to ALL pages, not just landing pages**: When fixing render-blocking Google Fonts on landing pages, always grep for remaining `rel="stylesheet"` font loads across the full repo. Found 90 pages still had sync loading even after landing pages were fixed. The one-line pattern change (preload + noscript fallback) is safe to batch across all pages.
- **Multiple font URL variants exist in the repo**: Most pages use `Inter:wght@400;500;600;700&family=Playfair+Display` but internal/old pages use `Inter:wght@300;400;500;600;700` (no Playfair). Use regex matching when batch-processing, not string matching, to catch both variants.
- **Internal pages get the same fix**: ops.html, dashboard.html, marketing pages — even though they're noindexed, fixing fonts improves load time for Adam's own use.
- **Thank-you redesigns often drop noindex**: Already noted in previous run, but confirmed again — monitor anytime thank-you.html is touched.

---

## 2026-03-27 — Weekly Content: Down Payment Assistance Texas 2026

### Patterns
- **NotebookLM recommendation vs. keyword gap**: NotebookLM recommended Spring Market + Pre-Approval. Pivoted to DPA keyword gap instead because the existing /austin-down-payment-assistance.html is dated 2025 and the 2026 version is an uncontested search target. When NotebookLM recommendation overlaps with an existing page, favor the keyword gap.
- **DPA content drives pre-approval submissions**: Down payment assistance posts are high-intent — someone reading about DPA is ready to apply. CTA to /get-preapproved is the natural next step; include it prominently.
- **Meta description 160-char limit is easy to miss**: Wrote 176-char meta initially. Always count before publishing. Trim the attribution ("Adam Styer, NMLS #513013") from the meta desc — the title tag already carries that.
- **NotebookLM import must happen after deploy**: URL-based source import fails if the page isn't live yet. Flag for follow-up import once Netlify builds (usually 2-4 min after push).
- **Keyword clusters covered so far**: Austin mortgage rates (March 2026), cash-out refinance Austin, down payment assistance Texas 2026. Remaining gaps: Kyle TX mortgage guide, Austin spring market 2026, self-employed mortgage Austin 2026, DSCR loan requirements Texas 2026.


This file is updated by every daily run. Each entry is a pattern or insight worth remembering.
Newest entries at the top.

---

## 2026-03-27 — New Suburb Pages Verified + Blog Meta Cleanup

### Patterns
- **Thank-you page redesigns often drop noindex**: When redesigning /thank-you, always verify `<meta name="robots" content="noindex, nofollow">` is present. Organic visitors landing there without submitting a form pollutes conversion data and bounce rate. Check this any time thank-you.html is modified.
- **Blog manifest title drift**: Blog manifest JSON (inline in blog.html) can diverge from actual post content — check titles match the H1/content when auditing. Found "Newsletter" pointing to the surrender story, and lowercase "the ai trap" in manifest while HTML title was properly cased.
- **Double FAQPage count in grep is not a duplicate**: When grepping for "FAQPage" count, `FAQPage:2` on a page means there's a comment `<!-- FAQPage Schema -->` AND the actual `"@type": "FAQPage"` — both are on adjacent lines. Not a structural duplicate.
- **Prior session uncommitted changes**: Check `git status` early in every run. Uncommitted changes from prior sessions can carry important improvements (TCPA, layout) that need to be committed even when not the focus of today's run.
- **TCPA best practice**: 2026 FCC one-to-one consent rules require SMS opt-in to be a SEPARATE checkbox from general contact consent. SMS opt-in should be optional (not required). Main TCPA consent must include "Consent is not a condition of purchase."

---

## 2026-03-26 — Trust Bar Audit + H1 SEO Fix

### Patterns
- **Trust bar drift is a real maintenance problem**: 39 pages had "#1 Austin Mortgage Team" instead of the standard "Licensed in Texas | NMLS #513013". These had diverged silently over many sessions. Run a grep for `#1 Austin Mortgage Team` at the start of any design audit to catch future drift.
- **Homepage H1 was keyword-weak**: "Your Austin Home Loan Simplified" has zero searchable keywords. Changed to "Mortgage Broker Austin TX — Adam Styer | NMLS #513013" per NotebookLM recommendation. Front-loading the primary geo keyword is the highest single SEO ROI change confirmed by research.
- **Suburb forms don't feed Google Ads conversions**: suburb quick-forms show inline success (no redirect to /thank-you). Only `thank_you_page_view` triggers Google Ads conversion, so suburb lead form completions are invisible to Google Ads bidding algorithm. This suppresses Quality Score and ROAS tracking.
- **Answer-First formatting matters more every month**: NotebookLM confirmed 25%+ of searches now have AI Overviews. Pages with a 40–60 word direct answer in the first paragraph get extracted for citations. This is increasingly competitive.
- **Form fields and conversion rates**: 5 fields is the recommended maximum for mortgage initial contact. Research benchmark: 11 → 4 fields = 120% conversion lift. Consider combining first/last → full name on /get-preapproved.

---

## 2026-03-25 (Week 3) — Pflugerville & Kyle Suburb Analysis

### Competitive Notes
- **Kyle is the most open suburb keyword found to date** — NO dedicated lender page ranks in top 3. All positions held by directories (Greater Texas CU, Zillow, Yelp). First lender with a dedicated page could own this keyword.
- **Big Life now ranks #1 for 3 suburbs** (Pflugerville, Leander, Georgetown) — all with generic templated content. They do NOT have a Kyle page (gap).
- **Lone Star's Austin metro blind spot is massive**: Only 6 pages in Austin metro (Austin, Round Rock, Lakeway, San Marcos, Killeen, Waco) out of 35+ total city pages. Missing: Pflugerville, Kyle, Cedar Park, Leander, Georgetown, Buda, Dripping Springs, Westlake, Hutto, Liberty Hill.
- **Arnaiz Mortgage uses FAQPage schema** — first competitor found with this. Also uses LocalBusiness, MortgageLoan, Service, BreadcrumbList. Multi-state templated operation (50+ city pages). FAQPage is no longer an uncontested opportunity — it's urgent.
- **Geneva Financial Pflugerville page has Arizona address** — worst quality #3 ranking seen. Trivially displaceable.
- **Nest Mortgaging (Kyle)**: Erica Bille, local Kyle broker with strong personal branding and 5-star reviews. Uses my1003app.com (same platform as Adam). Weak SEO now but could improve — potential future threat for Kyle keyword.
- **No Google Ads competition** on suburb keywords (Pflugerville, Kyle) — zero paid competition, cheap CPCs available.
- **Kyle market data for future pages**: ~$350-380K median home price, Hays County, I-35 corridor, 90%+ population growth last decade, Plum Creek and Steeplechase key neighborhoods, Kyle ISD.
- **Pflugerville market data for future pages**: Key neighborhoods include Blackhawk, Falcon Pointe, Meadows of Blackhawk, Wells Branch. PISD (Pflugerville ISD). Travis/Williamson County overlap. Growing tech corridor.

### Suburb Keyword Patterns (Pflugerville + Kyle)
- Kyle: Directories completely dominate — no mortgage lender has a dedicated page ranking. Easiest suburb keyword to win.
- Pflugerville: Big Life ranks #1 with generic content (25 reviews). Geneva ranks #3 with wrong-state address. Both beatable with localized content.
- Credit unions (Greater Texas CU, Austin Telco) rank well for suburb keywords — they're trusted local brands but rarely have dedicated suburb landing pages.
- Review counts on suburb pages matter: Big Life's 25 reviews on Pflugerville page vs 265 on Georgetown page correlate with ranking strength.

---

## 2026-03-24 (Week 2) — Suburb Keyword Deep Dive

### Competitive Notes
- **NEW competitor: Big Life Home Loan Group** — ranks top 3 for both "leander mortgage lender" and "georgetown mortgage lender". Uses AggregateRating schema (265 reviews on Georgetown page). Templated but localized content. Parent: Cornerstone Capital Bank.
- **Lone Star has NO Leander or Georgetown pages** — despite having 30+ suburb pages elsewhere. Major gap in Williamson County suburbs.
- **Review benchmark raised**: Big Life claims 265 reviews on Georgetown page (up from Highlander's ~168 as the reference). Review gap is widening.
- **Arnaiz Mortgage and LendFriend Mortgage** are new entrants in refinance keywords — refinance SERP getting more competitive.
- **Templated city pages at scale** (JVM Lending model) are becoming common — national lenders auto-generating pages. Must differentiate with genuinely local content (market stats, neighborhoods, school districts).
- **Directory dominance growing**: WalletHub "2026's Best" list now ranking for commercial keywords alongside Zillow and Yelp.
- **Lone Star updating content**: Austin page shows Feb 2026 market data ($572,479 avg price). Content freshness is a ranking signal they're investing in.
- **Big Life weakness**: Their suburb pages are generic mortgage education with city name swapped in. No local market data, no neighborhood info. Beatable with better localization.
- **Schema triple play opportunity**: No competitor uses LocalBusiness + FAQPage + AggregateRating together on suburb pages. First to do all three wins rich snippet real estate.

### Suburb Keyword Patterns (Leander + Georgetown)
- Zillow and local directories rank #1 for suburb keywords — organic lender pages compete for positions 2-5
- Dedicated suburb pages with schema markup outrank generic "serving all of Austin" pages
- Content that mentions specific neighborhoods, school districts, and local data ranks better than templated pages
- Georgetown Mortgage Bank benefits from exact-match domain — hard to displace but niche

---

## 2026-03-24 — Initial Setup

### Patterns
- Google Fonts loaded synchronously block render on mobile — always use async preload pattern
- Form must render ABOVE headline on mobile via `order: -1` in CSS media query
- Ad landing pages (/get-preapproved, /refinance-quote) must NEVER have site navigation
- Netlify Forms require both `netlify` attribute AND hidden `form-name` input

### NotebookLM Insights (cached)
- Suburb pages are the #1 priority — Lone Star has 30+ and dominates suburb keyword SERPs
- FAQPage schema is an uncontested opportunity — neither Lone Star nor Highlander uses it
- Answer Engine Optimization (AEO): lead every page with a direct 40-60 word answer to the primary query
- Atomic paragraphs (2-4 sentences) + bulleted lists get extracted by AI search engines
- E-E-A-T is non-negotiable in YMYL: NMLS#, license badges, author bios on every page

### Competitive Notes
- **Baseline (2026-03-24):** 0/9 target keywords in top 10
- **Primary threat:** Lone Star Financing — 30+ suburb pages, LocalBusiness schema, since 2007
- **Secondary threat:** Highlander Mortgage — 168+ reviews at 4.9 stars, ranks #1 for "mortgage broker austin tx"
- **Schema gap:** Neither top competitor uses FAQPage schema — first mover advantage available
- **Suburb page gap:** We have 0 suburb pages vs. Lone Star's 30+ — this is the biggest SEO deficit
- **Review gap:** Highlander has ~168 reviews — we need a systematic review solicitation campaign
- **"Cash out refinance austin tx"** — winnable keyword, current #1 is a small player (Austin Capital Mortgage)
- **"Get pre-approved austin tx"** — auto lenders dominate, mortgage-specific modifier could own this
- **Directory profiles matter:** Zillow and Yelp directories rank in top 3 for multiple keywords — optimize those profiles too
- **Low Google Ads competition** on target keywords — potential for cheap CPCs
- **Key competitors to track:** Lone Star Financing, Highlander Mortgage, Austin Capital Mortgage, Max Leaman/Loan People, CrossCountry Mortgage

### Content Strategy
- Keyword clusters not yet covered: cash out refinance Austin, DSCR loan requirements Texas 2026, down payment assistance Texas 2026
- Blog should publish weekly minimum for SEO freshness signals
- Every blog post needs CTA to /get-preapproved or /refinance-quote

### GBP Notes
- Posts rotate on 5-theme cycle: Rate Commentary → Loan Product → Homebuyer Tip → Misconception Buster → Client Story

---

## 2026-04-06b — Blog Template Confirmed Correct; AEO Homepage + Person Schema

### Patterns
- **Blog title drift root cause is human process, NOT the template**: `_template.html` line 28 already has `<title>@@POST_TITLE@@ | Adam Styer | NMLS #513013</title>` with a prominent enforcement comment block. The template was correct. Authors publishing new posts without using the template is the only explanation. Pre-publish lint (`grep "<title>" blog/*.html | grep -v "Adam Styer"`) is the only automated defense. Add this to the start of every run as a non-negotiable step.
- **Homepage AEO paragraph placement**: The correct injection point is before the first H2 in a non-hero content section — specifically before the "Why Choose Adam Styer" H2 inside the `<section>` after the stats strip. Adding `<p class="text-center" style="max-width:760px;margin:0 auto 2rem;"><strong>...</strong></p>` at that position ensures the paragraph is both structurally early and visually coherent as a section intro.
- **Person schema + MortgageBroker schema co-existence**: Both should live on the homepage. The `worksFor` relationship in Person schema pointing to the MortgageBroker entity creates a named-entity graph connection that helps Google treat Adam Styer as a real licensed professional rather than an anonymous business. `sameAs` with LinkedIn/Zillow/Facebook is the cross-domain entity validation signal.
- **GSC "Request Indexing" vs. sitemap submission**: Sitemap submission tells Google pages exist. Manual "Request Indexing" in URL Inspection signals urgency and puts pages in the crawl priority queue. For a 24-page suburb URL set with a 5-week indexing lag, manual request indexing for the top 5-10 pages is the recommended acceleration path. Adam must do this in GSC — not automatable.
- **NotebookLM timeout pattern**: Run 1 (early AM) timed out; Run 2 (later) succeeded. Likely an initialization/cold-start issue. If a scheduled run hits timeout, cached NOTEBOOK_INSIGHTS are sufficient to proceed. No action needed beyond logging.


---

## 2026-04-07 — Meta Description Length Audit: va.html Was Over-Length

### Patterns
- **va.html had the only over-length meta description at 181 chars**: All other loan pages were short (142-149), construction/jumbo/investment were already in range (150-156). The over-length va.html description crept in from the April 1 meta expansion that added "Get pre-approved in 24 hours" to an already full description. Rule: after any meta description edit, verify character count with Python before committing.
- **Tuesday meta audit revealed 6 of 10 loan pages needed fixes**: Not all pages got attention in the same pass. The pattern now: run `python3 -c "..."` to bulk-check all loan page meta lengths at the start of every Tuesday rotation rather than reading files one by one.
- **AEO suburb coverage progress**: Taylor, Smithville, Spicewood added 2026-04-07. Remaining suburb pages without confirmed AEO body paragraphs (needs audit): New Braunfels, Bastrop, Bee Cave, Marble Falls, Elgin, Florence, Jarrell. Next Wednesday rotation should pick up from alphabetical order.


---

## 2026-04-09 — AEO Grep Pattern Clarification + San Marcos Added

### Patterns
- **`content-narrow + <p><strong>` multiline grep hits footer — not reliable for AEO detection**: The grep pattern `content-narrow.*\n.*<p><strong>` matched san-marcos because the footer section in some files has a `<div class="footer-section">` that is a sibling inside the same content-narrow parent, or because the multiline match spans beyond the intended section. **More reliable check: grep for `<strong>To get a mortgage in`** which is the exact AEO pattern used on all suburb pages. Only Round Rock matched — confirming all others use `<p><strong>` for other purposes (footer company name, etc.). Use this exact pattern for future AEO coverage audits.
- **AEO coverage: 13/25 suburb pages confirmed as of 2026-04-09**: Confirmed with answer-first paragraph: Round Rock, Georgetown, Leander, Pflugerville, Cedar Park, New Braunfels, Bastrop, Bee Cave, Elgin, Florence, Jarrell, Marble Falls, Hutto, Spicewood, Liberty Hill, Lakeway, Smithville, Manor, Taylor, Kyle, Austin-area, Dripping Springs, San Marcos (added today). Remaining: Buda, Westlake.
- **San Marcos AEO angle: USDA + investment**: San Marcos has two unique differentiators vs. other Austin suburbs — (1) USDA eligibility throughout Hays County (zero-down) and (2) Texas State University rental market for DSCR investors. These are the two points most banks don't lead with. The AEO paragraph used this angle rather than the generic "40+ lenders" opener to improve specificity for AI extraction.

---

## 2026-04-12b — AEO Completion: Last 5 Suburb Pages + Template Pattern Differences

### Patterns
- **"AEO done 2026-04-09b" in TODO.md was wrong — 5 pages still missing**: Kyle, Leander, Pflugerville, Georgetown, and austin-area all lacked AEO answer-first paragraphs. The TODO entry was premature. **Rule: don't mark AEO as "all done" until you've explicitly grepped every page for `<p><strong>To get a mortgage` or equivalent. A count-based check (`<p><strong>` ≥ 2) is insufficient because some strong blocks are city enrichment or footer text.**
- **Two distinct templates require different AEO insertion points**: The newer template (buda, westlake, san-marcos, etc.) uses a dedicated `<!-- AEO Answer -->` section with its own `<section>` block. The older template (leander, pflugerville, georgetown) uses a single `<!-- Intro Content -->` section where the AEO paragraph goes as the first element inside `content-narrow`, before the H2. Never insert AEO text inside a `<header>` tag or after an H2 — always the FIRST child of the content-narrow div.
- **Austin-area hub page also needs AEO**: The `/austin-area-mortgage-lender.html` hub page listing all 24 cities had no answer-first paragraph. Hub pages that answer broad queries (e.g., "mortgage broker Austin area") are high-value AEO targets too — treat them the same as suburb pages.
- **"At a glance" blocks are not AEO — confirmed again**: Georgetown and Leander both had `<p><strong>[City] at a glance:</strong>` enrichment blocks. These are mid-section factual summaries, not answer-first paragraphs. The AEO requirement is specifically the opening answer to the primary query, before any H2 or narrative prose.

---

## 2026-04-21b — Loan Page Title Audit: Two Fixes + NMLS in Hook Titles

### Patterns
- **DSCR and Refinance titles were over 65 chars — simple redundancy trim resolved both**: DSCR had "| Investor Mortgage" after the product name (redundant — DSCR already implies investor mortgage). Refinance had "Mortgage" after "Refinance" (redundant) plus title order buried the product keyword. Fix: remove redundant segment, reorder "Cash-Out Refinance" to lead. Both now under 60 chars.
- **FHA and Jumbo use deliberate hook titles that exclude NMLS (too long to include)**: "Austin FHA Loans: Broker, Not a Call Center | Adam Styer" and "Jumbo Loan Austin: 10% Down to $1.5M | Adam Styer" both have strong CTR hooks but no NMLS in the title. Adding NMLS pushes them over 65 chars. Decision: NMLS is in meta description + page body, which satisfies compliance. Hooks are deliberate differentiators — do not change.
- **Second-run same-day correctly defaults to the skipped rotation**: Morning run overrode Tuesday rotation in favor of TOMORROW_PRIORITY items. Second run correctly executed the skipped Tuesday rotation (loan page title/meta audit) rather than repeating the morning work or jumping to Wednesday.

## 2026-04-21 — AEO Paragraph on Pre-Approval Page + Refinance FAQ Expansion

### Patterns
- **TOMORROW_PRIORITY items correctly override Tuesday rotation when set**: The pre-approval AEO + refinance FAQ schema items from yesterday's TOMORROW_PRIORITY took precedence over the Tuesday title/meta rotation per the SKILL instructions. Both were directly aligned with GOALS.md ("SEO fixes: high impressions, low CTR" + "content quality"). Pattern: always check if TOMORROW_PRIORITY aligns with weekly GOALS before deciding whether to override or complete both.
- **Duplicate sitemap entry discovered for mortgage-pre-approval-austin.html**: Page appears at line 49 (priority 0.9) and line 95 (priority 0.8). These were both updated to 2026-04-21. Duplicates in sitemap.xml can cause Google to treat one entry as canonical and ignore the other — or send mixed signals. Safe to remove the lower-priority duplicate (line 95). Flag for cleanup when ZERO_RISK items are needed.
- **NotebookLM now 3-4 weeks stale on already-fixed items**: Five consecutive Monday runs have returned at least one recommendation that was already completed weeks prior. Strategy: treat NotebookLM as a competitive/ranking intelligence tool only. Never use it for specific on-page fix recommendations without verifying against live files first.
- **Refinance FAQPage schema can have 6 questions without Rich Results Test issues**: Google's FAQPage rich results support unlimited questions. Adding a 2026-dated, process-specific question ("How does a Texas cash-out refinance work in 2026?") differentiates from competitors who have the same generic "What is a Texas cash-out refi?" question. Process-oriented questions have higher AI Overview extraction probability.

---

## 2026-04-22 — Bee Cave OG Description ISD Error + broker-vs-bank AEO H2 Pattern

### Patterns
- **OG description can contain factual errors invisible to on-page audits**: Bee Cave's OG description said "near Eanes ISD" despite the page body and schema consistently saying "Lake Travis ISD". Eanes ISD covers Westlake Hills/Rollingwood (78746 ZIP), NOT Bee Cave (78738 ZIP). OG descriptions are visible to social share previews and LinkedIn/Facebook previews — a wrong ISD claim can undermine trust for buyers doing their homework. Rule: when auditing suburb pages, always check OG description for ISD name against the page body.
- **TOMORROW_PRIORITY checklist can contain items already done**: Bee Cave TOMORROW_PRIORITY said "AEO paragraph present?" and "comparison table + FAQPage schema" for mortgage-broker-vs-bank — both were already complete. Confirmed via live grep before touching anything. The Re-Verify Gate cleared all three items as already resolved. Pattern: always re-verify TOMORROW_PRIORITY items against live files before executing.
- **mortgage-broker-vs-bank CTAs pointed to /prequal.html (noindexed, in robots.txt Disallow)**: 3 CTAs all linked to /prequal.html. Changing these to /get-preapproved aligns all CTAs with the primary conversion tracking flow (generate_lead event). The /prequal.html page still exists and is functional but the indexed comparison page should funnel to the primary tracked conversion page. Rule: after any batch CTA audit, check resource/article pages (not just suburb pages) for stale /prequal.html or other non-tracked landing page links.

## 2026-04-29 — Round Rock USDA Cleanup + Re-Verify Gate Auto-Resolves a 4-Run Phantom

### Patterns
- **Re-Verify Gate caught a phantom 4-run carry-forward**: yesterday's log escalated thank-you.html "alt-paths uncommitted" to HIGH on the 4th run. Today's `git diff thank-you.html` returned empty — zero unstaged changes. The phantom was real for the first 3 runs but somehow resolved between yesterday's log and today's start. Without the gate, this would have been a 5th-run HIGH item. Lesson: re-run `git diff <file>` on EVERY carry-forward file claim before resurfacing — claims about working-tree state go stale fast.
- **Suburb structural audit ≠ voice/compliance audit**: Round Rock passed all 15 structural checks (form, schema, AEO, links, trust bar, NMLS, GTM, TCPA) but had 3 USDA mentions in product copy. Adam doesn't originate USDA. The structural audit was clean; the compliance audit caught a real misrepresentation. Both audits must be in the suburb deep dive — they catch different classes of issues.
- **DSCR + bank-statement is the right voice-guide-aligned substitute for USDA in product menus**: Round Rock's FAQ "loan programs" answer needed a replacement after USDA removal. Voice guide explicitly lists DSCR and bank-statement as products Adam DOES originate (and wants to grow). Substituting "DSCR or bank-statement loans for self-employed and investor buyers" replaces the missing item with copy that's accurate, on-voice, and serves the same "complete loan menu" reader goal.
- **Site-wide nav USDA dropdown is a single Adam decision that unblocks ~88-page cleanup**: removing USDA from 1 suburb body was easy. But the global nav dropdown (header + footer) on every page links to `/loans/usda.html`. Touching that requires a single decision (keep, redirect, or delete the page) before the cascade can happen. Lesson: when finding a misrepresentation that exists in global nav, consolidate ALL the per-page mentions into a single Adam-decision flag rather than flagging each page individually.
- **Sitemap rate-alert finding closed in 1 run via direct implementation**: yesterday flagged rate-alert.html as missing from sitemap.xml. Today verified (200, no noindex, not in robots Disallow), added with weekly/0.7 priority, deployed, verified live. Total cycle time: <2 minutes. Decision Test bias is correct — when the work is reversible and voice-guide-aligned, just do it; flag only when irreversible or judgment-required.

## 2026-05-01 — Friday CTA audit + post-sweep footer drift

- **Friday CTA audit catches conversion-funnel breakage that title/meta audits miss.** A post can have a perfect title, perfect meta, perfect schema, perfect intro paragraph — and still leak its entire SEO investment if its bottom CTA points at a legacy path. Found `blog/2026-04-27-why-home-prices-arent-crashing.html` with 0 links to `/get-preapproved` or `/refinance-quote` while every other 2026 post had 2-6. The single bottom CTA used `../prequal.html` (legacy quick form, lower-value funnel) + `../contact.html` (no funnel). The exact grep that caught it: `for f in blog/2026-*.html; do echo "$f | preapproved:$(grep -c get-preapproved $f) refi:$(grep -c refinance-quote $f)"; done`. Run this on every Friday rotation. Any post returning 0:0 — fix immediately, don't flag.
- **The fix pattern is href + label together.** Don't just swap the URL — also update the button label to match. `../prequal.html` legacy text is "Get Pre-Qualified" (a self-service form). `../get-preapproved` canonical text is "Get Pre-Approved" (Adam issues the PA letter himself; correspondent-lender voice). Mismatched href/label looks like a template error.
- **Sitewide sweeps go stale by ONE the moment a new post is published with the legacy template.** The 2026-04-25 footer Awards sweep was 88/88 site pages. The 2026-04-27 blog post was published 2 days later and imported the legacy `91 Google + 45 Zillow Reviews` 2-line block. The metric was "88/88 ✅" but the sweep had already gone stale — and would have stayed stale until the next sitewide pass. Lesson: sweeps are point-in-time; any post published after a sweep needs a per-post footer lint at publish time. Add to TOMORROW_PRIORITY for any future weekly content runs: `grep "91 Google\|45 Zillow" blog/<latest>.html` before deploy.
- **CTA audit corrected the previous run's "21/21" implicit assumption.** The 2026-04-30 PM run reported on cluster denominators (`11/16 OK, 5 remaining`) but didn't check the conversion-funnel coverage. I'd implicitly assumed all 21 dated posts had a CTA — they didn't. Add explicit CTA-coverage metric to METRICS section ("Blog CTA conversion-funnel coverage: 21/21 ✅") so future runs surface drift the same way Footer Awards consistency does.

## 2026-05-01 PM — Bookkeeping run discipline

### Pattern
- **PM bookkeeping run is most useful when AM was thorough.** Today's PM run found nothing new because AM caught the actionable Friday-rotation items (CTA breakage on 2026-04-27 post + footer Awards drift). The right PM behavior is: re-verify the AM commit propagated live, confirm no rollback needed, write the run log, commit bookkeeping. **Don't manufacture work to fill the run.**
- **Re-verify gate primary value on PM runs is propagation confirmation** — not catching new findings. AM-fix-went-live verifications are quick (curl + grep) and provide the audit trail that a downstream task or future-Adam can use to confirm the fix is healthy.
- **Bookkeeping commit is optional when nothing changed in HTML.** This run wrote run-logs/2026-05-01b.md, refreshed run-logs/latest.md, appended one CHANGELOG entry, and updated CONTEXT.md (Last Worked On + 1 counter). All non-mutating files. Single commit captures the full state.
- **Recurrence-counter discipline:** when a stale flag hits 8+ runs (about.html address) or 3+ recurrences (why-home-prices structural), the right move is still to log it forward, not give up. Adam reads CONTEXT.md and CHANGELOG; the counter signals priority drift.

## 2026-05-02 — Netlify pretty-URL strips `.html` in served HTML; gate grep must be path-agnostic

### Pattern
- **Netlify "pretty URLs" rewrite internal links in the served HTML to remove `.html`**, but the local source files keep the `.html` extension. This breaks naive Re-Verify Gate grep checks. Today's near-miss: an initial `curl https://styermortgage.com/how-to-buy-a-house-in-austin-tx.html | grep -c '/loans/usda.html'` returned 0, which read like the HIGH USDA-in-loan-table blocker had auto-resolved. Widening to `grep -ci usda` revealed 6 live mentions including the loan table cell as `<a href="/loans/usda">USDA</a>` (no `.html`).
- **Rule for Re-Verify Gate grep on live Netlify HTML:** search by resource name (`usda`, `prequal`, `get-preapproved`), not literal local file path. The local-file vs. served-HTML mismatch is structural, not a one-off.
- **Diagnostic when an auto-resolve seems too easy:** if a HIGH blocker that's been carried for multiple runs appears to clear in a single live grep, widen the pattern before logging RESOLVED. False-clears are worse than false-opens — they remove urgency from a real problem.
- **The Re-Verify Gate self-corrected today.** The gate exists exactly to catch claims-against-live-state errors; today it caught its own pattern bug. Logging the lesson is what keeps the gate honest in future runs.

## 2026-05-05 — Tuesday meta/title batch + Monday Schema-AEO carry-over

### Patterns
- **After ANY data-refresh edit (Redfin median, review count, year, market month), re-run a Python `len()` check on title + meta in the same edit batch.** Bee Cave (168), Dripping Springs (218), and Georgetown (182) all drifted to over-length AFTER an earlier Redfin median-price refresh appended new data without re-counting. The Tuesday rotation caught them weeks later. Inline length verification at edit time would have prevented the drift entirely. Add to BATCH PROCESSING RULES in agent-rules.md.
- **Two title-length classes coexist on the site by Adam decision (per 2026-04-21b learnings).** Class (a) public-funnel pages follow canonical `[Topic] | Adam Styer | NMLS #513013` capped at 65. Class (b) deliberate-hook pages (FHA "Broker, Not a Call Center" 56, Jumbo "10% Down to $1.5M" 49) keep the hook even at 67-68 chars — those hooks are brand differentiators. When auditing, separate (a) drift candidates from (b) intentional-hook candidates before flagging. Skipping marble-falls/get-preapproved/improve-credit-score titles at 67-68 was the right call.
- **about.html LocalBusiness address (5900 Balcones) appears in only 1 schema block site-wide; 5718 Sam Houston Circle appears in 3 (homepage MortgageBroker, homepage Person, about Person + Person.worksFor entity).** That's a 3:1 weight-of-evidence signal that 5718 is canonical. The mismatch has been carried for 12 runs — recommend aligning to 5718 with a one-line edit on about.html line 123 if Adam confirms. Going to surface this as a concrete recommendation, not just an open question.
- **Monday-rotation work can be batched into Tuesday safely when Sunday's TOMORROW_PRIORITY items are mostly verification-class.** Sunday's TOMORROW_PRIORITY for Monday was Schema/AEO audit + RRT sweep + entity consistency — all read-only verification with one Adam-decision finding (about.html mismatch). Tuesday's rotation (title/meta) is mutate-class. Combining both in one Tuesday run gives a single commit and a single deploy verification cycle. Pattern: when missed-rotation work is read-only, batch it forward; when it's mutate-class, run it as a separate "carry-over" pass.
- **Live blog cadence is a real signal — track days-since-last-post in METRICS, not just on Friday.** Last post 2026-04-27 → 8 days as of 2026-05-05 = trips the 7-day flag on Tuesday, not Friday. The Friday-only check would have missed 4 days of overdue cadence. Move "days since latest blog post" to a daily-tracked metric in METRICS section so any rotation run can see and flag the slip.

## 2026-05-05 PM
- Length checks must use UTF-8 byte count, not Python str len(), when meta description contains `$` ($24 = 1 byte but renders different width) or em-dashes (3 UTF-8 bytes vs 1 char). AM logged 158/153/150 char-lengths but live byte-lengths were 162/159/150. Both fall in Google's 110-165 display range so no functional drift, but the AM target value miscounts the actual rendered string. Future Tuesday rotation: switch length helper to `len(s.encode('utf-8'))` or measure pixel width via approximation (~1px per ASCII, ~3px per em-dash).
- Same-day second invocations of styer-site-daily should default to verify-only PM pass: sitemap 200 check, propagation curl on AM-edited pages, re-verify gate on carry-forward flags. Skip rotation (already done), skip notebook query (broken anyway), skip master-log full re-paste (would duplicate). Append a clearly labeled PM ADDENDUM to today's run log + thin PM entry to master log + cp to latest.md.

## 2026-05-06 — Wednesday: Cluster Re-Audit Caught 2 Missed Pages + Multi-Page CTA Pattern Fix + 20th Dead NotebookLM Run

### Patterns
- **Re-audit a freshly-shipped multi-page cluster on the next run, even if the prior run "audited" it.** Tue eve sampled 4 of 7 new non-QM pages and reported "2 missing tracked-CTA pages." Today's full sweep found **4** missing-CTA pages. The 2 incremental discoveries (fredericksburg + HNW) would have been added to a future TOMORROW_PRIORITY queue and stretched the fix across 2-3 daily runs. The cleaner pattern: when a cluster ships, do a complete N-of-N sweep on the next run before naming the discovery count. Cheaper than incremental discovery.
- **HNW pages need different CTA patterns than self-employed/investor non-QM.** All 7 non-QM pages share template structure, but HNW correctly uses Calendly as primary (consultation-first) while the other 6 use /get-preapproved as primary (form-capture-first). When fixing the CTA gap, swap the SECONDARY only on HNW — preserves the consultation-first pattern. The Tue eve flag treated the cluster as homogeneous; the right read is "5 of 7 share the form-first pattern, 1 (HNW) is consultation-first by design, and 1 (Dripping Springs) had the form-first pattern correctly set already."
- **When swapping internal links from external (target=_blank) → internal (`/path`), drop BOTH `target="_blank"` and `rel="noopener"`.** They're noise on internal links and a code smell — easy to miss when copy-pasting. Made 8 of these swaps today, kept the diff clean.
- **Class-b deliberate-hook titles can drift to actually-truncating territory if not capped.** The non-QM cluster shipped with 5 titles between 77-90 chars. Most are within the deliberate-hook ≤68 norm with a small drift, but Dripping Springs at 90 is genuinely SERP-truncating. Lesson: the deliberate-hook class still has an upper bound at ~68 chars. Anything past that needs trim regardless of wedge value. Trimmed Dripping Springs 90 → 65 while preserving the "Wedding Venue STR" wedge.
- **Suburb deepening Round 2 should target NAMED gaps over generic enrichment.** TODO listed 5 specific neighborhoods + 2 specific transit identifiers. The pre-edit audit confirmed 5 were genuine gaps (Caballo Ranch, Bryson, Leander Springs all 0; CapMetro and Leander Station 0). One short paragraph naming all 5 closes the gap without bloating the page. The 19+ Crystal Falls and 15+ Travisso mentions show "deep" coverage already exists; the page didn't need MORE depth on those communities, it needed coverage on the gap communities. Lesson: depth audit + gap audit are different — don't conflate.
- **NotebookLM 20th consecutive dead run.** Same pattern. SKILL.md retirement diff still pending Adam's apply.
- **Blog cadence trip is now 9 days.** Friday escalation guidance from Tue is firm — if no post lands by Friday, the flag escalates from MEDIUM to HIGH. Wednesday's Leander Round 2 work is a natural blog-post topical hook (Crystal Falls / Bryson / Caballo Ranch / Leander Station — could anchor a "Where to buy in Leander 2026" pillar post).

## 2026-05-08

- **Funnel-CTA audit grep MUST include both `/path` and `../path` to avoid false negatives on older blog posts.** Five+ runs flagging `2026-04-27-why-home-prices` as missing CTA were a methodology error — post had `../get-preapproved` the whole time. Future audits use: `grep -cE 'href="(/get-preapproved|\.\./get-preapproved|/refinance-quote|\.\./refinance-quote)'`.
- **Re-Verify Gate HTTP-status check catches phantom-page carries.** A flag claiming N occurrences on a URL that returns 404 should auto-resolve, not perpetuate. Yesterday's PM log carried `how-to-buy-a-house USDA live=5/local=5` for 5 runs without verifying URL existed. Pattern: any carry that claims occurrence counts on a specific URL must `curl -sI` first; if 404 → auto-resolve as phantom.
- **Personal-fact dates in about-page timelines aren't auto-stale.** "16 years in Austin", "10 years sober" are personal facts, not auto-updated. Don't flag as date-drift unless inconsistent with another date in same content (e.g., "since 2017" + "21 years experience" would actually conflict).
- **GOALS.md trumps daily rotation escalations.** When weekly direction explicitly excludes work ("No new content on any site this week"), automated escalation rules (blog cadence Day-11 → HIGH) defer until next Monday's GOALS refresh. Log the exemption in run log + CONTEXT.md so it doesn't re-escalate every day.
- **Re-Verify Gate self-correction.** Auto-resolved a claim under wrong framing (date span) when the actual claim was about review-count breakdown. Pattern: before auto-resolving any carry that mentions specific numbers ("91/45"), re-read the most recent TODO.md context for that exact item — don't trust the run-log carry text alone. The TODO.md or DECISIONS.md is the canonical source for the claim's intent.
- **Re-Verify Gate UNVERIFIED tag is the right answer when live verification needs Adam's UI-only credentials** (GBP, Zillow, GSC). Don't auto-resolve those — tag UNVERIFIED:<date> and surface for Adam.

## 2026-05-08 PM rerun — added insights
- Programmatic conversion-tracking grep should test for `Netlify` (case-insensitive) and `netlify/functions/lead-intake`, NOT `data-netlify`. The LP forms post via JS to a Netlify Function, not bare-attribute Netlify Forms. Update grep templates accordingly.
- When Adam pushes mid-day commits between AM and PM runs, sanity-check JUST those impacted URLs with HTTP+CTA+sitemap-lastmod — don't re-walk the whole site. Marginal-cost-aware verification.
- PM rerun on a same-day cycle should re-state and re-affirm AM's conscious deferrals, not silently re-litigate them. If AM chose to defer X, and nothing has changed since, PM defers X.

## 2026-05-10

- **Always carry the FULL slug in run-log carry text — truncating produces 404 false-regressions.** Sat PM run carried "2026-04-27-why-home-prices" (truncated); today's curl to that URL returned 404. Full slug "2026-04-27-why-home-prices-arent-crashing" returns 200 with state unchanged (2 `../get-preapproved`, 0 `/refinance-quote`). If a previously-OK URL 404s and you can't explain why, suspect run-log slug drift before flagging a regression. Re-Verify Gate works on the gate's own metadata, not just inherited claims. Pattern: never abbreviate slugs in carry text — copy/paste the full filename without `.html`.
- **24h+ post-deploy cache-window check is the right point to drop a Liberty Hill–style suburb-editor commit from the active sanity table.** Adam's Sat 09:22 Liberty Hill R2 commit at 27h+ post-deploy is fully propagated through Netlify edge. Token-count drift between AM (67) and PM (77) is grep-methodology drift, not content drift. Drop-from-table rule = next run after 24h+ confirmation, unless an issue emerges.
- **Sunday no-rotation by convention** (matching Saturday's no-rotation pattern). Run shape: Re-Verify Gate + sitemap + conversion check + Step 4B backlog sweep only. Zero new edits expected, zero made. Monday resumes full rotation.

---

## 2026-05-10 PM — Sunday No-Rotation Re-Verify + Intra-Day Manor R2 Sanity (29th Dead NotebookLM Run)

### Patterns
- **`curl` in zsh-on-darwin inline-expansion contexts can fail to resolve from $PATH inside a `for ... do` loop invoked via the harness — even when `which curl` returns `/usr/bin/curl` directly seconds earlier.** First conversion-trace call this run printed `command not found` four times in a row inside the loop. Workaround: `CURL=/usr/bin/curl` at the top of multi-call verification blocks, then use `$CURL` inside the loop. Sub-second fix once spotted. Worth adding as a defensive prelude to any future programmatic-trace block in this SKILL or sister SKILLs that runs multi-URL curl loops.
- **PM run after Sunday-AM no-rotation continues to be a high-value bookkeeping shape.** AM ran the Re-Verify Gate, caught a methodology bug (truncated slug → full slug), committed zero site changes. PM repeats the gate against live state ~14h later: zero drift on 10 claims, plus one fresh data point — Adam pushed an intra-day Manor R2 commit (`2e6e19c`) between the two runs. PM's value-add was the targeted Manor sanity-check (HTTP + funnel CTA + R2-token grep + sitemap lastmod) — the rest was confirming-still-true. Same pattern as 2026-05-02/03 and 2026-05-09 PM runs. **Skip rule for future Sunday PM runs: only skip PM entirely if AM was incomplete/broken AND no intra-day commits between AM and PM.** An intra-day commit alone is sufficient justification to run PM as a targeted sanity-check.
- **Adam's suburb-page editing cadence now includes Sunday-afternoon intra-day commits.** Both 2026-05-09 (Saturday: Liberty Hill R2) and 2026-05-10 (Sunday: Manor R2) shipped on weekend days. The "weekend no-rotation" convention is for THIS task (no site edits from the agent) — it does not mean the site itself is frozen. Manor R2 deepening propagated live within Netlify's normal cache window; 12h+ post-deploy sanity check showed clean propagation. Pattern locked in: weekend PM runs should always check `git log --oneline -5` against AM's last sha to detect intra-day commits before deciding the PM scope.
- **NotebookLM 29th consecutive dead run.** SKILL.md retirement diff still pending Adam apply. No new framing — just the increment.


---
## 2026-05-11 PM — Monday no-rotation drift sweep

**Canonical Netlify-fn grep token = `/.netlify/functions/lead-intake` (slash form).**
The hyphenated `netlify-lead-intake` label seen in older logs was a conceptual shorthand. The literal hyphenated string does NOT appear in served HTML. Going forward, conversion-tracking critical-token grep uses the slash form. Both labels prove the same endpoint is wired, but only one matches the body literally.

**Root-path SEO landing pages do NOT live under `/blog/`.**
`/how-to-buy-a-house-in-austin-tx` is a top-level page, not a blog post. When re-verifying carries on this slug (or similarly-named landing pages), use the root-path URL. Reconstructing from `/blog/<slug>` produces a 404 false-regression. Defensive pattern: store the exact canonical URL (with leading-slash prefix) in carry text — don't infer from slug.

**Same-day PM fire = no-rotation drift sweep, not next-day rotation.**
TOMORROW_PRIORITY in latest.md is for the next CALENDAR DAY's AM fire. If two fires happen on the same calendar date (AM full + PM same-day), the PM run follows the Sunday PM precedent: re-verify carries, retry PSI, sweep backlog, no new rotation work. Detection: `[ "$(date +%Y-%m-%d)" = "<latest.md filename date>" ]`. Skip Step 4 rotation if true.

**PSI daily-drain pattern is now established.**
Two consecutive Mondays (2026-05-04 and 2026-05-11) AND today's PM retry: all 429. The SKILL.md "quota refreshes Mondays" guidance is no longer accurate. Some other consumer of project 583797351490 drains the daily quota before this task's first fire. Mitigation requires either (a) provisioning a dedicated PSI API key for this task, or (b) accepting UNVERIFIED on the PSI metric Mondays. AM run already escalated to HIGH FLAG_FOR_ADAM; not re-pitching PM.

---

## 2026-05-12 (Tuesday — Title+Meta rotation)

**Loan-type page roster correction (NEW, important):** The SKILL.md Tuesday rotation lists `/conventional-loan-austin-tx`, `/fha-loan-austin-tx`, `/va-loan-austin-tx`, `/jumbo-loan-austin-tx` as audit targets. Those slugs return 404. The canonical pages are under `/loans/`: `/loans/conventional`, `/loans/fha`, `/loans/va`, `/loans/jumbo` (all linked from `index.html`). Other loan-type pages live at root (`/dscr-loan-austin-tx`, `/bank-statement-loans`, `/non-qm-loans`, `/investor-loans`, `/high-net-worth-mortgage`, `/self-employed-mortgage-austin`, `/austin-down-payment-assistance`, `/first-time-home-buyer`). Mixed root/`/loans/` pattern across the site. Future Title+Meta audits: use the actual roster, not the SKILL.md example list.

**Polling a Netlify deploy via `until grep`: marker must be UNIQUE to NEW content.** Today's first verify attempt polled `until grep -q "24-hour pre-approval"` to confirm the first-time-home-buyer meta edit went live. The until-loop exited immediately — but the live page still served the OLD meta. Root cause: the page's `og:description` already contained "24-hour pre-approval" verbatim before my edit. The grep matched the OG, not my new meta. Re-polled with `"Williamson Counties"` (a phrase I'd introduced in the *other* file edit) and got an accurate deploy signal. **Defensive pattern going forward:** when verifying a meta-description edit live via grep poll, choose a phrase that exists ONLY in the new version of the file — check og:, twitter:, JSON-LD, body copy, and headers first. Or use a phrase from a different edit that landed in the same commit.

**OG ↔ meta intra-page consistency is a free win.** First-time-home-buyer.html had `og:description` = "...24-hour pre-approval. NMLS #513013." and `meta name="description"` = "...Pre-approved in 24 hours. Adam Styer, NMLS #513013." Trimming the meta to match the OG copy (a) brought it into the 150-160 char window AND (b) eliminated an intra-page consistency mismatch. One edit, two wins. Pattern: when rewriting a meta description for length, check the page's own og:description first — matching it is often the right answer.

**PSI multi-day drain pattern is now confirmed.** Mon AM + Mon PM + Tue AM all 429 on Google Cloud project 583797351490. The "quota refreshes Mondays" assumption AND the "quota refreshes daily" assumption are both wrong — quota is drained by another consumer of project 583797351490 every calendar day before this task fires. Escalation: this is no longer a "Monday quota issue", it's a "permanent UNVERIFIED unless Adam provisions a dedicated PSI API key for this task" issue.

---

## 2026-05-12 PM — daily-opt Tuesday PM: Substring-Overlap Double-Count + 4-of-4 PSI Drain Lock-In + 33rd Dead NotebookLM Run

### Patterns
- **Substring-overlap double-counting in CTA grep audits.** AM's "2 `/get-preapproved` + 2 `../get-preapproved`" on the why-home-prices-arent-crashing blog post was a methodology artifact, not an actual 4-CTA count. The relative pattern `../get-preapproved` literally contains the substring `/get-preapproved`, so a bare `grep -o '/get-preapproved'` counted each relative href twice (once for itself, once for the substring match). The live file actually has 2 total CTAs (both relative). Defensive pattern: when counting href types separately, anchor the regex on `href="` to make patterns mutually exclusive (`grep -oE 'href="/get-preapproved'` vs. `grep -oE 'href="\.\./get-preapproved'`). Sum is then the true total. This explains why the carry has been a stable "2+2" for 14+ runs — it's a counting artifact that never changes because the underlying 2 CTAs never changed.
- **Sitemap lastmod regenerates within hours of a Netlify push.** Today's AM Tuesday Title+Meta commits at 07:15 + suburb-editor Lakeway R2 at 09:17 — both showing as `2026-05-12` in the live sitemap by 23:09 (~16h later). Netlify's sitemap is regenerated on deploy and reflects the latest commit timestamps. Useful for sitemap-freshness verification: a freshly committed page should appear with today's lastmod within ~1 deploy cycle, not "a few days later" as I'd been assuming.
- **PSI 4-of-4 consecutive drain confirms the failure mode.** Mon AM + Mon PM + Tue AM + Tue PM all 429 on project 583797351490. Crosses the threshold from "intermittent issue" to "this is the steady state." The SKILL.md "quota refreshes Mondays" assumption AND the implicit "quota refreshes daily" assumption are both false. Going forward, treat PSI as permanently UNVERIFIED until a dedicated key is provisioned; stop bumping the FLAG_FOR_ADAM count on each new 429 — escalation is at its ceiling.
- **PM no-rotation drift sweep is structurally sound.** 3rd consecutive PM re-fire (Sunday PM 05-10, Monday PM 05-11, Tuesday PM 05-12) following the same template: sitemap + conversion tracking + PSI retry + re-verify gate + backlog sweep, no site edits. Pattern is now durable. The methodology corrections (CTA double-count today, lead-intake hyphen/slash form Mon PM, marker-uniqueness Tue AM) each get captured in NOTEBOOK_INSIGHTS in the same run they're discovered, so future runs benefit immediately.
- **NotebookLM 33rd consecutive dead run.** Same pattern. SKILL.md retirement diff still pending Adam apply queue. Bumping not adding signal at this point.


---

## 2026-05-13 — daily-opt Wednesday: Suburb body-intro audit boundary + voice-guide vs. site-pattern divergence + 5-of-5 PSI drain + 34th NotebookLM dead run

### Patterns
- **Audit suburb body intro at the first `<section class="section">` after the hero, NOT the first `<p>` after `<h1>`.** Today's initial Pflugerville AEO audit flagged "first paragraph not answer-first" by pulling P1/P2/P3 directly under `<h1>` — which on suburb pages captures the hero-subtitle (`<p class="hero-subtitle">`), the Call (512) 956-6010 CTA, and the form's "Quick Quote" label. Those aren't the body intro. The actual answer-first paragraph for AEO purposes lives ~80 lines down, after the `<!-- Intro Content -->` marker, inside the first `<section class="section"><div class="container content-narrow">`. Pflugerville already has a textbook answer-first opener there: "To get a mortgage in Pflugerville TX, work with an independent broker like Adam Styer (NMLS #513013) who shops 40+ wholesale lenders...". **Defensive grep pattern:** `python3 -c "import re; html=open(...).read(); m=re.search(r'<!-- Intro Content -->[\s\S]*?<p[^>]*>([\s\S]*?)</p>', html); print(re.sub(r'<[^>]+>', '', m.group(1)))"`. Or grep for the first `content-narrow` block. Prevents false AEO flags on suburb pages.
- **Voice-guide vs. site-pattern conflict on "broker" vs. "correspondent lender".** Adam's `adam-voice-and-workflow.md` (2026-03-29 update) states he's a **correspondent lender** — "not a broker, not a bank". But the entire site — homepage, suburb pages (24+), loan-type pages, FAQ schema bodies — uses "independent broker" / "mortgage broker" extensively. This is a load-bearing editorial decision Adam has made; do NOT introduce "correspondent lender" wording in one-off edits — preserve site-wide consistency. If Adam wants to refresh site language to match the voice guide, that's a coordinated batch change, not a per-page rewrite. Today's Pflugerville meta edit kept "broker" to match the rest of the page (and site).
- **PSI 5-of-5 consecutive drain — locked.** Mon AM + Mon PM + Tue AM + Tue PM + Wed AM all 429 on project 583797351490. Yesterday's prediction confirmed: PSI metric is now permanently UNVERIFIED. Going forward: do not bump FLAG_FOR_ADAM count on subsequent 429s — escalation is at action-required ceiling.
- **Roster-wide gap ≠ per-page bug.** Wednesday rotation calls for `/calculators` internal links on suburb pages. Pflugerville has 0. But 4 of 5 sampled suburb pages also have 0 (only cedar-park has 1). This is a roster-wide pattern, not a Pflugerville-specific deficiency. Resist the urge to fix the audited-page-of-the-day in isolation when the pattern is roster-wide — that creates 24 different solutions over 9 weeks of rotation. Flag once for Adam decision, then implement (or not) as a batch. Same principle that prevented per-suburb meta-fix drift.
- **NotebookLM 34th consecutive dead run.** Same pattern. Retirement diff still pending Adam apply queue. Bumping not adding signal.

### Methodology improvements over time
- Tue AM + PM + Wed AM all caught and captured fresh methodology corrections — substring double-count (Tue PM), marker uniqueness on Netlify deploy polls (Tue AM), and now suburb body-intro audit boundary (Wed AM). The pattern of capturing one methodology correction per rotation week is durable. Each correction permanently reduces false positives in subsequent rotations.

---

## 2026-05-13 PM — Bonus PM run patterns

- **PM bonus runs should be no-rotation drift sweeps.** Sun PM + Mon PM + Tue PM + Wed PM all chose this pattern when SKILL.md fired a second same-day run. Scope: mandatory health checks (sitemap, conversion tracking) + Re-Verify Gate sweep + Step 4B backlog scan + PSI retry. Do NOT duplicate the same-day rotation.
- **Sibling-agent commits between AM/PM can shift `dataLayer` counts** (today: AM 5/7/7/8 → PM 7/10/10/14 from `30fe26a` Bee Cave R2 + `90bf131` session-end docs). Don't flag this as a regression. Critical tokens (`generate_lead`, `lead_type`, `lead-intake`, `form-name`, `thank_you_page_view`) are the load-bearing checks; raw `dataLayer` count is noise-prone.

---

## 2026-05-15 PM — Same-day duplicate fire pattern + scheduler reliability degradation

### Patterns
- **Same-day duplicate fire 4 minutes after AM cleanup.** AM ran 09:49 (Friday rotation: Content Planning + AEO Review). PM fired 09:53 — same Friday, same date. This is a new operational failure mode: not a Tue PM / Wed PM / Sun PM "bonus PM" pattern (those came hours after AM, with sibling agents in between), but a back-to-back re-trigger with effectively no system-state change. Correct response: every-run health checks are idempotent and should still run; rotation work must NOT re-execute. Treat as a bonus drift sweep with no editorial scope.
- **PM `dataLayer` drift vs AM was minimal** today (TY 8→10, others identical). Confirms the AM theory that yesterday's larger swings (5/7/7/8 → 7/10/10/14 in Wed PM) were sibling-agent commits, not duplicate-fire side effects. When the gap is 4 minutes with zero commits between, dataLayer count is stable to ±2. Use that as a "no other agent ran" signature.
- **Scheduler reliability is now a 2-data-point regression.** Thursday 2026-05-14 missing-fire + Friday 2026-05-15 duplicate same-day fire. Both within the same week. Pattern to watch: if Monday 5/18 fires correctly, this is two unrelated incidents. If Monday fires at an unusual time or duplicates, scheduler infra is the root cause. Logged as operational MEDIUM for Adam.
- **PSI 8/8 drain — same project ID, same error verbatim.** Project 583797351490 daily-queries quota saturated. Confirm: only one PSI run per day (AM only). Skipping `/refinance-quote` 2nd call to preserve any remaining quota for legitimate ad-hoc checks.

### Methodology improvements over time
- **Establishing "AM call type vs PM call type" as a first-class run-mode distinction.** The previous learnings entry (2026-05-13 PM) established the no-rotation rule for late-day bonus runs. Today's entry refines it: even minutes-apart duplicate fires follow the same rule. The decision is based on whether the AM run wrote `run-logs/<date>.md` and ran Step 4 rotation, NOT on clock time. Single test: if today's date-stamped log exists with rotation work done, PM is a no-rotation drift sweep.
- **Path: read `run-logs/<today>.md` before deciding rotation scope.** Today this single read informed the entire run shape. Cheaper than re-running the rotation; safer than skipping every-run checks.

## 2026-05-16 — Saturday weekend fire (scheduler-anomaly pattern expands)

- Saturday styer-site-daily fired at 07:12 CDT on a day not in the Mon–Fri rotation playbook. Confirms Friday NIGHT's hypothesis that the scheduler is firing on no-fire days.
- 4 anomalies in 3 days, 3 distinct types: (1) Thu 5/14 no-fire, (2) Fri 5/15 triple-fire same day, (3) Sat 5/16 weekend fire. Pattern is structural, not flakiness.
- Operational discipline confirmed: the "abbreviated weekend playbook" (every-run + Re-Verify Gate, no rotation) is the right shape because Saturday/Sunday have no rotation work to legitimately re-execute. Following the rotation rotation on a weekend fire would either duplicate Friday's work or skip ahead to Monday's — both wrong.
- Re-Verify Gate continues to hold under repeated runs: 16 carries across 5 consecutive fires (AM/PM/NIGHT Fri + Sat) with no false regressions and no carry creep. Discipline is sustainable.

---

## 2026-05-16 NIGHT — Saturday Double-Fire + 6-Anomaly 3-Day Window + 40th Dead NotebookLM Run

### Patterns
- **Scheduler weekend-and-extra-fire pattern is now sustained, not transient.** Three days, three distinct anomaly types: Thu 5/14 zero fires, Fri 5/15 three fires same day (09:49/09:53/23:09), Sat 5/16 two fires same day (07:12/23:13). 6 anomalies in 3 days. Idempotent every-run checks held: sitemap 200, conversion 10/10, working-tree style.css untouched, zero false regressions across all 5 anomalous runs. This is the right "defensive design" outcome — when scheduler misbehavior is high, the only thing that protects against bad data is making each run independently safe. Generalizes: any scheduled task that may fire unexpectedly needs (a) idempotent every-run checks, (b) rotation gated explicitly by weekday, (c) carry-state stored on disk not in-memory.
- **TY dataLayer 8↔10 bounce is now a documented cache-regen artifact, not drift.** Four consecutive runs show: Fri NIGHT 8 → Sat AM 10 → Sat NIGHT 8. Page hasn't been edited. Each value is observed at the edge cache after Netlify regenerates that node's response. The "critical token" set (`ty=1`, `ni=1`, `gtm=1` of GTM-PQQ6PGLR) holds across all bounces; the dataLayer count is a noisy total that includes both static-init lines and conditional event lines. Don't write the dataLayer count as a regression signal; do write `ty` and `ni` as regression signals.
- **Saturday "second fire" naming convention: use `-night.md` (matches Friday's pattern).** Sat AM = `2026-05-16.md`, Sat NIGHT = `2026-05-16-night.md`. Mirrors Fri 5/15 (which has `.md`, `-pm.md`, `-night.md`). The `-pm.md` slot was unused for Sat AM-fire because the AM fire already covered the morning slot. Slot picker: AM=`.md`, midday=`-pm.md`, evening=`-night.md`. Don't invent new suffixes when the existing ones map cleanly.


## 2026-05-17 — Sunday weekend fire — scheduler-anomaly streak hits 7

Yesterday's TOMORROW_PRIORITY exact prediction held: "Sunday 2026-05-17 is also a no-fire day per playbook. If Sunday fires: ... Tally would advance to 7+ anomalies in 4 days." Sunday fired at 07:08 CDT. Pattern is now: Thu 5/14 no-fire, Fri 5/15 triple-fire, Sat 5/16 double-fire, Sun 5/17 single fire = 7 anomalies / 4 distinct anomaly types / 4 consecutive days.

Key learning: the abbreviated weekend playbook (every-run non-negotiables + Re-Verify Gate, no rotation) is the right shape for unexpected weekend fires. It costs minimal quota (no PSI, no NotebookLM), surfaces no false regressions, and produces durable scheduler-anomaly evidence for Adam. Rotation MUST remain skipped on weekends regardless of fire count — the playbook is the source of truth, not the scheduler's behavior.

Edge cache TY page dataLayer continues to bounce 8↔10 between fires. Critical-token signal (ty=1, ni=1) holds across the oscillation. The bounce is cosmetic, not a regression.

## 2026-05-18 — Week 11 competitive intel: positioning shift reframe + complicated-income SERP gap

### Patterns
- **GOALS.md repositioning shifts what "competitive intel" means for the report.** Pre-2026-05-17, the report was suburb-focused (Hutto/Leander/Pflugerville/Kyle/Round Rock as the strategic SERPs). Post-2026-05-17, GOALS Phase A is "complicated income specialist." The report had to add an entire new tracking dimension (jumbo / asset depletion / self-employed / bank statement / non-QM / DSCR / 1099 SERPs) that wasn't in the SKILL.md keyword list. **Pattern:** when GOALS shifts strategy, the agent must reframe its tracking dimensions, not just its findings. The SKILL.md keyword list is a baseline, not a ceiling.
- **The "no new content" constraint changes the structure of TOMORROW_PRIORITY.** Last week's TOMORROW_PRIORITY was 8 items of which 4-5 were "audit page for neighborhoods/schools/depth-additions." This week's GOALS forbids new content. So most carry-forward neighborhood-depth recommendations had to be re-classified as "defer to Phase B" while structural recommendations (schema, GSC URL Inspection, meta edits) stayed in scope. **Pattern:** filter every recommendation through GOALS's "do/don't" lists before surfacing it — a recommendation that was right last week may be off-strategy this week.
- **The structural-strength vs ranking-strength gap is real and audit-able via head-to-head comparison.** Adam's `/non-qm-loans.html` is structurally stronger than LendFriend's #5 page on 4 of 8 audited dimensions (word count, loan-type list, suburbs in body, 2026 freshness, internal cluster) yet doesn't rank. LendFriend wins on schema (FAQPage) + topic-cluster authority. **Pattern:** when a page exists but doesn't rank, do a head-to-head competitor audit on schema + topic-cluster signals BEFORE assuming the gap is content-depth. This week's audit format (8-dimension comparison table) was a good first surface — keep it.
- **Suburb rotation continues to under-measure.** Round Rock and San Marcos both ranked when first measured this week (Round Rock with no rotation entry; San Marcos as new rotation). Same pattern as Week 10's Pflugerville+Kyle first-measurement-wins. Now confirmed across 4 suburbs (Pflugerville #4, Kyle #8, Round Rock #9, San Marcos #9) — the rotation under-measures by a structural amount. **Pattern:** at this point bulk audit is overdue; the rotation should be retired. Flagged for Adam 2nd consecutive week.
- **Directory aggregator (Zillow) is the new SERP threat shape on suburbs.** Now #1 or #2 on 4 of 6 tracked suburbs (Pflugerville, Kyle, Round Rock, San Marcos). Caused Hutto demotion (#2 → #3). **Pattern:** broker competitors are no longer the primary SERP threat; directory aggregators are. Content-vs-content competitive analysis is necessary but not sufficient — directory-slot inserts are now a regular cause of rank loss.
- **NotebookLM CLI auth expiration is a Step 6 blocker, not a Step 0/4 blocker.** Step 0/4 dependency (`notebook_advisor.py`) has been dead for 43 weeks (script absent). Step 6 dependency (`notebooklm` CLI source upload) was working until 2026-05-11 when auth expired. Distinct failure modes; both flagged. **Pattern:** when documenting NotebookLM blockers, name which step it blocks — they're not interchangeable.

### Methodology improvements
- New 8-dimension head-to-head comparison table format (Adam page vs top-ranking competitor) for any "exists but doesn't rank" page. Worked well for `/non-qm-loans.html` vs LendFriend. Apply to bank-statement / self-employed / DSCR pages in Week 12.
- Added new tracking metric: "complicated-income pages on disk vs ranking" (7 on disk, 1.5 ranking). Sits alongside suburb tracking. Should persist as a permanent dimension in this report.
- Added new "GOALS-filter" pass over OPPORTUNITIES — explicitly tagged which recommendations fit Phase A scope and which were deferred to Phase B per GOALS rules. This should become a standard report section header.

---

## 2026-05-18 (Monday — second pass, scheduled re-fire)

### Insights worth keeping

- **An "AUTO-RESOLVED" flag is only as good as its grep scope.** First pass cleared audit finding #4 (AggregateRating fabricated/exposed) on the homepage and marked the parent audit item resolved. Second pass found the same `reviewCount: "136"` block still live on 6 loan-type pages + Dripping Springs. The cleanup pattern Adam used on homepage had not been carried across the rest of the site. **Pattern:** when auto-resolving an audit finding scoped to one URL, do not mark the parent finding resolved without grepping the same schema/copy fragment across the whole repo (`grep -rnE "AggregateRating" --include="*.html" .`). The schema fragment is the unit, not the page URL.
- **JSON-LD post-edit safety = Python `json.loads` on every `<script type="application/ld+json">` block.** Pulled into the toolkit. Caught zero issues today (all 7 cleanups parsed clean) but is the right cheap gate for any future schema mutation. Use it before commit, not after deploy.
- **GOALS.md Phase B imminence overrides scheduled rotation work.** Previous pass approved a Tuesday self-execute on `/investor-loans` and `/high-net-worth-mortgage` title rewrites (add "Adam Styer" before NMLS). This pass declined to act early because GOALS.md flags Phase B (company name swap) as imminent — modifying titles now risks rework when the name swap lands. **Pattern:** when GOALS.md has a "Phase B is coming" flag, defer brand-coupled edits even if Decision Test technically passes. Phase B impendingness is itself a Phase-B-coupling judgment call only Adam can make.
- **Same-calendar-day re-fires need a second-pass log convention.** Today's scheduler fired the daily task twice on the same calendar date (Sun 23:16 CDT / Mon UTC, then Mon 07:09 CDT). The first pass already wrote `2026-05-18.md`. Naming today's run `2026-05-18-pass2.md` keeps history clean. Copy to `latest.md` so the next-day fire picks up the most recent state. **Pattern:** if `run-logs/YYYY-MM-DD.md` already exists, write `YYYY-MM-DD-passN.md`, never overwrite.

## 2026-05-18 (pass-3) — Sister-task escalation pattern + Adam-commit verification
- **Sister-task escalation:** when another scheduled task (e.g. styer-calculator-audit-weekly) flags a finding and defers to Adam, this task should re-flag from its own FLAG_FOR_ADAM if Adam-action is required. Single-source-of-truth deferral risks "between-the-cracks" follow-up failures. Today: calculator P0-A (12× inflated PITI) escalated from this surface even though sister owns the surface, because the patch has been deferred 4 weeks running and is now silently shipping wrong numbers to users.
- **Verify Adam's same-day commits:** when Adam pushes commits between agent runs (today: nav consolidation 82 files + scenario page + sitemap bulk-bump), always spot-check (a) conversion tracking on any rewritten page, (b) schema blocks on any new structured data, (c) sitemap deltas. Both Adam commits today passed verification cleanly.
- **`name="robots"` is not the same as noindex:** previous passes had a misleading column counting `name="robots"` token presence. The actual content can be `index, follow` or `noindex, nofollow` — must inspect the `content="…"` attribute. Today's pass-3 log specifies actual content.
- **Multi-pass same-day file naming:** use `2026-MM-DD-passN.md` suffix to preserve prior pass logs (which have substantive work records) rather than overwriting `2026-MM-DD.md`. Always copy newest to `latest.md`.

## 2026-05-21
- **Entity-swap gotcha:** image `alt` attributes survive name-change sweeps that only grep the visible copy. When doing an entity scrub, ALWAYS grep against the legacy phrase across ALL of HTML (including alt, title, aria-label, meta tags, structured data) — not just the visible page body. Adam's three earlier name-swap commits (2a0c1e8, c427b15, efd26fe) all missed the realtors.html alt. Today's run caught it by greping "Styer Team" across `*.html` instead of trusting prior runs.
- **Funnel-direction signal:** a single hero-cleanup commit (`71b8590`) can silently rewire the conversion funnel. After significant homepage refactors, do an explicit anchor-inventory check for both `/get-preapproved` and `/refinance-quote` to verify the conversion paths the daily task is tracking still exist. If 0 links, that's a funnel-direction question, not a tracking bug.
- **Decision Test in action:** banned-phrase scrub = ZERO judgment = fix inline; funnel-direction change with multiple valid paths = judgment call only Adam can make = flag, don't auto-fix.

## 2026-05-31 — daily-opt Sunday Gap-Day Recovery: PM-Finding Auto-Resolution via JS-Layer Bridge + Live↔Local Sitemap Divergence as a New Diagnostic Class (59th Dead NotebookLM Run)

### Patterns

- **Inline-HTML grep is the WRONG instrument for verifying dataLayer event coverage on a JS-driven form binding.** Today's auto-resolution: PM 2026-05-28 NEW finding "Homepage `#contact-form` lacks `generate_lead` dataLayer push" was based on `curl -L | grep -c 'generate_lead'` against rendered HTML — which returns 0 even when the JS layer pushes `generate_lead` to dataLayer at submit time. The right verification is two-step: (a) inspect the form's class/id list for known binder selectors (`.js-quick-contact`, `#hero-lead-form`, `#contact-form`, `#prequal-form`), (b) grep `script.js` for `dispatchLeadSubmitted()` (or whatever push helper) and confirm that binder is invoked for that selector. Single-step HTML grep is reliable ONLY for inline dataLayer pushes (e.g., `onsubmit="dataLayer.push(...)"`) which is not Adam's pattern. Generalized rule for any future "form X doesn't fire conversion event" finding: do the two-step check before flagging. Costs 1 extra grep, saves a false positive. Added to canonical conversion-tracking-gate toolkit.

- **Sunday gap-day recovery fire = re-verify gate + auto-resolution recording, NOT new-rotation work.** Today: Wed 5-27 + Fri 5-29 + Sat 5-30 full-rotation fires all missed, but Adam's high-velocity Saturday + Sunday work + blog-writer task launch auto-progressed multiple findings. The right Sunday run shape is: confirm scheduler-missed days haven't created regressions, record auto-resolutions from inter-fire Adam work, surface any new findings. Generic Sunday rotation work (Schema audit / Title sweep) should default to next Monday's scheduled fire rather than piling onto Sunday. This is consistent with the existing "Sunday unscheduled fires are now the cheapest possible run" cached learning, generalized to gap-day recovery fires. **Refined rule:** the 4th gap-day recovery in this task's history confirms a stable pattern — when N+1 scheduled fires are missed and the next fire lands on the weekend, treat it as recovery (not rotation execution).

- **Live↔local sitemap.xml divergence after a `_redirects` force-flag change is its own diagnostic class.** Today's 11-URL drift maps to: (a) 5 `.html` vs extensionless URL form pairs (consistent with `e24b973` force-flag change pattern; local sitemap modernized but live still has both forms), (b) 6 real content URLs missing live (bank-statement-loans-austin-tx, first-time-buyer-guide, non-qm-loans-self-employed-austin, refinance-calculator, 2 updates posts). Reusable rule: when sitemap drift includes both extensionless duplicates AND real content gaps, the cause is likely a combination of `_redirects` modernization + missed earlier sitemap.xml commits. Don't auto-fix; surface for Adam to triage. Diagnostic: `git log -p sitemap.xml` walk shows which commits added/removed URLs and which forms. Added to canonical sitemap-gate toolkit.

- **Sitemap count is a high-leverage gate signal (carried from 2026-05-28, reconfirmed today).** Today: PM 5-28 was 136, today's live is 131 = –5 net live. But local is 137 = +1 vs PM 5-28 (physician post adds, scenarios already in). The split-signal (live vs local) is itself information — caught the divergence in 1 grep. Generic upgrade to the gate: run BOTH live and local sitemap counts every fire; a divergence between them is a NEW-finding trigger.

- **High-velocity day classes (10+ Adam commits between AM/PM, OR 8+ across multi-day gap) need bespoke sitemap-lastmod sweep with risk-tier framing per file class.** Refined from PM 5-28's 147-file sweep insight: cache-buster bumps on 102 HTML files (today's `?v=20260530b` change) are mechanical/non-content and do NOT warrant sitemap lastmod bumps; content changes (e.g., scenarios pages, blog posts) DO warrant bumps. Today: 0 lastmod bumps owed by this task because (a) the physician post already shipped with correct 2026-05-30 lastmod via Adam's commit, (b) the homepage two-column restructure is HIGH_RISK file deferred per learning, (c) other Adam Saturday touches were cache-buster bumps / docs / scripts. Generalized rule: the "owed sitemap lastmod bumps after N Adam commits" count is usually much smaller than the raw "files touched" count. Triage by file class first, then by content-vs-mechanical edit type.

- **JS-layer cache-buster bumps need a separate verification gate from sitemap lastmod.** Today Adam bumped `script.js?v=20260530b` sitewide on 102 HTML files to force returning visitors to refetch the new JS (essential for the GA4 conversion event landing on Google Ads conversion tags). Conversion-tracking grep validates that the script.js endpoint serves the new code (✅ verified). Sitemap lastmod doesn't need bumps for cache-buster-only edits. Two separate concerns. Add to canonical post-Adam-JS-change check.

- **Audit Issue #5 line-number drift after a HIGH_RISK file restructure is precision-test material.** Today: PM 5-28 said homepage H1 was at line 362; today's grep finds it at line 363 (+1). Cause: Adam's `40028bb` two-column hero restructure shifted markup. The `grep -n` returns the exact current line; PM-5-28's stored "362" is now stale. Reusable rule: when re-verifying a line-number-anchored carry on a HIGH_RISK file that has been touched between fires, ALWAYS `grep -n` for the actual current line and update the carry text — don't carry the stale line number forward. PM 5-26's "PM gate's job is precision" rule applies here even for non-PM fires when the inter-fire window touched the file.

- **NotebookLM 59th consecutive dead run.** Pattern unchanged. SKILL.md retirement diff still pending Adam apply queue.

---

## 2026-06-02 — daily-opt Tuesday Title/Meta Rotation: 4 Reusable Patterns + 1 SKILL.md Drift Carry (61st Dead NotebookLM Run)

### Patterns

- **Adam-touched-yesterday is a strong skip signal.** scenario.html title looked like a clear "Adam Styer Mortgage" non-compliance during today's brand-display audit. But `git log --oneline -1 -- scenario.html` showed Adam's commit `0e54435 Use portal language and short scenario forms` landed 21 hours ago. Editing a file Adam shipped overnight risks reverting deliberate intent. Combined with second-line evidence (llms.txt:1 + tests/aeo-structure-regression.test.js:38 asserting the phrase), the right move was skip-and-carry rather than auto-edit. **Reusable rule:** before any title/meta edit, run `git log --oneline -1 -- <file>`. If the most recent commit is < 24h old and authored by Adam, the file is Adam-pivot territory unless the planned edit is exactly inside the commit's stated scope. Adds 1 cheap git call to the pre-edit gate.

- **`llms.txt` + regression tests are authoritative brand-string sources.** Today: a legacy-looking `Adam Styer Mortgage` string is NOT always non-compliance — `llms.txt:1` uses it as the AEO heading and `tests/aeo-structure-regression.test.js:38` asserts it via regex. The phrase is intentional dual-use brand vocabulary, not drift. **Reusable rule:** before flagging a "legacy" brand pattern as non-compliance, grep `llms.txt` + `tests/` for the same string. If asserted in tests or used as a heading in llms.txt, it's intentional — demote to brand-style choice. Saves false flags on intentional dual-use vocabulary.

- **Duplicate title/meta dedup via Python set is cheap and authoritative.** Today's Tuesday rotation: pipe `printf "%s\t%s\n" $title $file` for every root .html into a Python defaultdict — single-pass O(n), catches duplicates that bash sort+uniq misses when descriptions contain tabs/quotes. 101 files scanned, 0 duplicate titles + 0 duplicate descriptions in ~1 second. Added to Tuesday rotation canonical alongside the Monday JSON-LD schema-parse pattern. Two-pass-by-instrument: Python for dedup (set/dict), Python for schema parse (regex+json.loads), bash for length distribution + cross-file presence.

- **Title-length and meta-length distributions are the right Tuesday-canonical artifact, not per-file lists.** Reporting "67 ideal / 32 over-65 / 4 admin / 1 verification" and "77 ideal / 14 short / 0 long / 10 missing" gives Adam a single-glance summary of where SEO truncation risk and meta-card-fill opportunity sit sitewide. The 32 over-65 titles cluster heavily in the complicated-income Adam-pivot zone — distribution is the right surface, file-by-file list is noise. Same for the 10 missing-meta pages: 9 are correctly noindex'd (the pattern is the signal), 1 is LoanOS-paused. Generalized rule: for distribution-class findings on Tuesday, surface the bucket counts + cluster characterization, not the file list.

- **Style.css drift is highest-leverage SKILL.md update signal.** Today: gold hex `#C9A84C` (SKILL.md design audit spec) is no longer in style.css — Adam shipped `#8B6E24` (deeper antique-gold). SKILL.md design-audit line is now subtly wrong. **Reusable rule:** when running design audits, treat style.css `:root` `--color-*` variables as authoritative; SKILL.md hex references are documentation that drifts. Flag SKILL.md updates back to Adam as compliance-doc maintenance, not site-edit candidates. Adds a new carry class: "doc-drift vs site-drift" — same as we already separate sister-task vs this-task drift.

- **Within-page meta consistency (title↔og:title↔twitter:title) is reliable LOW_RISK auto-edit territory.** Today's 2 shipped fixes: texas-complaint-notice.html title brought in line with the page's existing og:title body (`HyperSmart Home Loans`); rate-buydown-calculator.html twitter:title brought in line with same-page og:title. Both were 1-line changes that reduced internal inconsistency without changing the page's brand intent. **Reusable rule:** for any "legacy brand string in meta" finding, check whether the SAME page already has the corrected string in a sibling meta field — if yes, the fix is a within-page-consistency edit (LOW_RISK auto-edit OK); if no, it's a site-wide brand decision (carry for Adam). The two within-page consistency fixes were strictly inside GOALS.md "compliance cleanup" scope.

- **GOALS.md scope check overrides Decision Test (carried from Monday, applied again today).** Today's 2 title fixes (compliance + within-page consistency) are inside GOALS.md "compliance cleanup" scope. The 14 short meta descriptions are inside GOALS.md "no new content beyond repositioning + compliance fixes" caution — short descriptions are SEO upgrade opportunities, not compliance fixes, so they correctly stayed unedited. The Decision Test's "try first" guidance is correctly bounded by GOALS.md project scope.

- **NotebookLM 61st consecutive dead run.** Pattern unchanged. SKILL.md retirement diff still pending Adam apply queue.

---

## 2026-06-04 — Thursday rotation (Internal Linking + Funnel Flow)

- **Cluster-wide grep is the highest-leverage Thursday-rotation byproduct on Adam-pivot territory (NEW today).** Today's mechanical Thursday work (3-page link audit + funnel trace) found zero blockers — internal linking density healthy, funnel instrumented. Real value: running cluster-wide grep to convert hand-wavy carry framings ("a few pages with this issue") into exact counts (25/25 suburbs / 89 surfaces / 143 mentions / 34 surfaces). 5× scope refinements upgrade Adam's decision class — what was "MEDIUM cleanup of 5 pages" becomes "MEDIUM batch of 143 surfaces across 12 suburbs," which materially affects whether and when Adam batches the work. Reusable rule: when a mechanical rotation finds nothing to fix, spend the time converting existing carry framings to hard cluster counts.

- **Homepage is a funnel surface — Adam-pivot funnel-architecture carries should explicitly include it (NEW today).** Yesterday's Kyle audit framed the funnel-architecture decision as "24 suburb pages route through /scenarios.html, not tracked LPs." Today's Thursday funnel trace shows the homepage itself has 0 tracked-LP links. So organic-traffic top-of-funnel has zero Google-Ads-attribution path. Decision frame upgraded: not "should suburbs have tracked LPs" — "should the entire organic funnel route through tracked LPs, or is /scenarios.html the canonical organic LP?" Both valid; Adam needs the bigger framing. Reusable rule: when a carry references "the X cluster," check whether the canonical hub (homepage, /products) ALSO carries the same pattern — usually does.

- **Round Rock 2026-04-29 USDA cleanup was a partial pass (NEW today).** The carry has been "Round Rock cleared 3 surfaces" — implying it was a complete pass that cleared the page. Today's grep shows 9 USDA mentions remain on Round Rock. So either (a) the 9 are legitimate context (e.g., "USDA loans for rural areas — not our focus") and the carry framing was correct, or (b) the cleanup missed surfaces. Friday's priority: spot-read Round Rock's USDA content to determine which. Reusable rule: when a carry says "N surfaces cleared," verify N is the total count not a partial — partial passes leave invisible drift.

- **Counts drift as pages ship — verify with fresh ls/grep (NEW today).** CONTEXT.md, prior logs, and SKILL.md notes consistently reference "24 suburb pages." Actual = 25 (austin-area-mortgage-lender.html was missed). Likely added after prior counts were committed to convention. Reusable rule: whenever a daily log references "N pages in cluster X," double-check with `ls` or `grep -c` — the framing is cheap to correct but compounds errors when carried forward.

- **SEO/SEM backlog "0 eligible items" is structurally expected during Phase A (NEW today).** P0/P1/P2 complete; P3 remainder = content additions GOALS.md "no new content" explicitly paused. There's no in-scope work for the SEO/SEM agent right now. Reusable rule: log "0 eligible" honestly when the backlog has no in-scope items; don't invent low-value mechanical work to fill the slot. The right output on these days is the cluster-wide grep refinements above, not manufactured tasks.

- **Carry refinement compounds across consecutive runs (NEW today).** Wednesday refined two carries on Kyle alone (USDA 4→5; 0-CTAs 8→32). Thursday's cluster sweep took both further (USDA 5→12; 0-CTAs 32→34). Pattern: cluster-level refinement compounds because each daily rotation surfaces a different slice. Today's refinements ARE complete cluster sweeps — no more expansion expected from these dimensions. Reusable rule: mark whether a carry refinement is "complete cluster sweep done" or "next-day expansion expected." When complete, Adam has the final scope number; when expansion expected, the carry is still moving.

- **NotebookLM 63rd consecutive dead run.** Pattern unchanged. SKILL.md retirement diff still pending Adam apply queue.

---

## 2026-06-06 (Saturday weekend re-verify)
- **Audit new surfaces the moment they appear, not just on rotation day.** 2 DSCR posts shipped 2026-06-05 (off-cycle, manual writer run). Today is Saturday with no blog rotation, but new content = new compliance surface. A cheap CTA/FAQPage/NMLS/blog.html-wiring grep confirmed both clean — catching a missing schema or unwired blog.html entry the day after publish beats finding it weeks later. Rule: when git history shows content shipped since last audit, audit it this run regardless of the rotation calendar.
- **A queued "flag tomorrow" item is a re-verify candidate, not a guaranteed flag.** Yesterday queued "blog hits 7 days tomorrow → flag weekly content." Today it would've been a FALSE flag — 2 posts shipped overnight, latest now 1 day old. Always re-check the live condition before acting on a queued flag; the world moves between runs. This is the Re-Verify Gate applied to your own TOMORROW_PRIORITY, not just to external-system claims.
- **Sitemap count changes are signal, not noise.** Count jumped 131→133 — instead of just noting drift, traced it to commit 50d1919 (DSCR publish) and confirmed it's legitimate new content (md5 live=local still identical). A count change without a traced cause would be the thing to flag.

## 2026-06-07 (Sunday weekend re-verify)
- **A document-level decision (GOALS.md edit) silently resolves dependent TODO/NEEDS-ADAM carries — re-check the SOURCE doc, not the carry text.** The 4th-run "suburb-editor status NEEDS ADAM" carry was written under a content freeze with the task in neither Pause nor Keep-Running. Adam resolved it 2026-06-06 by editing GOALS.md (freeze line deleted, task added to Keep-Running). The carry text itself never changed, so on muscle memory it would have re-surfaced as a 5th carry. Rule: when a carry's resolution lives in an upstream policy doc (GOALS.md, memo), re-read that doc's CURRENT state before re-logging — the carry is downstream of a decision that may already be made. This is the TODO-side analog of the sitemap-count / blog-7-day live re-verify pattern.
- **Don't falsely increment a failure counter you didn't actually test.** PSI quota-drain carry held at 31/31 (not bumped to 32) because Sunday is not a PSI-rotation day and no live attempt was made — incrementing the consecutive-failure count without an actual failed call would be dishonest reporting. Honest counter discipline: a streak count only advances on a real attempt.

## 2026-06-08 (Monday — full rotation: Schema + Google Ads + AEO Entity Audit)
- **A SKILL.md audit item can go off-strategy after a GOALS.md pivot — re-frame it before treating a "miss" as a gap.** SKILL.md's Monday AEO check asks whether the homepage answers "Who is the best mortgage broker in Austin TX?" GOALS.md Phase A deliberately moved Adam OFF superlative ("best") onto specialist ("complicated income") positioning, and "superlatives" are a NAMED Phase A compliance-cleanup target (also a mortgage-advertising compliance risk). A literal read would flag a "missing best-broker answer" that is actually the correct strategic choice. Rule: when a SKILL.md audit references generic-superlative or performance-metric framing, filter through GOALS.md first — the check may be testing for something current strategy intentionally avoids. Same class as "filter every recommendation through GOALS's do/don't lists" but applied to the agent's OWN checklist, not just to findings.
- **"Schema present" ≠ "schema valid" — close the gap with `json.loads`, not grep.** The 2 DSCR posts were logged "schema present" 06-06 (grep `application/ld+json`). Today's per-block `json.loads` upgraded that to "schema valid" (Article+FAQPage+BreadcrumbList all parse). 17/17 blocks valid across home/about/round-rock + both DSCR posts. Cheap, authoritative close for any "confirm schema" carry. Present is a grep claim; valid is a parse claim — don't conflate them in a run log.
- **One shared per-day quota = one honest increment, not one-per-target.** PSI quota is a single API-project per-day limit. After the first `/get-preapproved` call proved it exhausted, re-calling for `/refinance-quote` would not be an independent attempt — same quota, same proven failure. Advanced the counter 31→32 once (a real Monday attempt was made), not 31→33. Honest-counter discipline includes not double-counting a single shared failure mode across multiple targets.
- **AEO entity-consistency is a 2-page schema-field diff, not a vibe check.** Extracted name/legalName/telephone/address/NMLS from every JSON-LD block on home+about via Python and set-compared. Confirmed identical entity (HyperSmart/Kyber, NMLS 513013+2653540, 9050 N Capital of TX Hwy) with only cosmetic phone-format variance (both E.164-consistent). The reusable artifact is the field-extract-and-set-compare, which surfaces real drift (mismatched NMLS, address typo) that a human read would skim past — and confirms the schema-type carry (home MortgageBroker vs about LocalBusiness) is the ONLY real inconsistency, and it's an intentional Adam-gated choice, not drift.
- **NotebookLM 67th consecutive dead run.** Script absent + CLI auth expired. Pattern unchanged; SKILL.md retirement diff still pending Adam apply queue.

## 2026-06-09 (Tue — Title Tags + Meta Descriptions)
- **A wide audit flag is not a wide defect — read the live tag before fixing.** Tuesday meta-description audit flagged ~10 pages "SHORT", all regex artifacts: `content="(.*?)"` truncated at apostrophes in contractions ("Austin's", "today's"). Real descriptions were all 140–160 chars. One cheap grep per suspicious hit prevented ~10 destructive "fixes." Apostrophe truncation makes strings look SHORTER, never LONGER — so the 2 "LONG" flags (217/203) were the only trustworthy ones and the only real defects.
- **og:title ≠ title is by design** on ~80 pages (punchier social variant). Do NOT mass-equalize — would strip deliberate social-share copy. Align only when the variant itself is defective.
- Fixed 2 real over-length descriptions: asset-depletion-calculator 217→152, loans/jumbo 203→160 (jumbo now matches its own og/twitter desc). Commit 930da40, both verified live 200.

---

## 2026-06-10 — Verification-day learnings (styer-site-daily Wednesday)

### A fresh automated run-log can be STALER than CONTEXT.md — reconcile both before carrying a flag
Tuesday 2026-06-09's automated run-log said the Adam batch-memo was "still unanswered" and the calculator P0-A was "52 runs open." Both were FALSE by Wednesday: Adam answered the memo (1=b, 2=a) and the P0-A fix shipped in a 2026-06-09 **evening interactive session** that ran AFTER the Tuesday automated job. Interactive sessions write to CONTEXT.md ("Last Worked On"), not to the automated run-log. So the run-log froze state at automated-run time while CONTEXT moved on. **Rule:** when a carry claims "Adam hasn't answered X" or "bug Y still open," check CONTEXT.md's freshest "Last Worked On" entry (and the relevant commit) BEFORE re-surfacing. This one reconciliation cleared 5 stale recurring carries in a single pass. The Re-Verify Gate's spirit ("don't trust prior runs") applies to your OWN prior run-logs, not just external systems.

### Netlify strips `data-netlify`/`netlify` from served HTML at build time
A live page showing NO `data-netlify` attribute on its `<form>` does NOT mean the form is undetected. Netlify consumes the attribute during the build, registers the form server-side, and serves HTML without it. The known-good `/get-preapproved` form shows exactly this stripped state live. **Verify form wiring against LOCAL source** (`data-netlify="true"` + matching `<input name="form-name" value="X">`), never the served HTML. A "form not wired for Netlify" alarm based on the served page is a false positive. `forms.html` in this repo is a single generic form, NOT a central detection registry — detection is per-page via the attribute.

### Extensionless URLs 301→.html — always follow redirects in conversion/token checks
`curl` (without `-L`) to `https://styermortgage.com/get-preapproved` returns a 37-byte "Redirecting to /get-preapproved.html" stub, so every dataLayer/GTM/form token grep returns 0 → a false "conversion tracking broken" 10/10→0/10 alarm. **Always `curl -L`** (follow redirects) or hit the `.html` path directly when running the conversion HTML-token matrix. This is distinct from the live-submit-pollutes-CRM rule: the method is right, but the URL must resolve to the real page first.

### Buydown P0-A root cause (for future calculator audits)
The 12× PITI bug was a value/comparison mismatch: radios emit `value="yr"`/`value="mo"` but the JS checked `taxFreq === 'annual'` (a string that never matched), so the annual→monthly `/12` divide never ran. Fix was `=== 'yr'`. **Lesson for calculator audits:** when a calculator "ignores" a toggle, diff the radio/select `value=` attributes against the exact strings the JS compares — silent string mismatches produce plausible-but-wrong math, not errors.

---
## 2026-06-11 (Thursday — Internal Linking + Funnel Flow)
- **Resolved-by-design beats re-surfacing: trace the live form `action` before carrying a "page X doesn't convert" flag.** prequal.html was carried as a MEDIUM conversion-parity gap. Its form already `action="/thank-you"`, so organic submissions already fire the Ads conversion (`thank_you_page_view`) — identical to the 7 inline quote forms. The missing `generate_lead`/`lead_type` push is analytics granularity, NOT a conversion-firing gap. Re-characterized MEDIUM→LOW. Rule: grep the live form `action` first; if it lands on /thank-you, the conversion fires.
- **Honeypot lives on the `<input>`, not the attribute, in served HTML.** Netlify strips `data-netlify` (and likely `netlify-honeypot`) from served pages at build, but a normal hidden `bot-field` `<input>` IS served — that's the reliable post-deploy verification signal. Placing the bot-field input INSIDE the form (rather than get-preapproved's just-outside placement) ensures it's submitted with the payload. Shipped to all 7 inline forms (kyle/cedar-park/dscr/bank-statement/self-employed/non-qm/hnw), verified 7/7 live.
- **Concurrent-writer discipline held again:** working tree had `M hutto-mortgage-lender.html` (suburb-editor task mid-edit) + untracked `gbp-posts/2026-06-07.md` (sister task). Staged ONLY my 7 form files by explicit path — never `-A`. Both foreign files left untouched.

## 2026-06-12 (styer-site-daily Friday — Content Planning + AEO)
- **A clean AEO audit is a valid, complete output — don't manufacture an edit to "show work."** Friday's two audited posts (DSCR cash-out BRRRR, self-employed) were already answer-first with natural-language question FAQs and extractable answer-first intros + FAQPage schema. The surgical-discipline move is to log "AEO-excellent, no defect" and move on, NOT invent a rewrite of strong copy. Zero site mutations is the correct outcome when everything verifies.
- **Blog freshness boundary math:** latest post 2026-06-05, run date 2026-06-12 = exactly 7 days. The ">7 days" flag rule means 7 is still within window (not past). Surface as a soft heads-up to styer-content-weekly, escalate to hard-due the day it tips to 8. Don't over-flag a boundary value.
- **Homepage `/get-preapproved` count of 0 is by-design, not a defect** — homepage hero CTAs deliberately route to `#contact-form` (Decision Q10), and that inline form submits to /thank-you where the Ads conversion fires. Before flagging a "missing tracked-LP link" on the homepage, confirm the inline-form-to-/thank-you path.

## 2026-06-13 (Saturday weekend recovery)
- **A speed-number token is not automatically a banned claim — grep its surrounding context before flagging.** GOALS.md bans the sitewide "21-Day Avg. Close" performance-metric trust claim. But a scenario/testimonial *outcome* ("Outcome: Closed in 21 days at a rate within 0.25% of conventional pricing") is a real closed-file story — exactly the "story-driven, closes what banks can't" content GOALS.md endorses. Same number, opposite compliance posture. Decision-2 already settled this class: real outcome facts in scenario/testimonial context are KEPT (can't honestly rewrite a true fact). Surface as LOW awareness; don't edit.
- **Bash-tool PATH quirk in this environment:** `cd <dir>` triggers a cwd reset that can drop PATH, AND command-substitution subshells (`$(... | grep ...)`) inside a `for` loop lost PATH even after `export PATH`. Standalone commands worked; nested substitutions failed with "command not found". Fix: use absolute tool paths (`/usr/bin/curl`, `/usr/bin/grep`, `/usr/bin/sort`, `/usr/bin/tr`, `/usr/bin/head`) or save pages to temp files first, then process in separate top-level commands. Avoid `cd` — pass absolute paths to `git -C`, `find`, etc.
- **Form `action` on the two LPs is single-quoted** — `action='/thank-you'`. A `grep 'action="..."'` (double-quote) returns empty and can read as a missing redirect. Use a quote-agnostic grep (`<form[^>]*>`) to confirm the /thank-you action.

## 2026-06-14 (styer-site-daily Sunday)
- **zsh glob-expansion bites grep patterns:** an unquoted regex with a bare `?` or `/` (e.g. `grep -oE action=.?/thank-you`) makes zsh try to glob-match it as a filename → `no matches found` and the command never runs. Always quote the pattern or use `grep -F`. Pairs with the existing absolute-tool-path PATH-quirk note.
- **Re-Verify Gate caught nothing to resolve today, but worked as intended:** the carried "21-day" homepage finding was re-checked live (line 493) → confirmed still the scenario-outcome card, not the banned trust claim → surfaced as awareness, not re-flagged as a violation. The gate's value is as much "confirm it's still benign" as "auto-resolve stale flags."
- **Blog freshness is a cadence signal, not a defect:** 2nd consecutive run past the 7-day window with content task not shipping since 2026-06-05. This belongs to `styer-content-weekly`, not this task — re-surface, never auto-author (GOALS.md positioning = complicated income).

## 2026-06-15 (Monday — styer-competitive-weekly, Wk 15)
- **A held top-10 COUNT can hide a reversed trend — read direction, not just the headline number.** Tracked complicated-income top-10 stayed "5 of 7" two weeks running, but Wk 14 had 2 of 5 *gaining* and Wk 15 had 4 of 5 *losing* position. Reporting "5 of 7 held" alone would have buried a cluster-wide pullback. Rule: when a count metric is flat, always compute the per-item position deltas underneath it — a stable aggregate over a churning set is itself the signal.
- **A large single-week SERP swing on a flagship term = flag-and-verify, NOT assert.** Asset depletion measured #1+#5 → single #6 (a 5-position swing on the site's strongest moat). WebSearch is a US-generic proxy, not Adam's localized/personalized SERP, and result *composition* shifts run-to-run. The honest move: clear the prior claim per the Re-Verify Gate (don't carry "#1+#5" forward), report the measured #6, AND tag it "verify manually" + route a VERIFY-FIRST TOMORROW_PRIORITY to the implementation task — rather than declaring the moat dead OR ignoring the swing. Magnitude warrants attention; method warrants caution.
- **A NEW #1 competitor isn't automatically a stronger page — fetch it before reacting.** Champions Mortgage debuted #1 self-employed, displacing Styer. Fetched it: ~1,300 words, NO FAQ section, NO FAQPage/AggregateRating schema, no suburb depth, generic CTA. It out-ranked Styer on off-page authority/freshness, NOT on-page structure. The wrong reaction is content panic (rewriting a stronger page); the right read is "same schema/authority lever as always." Always diff the new winner's actual page against ours before recommending a response.
- **Re-Verify Gate caught a wide re-rank week:** of 16 carried Wk-14 claims, 10 CHANGED (jumbo/self-employed/bank-statement/asset-depletion/1099 all moved; Highlander lost #1 broker, MortgageAustin lost #1 pre-approved, Barton Creek lost #1 lender) and 6 held. Pasting last week's table unchanged would have mis-stated more than half the board. The gate's value compounds in high-churn weeks.
- **NotebookLM now dead on BOTH legs:** advisor script absent (stale loanos repo) AND CLI auth expired (verified live: `notebooklm list` → "Authentication expired or invalid"). Even the Step-6 binary push (which is independent of the missing advisor script) can't run without `notebooklm login`. Local-file persistence (report + growth log) is the only working output path.

---

## 2026-06-16 — Tuesday title+meta rotation: two durable patterns

**1. zsh does NOT word-split unquoted `$VAR`.** A `for f in $PAGES` loop where `PAGES` is a space-separated string passes the *entire string* as one argument under zsh (this shell), unlike bash. Symptom: `grep: <the whole list>: No such file or directory`. Fix: `printf '%s\n' file1 file2 ... | while read -r f; do ...; done`. Adds to the existing PATH-drop + bare-`?`-glob zsh gotchas already cached. Use absolute tool paths (`/usr/bin/grep`, `/usr/bin/head`) inside loops too — `head` was the casualty in the first attempt.

**2. A superlative isn't automatically a banned claim — grep its live context first (superlative variant of the speed-token rule).** On loans/va.html: "guaranteed by the U.S. Department of Veterans Affairs" is *factually correct* (VA loans literally are VA-guaranteed) — benign. But "lowest rates available" / "deserves the best, and we deliver" is unsubstantiated puffery — cleaned per GOALS.md Phase A. When removing a meta-description superlative, also sweep the matching og:description + twitter:description + any body hero copy carrying the same phrase, so the social cards don't keep the banned claim live. Testimonial superlatives ("found us the best rate") are quoted speech — Adam-gated, flag don't edit.

**3. Meta-description rotation discipline:** fix only deficient metas (under 150, over 160, missing, duplicate, generic, or superlative); leave the 150–160 already-good ones untouched (surgical — no churn). Indexed-page *title* rewrites stay HIGH_RISK → flag positioning conflicts (e.g. fha.html "broker" vs correspondent-lender), never blanket-rewrite.

## 2026-06-18 (Thursday — Internal Linking + Funnel Flow)

- **Netlify HTML post-processing rewrites form attributes on deploy:** double→single quotes, alphabetical attribute reorder, and `data-netlify` is consumed server-side (absent from served HTML). A quote-sensitive verification grep (`action="/thank-you"`) gives a FALSE NEGATIVE against live HTML even when the deploy is correct. Burned ~4 poll attempts chasing a phantom "cache miss" before checking the deploy permalink + Netlify deploy API manifest (authoritative). Fix: make live greps quote-agnostic (`action=['\"]?/thank-you`) and trust the deploy API/permalink over the prod edge (which can serve a stale cached copy captured right at publish).
- **A Netlify form with `data-netlify` but no `action` silently breaks the conversion funnel:** it does a native POST to Netlify's default success page, bypassing `/thank-you`, so `generate_lead`/`thank_you_page_view` never fires AND the lead sees a bare Netlify page. Before declaring such a form broken, confirm there's no shared JS submit handler (grep `js/*.js` for submit/FormData/preventDefault). Fix = add `action="/thank-you"` to match the working pages. This is NOT the form-approval gate (no new form-name, no field/compliance change, n8n alerting unaffected) — distinct from ADDING a new inline form, which IS gated.
- **A static SKILL checklist item can go stale vs. a repositioned live site.** The 'homepage hero CTA → /get-preapproved' check is now wrong — the repositioned homepage uses an inline `#contact-form`/`#quick-scenario-form` anchor pattern. Verify live intent before 'fixing' to match an outdated checklist; don't manufacture a regression.
- **Netlify deploy API gives free Lighthouse mobile scores** (in `lighthouse_plugin_scores` / `lighthouse.averages`) — a data point that bypasses the drained PSI quota. Homepage this run: Perf 81 / A11y 100 / BP 100 / SEO 100 (mobile).

## 2026-06-22 (Monday — styer-competitive-weekly, Wk 16)
- **Single-week SERP swings are PROVISIONAL — Wk 16 proved it by reverting four large Wk 15 "losses" at once.** Wk 15 measured Highlander losing #1 broker, Barton Creek losing #1 lender, MortgageAustin losing #1 pre-approved, AND asset depletion collapsing #1→#6. ALL FOUR reverted to prior positions this week. This is hard evidence that WebSearch's US-generic result *composition* shifts week-to-week independent of true rank, especially on localized/head terms. Rule: never assert a ranking collapse (or a competitor's rise) from one run — flag it, re-verify next run, and only treat a move as real after a second confirming measurement. The Wk15 "flag-and-verify, don't assert" call on asset depletion was vindicated: had we declared the moat dead, we'd have been wrong.
- **WebFetch markdown conversion silently strips `<script type="application/ld+json">` → false "schema absent" readings. ALWAYS curl+grep for schema claims.** The long-carried weekly claim "LendFriend has NO FAQPage schema" was FALSE: direct `curl -sL ... | grep -c 'application/ld+json'` + `grep -c FAQPage` showed LendFriend's self-employed page DOES carry FAQPage JSON-LD (1 block). Prior runs almost certainly read it via WebFetch (which converts to markdown and drops the script tag) and recorded a false negative that downstream agents amplified for weeks. The Re-Verify Gate caught it only because this run used the authoritative path. Burn this in: schema presence/absence is a curl+grep question, never a WebFetch question.
- **Correcting a wedge changes the RECOMMENDATION, not just the fact.** The "LendFriend lacks FAQPage" error had built a "catch up on FAQ schema" narrative. Truth: both Styer and LendFriend have FAQPage; neither has AggregateRating. So the move is a *get-ahead* on AggregateRating (symmetric gap, first-mover wins rich-result eligibility), not a catch-up. When you correct a competitive fact, re-derive the action it implied — a stale fact quietly mis-aims the strategy.
- **A #1-ranking page with no on-page conversion form is leaking its best traffic.** Pflugerville ranks #1 (above directories) but is among the 20 suburb pages without an inline `/thank-you` form — its only conversion path is a click-through. Cross-referencing the SERP win against CONTEXT's "suburb inline-form 5/25" gap turned an abstract Adam-gated batch into a prioritized one (do Pflugerville #1 + Georgetown #7 first). Competitive intel is most actionable when joined to the site's own known gaps.
- **Suburb rotation cadence under-measures wins (recurring).** Pflugerville (#1, last measured #2 in Wk11) and Kyle (#3, last measured #6 in Wk11) both improved while un-measured for 5 weeks. The bi-weekly-per-suburb rotation means a suburb's true position is usually stale. Treat rotation positions as point-in-time confirmations, not continuous tracking.

---

## 2026-06-22 (Monday — Schema + AEO Entity Audit)

- **Post-entity-transition schema audit pattern:** when the business legal name/NMLS changes (here: Mortgage Solutions LP → HyperSmart Home Loans / Kyber Mortgage Corporation, company NMLS 2526130 → 2653540), the JSON-LD `name`/`legalName` and NMLS identifier fields are a distinct drift surface from displayed copy. Audit them separately: grep the legacy display names INSIDE ld+json blocks (should be 0) and confirm both NMLS numbers (personal 513013 + company 2653540) are present. Today: clean.
- **Don't manufacture an AEO superlative answer.** The Monday SKILL checklist asks whether the homepage answers "Who is the best mortgage broker in Austin TX?" in the first 150 words. The CURRENT positioning (GOALS.md) deliberately drops superlative framing for specialist/complicated-income framing. The extractable answer "The loans your bank said no to… complicated income, jumbo, DSCR — I close the files banks decline" IS the correct AEO answer. Adding a "best mortgage broker" claim would violate GOALS.md and the no-superlatives audit. SKILL checklist text can lag the live positioning — defer to GOALS.md.
- **Business-entity schema NMLS vs personal NMLS:** a `MortgageBroker` named after the individual originator ("Adam Styer | HyperSmart Home Loans") can carry either the originator's NMLS (513013) or the company's (2653540). Not clearly wrong either way → flag low-confidence for Adam, don't auto-change (legal/NMLS identifier = Adam-gate).
- **Clean Monday = 0 mutations is correct.** Schema valid, entity clean, AEO strong, landing/design unchanged, no non-gated/non-GSC-blocked backlog item. Manufacturing an edit to "show work" would violate the surgical-changes discipline.
- **Run crossed midnight (06-22 → 06-23).** Dated the run log to the rotation it served (Monday 06-22), not wall-clock, to stay consistent with TOMORROW_PRIORITY. Task-run emit uses wall-clock.

---

## 2026-06-23 (styer-site-daily — Tuesday: Title Tags + Meta Descriptions)

- **Fix real defects, not length-rounding churn.** A meta at 148–149 chars is within Google's display tolerance — padding it to hit a literal "150" is low-value churn. Spend the edit budget on a genuinely off-positioning or broken description. Today's real defect was the `mortgage-pre-approval-austin` "24–48 hours" turnaround claim (GOALS: no performance-metric marketing; Decision 2(a) swept "24-hour" variants, kept only "same-day").
- **Apostrophe-in-single-quote regex trap.** A meta `content='…in today's market.'` (apostrophe inside the value) makes a naive `content=['"](.*?)['"]` regex stop at the apostrophe → false "truncated/short" reading (showed D:112 when the real meta was ~152 and fine). ALWAYS read the raw tag before flagging a meta as truncated. Better: anchor the regex to `content="(.*?)"` (double-quote only) for source files, since this repo authors metas with double quotes.
- **zsh does not word-split unquoted `$VAR` in `for f in $PAGES`.** The whole string becomes one "filename." Use Python for multi-file passes, or `for f in ${=PAGES}` / an explicit array. (Bash-isms silently misfire in this zsh environment.)
- **Indexed titles >60 chars are usually intentional here** (keyword + " | Adam Styer | NMLS #513013" suffix). Shortening them = HIGH_RISK (title change on indexed page) and prior runs deliberately preserved them. Audit titles for drift/superlatives/factual errors only — don't "fix" length.
- **Concurrent-writer drift is normal in this tree.** Sister tasks (suburb-editor, blog-writer) commit the same day; prior runs sometimes leave CONTEXT/latest/learnings + run-logs uncommitted. Commit your own deploy-critical HTML first/atomically (file-specific `git add`), push, verify live, THEN handle docs. Never `-A`.

## 2026-06-29 (Monday — Schema + AEO Entity Audit)
- **The about page is the entity anchor — authoritative `sameAs` belongs there.** Found about.html Person schema missing the NMLS Consumer Access (INDIVIDUAL/513013) link + Yelp that the homepage Person carries. This is a genuine AEO/E-E-A-T disambiguation gap, not cosmetic — the NMLS authority link on the originator's own entity page is exactly the kind of signal answer engines use to verify identity. Fixed (commit 265cbb7), display-invisible, verified live. Pattern: when auditing entity consistency, diff `sameAs` SETS across homepage↔about, not just counts — a count match could still hide a missing authoritative profile.
- **aggregateRating homepage-only is correct, not a gap.** Don't "fix" the about page by adding aggregateRating — duplicate self-serving review schema yields no extra SERP value (per memory: self-serving AggregateRating gets no SERP stars at all). Single authoritative instance on the homepage MortgageBroker is the right pattern.
- **No "best mortgage broker" superlative on the homepage is BY DESIGN** (the May positioning shift dropped superlatives/performance claims). Do NOT flag its absence as an AEO miss — flagging it would push copy that conflicts with the active positioning + compliance guardrails.
- **A clean audit is valid, but a real defect is a real defect.** The balance: don't manufacture churn (length-rounding, cosmetic), but DO ship a genuine, reversible best-practice fix when found (missing authoritative entity sameAs qualifies). The DECISION TEST cleared it: no Adam judgment call (NMLS # is public + already displayed), fully git-reversible, and attempted-not-just-flagged.

## 2026-06-30 (Tuesday — Title Tags + Meta Descriptions)
- **A "superlative" hit in the body can be a factual hedged comparative — read the live line before sweeping.** `loans/va.html` line 261 `<strong>Lowest rates</strong> — VA rates are typically 0.25-0.50% lower than conventional rates` triggers a `\blowest\b` grep, but it is a TRUE, specific, hedged comparative (a real spread vs conventional, qualified "typically"), not the banned "lowest rates available" puffery that 06-16 swept from the meta/og. Same family as the 06-16 "guaranteed by the VA = factual, benign" rule and the speed-token context rule. The 06-16 fix correctly removed the meta/og puffery and LEFT this body fact — so a fresh `grep -c 'lowest rates'` returning 1 is NOT evidence the fix regressed. Verify which instance the hit is before re-flagging.
- **Reconcile `ls -t` blog mtime against `datePublished` before trusting a freshness signal.** `ls -t blog/*.html` surfaced three files as "most recent," but their schema `datePublished` was 2026-04-12 — the mtime was a git/checkout artifact, not a publish. The manifest's dated entries are authoritative for freshness; the latest real post is 2026-06-23. A naive `ls -t` read would have falsely reported the blog as fresh. Pairs with the existing "sitemap count change → trace the cause" discipline: file mtime ≠ content date.
- **Blog freshness boundary: 7 days is the last in-window day, 8 is the hard flag.** Latest post 06-23, run 06-30 = exactly 7 → soft heads-up to styer-content-weekly, escalate tomorrow when it tips to 8. (Re-confirms the 06-12/06-14 boundary-math rule; don't over-flag the boundary value.)
- **0 mutations is the correct Tuesday outcome when the audit is clean.** 25 money/loan titles+metas audited; only two metas <150 (va 148, investor-loans 149), both complete + on-positioning → padding = churn, leave them. No defect met the fix threshold. Shipping nothing (and logging WHY each near-miss was left) is the disciplined result, not a sign of a skipped run. (Re-applies 06-23/06-29 "clean audit is valid output.")

---
## 2026-07-01 (Wed — Suburb Deep Dive + AEO: Pflugerville) — clean audit, 0 mutations

- **FAQ rich results are deprecated for non-gov/health sites (Google, 2023).** FAQPage schema now serves AEO/LLM answer extraction, not SERP rich results. Consequence: a schema-vs-visible FAQ question-text drift (Pflugerville schema Q4 "…that affect mortgage qualification" vs visible "What major employers…") is BELOW the fix threshold when the schema is well-formed — the more-specific schema variant can even be better AEO tuning. Don't mutate for parity that no longer buys anything.
- **The Re-Verify Gate can RESOLVE a queued escalation, not just clear a standing flag.** The 06-30 log queued a hard blog-freshness escalation for today ("if no post by 07-01, tip to 8 days → hard flag"). Live check: manifest latest = 06-30 post, URL 200, in sitemap, registered in blog.html. A new post shipped after the prior run → escalation NOT fired, flag cleared. Always re-check live manifest + a 200 on the newest post URL before firing any freshness flag.
- **Pflugerville is the strongest suburb template instance seen to date** — answer-first bolded intro, question-form H2s, 5 answer-first FAQ, valid MortgageBroker+FAQPage+BreadcrumbList+WebPage JSON-LD, deep sourced neighborhood/tax/employer/school entity coverage. This is the bar for the rest of the 25-page suburb set.
- Suburb pages route CTA to /scenario.html by design (Decision 3); `/get-preapproved`=0 on a suburb page is NOT a defect. Inline-form absence on 20/25 suburbs is the Adam-gated 5/25 carry, not a per-page finding.

## 2026-07-02 (Thu — Internal Linking + Funnel Flow)
- **Homepage quick form fires `generate_lead` from external `script.js` (L51), not inline in index.html.** A raw-HTML grep for `generate_lead` on the homepage returns 0 — a FALSE alarm. To verify the homepage lead event, trace the JS handler (`js-quick-contact` → `bindQuickContactForm` → `script.js` L485 `window.location.href='/thank-you?type=quick-contact'`), not the page source. Same pattern for the prequal form (L886). The Ads-tracked LPs (`/get-preapproved`, `/refinance-quote`) DO carry an inline `generate_lead` + `action='/thank-you'`, so the grep-the-HTML matrix is valid for THOSE but not for the homepage/scenario JS forms.
- **Funnel = two independent hops.** A form can capture leads (Netlify Forms POST to `/` + `/.netlify/functions/lead-intake`) and still NOT count as a Google Ads conversion if it fails to land on `/thank-you` (where `thank_you_page_view` fires). Verify BOTH: (a) lead capture, (b) `/thank-you` redirect. This is the exact class of the 06-18 silent break (suburb forms missing `action="/thank-you"`).

## 2026-07-03 (Fri — Content Planning + AEO Review)
- **Blog CTA convention is real and enforceable:** every blog post carries a `blog-dual-cta` conversion block routing to `/get-preapproved` + (where relevant) `/refinance-quote`. On 2026-07-03, 39/40 posts had it; the 06-23 no-ratio-DSCR post was the lone gap (it had /scenario + Calendly + tel — intent capture but no *tracked-conversion* path). Fix = replicate the exact sibling block (06-16/06-14/06-30), don't invent new markup. This is distinct from Decision 3 (money/suburb pages route to /scenario BY DESIGN) — Decision 3 is NOT license to skip the tracked CTA on *blog* posts.
- **Netlify Pretty-URLs mutate served HTML two ways at once:** (1) strips `.html` from internal hrefs (`/get-preapproved.html` → `/get-preapproved`), and (2) rewrites double-quote attrs to single-quote (`href="..."` → `href='...'`). Live-verify a just-shipped link with quote-agnostic AND extensionless greps (`grep -oE "(get-preapproved|refinance-quote)[^"'< ]*"`), never an exact `href="/x.html"` match — that returns 0 even when the link is correct. Confirmed live today: my `href="/get-preapproved.html"` served as `href='/get-preapproved'`.
- **"21-day" ≠ the banned claim.** GOALS bans the "21-day close" *performance/trust-strip* claim only. A sitewide sweep hit 7 files, all benign: `14–21 days` appraisal turnaround (jumbo), `121 days on market` substring (hutto), `14 to 21 days`/`21-day clock` contract-timing in educational blog copy, the `_TEMPLATE.html` line that literally instructs authors to avoid it, and a noindexed admin `<textarea>` placeholder (dashboard). Grep context (`grep -noE '.{0,30}21[- ]day.{0,45}'`) before flagging — verify-before-report killed a false escalation.
- **Shell hygiene (harness-specific):** a stray `cd /tmp` truncated PATH so `curl`/`wc`/`head`/`tr` became "command not found" on the *next* Bash call; and the harness aliases `grep`→ugrep (a shell function). Fix: start each Bash call with `export PATH="/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin"` and call `/usr/bin/grep` explicitly. Avoid `cd` in these audit commands — pass absolute paths.
- **Task-file drift caught:** the styer-site-daily SKILL.md HARD CONSTRAINT still says 'always "Adam Styer | Mortgage Solutions LP"' — that entity was retired 2026-05-20. Repo CLAUDE.md/CONTEXT.md (newer, "Adam Styer | HyperSmart Home Loans" / "Kyber Mortgage Corporation dba HyperSmart Home Loans") win per instruction-priority. Flagged for Adam to update the task file.

## 2026-07-05 (Sunday — off-rotation)
- **Weekend/off-rotation runs are legitimate but should NOT force a weekday rotation item.** Step 4's rotation is Mon–Fri only. On a Sunday: run the non-negotiables (sitemap/robots 200, conversion 10/10 HTML-token), the backlog check, and a design spot-check — then stop. Let the assigned weekday (Monday) own its rotation. Don't run Monday's schema/AEO pass a day early; Monday's run will do it and the double-spend is wasted.
- **A healthy funnel ships 0 mutations, and that's the correct output.** The remaining open backlog items were all Adam-gated new-page creation (glossary page, DSCR ROI section, doc-checklist post) or sister-task-owned (per-city local data → styer-suburb-editor). Manufacturing a net-new blog post off-rotation to look busy = scope creep against GOALS.md, not diligence. No un-gated reversible site work existed to attempt, so the DECISION TEST correctly resolved to "nothing to do."
- **Re-confirmed:** writing to `loanos-clone/tasks/seo-sem/backlog.md` triggers a Vercel build of the PAUSED LoanOS product (CONTEXT.md infra note). Keep backlog mutations out of loanos-clone until Adam relocates the path. Read-only reads are fine.

---
## 2026-07-15 (styer-site-daily, Wed — Kyle Suburb Deep Dive)
- **Same-page speed-claim inconsistency is a real, findable defect.** Kyle's meta/AEO/CTA all said "same-day pre-approval" but its FAQ (schema + visible) led with "24 to 48 hours" — the retired Decision-2a variant. When a suburb page markets one turnaround up top and a different one in the FAQ, the FAQ is usually the stale one (older template copy). Grep each rotated suburb page for its own speed language and check internal consistency, not just banned-phrase presence.
- **Grep CONTEXT before flagging "24–48 hours."** Sitewide it appeared on 4 pages; only 1 (Kyle) was Adam's own-service marketing claim. The other 3 were legitimate factual/educational uses (employment verification 24–48h before closing; wire-instruction timing; "a good broker can pre-underwrite within 24–48h"). Same rule as "21-day": the number isn't the ban, the marketing context is.
- **FAQPage edits require dual-write + parity check.** The answer lives twice — once in JSON-LD `acceptedAnswer.text`, once in the visible accordion `<p>`. Both must stay byte-identical or the rich-result validity breaks. Edit both, then verify parity programmatically (extract both, compare) before commit.
- **Concurrent-writer push conflict with an uncommitted foreign file: stash-by-path, not global stash.** Another session had `high-net-worth-mortgage.html` modified-but-uncommitted when my push was rejected. `git pull --rebase` refused ("unstaged changes"). Correct move: `git stash push <that-file-only>` → rebase → push → `git stash pop`. Never `git add -A` (would sweep their work into my commit), never `git stash` bare if it risks their file, never `git checkout` it (would destroy their work). Verified `git status` after pop showed their file still modified/untouched.
- **PATH truncates mid-session even after an explicit export.** One Bash call had curl/wc/head all "command not found" despite `export PATH` at top — the loop subshell lost it. Defensive fix: use absolute `/usr/bin/curl`, `/usr/bin/grep`, `/usr/bin/wc`, `/usr/bin/head`, `/bin/sleep` directly rather than relying on PATH.
- **Foreground `sleep` is blocked; post-deploy verification polls go in `run_in_background`.** A background bash loop (`/bin/sleep 20` × N, curl for the new content token, exit 0 on match) confirmed Netlify deploy in ~20s without blocking the session.
- **10-day scheduler outage detection = compare currentDate vs latest run-log date on every run.** Today (07-15) vs last log (07-05) = 10 missed fires. This is the single most important health check to run before assuming "nothing changed since yesterday."

## 2026-07-15 (styer-competitive-weekly — first run since Wk 16)
- **The gap-closing signal vs head-term noise is now empirically settled.** Across a 3-week measurement gap, complicated-income long-tail ranks moved SLOWLY and durably (non-QM NR→#1, asset-depl held #1, self-emp blog held #1, jumbo held #2), while EVERY head-term #1 flipped: all three Wk16 "reclaims" (Barton Creek lender, Highlander broker, MortgageAustin pre-approved) gave their #1 right back to a different competitor/directory this week. Rule: report long-tail movement as trend; report head-term movement as composition noise until a 2nd confirming run.
- **A NR→#1 debut on a money keyword must be live-verified, not taken from the SERP alone.** non-QM jumped from out-of-top-10 to #1 in one measured step — a big enough move to warrant a curl check. Confirmed HTTP 200 + `<title>` match + FAQPage present. The 2nd Styer URL at #10 was a 301 redirect (not a distinct live page) — the SERP listed it but it's not independent inventory. Always distinguish canonical 200 pages from 301s when counting "how many URLs we rank."
- **Suburb SERPs polluted by same-name out-of-state cities need manual disambiguation.** "westlake austin mortgage lender" returned Westlake Village, CA results (Zillow Village, Westlake Financial-auto, Dignified, Option Funding) interleaved with the real Austin/TX results. Styer is clearly #1 among TX-relevant results, but a naive "position in raw list" read would misrank it. This is why Westlake was DROPPED from rotation in Wk 11 — it now disambiguates better but still needs a human eye on the result set.
- **AggregateRating keeps trying to creep back as a "competitive wedge" — it is NOT one.** Per `reference_aggregate_rating_self_serving.md`: self-serving AggregateRating schema earns ZERO Google SERP stars (AEO-only). Neither Styer nor LendFriend has it, and that symmetry is irrelevant to rankings/stars. The real star lever is off-page (GBP + Zillow review velocity). Each competitive run should actively DROP this from recommendations, not re-surface it — downstream agents (CONTEXT blockers, growth log) have amplified it for weeks.
- **Rotation cadence under-measures suburb wins (standing pattern, reconfirmed).** Westlake/San Marcos hadn't been measured since Wk 11 (2026-05-18) because the 2-per-week rotation cycles through 9 suburbs. Westlake #1 and San Marcos #10 are "new" only to the log, not to Google. When a suburb re-enters rotation after ~8 weeks, treat its position as a fresh baseline, not a delta.
- **DSCR is a gap worth explicitly declining, not carrying open.** It's the last complicated-income term where Styer is NR, but the entire SERP is investor/hard-money (Newfi, Easy Street, Tidal, Defy, Longleaf) — non-owner-occupied, off the Phase A owner-occupied thesis. Carrying it as an "open gap" invites wasted effort chasing hard-money shops on their turf. Recommend deprioritize, don't chase.

## 2026-07-16 (styer-site-daily Thursday — Internal Linking + Funnel Flow)

- **A SERP-ranking slide is not automatically a page defect — verify structure before acting on a "improve page X" competitive recommendation.** Bank-statement slid #7→#8→#9, and competitive-weekly flagged it as a content-depth/AEO target. But a live structure grep showed the page already leads with an answer-first benefit subhead, carries a 10-question conversational FAQPage schema, and every FAQ answer leads with the direct answer. The slide is competitive crowding (a ranking blog at #1, two new entrants) — nothing to fix on-page. Rule: before touching a page a sister-task recommended, grep answer-first-lead + question-FAQ-schema + answer-lead-text; if all present, log "no defect, landscape crowding" and ship 0 mutations rather than manufacturing edits.
- **Bidirectional hub linking check is a fast, high-signal Thursday task.** For a cluster hub (non-QM, now #1), verify spoke→hub AND hub→spoke both exist. Found the hub links contextually to 9/10 spokes both ways for the 4 competitively-named spokes — the "consolidate authority" ask was already met. The one asymmetry (hub not linking generic `investor-loans`) was an editorial choice (hub routes investor intent through DSCR), not a defect. Don't force mechanical symmetry into a #1-ranking page.
- **Sandboxed Bash returns `000` on every network call.** The scheduled-task Bash sandbox blocks outbound network — `curl` returns HTTP `000` (no response), which reads identically to "site is down." Re-run any curl-based non-negotiable (sitemap, robots, conversion-token fetch) with the sandbox disabled before concluding anything. This is a false-positive trap for the RE-VERIFY gate: `000` is a tooling artifact, never a site-down signal.

## 2026-07-17 (styer-site-daily Friday — Content Planning + AEO Review)

- **"Missing CTA" is audience-relative — check who the page is for before flagging.** A blog-CTA audit scanning `blog/*.html` + `realtor-updates/*.html` returned "4 posts missing /get-preapproved|/refinance-quote." All 4 were `realtor-updates/` — realtor-facing pages that correctly carry the realtor CTA ("Send Your Scenario" → /scenario.html, plus phone + Contact). Forcing a borrower pre-approval CTA onto realtor content would violate the voice guide (realtor content = positioning/scenario, not borrower conversion). The 40/40 borrower-CTA metric applies to borrower-facing `blog/*.html` only. Grep the CTA pattern the page DOES have before concluding it's a defect.
- **The carried "2026-04-27 CTA structure" blocker points at a renamed realtor-update.** CONTEXT lists `2026-04-27-why-home-prices-arent-crashing` as a CTA/FAQPage-schema blocker; the live file is `realtor-updates/2026-04-27-the-crash-that-isnt-coming-data-for-your-buyers.html` — a realtor page with the correct realtor CTA. The blocker is stale-framed (it's realtor content, not a borrower-CTA gap) and paused per GOALS.md no-content policy anyway.
- **Blog structure: narrative statement-form H2s + a dedicated FAQPage block is the site's established, AEO-valid pattern.** Both audited posts (07-16 self-employed underwriting, 06-30 bank-statement) use section-header H2s ("The Add-Backs", "The Honest Tradeoffs") for the body and put the conversational question-form Q&A in the JSON-LD FAQPage + visible FAQ accordion. Don't flag statement-form body H2s as an AEO defect when the question-form Q&A lives in the FAQ block and the intro carries an extractable summary.
- **Answer-first AEO check that scales: extract the FAQPage `acceptedAnswer.text` leads programmatically.** A small python regex over the FAQPage JSON-LD block pulls each question + first ~100 chars of its answer. Both posts led every answer with the direct answer ("Usually, but not always.", "They are legal and fully documented…", "Not always."). Beat reading the whole post — verify the answer-lead in one pass.
- **PATH truncation recurred again (3rd+ session).** A Bash call with `export PATH` at top still lost `wc`/`tr`/`head` inside a `for`-loop subshell (curl and `/usr/bin/grep` survived because they were already absolute). Standing fix reconfirmed: absolute-path EVERY binary (`/usr/bin/wc`, `/usr/bin/tr`, `/usr/bin/head`, `/usr/bin/sed`, `/usr/bin/python3`) — don't rely on the top-of-script export surviving into subshells.

## 2026-07-18 (Saturday, off-rotation)
- **zsh eval breaks on quote-heavy `grep` inside a `for` loop.** The conversion-check loop with nested single+double quotes in `grep -oE` patterns failed with `unmatched '`. Fix: write the loop to a `.sh` file and run `/bin/bash file.sh` — sidesteps the interactive-shell eval-quoting parser entirely. Reusable for any multi-page grep matrix.
- **Off-rotation days are a real, defined state.** Sat/Sun have no assigned Step 4 weekday rotation (rotation is Mon–Fri). The correct output is non-negotiables (Steps 1–2) + read-only backlog + design spot-check + clean run log. Do NOT invent rotation work or manufacture edits to hit a quota — a healthy funnel legitimately ships 0 mutations (mirrors the 07-05 Sunday off-rotation run).
- Scheduler recovery holding: 07-15/16/17/18 all fired (4 clean days) after the 07-06→07-14 10-day outage — longest gap on record. Outage detection = compare currentDate vs latest run-log date each run.

---
## 2026-07-22 (Wednesday — Suburb Deep Dive + AEO → San Marcos)

### The HTML-token conversion matrix is blind to JS-bound forms — verify through `script.js` before crying "untracked funnel"
Every suburb page's primary borrower CTA ("Start Your Pre-Approval", "Start Your Application") and the global nav button point at `/scenario.html`. A live fetch of that page returns **0 `generate_lead`, 0 `lead_type`, 0 `/thank-you` references** while GTM loads ×2 — which, judged by this task's own standard HTML-token check, reads as *25 suburb pages funneling into a completely untracked form*. That would have been the highest-severity finding in months. **It was wrong.** The form carries `class='js-quick-contact'`; `script.js:490` binds every such form to `bindQuickContactForm`, which fires `generate_lead` with `lead_type: 'quick_contact'` then redirects to `/thank-you?type=quick-contact` (`script.js:479-485`). **Rule: the HTML-token matrix is only valid for pages with *inline* dataLayer pushes. For any form with a `js-` class, grep `script.js` before concluding anything.** Known JS-bound lead paths and their `lead_type`: `.js-quick-contact` → `quick_contact`; hero quick form → `quick_quote`; prequal → `prequal`. Generalizes the older audience-check insight into: **a missing-token finding must be checked against where the token actually lives.**

### Sitemap audits: exclusion ≠ omission — check robots + noindex + canonical + inbound links on every absentee
18 top-level `.html` files were absent from `sitemap.xml`. Naively adding them would have been actively harmful. **16 were absent on purpose:** `refinance-calculator.html` is `Disallow`ed in robots.txt, `ftb-dpa-guide.html` carries `noindex` (gated PDF landing), and the rest are dashboards / `ops.html` / `404.html` / the Google verification file / `index.html` (already covered by the root `/` entry — normalize the root when diffing, or you'll false-positive on it). **Only 2 were true omissions** — `dscr-calculator.html` and `asset-depletion-calculator.html`, both HTTP 200, no noindex, not disallowed, self-canonical, **159 internal inbound links each**, with six sibling calculators already listed. The tell that it was inconsistency rather than policy: siblings present, these two absent, no robots/noindex signal either way. Bonus: they're the two flagship *complicated-income* tools per GOALS.md, so the omission was suppressing exactly the positioning Adam is pushing.

### Shipping past a concurrent writer: use a throwaway worktree, never stash/rebase/`add -A`
Found the shared tree 9 commits behind `origin/main` (a live website-AI-assistant session) **and** holding 9 dirty files from that same session. `git pull --rebase` refused ("cannot pull with rebase: You have unstaged changes"), and `stash` / `reset --hard` / `add -A` would each have destroyed or swept another session's uncommitted work. **Working pattern:** confirm the remote hasn't touched your file (`git diff HEAD~1 origin/main -- <file>` — empty means safe; note that diffing `HEAD..origin/main` will show *your own* commit as a hit and look like a conflict), then `git worktree add --detach <tmp> origin/main`, `git cherry-pick <your-sha>`, validate, `git push origin HEAD:main`, `git worktree remove --force`. Finally rewind local main with `git reset --soft HEAD~1` + `git restore --staged <file>` + `git checkout -- <file>` — this touches only your own file and leaves every concurrent-writer file byte-identical (verify with `git status --porcelain` before and after).

### Scheduler failure has two distinct modes — "didn't fire" vs "fired but didn't log"
`latest.md` said 07-18 while today was 07-22, which looks like a 4-day outage. It wasn't. Commit `c675929` (`daily-opt: ... 2026-07-20`) proves **Monday 07-20 fired and shipped real site work but never completed Steps 7–8** — no `run-logs/2026-07-20.md`, no `latest.md` update. Only 07-19 and 07-21 were true no-fires. **Always cross-check `git log --oneline --since=<last-log-date>` against the run-log filenames before reporting an outage length** — otherwise you over-report the outage and, worse, lose the fact that a run is silently skipping its own logging step.

### `sleep`-then-verify is blocked; poll with an until-loop
`sleep 75 && curl ...` to wait for a Netlify deploy is refused by the harness. Use `until [ <condition> ] || [ $i -ge N ]; do i=$((i+1)); sleep 10; done`. Netlify deployed the sitemap change before the first poll (`polls=0`), so deploy latency here is well under ~15s for a static file change.

## 2026-07-24 (Friday — Internal Linking + Funnel Flow queued from 07-23 no-fire, + Friday fold-in)

### Netlify CONSUMES the `netlify` form attribute at build time — verify Netlify wiring against committed source, never served HTML
A live `curl` of `contact.html` returned `<form action='/thank-you' id='form-contact' method='POST' name='contact'>` with **no** `netlify`/`data-netlify` attribute — which, by a naive read, says "this form isn't registered with Netlify Forms, leads are lost." **False.** The committed source (contact.html:167) is `<form name="contact" method="POST" action="/thank-you" netlify netlify-honeypot="bot-field" id="form-contact">` + hidden `form-name=contact` + `bot-field` honeypot — a correctly-registered Netlify form per the repo's own convention (bare `netlify` attribute, not `data-netlify="true"`). Netlify's build bot detects the form, registers it, and **strips the `netlify`/`netlify-honeypot` attributes from the deployed HTML**. So the served tag legitimately shows neither. This is a THIRD way live-served HTML diverges from source (after Pretty-URL `.html`-stripping + quote-rewriting, and `js-`bound dataLayer living in `script.js`). Rule for the Thursday "check contact.html Netlify wiring" task: grep the **committed** `contact.html` for `netlify` + hidden `form-name` input — a served-HTML grep for `netlify` is guaranteed to return 0 even on a healthy form. Note script.js:541 binds generic `form[data-netlify="true"]:not(.js-quick-contact)` handlers — contact.html uses the bare `netlify` attr, so it relies on native Netlify POST interception + inline `generate_lead`, not that JS handler; both paths are valid.

### Homepage having NO `/get-preapproved` link is BY DESIGN — don't score it as a funnel leak
Grepping the live homepage for `get-preapproved`/`refinance-quote` returns 0. The repositioned homepage routes all primary CTAs to the inline `#contact-form` / `#quick-scenario-form` (js-quick-contact → `generate_lead` → `/thank-you` via script.js) plus the 1003 app link + Calendly. The Ads landing pages (`/get-preapproved`, `/refinance-quote`) are **separate ad-entry funnels**, not linked from the homepage. This is the same trap as the SKILL Step-5 "homepage hero CTA → /get-preapproved" check that's been stale since the repositioning. Confirm the inline-form conversion path exists (it does), then log the funnel healthy — don't manufacture a "homepage doesn't link to the LP" regression.

### A single-day no-fire is task-specific, not a scheduler outage, if sister tasks fired
07-23 had no `daily-opt:` commit and no `run-logs/2026-07-23.md` → looked like a miss. But `git log` for 07-23 shows `6c4e94e` (styer-suburb-editor) and 07-24 shows styer-content-weekly commits — both sister tasks fired on schedule. So `styer-site-daily` specifically missed its 07-23 fire while the shared scheduler was healthy. Distinguish "the scheduler is down" (all tasks silent) from "this task's cron slot misfired" (siblings ran) before sizing the blocker — the fix is different.

## 2026-08-17 (styer-competitive-weekly — first run after a 33-day gap)

### The "head terms = noise, long-tail = durable" rule just got falsified — and a 33-day gap makes it unresolvable
The 07-15 run established a confident rule: complicated-income long-tail moves slowly and durably, head terms churn as composition noise. This run inverted it exactly. **Five** Phase A long-tail terms fell together (non-QM #1→#5, asset-depletion #1→#5, jumbo #2→#4, 1099 #4→#6, self-employed blog #1→#3) while **two** core head terms rose sharply (get-pre-approved #9→**#1**, cash-out NR→**#2**). Five simultaneous same-direction long-tail moves is not random churn. But with four missed fires there are **no intermediate datapoints**, so "gradual erosion" and "single re-ranking event" are indistinguishable — and they call for different responses. **Rule going forward: a confident pattern claim needs consecutive runs behind it. When a gap swallows the intermediate points, report the endpoints and explicitly refuse to name the mechanism** rather than retrofitting the prior rule onto a result that contradicts it.

### Transactional-intent head terms ARE winnable; generic-identity head terms are not — the old "route around head terms" framing was too coarse
Months of reports treated head terms as uniformly directory-locked and told Phase A to route around them entirely. That is now measurably wrong in a specific way: Styer took **#1 on "get pre-approved austin tx"** and **#2 on "cash out refinance austin tx"**, while remaining NR on "mortgage lender austin tx" / "mortgage broker austin tx" / "home loan austin tx" (all Zillow/Yelp-topped). The split is **transactional intent vs. generic identity**, not head vs. long-tail. Directories win the "who is a lender near me" queries; a single broker can win "the thing I am trying to do right now." Worth reflecting in future keyword selection — and a #1 on a high-intent transactional term is arguably worth more than any of the three complicated-income #1s lost the same week.

### Rich schema is NOT what moves these SERPs — verified by direct counter-example, so stop recommending it
Live structure checks this run: **Truss ranks #1 bank statement with no FAQPage, no BreadcrumbList, no LocalBusiness, no MortgageBroker schema.** LendFriend ranks #1 asset depletion with FAQPage ×1 + Breadcrumb + Article. Styer's equivalent pages carry *strictly more* (FAQPage ×2, LocalBusiness, MortgageBroker, BreadcrumbList ×2, Article ×2) and rank #5–#7. **A competitor outranking you with less markup is a hard falsifier** — it kills the whole class of "add more structured data" recommendations in one observation. This is the same trap AggregateRating kept setting; the general form is: before recommending a schema addition, check whether the page currently beating you actually has it.

### Don't infer content depth from served byte size across different site platforms
LendFriend's asset-depletion page serves ~406KB vs Styer's ~60KB, which invites a "they have 7× the content" conclusion. That inference is unsafe — page weight across different builders (Webflow/Wix-style bundles vs. a hand-written static site) is dominated by inline JS/CSS, not prose. Reported the byte figures but explicitly declined to claim a content-depth gap from them. **If depth actually needs measuring, extract visible text, not bytes.**

### A ranked URL can be a 301 — always separate live inventory from redirect artifacts when counting positions
"asset depletion mortgage texas" returned two Styer URLs at #5 and #6. The **#5 is a 301** (`-austin-tx.html` → `-texas.html`, consolidated in commit `35336e4` on 06-09); only #6 is live inventory. Same trap the 07-15 run caught on non-QM. Also worth noting the near-miss on causality: the consolidation was tempting to blame for the moat loss, but the moat **held #1 on 07-15, a month after that commit** — so the drop is recent and competitor-driven. **Check whether a suspected cause predates the last known-good measurement before attributing anything to it.**

### The suburb playbook is transferable — Cedar Park proved it against the exact wall Round Rock faces
Cedar Park was the site's longest-standing suburb gap (NR across 4+ consecutive measurements) against CrossCountry's two branch URLs + Zillow + Yelp + physical-presence banks. It just hit **#6, first-ever top-10**, after the suburb-editor's hyper-local de-templating pass (Leander ISD tax-premium framing, named-neighborhood spotlights, local employers). Round Rock is NR against a structurally identical wall (Yelp ×2, Zillow, Herring/CrossCountry/Hometrust/JVM branches). **This converts "physical-presence dominance is unbeatable" — carried as settled for months — into a solved problem with a known recipe.**

### Concurrent-writer state: commit your own files, and say plainly which required files you skipped
Tree was 5 commits behind origin with **61 dirty files**, including `CHANGELOG.md` carrying another session's in-flight restructure (43-line staged deletion). Committing it via pathspec would have shipped their half-finished work. Skipped `CHANGELOG.md` and `CONTEXT.md` deliberately and said so in the report rather than either entangling or silently omitting. `run-logs/learnings.md` was also dirty — but with a *complete, coherent, orphaned* 07-24 entry, so appending and committing it rescued that work instead of endangering it. **Inspect the actual diff before deciding; "dirty" is not one condition.** Also skipped the SKILL's "TOMORROW_PRIORITY → run-logs/latest.md" step for the same reason (224-line in-flight rewrite) and routed those findings to the clean `TODO.md` instead.
