# Daily Run — 2026-09-03 — Wednesday rotation (Suburb Page Deep Dive + AEO), cursor Cedar Park

Followed `TOMORROW_PRIORITY` from the 09-01 log (Wednesday rotation, Cedar Park cursor). There was a
1-day scheduler gap (09-01 → 09-03, 09-02 no-fire) — no commits or logs exist for 09-02.

*(Clock note: session clock rolled 09-03 → 09-04 mid-run during browser-tool work, same pattern as
09-01's roll. One run, started and logged 09-03 to match `TOMORROW_PRIORITY` cursor and commit `c09be17`.)*

**Headline: one real AEO defect found and fixed — a Cedar Park FAQ answer opened with "It depends"
in both the JSON-LD and the visible accordion, failing the answer-first extractability standard this
rotation exists to enforce.** Fixed in place, live-verified. Also resolved the tap-target measurement
carried unresolved since 09-01 (browser tool was down both prior attempts).

## NOTEBOOKLM

Status: **Timed out (auth).** Re-probed live, not carried on faith:

```json
{"error": true, "code": "ERROR", "message": "Authentication expired or invalid. ... Run 'notebooklm login' to re-authenticate."}
```

`NOTEBOOK_INSIGHTS` cache applied. Same single blocker as every prior run — one interactive
`notebooklm login` from Adam.

## STEP 1 — SITEMAP + CORE PAGES

| Check | HTTP | Result |
|---|---|---|
| `sitemap.xml` | 200 | ✅ **153** `<loc>` / **153** `<lastmod>` / **0** duplicates |
| `robots.txt` | 200 | ✅ |
| `/` · `/get-preapproved` · `/refinance-quote` · `/thank-you` · `/blog` · `/contact` · `/calculators` · `/about` | 200 (via 301→200, standard Netlify pretty-URL redirect) | ✅ |

**`git fetch` precondition — 4-for-4.** Checkout was already at `origin/main`, tree clean. Fourth
consecutive run confirming this precondition; still never skipping it.

## STEP 2 — CONVERSION TRACKING

Status: **10/10 ✅.** HTML-token matrix over live-fetched pages (no test submissions — a real submit
pages Adam and writes a live Netlify/n8n lead, per standing practice since 08-31).

| Page | GTM | generate_lead | lead_type | thank_you_page_view | tel: | h1 | HTTP |
|---|---|---|---|---|---|---|---|
| `/` | 2 | — | — | — | 6 | 1 | 200 |
| `/get-preapproved` | 2 | 1 | `purchase_prequal` | — | 2 | 1 | 200 |
| `/refinance-quote` | 2 | 1 | `refi_quote` | — | 2 | 1 | 200 |
| `/thank-you` | 2 | — | — | 1 | 2 | 1 | 200 |

**`phone_click` needed one extra hop to verify, not a defect.** The homepage HTML shows 0 literal
`phone_click` tokens next to its 6 `tel:` links — that would read as broken tracking. It isn't:
`script.js` installs one delegated document-level listener (`a[href^="tel:"]` → `dataLayer.push({event:
'phone_click'})`) that covers every `tel:` link sitewide, and `script.js` loads on the homepage. Static
per-link grep can't see a delegated listener; confirmed by reading the script source directly.

## STEP 3 — LANDING PAGE AUDIT (mobile, 390×844 real render)

Only re-audited the item carried unresolved from 09-01 (tap-target height) per "compare against
yesterday's log" — everything else was unchanged and already ✅ as of 09-01.

**Tap-target height — RESOLVED.** 09-01 left this an inconclusive 43–48px range because the browser
tool timed out twice. This run rendered both pages at 390px and measured directly:
- `/get-preapproved` step-1 CTA ("Continue →"): **45.5px**
- `/refinance-quote` submit CTA ("Get My Free Quote →"): **50px**

Both clear the 44px minimum. Both pages share the same `.lp-form-submit .btn` base class, so this
resolves the ambiguity for the shared step-2 submit buttons too. **Closed — no longer carried.**

One screenshot mid-run showed the form-step H2 rendering faint/overlapping — checked via
`getComputedStyle`: `opacity: 1`, `color: rgb(10,31,63)`, class `reveal is-visible`. It was a
transition-timing artifact from the CSS fade-in caught mid-frame, not a real state — a second
screenshot a moment later rendered correctly. Not logged as a finding.

## STEP 4 — WEDNESDAY ROTATION: CEDAR PARK

| Check | Result |
|---|---|
| City-specific H1 | ✅ "Mortgage Lender Cedar Park TX" |
| Inline lead-capture form | ✅ `cedar-park-quote` |
| FAQPage schema | ✅ 5 Q&A, valid JSON |
| BreadcrumbList | ✅ |
| Question-phrased H2s | ✅ 7 of 7 |
| Links to `/get-preapproved` | ✅ 3 |
| Links to `/calculators` | ✅ 2 |
| GTM | ✅ 2 |
| NMLS 513013 | ✅ 14 occurrences |
| Canonical | ✅ exactly 1 |
| AEO: answer-first opening paragraph | ✅ — names the query intent, 40+ wholesale lenders, Leander ISD tax detail, zero preamble |

**The one real defect: an FAQ answer opened with "It depends" instead of leading with the answer.**
`What is the minimum down payment to buy in Brushy Creek or Ranch at Brushy Creek?` — both the
JSON-LD `acceptedAnswer.text` and the visible accordion `<p>` opened `"It depends on the program and
price [point/tier]:"` before any number appeared. That's a hedged, non-extractable opener — the exact
failure mode this rotation's AEO check exists to catch (4 of 5 sibling answers on the same page already
led with a direct number or yes/no).

**Fixed by reordering, not rewriting.** New opener: `"Minimum down payment ranges from 0% (VA) to
10–20% (jumbo), depending on the program and price point:"` — leads with the range, then the same
per-tier breakdown, verbatim, follows. Every dollar figure, percentage, and program name is unchanged;
only the first clause moved. Applied identically to both the JSON-LD text and the visible accordion
`<p>` (they'd drifted to two different hedge phrasings — "price point" vs "price tier" — both fixed to
match their own pre-existing wording, not unified, since unifying wasn't the task).

Diff: `cedar-park-mortgage-lender.html`, +2/−2, exactly the two hedge-opener lines. JSON-LD re-parsed
6/6 valid after edit. Commit `c09be17`, pushed, **live-verified**: both occurrences of the new opener
confirmed live via HTTP 200 fetch.

## STEP 4A — SITEWIDE GTM + JSON-LD SWEEP (per 09-01's carried priority #6)

Live-fetched all 153 sitemap URLs directly (not local-file path mapping — a documented source of past
false positives when `.html` vs extensionless normalization goes wrong):

- **GTM coverage: 153/153.** Zero pages missing the container.
- **JSON-LD parse errors: 0/153.**
- **No-JSON-LD: 9** — `rate-alert`, `get-preapproved`, `refinance-quote`, `texas-complaint-notice`,
  `privacy`, `terms`, `resources/`, `resources/first-time-buyer-guide/`,
  `resources/blog/2026-03-04-wait-for-rates-realtor.html` — the same expected 9 as 09-01.
- **Duplicate titles: 0. Duplicate descriptions: 0. Descriptions >160 chars: 0.**

No delta from 09-01 — clean baseline held.

## STEP 4B — SEO/SEM BACKLOG (read-only)

`loanos-clone` **not mutated** — standing guard (push deadlock + concurrent Codex writers). Re-read
`backlog.md` (168 lines, last additions 2026-05-07) and `BLOCKERS.md`. Per
`memory/project_seosem_program_abandoned.md`, SEOSEM-001 is closed at the evidence level and I did not
re-measure it. The two open, non-struck items are both explicitly Adam-gated in the backlog's own text
(`products.html` 7-card 1003 routing — MEDIUM_RISK; `/prequal.html` `generate_lead` parity — a two-path
form-behavior decision) — **no un-gated ZERO_RISK/LOW_RISK item exists**, same conclusion as the last
four runs.

## STEP 5 — DESIGN AUDIT (homepage + about)

| Check | Result |
|---|---|
| Homepage hero CTA → `/get-preapproved` | ✅ (×3, incl. `?intent=scenario&source=homepage_hero`) |
| Trust bar verbatim | ✅ "5.0 Google · 141 published reviews" — matches recorded review count |
| Navy `#0A1F3F` | ✅ intact |
| Gold `#8B6E24` | Standing known drift vs SKILL.md's documented `#C9A84C` — not new, still Adam's call |
| Retired "21-Day" claim | ✅ 0 occurrences, both pages |
| Mobile menu | ✅ **functionally verified** — `.mobile-menu-toggle` click flips `aria-expanded` false→true and renders the full nav panel (Loan Programs, Resources, Calculators, Testimonials, Contact, phone, Send Your Scenario CTA) |

**Headshot/WebP check target clarified, not a defect.** The `class="headshot"` element on `about.html`
renders `assets/family.jpg` (a family photo, not a solo headshot) — `assets/headshot.jpg` exists but is
only referenced in JSON-LD `Person.image`, never rendered as a visible `<img>`. `assets/headshot.webp`
(66KB) exists alongside the unused `headshot.jpg` (267KB), but since nothing renders it, there's no page
weight to save. `family.webp` exists too, but is the same byte size as `family.jpg` (99KB) — no
compression win available. **No action; this checklist item doesn't map to a real page-weight issue
under the site's current content choices.**

## RE-VERIFY GATE — claims checked live this run

- Sitemap 200 / 153-153-0: still true, re-measured.
- Conversion 10/10: still true, re-measured (HTML-token, not live submit).
- Tap-target 43–48px range: **resolved** — 45.5px / 50px measured directly. Removed from carry-forward.
- Gold hex drift: still true, unresolved, still Adam's call — not re-litigated further, both tokens
  remain in active use.
- SEOSEM-001 (program abandoned): not re-measured per memory instruction — stays closed at evidence
  level, Adam-decision-pending.

## METRICS

- Mobile PageSpeed `/get-preapproved`: **UNVERIFIED — quota drained** (not re-probed this run; carried,
  see RECURRING_ISSUES; last live probe was 08-31 PM, HTTP 429)
- Mobile PageSpeed `/refinance-quote`: same, UNVERIFIED
- Google Ads Optimization Score: not this rotation (Monday item)
- Conversion Tracking: **10/10**
- Landing Page Mobile UX: **10/10** (tap-target item now closed)
- SEO Coverage: **10/10** (153/153 GTM, 0 dup titles/descriptions, 0 JSON-LD errors)

## RECURRING_ISSUES (same issue 2+ runs)

- NotebookLM CLI auth expired — HIGH (Adam) — the single NLM blocker
- PSI quota drained — HIGH (Adam) — not re-probed today, last confirmed dead 08-31 PM
- GSC URL Inspection sweep overdue — HIGH (Adam) — gates all 7 open P4 backlog items
- Blog stale — MEDIUM — owned by `styer-content-weekly`, not re-measured here
- Ad LP positioning mismatch — MEDIUM (Adam) — needs ad-group headlines
- Suburb inline-form coverage 5/25 — MEDIUM (Adam form-gate)
- `products.html` 7-card 1003 routing — MEDIUM (Adam)
- `prequal.html` `generate_lead` parity — MEDIUM (Adam form-gate)
- Broker vs correspondent-lender + stale voice guide — MEDIUM (Adam)
- Indexed titles > 65 chars (61, per 09-01 measurement) — LOW (Adam) — deliberate no-op
- Gold hex drift `#C9A84C` vs `#8B6E24` — LOW (Adam) — both tokens in real use
- Homepage title pipe normalization — CLOSED, stale, not carried further (per 08-29 log)
- Hedged claims to verify vs wholesale rate sheets — 8 items — HIGH (Adam)

## NOTEBOOK_INSIGHTS (cache)

- (carried, unchanged) NotebookLM auth is the sole blocker on the NLM strategy query path; the script
  itself runs and reaches the API.
- (carried) `git fetch` before analysis is now a hard precondition, 4-for-4 confirmed stale-checkout
  near-misses in this repo.
- (carried) Parse HTML/JSON-LD with real parsers (`html.parser`, `json.loads`), never attribute regex —
  09-01's near-miss is the standing reason.
- **New this run:** a delegated (document-level) event listener will never show up in a per-page HTML
  grep for its event name — verify tracking implementation by reading the JS source, not just the
  rendered markup, before calling something untracked.
- **New this run:** live-fetching all 153 sitemap URLs directly for a sitewide sweep sidesteps the
  local-path normalization bug class (`.html` vs extensionless, trailing-slash vs `index.html`) that
  has produced false positives in this repo before. Prefer it over local-file mapping for any sitewide
  check that has a live equivalent.

## TOMORROW_PRIORITY

Next run = **Thursday rotation (Internal Linking + Funnel Flow)** if the calendar cursor holds, though
this task has repeatedly run whatever weekday's rotation the log cursor pointed to regardless of actual
calendar day — follow whichever `TOMORROW_PRIORITY` this log sets, which is:

1. **`git fetch origin && git status -sb` before any analysis — non-negotiable, now 4-for-4.**
2. **Parse JSON-LD with `json.loads` after extracting via `html.parser`; parse HTML attributes the same
   way. Never an attribute regex.**
3. Steps 1–2 non-negotiables (sitemap/robots/core 200 via `curl -L`; conversion 10/10 HTML-token; no
   live form submissions).
4. **Thursday rotation:** pick 3 pages, verify each links to 2+ relevant pages; trace the full funnel
   homepage → landing page → form → thank-you; check `contact.html` form wiring; audit `thank-you.html`
   (Calendly, phone, 3-step what-happens-next).
5. **Re-probe PSI quota** — last confirmed dead (HTTP 429) 08-31 PM, not re-checked since. If still
   drained, that's 2 consecutive confirmed-dead probes and worth tightening the recurring-issue wording
   from "drained" to "drained, reconfirmed twice."
6. Run the sitewide GTM/JSON-LD live-fetch sweep again if any content shipped since this run — it's now
   the fastest way to catch a new cluster missing the container, and it's proven clean 2 runs running.

## FLAG_FOR_ADAM

### Net new this run:

- **One FAQ answer on your Cedar Park page started with "it depends" instead of the number.** Google
  and AI assistants pull FAQ answers verbatim to show as direct answers — an answer that opens with a
  hedge doesn't get picked, even though the actual content two sentences later is exactly the kind of
  specific detail that does get picked. Rewrote to lead with the range (0%–20% depending on program),
  same facts, same order after that. **Fixed, pushed, verified live. No action needed.**

- **Resolved a small thing that was sitting unresolved for a couple of days:** whether your mobile
  landing-page buttons met the 44px minimum tap-target size. The math was ambiguous depending on a CSS
  detail I couldn't resolve without actually rendering the page, and the tool that renders pages had
  timed out twice. It's back — measured 45.5px and 50px on the two pages. **Both pass. Closed.**

### Standing — unchanged, still yours:

- **The highest-leverage 2 minutes on your list: run `notebooklm login` once in a terminal.** Same as
  every prior run — the script works, the credential is the only thing dead.
- PageSpeed Insights quota — still drained as of the last check (08-31 PM). Worth a dedicated API
  key/project if you want fresh Core Web Vitals numbers again.
- GSC URL Inspection sweep — still overdue, still gates 7 backlog items.
- Ad landing page positioning — still needs the ad-group headlines pasted in to rewrite safely.

## SELF-REVIEW GATE

Self-review: **PASS** — 1 file changed (`cedar-park-mortgage-lender.html`), 0 issues. Divs balanced
(85/85), NMLS #513013 intact (14 occurrences), no legacy brand strings, GTM intact (×2), diff scoped to
exactly the 2 targeted lines. `loanos-clone` unmutated.
