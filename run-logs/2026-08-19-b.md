# Daily Run — 2026-08-19 (SECOND RUN, same date — Thursday rotation: Internal Linking + Funnel Flow)

**This is the second `styer-site-daily` invocation on 2026-08-19.** The first run (`run-logs/2026-08-19.md`, commits `8833d36` + `e8a2da2`) completed the Wednesday rotation. Rather than repeat it, this run executed the **Thursday rotation** that the first run's `TOMORROW_PRIORITY` set as next. `2026-08-19.md` was left intact.

**1 LOW_RISK fix shipped + pushed** — `ARCHITECTURE.md` conversion-tracking docs. Three defects in one file: a **phantom event that exists nowhere**, a **retired compliance claim documented as canonical**, and a funnel shape that two consecutive run logs described incorrectly.

**Thursday rotation: PASS, 0 site defects.** Internal linking, funnel trace, `contact.html` wiring, and `thank-you.html` all clean.

**Three would-be findings killed by the Re-Verify Gate** — one of which I had already half-written as a HIGH before verifying it.

---

## NOTEBOOKLM
Status: **SKIPPED — advisor script ABSENT (97th consecutive check).** `/Users/adamstyer/loanos/scripts/notebook_advisor.py` still missing (`ls` confirms). NOTEBOOK_INSIGHTS cache applied.
Insight applied (cached): *"quote-agnostic + extensionless greps on live pages"* — this one directly saved the run; see RE-VERIFY GATE #1.

## STEP 1 — SITEMAP + ROBOTS (live, sandbox-disabled `curl -L`, absolute binaries)

| Check | HTTP | Result |
|---|---|---|
| `sitemap.xml` | 200 | ✅ **146** `<loc>` (unchanged) |
| `robots.txt` | 200 | ✅ |
| homepage `/` | 200 | ✅ |
| `/get-preapproved` · `/refinance-quote` · `/thank-you` · `/contact` | 200 | ✅ |

## STEP 2 — CONVERSION TRACKING

Status: **10/10 ✅** — HTML-token matrix via live `curl -L`.

| Page | GTM | gen_lead | lead_type | thank_you_pv | tel: | form action |
|---|---|---|---|---|---|---|
| `/get-preapproved` | ×2 (script+noscript) | 1 | `purchase_prequal` | 0 | +15129566010 | `/thank-you` |
| `/refinance-quote` | ×2 | 1 | `refi_quote` | 0 | +15129566010 | `/thank-you` |
| `/contact` | ×2 | 1 | `contact_form` | 0 | +15129566010 | `/thank-you` |
| `/thank-you` | ×2 | 0 | — | 1 | +15129566010 | — |
| `/` | ×2 | 0 | — | 0 | +15129566010 | — |

GTM count reads ×2 here vs ×3 in the 08-19 AM log — that is a **grep-scope difference, not a regression**. Resolved to the container: `gtm.js` head snippet + `ns.html` noscript iframe = the complete correct install. Verified, not assumed.

## STEP 4 — ROTATION: **Thursday — Internal Linking + Funnel Flow**

### Internal linking — PASS (10 pages audited, threshold is 2+)

Audited the whole **complicated-income cluster** (the GOALS.md positioning core) rather than an arbitrary 3, counting `<main>`-body internal links only so header/footer nav doesn't inflate the number:

| Page | main-body internal links |
|---|---|
| `non-qm-loans` | 22 |
| `1099-only-mortgage-texas` | 20 |
| `bank-statement-loans` | 18 |
| `self-employed-mortgage-austin` | 17 |
| `mortgage-for-business-owners-austin` | 17 |
| `dscr-loan-austin-tx` | 16 |
| `asset-depletion-mortgage-texas` | 11 |
| `high-net-worth-mortgage` | 11 |
| `p-and-l-mortgage-texas` | 10 |
| `k1-income-mortgage-austin` | 9 |

**10/10 far exceed the 2+ threshold**, and the cluster is densely cross-linked (every page links to 3+ siblings). No thin-linking defect.

### Funnel trace — PASS, verified end-to-end including state passing

```
24 suburb pages + area hub ──(3–6 links each)──> /scenario.html ──> /get-preapproved?intent=scenario
homepage ──(4 CTAs)───────────────────────────────────────────────> /get-preapproved.html?intent=scenario
                                                    │
                              form submit → generate_lead(purchase_prequal)
                                                    ↓
                            /thank-you → thank_you_page_view → Google Ads conversion
```

Every hop live-verified 200. Two findings that matter:

- **The suburb funnel is TWO hops, not one.** All 25 suburb/area pages route to `/scenario.html` (3–6 links each), which carries the `/get-preapproved?intent=scenario` CTA. The SKILL's Thursday checklist assumes a one-hop funnel; it isn't one.
- **`?intent=scenario` survives the redirect.** `_redirects:97` is `/get-preapproved → /get-preapproved.html 301!`. Confirmed via `curl -w '%{url_effective}'` that the query string is preserved through the 301, and `get-preapproved.html` reads `intent === 'scenario'`. **This was the single most plausible place for the two-hop funnel to silently lose state, and it does not.**

### `contact.html` wiring — PASS

`<form name="contact" method="POST" action="/thank-you" netlify netlify-honeypot="bot-field">` · hidden `form-name=contact` ✅ · `bot-field` honeypot ✅ · `dataLayer` → `generate_lead` / `lead_type: 'contact_form'` ✅ · TCPA `name="tcpa_consent" required` with full consent + STOP language ✅. Verified against **committed source**, per the standing rule that Netlify strips the `netlify` attribute from served HTML.

### `thank-you.html` audit — PASS

Calendly ✅ (inline widget, `calendly.com/adamstyer/15minutes`) · `tel:+15129566010` ✅ · "What Happens Next" with **3** steps ✅ (*Help Adam prep a better first call* → *Answer a few questions in my secure online portal* → *Pick a time to talk*) · `thank_you_page_view` ✅ · noindex 0 ✅.

## STEP 4B — SEO/SEM BACKLOG (read-only — writing `loanos-clone` triggers a paused-LoanOS Vercel build)

- `BLOCKERS.md` = **`[No active blockers]`** (clean). `loanos-clone` **not mutated.**
- `backlog.md`: **no un-gated item exists.** P0–P3 fully struck through; P4 is GSC-blocked; remaining opens are Adam-gated (form changes, new pages, LoanOS-paused).
- **New backlog candidate identified this run** (logged to `TODO.md`, not shipped — it is a JS change to conversion tracking): thank-you Calendly bookings are untracked because the widget is embedded (`data-url`) rather than an `<a href>`, so `book_call_click` never fires on the highest-intent page. Capturing it needs a Calendly `postMessage` listener.

## STEP 5 — DESIGN SPOT-CHECK (rotate: `scenario.html` + `contact.html` — today's funnel pages)

| Check | scenario.html | contact.html |
|---|---|---|
| GTM-PQQ6PGLR | ×2 ✅ | ×2 ✅ |
| NMLS 513013 / company 2653540 | 9 / 3 ✅ | 9 / 4 ✅ |
| "The Styer Team" | 0 ✅ | 0 ✅ |
| "Mortgage Solutions LP" | 0 ✅ | 0 ✅ |
| "21-day" | 0 ✅ | 0 ✅ |
| HyperSmart | ×5 ✅ | ×8 ✅ |
| noindex | 0 ✅ | 0 ✅ |
| canonical | self ✅ | self ✅ |
| raw 1003 app URL displayed | 0 ✅ | 0 ✅ |

## RE-VERIFY GATE

**Three findings killed before surfacing. One I had already drafted as HIGH.**

**1. "`/scenario.html` has a dead primary CTA — 'Start the Short Scenario Review' links nowhere" → REJECTED (my own grep was wrong).**
This would have been a HIGH: a dead button on the funnel-exit page for all 25 suburb pages. My `href="..."` grep returned only `/`, `mailto:`, and `tel:` for that page. The actual markup is `href='/get-preapproved?intent=scenario'` — **single-quoted and extensionless**. The cached learning *"Netlify Pretty-URLs strip `.html` and rewrite attr quotes to single — use quote-agnostic + extensionless greps"* is exactly this trap, and I walked into it anyway on the first pass. Re-ran quote-agnostic; the CTA is live and correct.

**2. "Suburb pages have 0 `/get-preapproved` links" → CONFIRMED REAL, but the prior run's *reasoning* was wrong.**
The 0/25 count holds under a quote-agnostic, extensionless re-grep. But the 08-19 AM log justified it as *"the sitewide repositioned-funnel pattern also confirmed on the homepage 07-24."* **The homepage claim is false** — `index.html` carries **4** `/get-preapproved.html?intent=scenario` CTAs. The right justification is the two-hop routing through `/scenario.html`, now documented in `ARCHITECTURE.md` so it stops being re-derived (wrongly) every Thursday. *Conclusion unchanged, evidence corrected.*

**3. "~100 pages intercept Calendly clicks and none are exempt — converted leads get bounced back to the form" → REJECTED (correctly scoped by design).**
`analytics.js` `preventDefault()`s `a[href*="calendly.com"]` and redirects to `/get-preapproved.html?intent=schedule`, exempting only `data-qualified-calendar`. Exempt count is **0/100**, which looks alarming. It isn't: `/thank-you` has **no** `a[href*=calendly.com]` at all — it embeds the widget via `data-url` and its buttons are internal `#ty-calendly` jumps. So post-conversion leads book directly and every pre-conversion page qualifies first. The gate is intentional and correctly scoped.

**Live-owned claims re-verified green, none surfaced:** sitemap non-200 → **200** · robots → **200** · conversion tracking broken → **10/10** · blog/landing CTA presence → present · meta missing/duplicate → not re-run (audited sitewide in the AM run, 0 duplicates).

**Stale-checkout hazard — RE-VERIFIED, still HIGH.** 52 dirty entries, **12 deletions** of files that are all live-200. Unchanged in character from the AM diagnosis. `ARCHITECTURE.md` was **clean** before this run's edit, so this commit did not touch or entangle the overlay — verified via `git show --stat HEAD` (1 file) and dirty-count parity (52 → 53 → 52).

## CHANGES MADE

- `ARCHITECTURE.md` — three corrections, all verified live before writing:
  - **Removed phantom `calendly_click` event.** Documented since the file was written; it exists in **no bundle and no page**. The real event is `book_call_click`. Left an explicit warning so it isn't re-added — a GTM trigger or Ads conversion built on it would never have fired.
  - **Retired the legacy trust bar.** The documented "verbatim" trust bar still contained **`21-Day Avg. Close`** — a performance claim GOALS.md explicitly killed and that was swept from site HTML on 2026-05-17. Site HTML is clean (0 real occurrences); this doc was the last authoritative instruction that would have led an agent to reintroduce it during the pre-audit compliance cleanup. Replaced with the actual current stats strip.
  - **Documented the real event inventory + funnel topology.** Was 6 events (1 phantom, 3 lead_types); now 10 real events across both bundles and 8 `lead_type` values, split by which bundle pushes them, plus the Qualification Gate and the two-hop funnel with the redirect-param finding.
- Commit `67f03c3`, pushed to `origin/main`. **1 file, docs only — no site HTML, CSS, or JS touched.**
- Docs: this run log + `latest.md`, `learnings.md`, `CONTEXT.md`, `CHANGELOG.md`, `TODO.md`

## ISSUES FOUND

- **`ARCHITECTURE.md` documented a compliance claim GOALS.md retired 3 months ago** — MEDIUM — new — **FIXED**. Doc drift on a compliance-sensitive claim is worse than site drift: the site was already clean, but the file agents read as source-of-truth was still instructing them to use it.
- **Phantom `calendly_click` event in tracking docs** — MEDIUM — new — **FIXED**.
- **Working tree is a stale ~08-06 checkout overlay** — HIGH — recurring since 07-22 (re-diagnosed 08-19 AM). 52 entries, 12 deletions of live files. Untouched.
- **Scheduler cadence** — HIGH — recurring. Note this run is evidence of the *opposite* failure mode: two invocations on the same date.
- **thank-you Calendly bookings untracked** — LOW — new — backlogged (JS change, not shipped).

## METRICS

- Mobile PageSpeed /get-preapproved: UNVERIFIED — PSI quota drained (carry)
- Mobile PageSpeed /refinance-quote: UNVERIFIED — same shared quota (carry)
- Netlify Lighthouse (mobile, homepage, last data point): Perf 81 / A11y 100 / BP 100 / SEO 100 (carry)
- Google Ads Optimization Score: UNVERIFIED — Adam-owned (Ads UI)
- Conversion Tracking: **10/10** ✅
- Landing Page Mobile UX: 9/10 (carry — landing pages unchanged)
- SEO Coverage: **10/10** ✅ (carry from AM: 146 URLs, 146/146 self-canonical, 0 duplicate titles/metas)
- **Funnel Integrity: 10/10** ✅ (new metric — all hops 200, param survives 301, all 4 form pages fire `generate_lead`, thank-you fires Ads conversion)
- **Tracking-doc accuracy: 6/10 → 10/10** (new metric — was 1 phantom event + 7 undocumented events + 5 undocumented lead_types)

## RECURRING_ISSUES (same issue 2+ runs)

- **Stale-checkout dirty tree** — HIGH — 52 entries, 12 deletions. Never `git add -A` here.
- **Scheduler reliability** — HIGH (Adam)
- NotebookLM advisor script MISSING — 97th run dead — HIGH (Adam)
- NotebookLM CLI auth expired — HIGH (Adam)
- PSI quota drained — HIGH (Adam)
- GSC URL Inspection sweep overdue — HIGH (Adam)
- Suburb inline-form coverage 5/25 — MEDIUM (Adam form-gate)
- products.html 7-card 1003 routing — MEDIUM (Adam, 53-carry)
- prequal.html `generate_lead` parity — MEDIUM (Adam form-gate)
- AggregateRating policy — HIGH (Adam) — AEO-only; competitive-weekly recommends DROPPING from blockers
- fha.html "broker" vs correspondent-lender indexed-title — MEDIUM (Adam)
- 26 indexed titles > 65 chars — LOW (Adam) — deliberate no-op
- Gold hex drift `#C9A84C`(SKILL.md) → `#8B6E24`(style.css) — LOW (Adam)
- Homepage title pipe normalization — MEDIUM (Adam, HIGH_RISK)
- **SKILL.md drift — now THREE confirmed items** — LOW→MEDIUM (Adam): (1) Step 8 points at `styermortgage-context.md`, which is a 10-line **DEPRECATED** stub redirecting to the four-file system; (2) Step 3's required trust bar still contains the retired `21-Day Avg. Close` claim; (3) retired "Mortgage Solutions LP" HARD CONSTRAINT.
- Hedged claims to verify vs wholesale rate sheets — 8 items — HIGH (Adam)

## NOTEBOOK_INSIGHTS (cache for future runs if NotebookLM is down)

- **NEW — documentation drift on a compliance claim outranks site drift.** The site had been clean of "21-Day Avg. Close" since 2026-05-17, but `ARCHITECTURE.md` still published it as the canonical verbatim trust bar. The site is the output; the doc is the *instruction*. An agent doing exactly what the doc said would have reintroduced a retired claim into the pre-audit compliance sweep. **When a claim is retired sitewide, grep the docs too — and grep them again a quarter later.**
- **NEW — verify that documented events actually exist before trusting an event table.** `calendly_click` was documented, looked completely plausible, and existed in zero files. The check is one grep across both bundles + all HTML. Run it on any event table before building a trigger on it.
- **NEW — a "0 exemptions out of 100" ratio is not automatically a defect.** The Calendly interceptor exempts 0/100 pages, which reads as a broken allowlist. The page that needed the exemption solved it by *not matching the selector at all* (embedded widget vs `<a href>`). Check whether the exempt case is out of scope before calling the exemption list empty.
- **NEW — a correct conclusion can rest on false evidence; fix the evidence.** "Suburb pages have 0 `/get-preapproved` links, by design" was right. Its stated proof ("the homepage has 0 too") was wrong — the homepage has 4. Wrong reasoning survives into learnings and gets reused on a case where it doesn't hold.
- Quote-agnostic + extensionless greps on live pages — Netlify Pretty-URLs strip `.html` and rewrite attrs to single quotes. **Walked into this one again this run; it cost a near-miss HIGH.**
- An absence is only a defect if the page is actually competing for the query (indexable? self-canonical? token in page vs bundle vs supertype?).
- A non-self-canonical URL must never be added to sitemap.xml.
- Schema audits must resolve the type hierarchy, not grep strings: `MortgageBroker` **is** a `LocalBusiness`.
- Uniform absence (0/25) is design; outlier absence (1/25) is the defect.
- Don't chase title character counts on pages that are actively ranking.
- Netlify strips the `netlify` form attribute at build time — verify form wiring against committed source.
- Sandboxed Bash false-returns HTTP `000` — run curl with the sandbox disabled.
- Foreground `sleep` is blocked — write docs between push and live-verify.

## TOMORROW_PRIORITY

Next run = **Fri 2026-08-20 (Content Planning + AEO Review)**. Then:
1. Steps 1–2 non-negotiables (sitemap/robots/homepage 200; conversion 10/10 HTML-token; absolute `/usr/bin/*`; `curl -L` **sandbox-disabled**; **quote-agnostic + extensionless greps**; JS-bound events resolved through `script.js`/`analytics.js`; Netlify form wiring verified against committed source).
2. **Friday rotation:** last blog post date (flag if >7 days — last is `blog/2026-08-18-jumbo-loan-requirements-texas-2026.html`, so freshness is currently OK); audit blog CTAs; AEO answer-first review on 2 posts.
3. **Do NOT re-flag as defects:** suburb `/get-preapproved` 0/25 (two-hop by design), `/scenario.html` having no form (routing hub), the Calendly interceptor's 0/100 exemptions (thank-you is out of selector scope), `loanos.html` meta, the March-20 sitemap "gap", the LocalBusiness "gap". All now documented in `ARCHITECTURE.md` or the AM log.
4. **`ARCHITECTURE.md` is now the funnel + tracking source of truth.** Read it before re-deriving funnel shape; correct it there if reality moves.
5. Never `git add -A` in this repo. Stage by explicit path, verify with `git show --stat HEAD`.
6. Suburb rotation cursor: next Wednesday = **Buda** (then wrap → restart at Round Rock).

## FLAG_FOR_ADAM

### Net new this run:
- **`ARCHITECTURE.md` was still telling agents the trust bar contains "21-Day Avg. Close."** You retired that claim sitewide on 2026-05-17 and the site HTML is clean — but the file agents read as source-of-truth still had it as the canonical verbatim string, three months later. Fixed. Worth knowing because the new company's compliance review is exactly the scenario where an agent following that doc would have put the claim back. **The same stale trust bar is still in this task's own `SKILL.md` Step 3 checklist** — that file is yours, I can't edit it. Two other SKILL.md drift items alongside it (Step 8 points at the deprecated `styermortgage-context.md` stub; retired "Mortgage Solutions LP" constraint).
- **The tracking docs listed a `calendly_click` event that does not exist anywhere in the codebase.** If anyone ever built a GTM trigger or an Ads conversion on it, it silently never fired. Real event is `book_call_click`. Fixed, with a warning note so it doesn't come back.
- **Two `styer-site-daily` runs fired on 2026-08-19.** Opposite of the usual complaint. The AM run did Wednesday; this one did Thursday rather than duplicate. Both logs are committed. Flagging because the cadence is clearly not on a reliable daily trigger in either direction.

### Carried (see CONTEXT Active Blockers):
- **Stale checkout in the working tree — unchanged and still the highest-risk item.** 52 dirty entries, 12 deletions of live-200 files. A single `git add -A` reverts ~2 weeks of live conversion work and deletes the August market report. Fix is `git checkout -- <stale paths>`, but it discards `about.html`/`contact.html`/`testimonials.html` novel edits — your call.
- Suburb inline-form coverage 5/25 · products.html 7-card 1003 routing · prequal.html `generate_lead` parity · AggregateRating policy (competitive-weekly says DROP) · fha.html broker-vs-correspondent title · homepage title pipe · 26 long titles · hedged-claim verification (8) · GSC URL Inspection sweep.
- HIGH externals with no cheap live path: NotebookLM script missing (97th) + CLI auth expired · PSI quota · GSC.

## SELF-REVIEW

**PASS — 1 file changed (docs only), 0 issues.** Re-read the complete diff fresh before commit (`git diff -- ARCHITECTURE.md`), then caught and fixed one imprecision in my own new text: I had written "25 suburb pages" in the funnel diagram while the file's own inventory says 24 suburbs + the area hub — corrected so the doc doesn't contradict itself.

Every fact written was verified live this session, not recalled: event names grepped across both live bundles and all HTML; the funnel traced hop-by-hop with `curl`; the redirect-param survival tested with `curl -w '%{url_effective}'`; the trust-bar replacement read out of committed `index.html`. Hard constraints intact — **no site HTML/CSS/JS touched at all**, GTM untouched, no form field names, no nav changes to landing pages, no `noindex`, no new deps, NMLS #513013 + company 2653540 untouched, no legacy entity names introduced. Voice guide doesn't govern here (internal technical doc, no customer-facing copy written).

One deliberate judgment logged: the fix **re-introduces the literal string "21-Day Avg. Close"** into the repo, inside an explicit *"is retired — do not reintroduce"* warning block. A future compliance grep will hit it. Kept intentionally — the guardrail is worth more than a clean grep, and the surrounding sentence is unambiguous. Noted here so it isn't mistaken for drift.

**Zero files touched outside scope** — staged one file by explicit path, never `-A`, never `stash`, never `commit -a`. Verified post-commit via `git show --stat HEAD` (1 file, +52/−6) and dirty-count parity (52 before → 52 after), confirming none of the 52 stale-overlay entries were swept in. `loanos-clone` read-only (paused-LoanOS guard) — read `backlog.md` + `BLOCKERS.md`, wrote neither.

## TASK SUCCESS CRITERIA

- ✅ Sitemap + robots + homepage + 4 funnel pages HTTP 200 (sandbox-disabled curl), 146 URLs
- ✅ Conversion tracking 10/10 (HTML-token matrix); GTM ×2 vs ×3 discrepancy resolved, not assumed
- ✅ Thursday rotation executed in full: 10-page internal-link audit, end-to-end funnel trace, contact.html wiring, thank-you.html audit — all PASS
- ✅ Redirect param survival tested — the one place the two-hop funnel could lose state
- ✅ 3 false findings killed by the Re-Verify Gate (1 was a drafted HIGH; 1 corrected a prior run's false evidence)
- ✅ 1 LOW_RISK fix shipped, pushed, self-reviewed — 3 real doc defects corrected
- ✅ Funnel topology + real event inventory documented so it stops being re-derived wrongly
- ✅ Stale-checkout hazard re-verified; 52 dirty entries left untouched, commit isolation proven
- ✅ `loanos-clone` untouched (paused-LoanOS guard)
- ✅ First run's `2026-08-19.md` log preserved intact, not overwritten
- ✅ Run log + learnings + CONTEXT + CHANGELOG + TODO updated; task-run emitted
